#ifndef _MACH_FIXMAP_H
#define _MACH_FIXMAP_H

#include <mach/platform.h>
#include <asm/memory.h>
#include "global_ddr_map.h"
#include "platform.h"

/* the fix map address space :0xFF200000 ~ 0xFF60000,4MB for A9PRI */

/* P:0xF6800000 V:0xFF200000 SIZE:4MB */
#define REG_BASE_VIRT             (GLOBAL_VIRT_ADDR + GLOBAL_VIRT_SIZE)

#define REG_BASE_A9PRI_VIRT       REG_BASE_VIRT

/* 0xFF600000~0xFFE00000 for DMA alloc, CONSISTENT_BASE~CONSISTENT_END */

/* the fix map address space :0xFFF00000 ~ 0xFFFE000,896KB for others */

/* P:0xFFF80000 V:0xFFF80000 SIZE:72KB */
#define REG_BASE_SRAM_OFF_VIRT    (0xFFF80000)

/* P:0xF8008000 V:0xFFFE0000 SIZE:4KB */
#define REG_BASE_TIMER0_VIRT      (0xFFFE0000)

/* P:0xF7010000 V:0xFFFE1000 SIZE:4KB */
#define REG_BASE_IOC_OFF_VIRT     (REG_BASE_TIMER0_VIRT + REG_TIMER0_IOSIZE)

/* P:0xF8001000 V:0xFFFE2000 SIZE:4KB */
#define REG_BASE_IOC_ON_VIRT      (REG_BASE_IOC_OFF_VIRT + REG_IOC_OFF_IOSIZE)

/* P:0xF7030000 V:0xFFFE3000 SIZE:4KB */
#define REG_BASE_SC_OFF_VIRT      (REG_BASE_IOC_ON_VIRT + REG_IOC_ON_IOSIZE)

/* P:0xF7800000 V:0xFFFE4000 SIZE:8KB */
#define REG_BASE_SC_ON_VIRT       (REG_BASE_SC_OFF_VIRT + REG_SC_OFF_IOSIZE)

/* P:0xF7032000 V:0xFFFE6000 SIZE:4KB */
#define REG_BASE_PMCTRL_VIRT      (REG_BASE_SC_ON_VIRT + REG_SC_ON_IOSIZE)

/* P:0xF7340000 V:0xFFFE7000 SIZE:4KB */
#define REG_BASE_IPF_VIRT         (REG_BASE_PMCTRL_VIRT + REG_PMCTRL_IOSIZE)

/* P:0xF800B000 V:0xFFFE8000 SIZE:4KB */
#define REG_BASE_TIMER3_VIRT      (REG_BASE_IPF_VIRT + REG_IPF_IOSIZE)

/* P:0xF800C000 V:0xFFFE9000 SIZE:4KB */
#define REG_BASE_TIMER4_VIRT      (REG_BASE_TIMER3_VIRT + REG_TIMER3_IOSIZE)

/* P:0xF4410000 V:0xFFFEA000 SIZE:4KB */
#define REG_BASE_MEDIA_SC_VIRT    (REG_BASE_TIMER4_VIRT + REG_TIMER4_IOSIZE)

/* P:0xF65A0000 V:0xFFFEB000 SIZE:4KB */
#define REG_BASE_ACPU_SC_VIRT     (REG_BASE_MEDIA_SC_VIRT + REG_MEDIA_SC_IOSIZE)

/* for example */
/* P:0xXXXXXXXX V:0xFFFEC000 SIZE:XXX */
/* #define REG_BASE_XXX_VIRT         (REG_BASE_MEDIA_SC_VIRT + REG_MEDIA_SC_IOSIZE) */



#endif

