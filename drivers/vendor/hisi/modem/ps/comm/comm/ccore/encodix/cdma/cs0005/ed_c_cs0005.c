/**************************************************************************
	Generated automatically by Codegenix(TM) - (c) 2000-2004 Dafocus
	EnDec version 1.0.154
	GENERATOR: ed-c
	http://www.Dafocus.it/
**************************************************************************/
/*lint -e959 -e530 -e774 -e572 -e778 -e525 -e539 -e701 -e123 -e438 -e119 -e506 -e529 -e550 -e666 -e713 -e719 -e730 -e734 -e744 -e801 -e801 -e813 -e830 -e40 -e10 -e52 -e63 -e115 -e409 -e118 -e49 -e718 -e746 -e737 -e760 -e830 */
#define ENCODIX_CS0005

#include "ed_c_cs0005.h"
#include "ed_lib.h"
#if ED_COMMON_CRC != 0x328E3625 && ED_VERSION != 10152 && ED_VERSION != 10153
#error Invalid Encodix library files linked; used those distributed with Encodix 1.0.154
#endif


/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->REV_PDCH_RETRX.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data (sp->REV_PDCH_RETRX.data[i0]);
		EDFree (sp->REV_PDCH_RETRX.data[i0]);
	}
	if (sp->REV_PDCH_RETRX.data) {EDFree (sp->REV_PDCH_RETRX.data); sp->REV_PDCH_RETRX.data = NULL; sp->REV_PDCH_RETRX.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_0 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_0 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_0* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_1 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_1 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_1* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_2 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_2 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_2* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_3 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_3 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_3* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c01 */
void FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c01 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c01* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c02 */
void FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c02 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c02* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH.value);
		sp->AUX_PILOT_WALSH.value = NULL;
	}
	sp->AUX_PILOT_WALSH.usedBits = 0;

}

/* Free function for struct c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c03 */
void FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c03 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c03* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_WALSH.value != NULL) {
		EDFree (sp->AUX_WALSH.value);
		sp->AUX_WALSH.value = NULL;
	}
	sp->AUX_WALSH.usedBits = 0;

}

/* Free function for struct c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c04 */
void FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c04 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c04* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05 */
void FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH___2.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH___2.value);
		sp->AUX_PILOT_WALSH___2.value = NULL;
	}
	sp->AUX_PILOT_WALSH___2.usedBits = 0;
	if (sp->AUX_PILOT_WALSH1_Present) {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
		sp->AUX_PILOT_WALSH1_Present = ED_FALSE;
	}
	if (sp->AUX_PILOT_WALSH2_Present) {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
		sp->AUX_PILOT_WALSH2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05_AUX_PILOT_WALSH1 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH1_Present == present) return;
	sp->AUX_PILOT_WALSH1_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH1 = (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05_AUX_PILOT_WALSH1*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05_AUX_PILOT_WALSH1));
		sp->AUX_PILOT_WALSH1->value = NULL;
		sp->AUX_PILOT_WALSH1->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
	}
}

void SETPRESENT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05_AUX_PILOT_WALSH2 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH2_Present == present) return;
	sp->AUX_PILOT_WALSH2_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH2 = (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05_AUX_PILOT_WALSH2*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05_AUX_PILOT_WALSH2));
		sp->AUX_PILOT_WALSH2->value = NULL;
		sp->AUX_PILOT_WALSH2->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
	}
}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->field_0_Present) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_0 (sp->field_0);
		EDFree (sp->field_0);
		sp->field_0_Present = ED_FALSE;
	}
	if (sp->field_1_Present) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_1 (sp->field_1);
		EDFree (sp->field_1);
		sp->field_1_Present = ED_FALSE;
	}
	if (sp->field_2_Present) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_2 (sp->field_2);
		EDFree (sp->field_2);
		sp->field_2_Present = ED_FALSE;
	}
	if (sp->field_3_Present) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_3 (sp->field_3);
		EDFree (sp->field_3);
		sp->field_3_Present = ED_FALSE;
	}

}

void SETPRESENT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_0 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->field_0_Present == present) return;
	sp->field_0_Present = present;
	if (present) {
		/*-->*/sp->field_0 = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_0*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_0));
		INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_0 (sp->field_0);
	} else {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_0 (sp->field_0);
		EDFree (sp->field_0);
	}
}

void SETPRESENT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_1 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->field_1_Present == present) return;
	sp->field_1_Present = present;
	if (present) {
		/*-->*/sp->field_1 = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_1*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_1));
		INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_1 (sp->field_1);
	} else {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_1 (sp->field_1);
		EDFree (sp->field_1);
	}
}

void SETPRESENT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_2 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->field_2_Present == present) return;
	sp->field_2_Present = present;
	if (present) {
		/*-->*/sp->field_2 = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_2*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_2));
		INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_2 (sp->field_2);
	} else {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_2 (sp->field_2);
		EDFree (sp->field_2);
	}
}

void SETPRESENT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_3 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->field_3_Present == present) return;
	sp->field_3_Present = present;
	if (present) {
		/*-->*/sp->field_3 = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_3*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_3));
		INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_3 (sp->field_3);
	} else {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields_field_3 (sp->field_3);
		EDFree (sp->field_3);
	}
}

/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->REV_PDCCH_EP_SIZE.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data (sp->REV_PDCCH_EP_SIZE.data[i0]);
		EDFree (sp->REV_PDCCH_EP_SIZE.data[i0]);
	}
	if (sp->REV_PDCCH_EP_SIZE.data) {EDFree (sp->REV_PDCCH_EP_SIZE.data); sp->REV_PDCCH_EP_SIZE.data = NULL; sp->REV_PDCCH_EP_SIZE.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_PDCCH_EP_SIZE_1.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data (sp->REV_PDCCH_EP_SIZE_1.data[i0]);
		EDFree (sp->REV_PDCCH_EP_SIZE_1.data[i0]);
	}
	if (sp->REV_PDCCH_EP_SIZE_1.data) {EDFree (sp->REV_PDCCH_EP_SIZE_1.data); sp->REV_PDCCH_EP_SIZE_1.data = NULL; sp->REV_PDCCH_EP_SIZE_1.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_PDCH_EP_SIZE.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data (sp->REV_PDCH_EP_SIZE.data[i0]);
		EDFree (sp->REV_PDCH_EP_SIZE.data[i0]);
	}
	if (sp->REV_PDCH_EP_SIZE.data) {EDFree (sp->REV_PDCH_EP_SIZE.data); sp->REV_PDCH_EP_SIZE.data = NULL; sp->REV_PDCH_EP_SIZE.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_PDCH_RETRX.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data (sp->REV_PDCH_RETRX.data[i0]);
		EDFree (sp->REV_PDCH_RETRX.data[i0]);
	}
	if (sp->REV_PDCH_RETRX.data) {EDFree (sp->REV_PDCH_RETRX.data); sp->REV_PDCH_RETRX.data = NULL; sp->REV_PDCH_RETRX.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_PDCH_EP_SIZE_1.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data (sp->REV_PDCH_EP_SIZE_1.data[i0]);
		EDFree (sp->REV_PDCH_EP_SIZE_1.data[i0]);
	}
	if (sp->REV_PDCH_EP_SIZE_1.data) {EDFree (sp->REV_PDCH_EP_SIZE_1.data); sp->REV_PDCH_EP_SIZE_1.data = NULL; sp->REV_PDCH_EP_SIZE_1.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data */
void FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.value != NULL) {
		EDFree (sp->CHARi.value);
		sp->CHARi.value = NULL;
	}
	sp->CHARi.usedBits = 0;

}

/* Free function for struct c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data */
void FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.value != NULL) {
		EDFree (sp->CHARi.value);
		sp->CHARi.value = NULL;
	}
	sp->CHARi.usedBits = 0;

}

/* Free function for struct c_RIR_Channel_Configuration_for_the_Supplemental_Channel */
void FREE_c_RIR_Channel_Configuration_for_the_Supplemental_Channel (c_RIR_Channel_Configuration_for_the_Supplemental_Channel* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data */
void FREE_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data (c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data */
void FREE_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data (c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields */
void FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}
	if (sp->c04_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
		sp->c04_Present = ED_FALSE;
	}
	if (sp->c05_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05 (sp->c05);
		EDFree (sp->c05);
		sp->c05_Present = ED_FALSE;
	}

}

void SETPRESENT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c01 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c01*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c01));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c01 (sp->c01);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c02 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c02*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c02));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c02 (sp->c02);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c03 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c03*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c03));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c03 (sp->c03);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

void SETPRESENT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c04 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c04_Present == present) return;
	sp->c04_Present = present;
	if (present) {
		/*-->*/sp->c04 = (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c04*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c04));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c04 (sp->c04);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
	}
}

void SETPRESENT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05 (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c05_Present == present) return;
	sp->c05_Present = present;
	if (present) {
		/*-->*/sp->c05 = (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05 (sp->c05);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields_c05 (sp->c05);
		EDFree (sp->c05);
	}
}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields (&(sp->Type_specific_fields));

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2_Type_specific_fields (&(sp->Type_specific_fields));

}

/* Free function for struct c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c01 */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c01 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c01* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c02 */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c02 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c02* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH.value);
		sp->AUX_PILOT_WALSH.value = NULL;
	}
	sp->AUX_PILOT_WALSH.usedBits = 0;

}

/* Free function for struct c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c03 */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c03 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c03* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_WALSH.value != NULL) {
		EDFree (sp->AUX_WALSH.value);
		sp->AUX_WALSH.value = NULL;
	}
	sp->AUX_WALSH.usedBits = 0;

}

/* Free function for struct c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c04 */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c04 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c04* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05 */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH___1.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH___1.value);
		sp->AUX_PILOT_WALSH___1.value = NULL;
	}
	sp->AUX_PILOT_WALSH___1.usedBits = 0;
	if (sp->AUX_PILOT_WALSH1_Present) {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
		sp->AUX_PILOT_WALSH1_Present = ED_FALSE;
	}
	if (sp->AUX_PILOT_WALSH2_Present) {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
		sp->AUX_PILOT_WALSH2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH1_Present == present) return;
	sp->AUX_PILOT_WALSH1_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH1 = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1));
		sp->AUX_PILOT_WALSH1->value = NULL;
		sp->AUX_PILOT_WALSH1->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
	}
}

void SETPRESENT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH2_Present == present) return;
	sp->AUX_PILOT_WALSH2_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH2 = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2));
		sp->AUX_PILOT_WALSH2->value = NULL;
		sp->AUX_PILOT_WALSH2->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
	}
}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c01 */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c01 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c01* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c02 */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c02 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c02* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH.value);
		sp->AUX_PILOT_WALSH.value = NULL;
	}
	sp->AUX_PILOT_WALSH.usedBits = 0;

}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c03 */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c03 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c03* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_WALSH.value != NULL) {
		EDFree (sp->AUX_WALSH.value);
		sp->AUX_WALSH.value = NULL;
	}
	sp->AUX_WALSH.usedBits = 0;

}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c04 */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c04 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c04* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05 */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH___1.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH___1.value);
		sp->AUX_PILOT_WALSH___1.value = NULL;
	}
	sp->AUX_PILOT_WALSH___1.usedBits = 0;
	if (sp->AUX_PILOT_WALSH1_Present) {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
		sp->AUX_PILOT_WALSH1_Present = ED_FALSE;
	}
	if (sp->AUX_PILOT_WALSH2_Present) {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
		sp->AUX_PILOT_WALSH2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH1_Present == present) return;
	sp->AUX_PILOT_WALSH1_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH1 = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1));
		sp->AUX_PILOT_WALSH1->value = NULL;
		sp->AUX_PILOT_WALSH1->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
	}
}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH2_Present == present) return;
	sp->AUX_PILOT_WALSH2_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH2 = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2));
		sp->AUX_PILOT_WALSH2->value = NULL;
		sp->AUX_PILOT_WALSH2->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
	}
}

/* Free function for variable-sized sequence c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06 */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->HRPD_NGHBRs.items; i0++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data (sp->HRPD_NGHBRs.data[i0]);
		EDFree (sp->HRPD_NGHBRs.data[i0]);
	}
	if (sp->HRPD_NGHBRs.data) {EDFree (sp->HRPD_NGHBRs.data); sp->HRPD_NGHBRs.data = NULL; sp->HRPD_NGHBRs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_RIR_Roaming_Information_SID_NID_data */
void FREE_c_RIR_Roaming_Information_SID_NID_data (c_RIR_Roaming_Information_SID_NID_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Service_Option_Information_Data */
void FREE_c_RIR_Service_Option_Information_Data (c_RIR_Service_Option_Information_Data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Multiplex_Option_Information_Data */
void FREE_c_RIR_Multiplex_Option_Information_Data (c_RIR_Multiplex_Option_Information_Data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Service_Configuration_Information_CON_RECs_data */
void FREE_c_RIR_Service_Configuration_Information_CON_RECs_data (c_RIR_Service_Configuration_Information_CON_RECs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->RLP_BLOB_Present) {
		if (sp->RLP_BLOB->value != NULL) {
			EDFree (sp->RLP_BLOB->value);
			sp->RLP_BLOB->value = NULL;
		}
		sp->RLP_BLOB->usedBits = 0;
		EDFree (sp->RLP_BLOB);
		sp->RLP_BLOB_Present = ED_FALSE;
	}
	if (sp->QOS_PARMS_Present) {
		if (sp->QOS_PARMS->value != NULL) {
			EDFree (sp->QOS_PARMS->value);
			sp->QOS_PARMS->value = NULL;
		}
		sp->QOS_PARMS->usedBits = 0;
		EDFree (sp->QOS_PARMS);
		sp->QOS_PARMS_Present = ED_FALSE;
	}

}

void SETPRESENT_c_RIR_Service_Configuration_Information_CON_RECs_data_RLP_BLOB (c_RIR_Service_Configuration_Information_CON_RECs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->RLP_BLOB_Present == present) return;
	sp->RLP_BLOB_Present = present;
	if (present) {
		/*-->*/sp->RLP_BLOB = (c_RIR_Service_Configuration_Information_CON_RECs_data_RLP_BLOB*)EDAlloc (sizeof (c_RIR_Service_Configuration_Information_CON_RECs_data_RLP_BLOB));
		sp->RLP_BLOB->value = NULL;
		sp->RLP_BLOB->usedBits = 0;
	} else {
		if (sp->RLP_BLOB->value != NULL) {
			EDFree (sp->RLP_BLOB->value);
			sp->RLP_BLOB->value = NULL;
		}
		sp->RLP_BLOB->usedBits = 0;
		EDFree (sp->RLP_BLOB);
	}
}

void SETPRESENT_c_RIR_Service_Configuration_Information_CON_RECs_data_QOS_PARMS (c_RIR_Service_Configuration_Information_CON_RECs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->QOS_PARMS_Present == present) return;
	sp->QOS_PARMS_Present = present;
	if (present) {
		/*-->*/sp->QOS_PARMS = (c_RIR_Service_Configuration_Information_CON_RECs_data_QOS_PARMS*)EDAlloc (sizeof (c_RIR_Service_Configuration_Information_CON_RECs_data_QOS_PARMS));
		sp->QOS_PARMS->value = NULL;
		sp->QOS_PARMS->usedBits = 0;
	} else {
		if (sp->QOS_PARMS->value != NULL) {
			EDFree (sp->QOS_PARMS->value);
			sp->QOS_PARMS->value = NULL;
		}
		sp->QOS_PARMS->usedBits = 0;
		EDFree (sp->QOS_PARMS);
	}
}

/* Free function for struct c_RIR_Service_Configuration_Information_FOR_SCHs_data */
void FREE_c_RIR_Service_Configuration_Information_FOR_SCHs_data (c_RIR_Service_Configuration_Information_FOR_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_RIR_Channel_Configuration_for_the_Supplemental_Channel (&(sp->SCH_CC_Type_specific_field));

}

/* Free function for struct c_RIR_Service_Configuration_Information_REV_SCHs_data */
void FREE_c_RIR_Service_Configuration_Information_REV_SCHs_data (c_RIR_Service_Configuration_Information_REV_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_RIR_Channel_Configuration_for_the_Supplemental_Channel (&(sp->SCH_CC_Type_specific_field2));

}

/* Free function for struct c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data (c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data (c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_SYNC_ID_SYNC_IDs_data */
void FREE_c_RIR_SYNC_ID_SYNC_IDs_data (c_RIR_SYNC_ID_SYNC_IDs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->SYNC_ID.value != NULL) {
		EDFree (sp->SYNC_ID.value);
		sp->SYNC_ID.value = NULL;
	}
	sp->SYNC_ID.usedBits = 0;

}

/* Free function for struct c_RIR_Extended_Terminal_Information_SO_GROUP_data */
void FREE_c_RIR_Extended_Terminal_Information_SO_GROUP_data (c_RIR_Extended_Terminal_Information_SO_GROUP_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->SO_BITMAP.value != NULL) {
		EDFree (sp->SO_BITMAP.value);
		sp->SO_BITMAP.value = NULL;
	}
	sp->SO_BITMAP.usedBits = 0;

}

/* Free function for struct c_RIR_Extended_Service_Option_Information_supports_data */
void FREE_c_RIR_Extended_Service_Option_Information_supports_data (c_RIR_Extended_Service_Option_Information_supports_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Extended_Service_Option_Information_SO_GROUP_data */
void FREE_c_RIR_Extended_Service_Option_Information_SO_GROUP_data (c_RIR_Extended_Service_Option_Information_SO_GROUP_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->FOR_SUP_SO_BITMAP.value != NULL) {
		EDFree (sp->FOR_SUP_SO_BITMAP.value);
		sp->FOR_SUP_SO_BITMAP.value = NULL;
	}
	sp->FOR_SUP_SO_BITMAP.usedBits = 0;
	if (sp->REV_SUP_SO_BITMAP.value != NULL) {
		EDFree (sp->REV_SUP_SO_BITMAP.value);
		sp->REV_SUP_SO_BITMAP.value = NULL;
	}
	sp->REV_SUP_SO_BITMAP.usedBits = 0;

}

/* Free function for variable-sized sequence c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data_SUBCLASS_SUP */
void FREE_c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data_SUBCLASS_SUP (c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data_SUBCLASS_SUP* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data */
void FREE_c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data (c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->SUBCLASS_SUP.data) {EDFree (sp->SUBCLASS_SUP.data); sp->SUBCLASS_SUP.data = NULL; sp->SUBCLASS_SUP.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_PilotRecord_Pilot_Record_Type_Specific_Fields */
void FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (c_PilotRecord_Pilot_Record_Type_Specific_Fields* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields_Type_specific_fields (&(sp->Type_specific_fields));

}

/* Free function for struct c_InfoRec_Extended_Display_data_01_data_data_02_data */
void FREE_c_InfoRec_Extended_Display_data_01_data_data_02_data (c_InfoRec_Extended_Display_data_01_data_data_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->MASK.value != NULL) {
		EDFree (sp->MASK.value);
		sp->MASK.value = NULL;
	}
	sp->MASK.usedBits = 0;

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->MUX_HEADER.value != NULL) {
		EDFree (sp->MUX_HEADER.value);
		sp->MUX_HEADER.value = NULL;
	}
	sp->MUX_HEADER.usedBits = 0;
	for (i0=0; i0<sp->PARTITIONSs.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data (sp->PARTITIONSs.data[i0]);
		EDFree (sp->PARTITIONSs.data[i0]);
	}
	if (sp->PARTITIONSs.data) {EDFree (sp->PARTITIONSs.data); sp->PARTITIONSs.data = NULL; sp->PARTITIONSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->BCMC_FLOW_DISCRIMINATOR.value);
		sp->BCMC_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->BCMC_FLOW_DISCRIMINATOR.usedBits = 0;

}

/* Free function for variable-sized sequence c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs */
void FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data */
void FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs */
void FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data */
void FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_FCH_Type_Specific_Fields */
void FREE_c_FCH_Type_Specific_Fields (c_FCH_Type_Specific_Fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->FOR_FCH_RC_MAP.value != NULL) {
		EDFree (sp->FOR_FCH_RC_MAP.value);
		sp->FOR_FCH_RC_MAP.value = NULL;
	}
	sp->FOR_FCH_RC_MAP.usedBits = 0;
	if (sp->REV_FCH_RC_MAP.value != NULL) {
		EDFree (sp->REV_FCH_RC_MAP.value);
		sp->REV_FCH_RC_MAP.value = NULL;
	}
	sp->REV_FCH_RC_MAP.usedBits = 0;

}

/* Free function for struct c_DCCH_Type_Specific_Fields */
void FREE_c_DCCH_Type_Specific_Fields (c_DCCH_Type_Specific_Fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->FOR_DCCH_RC_MAP.value != NULL) {
		EDFree (sp->FOR_DCCH_RC_MAP.value);
		sp->FOR_DCCH_RC_MAP.value = NULL;
	}
	sp->FOR_DCCH_RC_MAP.usedBits = 0;
	if (sp->REV_DCCH_RC_MAP.value != NULL) {
		EDFree (sp->REV_DCCH_RC_MAP.value);
		sp->REV_DCCH_RC_MAP.value = NULL;
	}
	sp->REV_DCCH_RC_MAP.usedBits = 0;

}

/* Free function for variable-sized sequence c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data */
void FREE_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data (c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields */
void FREE_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields (c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->FOR_SCH_RC_MAP.value != NULL) {
		EDFree (sp->FOR_SCH_RC_MAP.value);
		sp->FOR_SCH_RC_MAP.value = NULL;
	}
	sp->FOR_SCH_RC_MAP.usedBits = 0;
	for (i0=0; i0<sp->FOR_SCH_Type_Specific_Fields_data.items; i0++) {
		FREE_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data (sp->FOR_SCH_Type_Specific_Fields_data.data[i0]);
		EDFree (sp->FOR_SCH_Type_Specific_Fields_data.data[i0]);
	}
	if (sp->FOR_SCH_Type_Specific_Fields_data.data) {EDFree (sp->FOR_SCH_Type_Specific_Fields_data.data); sp->FOR_SCH_Type_Specific_Fields_data.data = NULL; sp->FOR_SCH_Type_Specific_Fields_data.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data */
void FREE_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data (c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields */
void FREE_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields (c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->REV_SCH_RC_MAP.value != NULL) {
		EDFree (sp->REV_SCH_RC_MAP.value);
		sp->REV_SCH_RC_MAP.value = NULL;
	}
	sp->REV_SCH_RC_MAP.usedBits = 0;
	for (i0=0; i0<sp->REV_SCH_Type_Specific_Fields_data.items; i0++) {
		FREE_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data (sp->REV_SCH_Type_Specific_Fields_data.data[i0]);
		EDFree (sp->REV_SCH_Type_Specific_Fields_data.data[i0]);
	}
	if (sp->REV_SCH_Type_Specific_Fields_data.data) {EDFree (sp->REV_SCH_Type_Specific_Fields_data.data); sp->REV_SCH_Type_Specific_Fields_data.data = NULL; sp->REV_SCH_Type_Specific_Fields_data.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_FOR_PDCH_Type_Specific_Fields */
void FREE_c_FOR_PDCH_Type_Specific_Fields (c_FOR_PDCH_Type_Specific_Fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->FOR_PDCH_RC_MAP.value != NULL) {
		EDFree (sp->FOR_PDCH_RC_MAP.value);
		sp->FOR_PDCH_RC_MAP.value = NULL;
	}
	sp->FOR_PDCH_RC_MAP.usedBits = 0;
	if (sp->CH_CONFIG_SUP_MAP.value != NULL) {
		EDFree (sp->CH_CONFIG_SUP_MAP.value);
		sp->CH_CONFIG_SUP_MAP.value = NULL;
	}
	sp->CH_CONFIG_SUP_MAP.usedBits = 0;

}

/* Free function for struct c_REV_PDCH_Type_Specific_Fields */
void FREE_c_REV_PDCH_Type_Specific_Fields (c_REV_PDCH_Type_Specific_Fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->REV_PDCH_RC_MAP.value != NULL) {
		EDFree (sp->REV_PDCH_RC_MAP.value);
		sp->REV_PDCH_RC_MAP.value = NULL;
	}
	sp->REV_PDCH_RC_MAP.usedBits = 0;
	if (sp->REV_PDCH_CH_CONFIG_SUP_MAP.value != NULL) {
		EDFree (sp->REV_PDCH_CH_CONFIG_SUP_MAP.value);
		sp->REV_PDCH_CH_CONFIG_SUP_MAP.value = NULL;
	}
	sp->REV_PDCH_CH_CONFIG_SUP_MAP.usedBits = 0;

}

/* Free function for struct c_FUNDICATED_BCMC_Type_Specific_Fields */
void FREE_c_FUNDICATED_BCMC_Type_Specific_Fields (c_FUNDICATED_BCMC_Type_Specific_Fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->FUNDICATED_BCMC_CH_SUP_MAP.value != NULL) {
		EDFree (sp->FUNDICATED_BCMC_CH_SUP_MAP.value);
		sp->FUNDICATED_BCMC_CH_SUP_MAP.value = NULL;
	}
	sp->FUNDICATED_BCMC_CH_SUP_MAP.usedBits = 0;

}

/* Free function for struct c_Channel_Configuration_for_the_Supplemental_Channel */
void FREE_c_Channel_Configuration_for_the_Supplemental_Channel (c_Channel_Configuration_for_the_Supplemental_Channel* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->infoRec1_Present) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1 (sp->infoRec1);
		EDFree (sp->infoRec1);
		sp->infoRec1_Present = ED_FALSE;
	}
	if (sp->infoRec2_Present) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2 (sp->infoRec2);
		EDFree (sp->infoRec2);
		sp->infoRec2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->infoRec1_Present == present) return;
	sp->infoRec1_Present = present;
	if (present) {
		/*-->*/sp->infoRec1 = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1));
		INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1 (sp->infoRec1);
	} else {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1 (sp->infoRec1);
		EDFree (sp->infoRec1);
	}
}

void SETPRESENT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->infoRec2_Present == present) return;
	sp->infoRec2_Present = present;
	if (present) {
		/*-->*/sp->infoRec2 = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2));
		INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2 (sp->infoRec2);
	} else {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec2 (sp->infoRec2);
		EDFree (sp->infoRec2);
	}
}

/* Free function for struct c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}
	if (sp->c04_Present) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
		sp->c04_Present = ED_FALSE;
	}
	if (sp->c05_Present) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05 (sp->c05);
		EDFree (sp->c05);
		sp->c05_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c01 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c01*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c01));
		INIT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c01 (sp->c01);
	} else {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c02 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c02*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c02));
		INIT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c02 (sp->c02);
	} else {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c03 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c03*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c03));
		INIT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c03 (sp->c03);
	} else {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

void SETPRESENT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c04 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c04_Present == present) return;
	sp->c04_Present = present;
	if (present) {
		/*-->*/sp->c04 = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c04*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c04));
		INIT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c04 (sp->c04);
	} else {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
	}
}

void SETPRESENT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05 (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c05_Present == present) return;
	sp->c05_Present = present;
	if (present) {
		/*-->*/sp->c05 = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05));
		INIT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05 (sp->c05);
	} else {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields_c05 (sp->c05);
		EDFree (sp->c05);
	}
}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}
	if (sp->c04_Present) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
		sp->c04_Present = ED_FALSE;
	}
	if (sp->c05_Present) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05 (sp->c05);
		EDFree (sp->c05);
		sp->c05_Present = ED_FALSE;
	}
	if (sp->c06_Present) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06 (sp->c06);
		EDFree (sp->c06);
		sp->c06_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c01 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c01*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c01));
		INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c01 (sp->c01);
	} else {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c02 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c02*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c02));
		INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c02 (sp->c02);
	} else {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c03 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c03*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c03));
		INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c03 (sp->c03);
	} else {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c04 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c04_Present == present) return;
	sp->c04_Present = present;
	if (present) {
		/*-->*/sp->c04 = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c04*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c04));
		INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c04 (sp->c04);
	} else {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
	}
}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c05_Present == present) return;
	sp->c05_Present = present;
	if (present) {
		/*-->*/sp->c05 = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05));
		INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05 (sp->c05);
	} else {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c05 (sp->c05);
		EDFree (sp->c05);
	}
}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c06_Present == present) return;
	sp->c06_Present = present;
	if (present) {
		/*-->*/sp->c06 = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06));
		INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06 (sp->c06);
	} else {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06 (sp->c06);
		EDFree (sp->c06);
	}
}

/* Free function for struct c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data */
void FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data */
void FREE_c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->BCMC_FLOW_DISCRIMINATOR.value);
		sp->BCMC_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->BCMC_FLOW_DISCRIMINATOR.usedBits = 0;

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data_Type_specific_fields (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data_Type_specific_fields (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data_Type_specific_fields (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data_Type_specific_fields (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data_Type_specific_fields (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data_Type_specific_fields (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c00 */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c00 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c00* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c01 */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c01 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c01* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH.value);
		sp->AUX_PILOT_WALSH.value = NULL;
	}
	sp->AUX_PILOT_WALSH.usedBits = 0;

}

/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c02 */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c02 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c02* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_WALSH.value != NULL) {
		EDFree (sp->AUX_WALSH.value);
		sp->AUX_WALSH.value = NULL;
	}
	sp->AUX_WALSH.usedBits = 0;

}

/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c03 */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c03 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c03* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04 */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH.value);
		sp->AUX_PILOT_WALSH.value = NULL;
	}
	sp->AUX_PILOT_WALSH.usedBits = 0;
	if (sp->AUX_PILOT_WALSH1_Present) {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
		sp->AUX_PILOT_WALSH1_Present = ED_FALSE;
	}
	if (sp->AUX_PILOT_WALSH2_Present) {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
		sp->AUX_PILOT_WALSH2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04_AUX_PILOT_WALSH1 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH1_Present == present) return;
	sp->AUX_PILOT_WALSH1_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH1 = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04_AUX_PILOT_WALSH1*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04_AUX_PILOT_WALSH1));
		sp->AUX_PILOT_WALSH1->value = NULL;
		sp->AUX_PILOT_WALSH1->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
	}
}

void SETPRESENT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04_AUX_PILOT_WALSH2 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH2_Present == present) return;
	sp->AUX_PILOT_WALSH2_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH2 = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04_AUX_PILOT_WALSH2*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04_AUX_PILOT_WALSH2));
		sp->AUX_PILOT_WALSH2->value = NULL;
		sp->AUX_PILOT_WALSH2->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
	}
}

/* Free function for variable-sized sequence c_RIR_Keypad_Facility_CHARi */
void FREE_c_RIR_Keypad_Facility_CHARi (c_RIR_Keypad_Facility_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Keypad_Facility */
void FREE_c_RIR_Keypad_Facility (c_RIR_Keypad_Facility* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Called_Party_Number_CHARi */
void FREE_c_RIR_Called_Party_Number_CHARi (c_RIR_Called_Party_Number_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Called_Party_Number */
void FREE_c_RIR_Called_Party_Number (c_RIR_Called_Party_Number* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Calling_Party_Number_CHARi */
void FREE_c_RIR_Calling_Party_Number_CHARi (c_RIR_Calling_Party_Number_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Calling_Party_Number */
void FREE_c_RIR_Calling_Party_Number (c_RIR_Calling_Party_Number* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_RIR_Call_Mode */
void FREE_c_RIR_Call_Mode (c_RIR_Call_Mode* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_RIR_Terminal_Information_SERVICE_OPTION */
void FREE_c_RIR_Terminal_Information_SERVICE_OPTION (c_RIR_Terminal_Information_SERVICE_OPTION* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Terminal_Information */
void FREE_c_RIR_Terminal_Information (c_RIR_Terminal_Information* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->SERVICE_OPTION.data) {EDFree (sp->SERVICE_OPTION.data); sp->SERVICE_OPTION.data = NULL; sp->SERVICE_OPTION.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Roaming_Information_SID_NID */
void FREE_c_RIR_Roaming_Information_SID_NID (c_RIR_Roaming_Information_SID_NID* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Roaming_Information_SID_NID_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Roaming_Information */
void FREE_c_RIR_Roaming_Information (c_RIR_Roaming_Information* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SID_NID.items; i0++) {
		FREE_c_RIR_Roaming_Information_SID_NID_data (sp->SID_NID.data[i0]);
		EDFree (sp->SID_NID.data[i0]);
	}
	if (sp->SID_NID.data) {EDFree (sp->SID_NID.data); sp->SID_NID.data = NULL; sp->SID_NID.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_RIR_Security_Status */
void FREE_c_RIR_Security_Status (c_RIR_Security_Status* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_RIR_Connected_Number_CHARi */
void FREE_c_RIR_Connected_Number_CHARi (c_RIR_Connected_Number_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Connected_Number */
void FREE_c_RIR_Connected_Number (c_RIR_Connected_Number* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_RIR_IMSI */
void FREE_c_RIR_IMSI (c_RIR_IMSI* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_ESN */
void FREE_c_RIR_ESN (c_RIR_ESN* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Band_Class_Information */
void FREE_c_RIR_Band_Class_Information (c_RIR_Band_Class_Information* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Power_Class_Information */
void FREE_c_RIR_Power_Class_Information (c_RIR_Power_Class_Information* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Operating_Mode_Information */
void FREE_c_RIR_Operating_Mode_Information (c_RIR_Operating_Mode_Information* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_RIR_Service_Option_Information_info */
void FREE_c_RIR_Service_Option_Information_info (c_RIR_Service_Option_Information_info* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Service_Option_Information_Data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Service_Option_Information */
void FREE_c_RIR_Service_Option_Information (c_RIR_Service_Option_Information* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->info.items; i0++) {
		FREE_c_RIR_Service_Option_Information_Data (sp->info.data[i0]);
		EDFree (sp->info.data[i0]);
	}
	if (sp->info.data) {EDFree (sp->info.data); sp->info.data = NULL; sp->info.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Multiplex_Option_Information_info */
void FREE_c_RIR_Multiplex_Option_Information_info (c_RIR_Multiplex_Option_Information_info* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Multiplex_Option_Information_Data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Multiplex_Option_Information */
void FREE_c_RIR_Multiplex_Option_Information (c_RIR_Multiplex_Option_Information* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->info.items; i0++) {
		FREE_c_RIR_Multiplex_Option_Information_Data (sp->info.data[i0]);
		EDFree (sp->info.data[i0]);
	}
	if (sp->info.data) {EDFree (sp->info.data); sp->info.data = NULL; sp->info.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Service_Configuration_Information_CON_RECs */
void FREE_c_RIR_Service_Configuration_Information_CON_RECs (c_RIR_Service_Configuration_Information_CON_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Service_Configuration_Information_CON_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Service_Configuration_Information_FOR_SCHs */
void FREE_c_RIR_Service_Configuration_Information_FOR_SCHs (c_RIR_Service_Configuration_Information_FOR_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Service_Configuration_Information_FOR_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Service_Configuration_Information_REV_SCHs */
void FREE_c_RIR_Service_Configuration_Information_REV_SCHs (c_RIR_Service_Configuration_Information_REV_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Service_Configuration_Information_REV_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Service_Configuration_Information */
void FREE_c_RIR_Service_Configuration_Information (c_RIR_Service_Configuration_Information* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->CON_RECs.items; i0++) {
		FREE_c_RIR_Service_Configuration_Information_CON_RECs_data (sp->CON_RECs.data[i0]);
		EDFree (sp->CON_RECs.data[i0]);
	}
	if (sp->CON_RECs.data) {EDFree (sp->CON_RECs.data); sp->CON_RECs.data = NULL; sp->CON_RECs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FOR_SCHs.items; i0++) {
		FREE_c_RIR_Service_Configuration_Information_FOR_SCHs_data (sp->FOR_SCHs.data[i0]);
		EDFree (sp->FOR_SCHs.data[i0]);
	}
	if (sp->FOR_SCHs.data) {EDFree (sp->FOR_SCHs.data); sp->FOR_SCHs.data = NULL; sp->FOR_SCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs.items; i0++) {
		FREE_c_RIR_Service_Configuration_Information_REV_SCHs_data (sp->REV_SCHs.data[i0]);
		EDFree (sp->REV_SCHs.data[i0]);
	}
	if (sp->REV_SCHs.data) {EDFree (sp->REV_SCHs.data); sp->REV_SCHs.data = NULL; sp->REV_SCHs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Called_Party_Subaddress_CHARi */
void FREE_c_RIR_Called_Party_Subaddress_CHARi (c_RIR_Called_Party_Subaddress_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Called_Party_Subaddress */
void FREE_c_RIR_Called_Party_Subaddress (c_RIR_Called_Party_Subaddress* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Calling_Party_Subaddress_CHARi */
void FREE_c_RIR_Calling_Party_Subaddress_CHARi (c_RIR_Calling_Party_Subaddress_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Calling_Party_Subaddress */
void FREE_c_RIR_Calling_Party_Subaddress (c_RIR_Calling_Party_Subaddress* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Connected_Subaddress_CHARi */
void FREE_c_RIR_Connected_Subaddress_CHARi (c_RIR_Connected_Subaddress_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Connected_Subaddress */
void FREE_c_RIR_Connected_Subaddress (c_RIR_Connected_Subaddress* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_RIR_Power_Control_Information */
void FREE_c_RIR_Power_Control_Information (c_RIR_Power_Control_Information* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_IMSI_M */
void FREE_c_RIR_IMSI_M (c_RIR_IMSI_M* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_IMSI_T */
void FREE_c_RIR_IMSI_T (c_RIR_IMSI_T* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Capability_Information */
void FREE_c_RIR_Capability_Information (c_RIR_Capability_Information* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->RLP_BLOB.value != NULL) {
		EDFree (sp->RLP_BLOB.value);
		sp->RLP_BLOB.value = NULL;
	}
	sp->RLP_BLOB.usedBits = 0;

}

/* Free function for struct c_RIR_Extended_Record_Type_International */
void FREE_c_RIR_Extended_Record_Type_International (c_RIR_Extended_Record_Type_International* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->RECORD.value != NULL) {
		EDFree (sp->RECORD.value);
		sp->RECORD.value = NULL;
	}
	sp->RECORD.usedBits = 0;

}

/* Free function for struct c_RIR_Channel_Configuration_Capability_Information */
void FREE_c_RIR_Channel_Configuration_Capability_Information (c_RIR_Channel_Configuration_Capability_Information* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->FCH_Type_specific_fields_Present) {
		FREE_c_FCH_Type_Specific_Fields (sp->FCH_Type_specific_fields);
		EDFree (sp->FCH_Type_specific_fields);
		sp->FCH_Type_specific_fields_Present = ED_FALSE;
	}
	if (sp->DCCH_Type_specific_fields_Present) {
		FREE_c_DCCH_Type_Specific_Fields (sp->DCCH_Type_specific_fields);
		EDFree (sp->DCCH_Type_specific_fields);
		sp->DCCH_Type_specific_fields_Present = ED_FALSE;
	}
	if (sp->FOR_SCH_Type_specific_fields_Present) {
		FREE_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields (sp->FOR_SCH_Type_specific_fields);
		EDFree (sp->FOR_SCH_Type_specific_fields);
		sp->FOR_SCH_Type_specific_fields_Present = ED_FALSE;
	}
	if (sp->REV_SCH_Type_specific_fields_Present) {
		FREE_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields (sp->REV_SCH_Type_specific_fields);
		EDFree (sp->REV_SCH_Type_specific_fields);
		sp->REV_SCH_Type_specific_fields_Present = ED_FALSE;
	}
	if (sp->FOR_PDCH_Capability_Type_specific_fields_Present) {
		FREE_c_FOR_PDCH_Type_Specific_Fields (sp->FOR_PDCH_Capability_Type_specific_fields);
		EDFree (sp->FOR_PDCH_Capability_Type_specific_fields);
		sp->FOR_PDCH_Capability_Type_specific_fields_Present = ED_FALSE;
	}
	if (sp->REV_PDCH_Capability_Type_specific_fields_Present) {
		FREE_c_REV_PDCH_Type_Specific_Fields (sp->REV_PDCH_Capability_Type_specific_fields);
		EDFree (sp->REV_PDCH_Capability_Type_specific_fields);
		sp->REV_PDCH_Capability_Type_specific_fields_Present = ED_FALSE;
	}
	if (sp->FUNDICATED_BCMC_Type_specific_fields_Present) {
		FREE_c_FUNDICATED_BCMC_Type_Specific_Fields (sp->FUNDICATED_BCMC_Type_specific_fields);
		EDFree (sp->FUNDICATED_BCMC_Type_specific_fields);
		sp->FUNDICATED_BCMC_Type_specific_fields_Present = ED_FALSE;
	}

}

void SETPRESENT_c_RIR_Channel_Configuration_Capability_Information_FCH_Type_specific_fields (c_RIR_Channel_Configuration_Capability_Information* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FCH_Type_specific_fields_Present == present) return;
	sp->FCH_Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->FCH_Type_specific_fields = (c_FCH_Type_Specific_Fields*)EDAlloc (sizeof (c_FCH_Type_Specific_Fields));
		INIT_c_FCH_Type_Specific_Fields (sp->FCH_Type_specific_fields);
	} else {
		FREE_c_FCH_Type_Specific_Fields (sp->FCH_Type_specific_fields);
		EDFree (sp->FCH_Type_specific_fields);
	}
}

void SETPRESENT_c_RIR_Channel_Configuration_Capability_Information_DCCH_Type_specific_fields (c_RIR_Channel_Configuration_Capability_Information* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->DCCH_Type_specific_fields_Present == present) return;
	sp->DCCH_Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->DCCH_Type_specific_fields = (c_DCCH_Type_Specific_Fields*)EDAlloc (sizeof (c_DCCH_Type_Specific_Fields));
		INIT_c_DCCH_Type_Specific_Fields (sp->DCCH_Type_specific_fields);
	} else {
		FREE_c_DCCH_Type_Specific_Fields (sp->DCCH_Type_specific_fields);
		EDFree (sp->DCCH_Type_specific_fields);
	}
}

void SETPRESENT_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields (c_RIR_Channel_Configuration_Capability_Information* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FOR_SCH_Type_specific_fields_Present == present) return;
	sp->FOR_SCH_Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->FOR_SCH_Type_specific_fields = (c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields*)EDAlloc (sizeof (c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields));
		INIT_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields (sp->FOR_SCH_Type_specific_fields);
	} else {
		FREE_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields (sp->FOR_SCH_Type_specific_fields);
		EDFree (sp->FOR_SCH_Type_specific_fields);
	}
}

void SETPRESENT_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields (c_RIR_Channel_Configuration_Capability_Information* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->REV_SCH_Type_specific_fields_Present == present) return;
	sp->REV_SCH_Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->REV_SCH_Type_specific_fields = (c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields*)EDAlloc (sizeof (c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields));
		INIT_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields (sp->REV_SCH_Type_specific_fields);
	} else {
		FREE_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields (sp->REV_SCH_Type_specific_fields);
		EDFree (sp->REV_SCH_Type_specific_fields);
	}
}

void SETPRESENT_c_RIR_Channel_Configuration_Capability_Information_FOR_PDCH_Capability_Type_specific_fields (c_RIR_Channel_Configuration_Capability_Information* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FOR_PDCH_Capability_Type_specific_fields_Present == present) return;
	sp->FOR_PDCH_Capability_Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->FOR_PDCH_Capability_Type_specific_fields = (c_FOR_PDCH_Type_Specific_Fields*)EDAlloc (sizeof (c_FOR_PDCH_Type_Specific_Fields));
		INIT_c_FOR_PDCH_Type_Specific_Fields (sp->FOR_PDCH_Capability_Type_specific_fields);
	} else {
		FREE_c_FOR_PDCH_Type_Specific_Fields (sp->FOR_PDCH_Capability_Type_specific_fields);
		EDFree (sp->FOR_PDCH_Capability_Type_specific_fields);
	}
}

void SETPRESENT_c_RIR_Channel_Configuration_Capability_Information_REV_PDCH_Capability_Type_specific_fields (c_RIR_Channel_Configuration_Capability_Information* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->REV_PDCH_Capability_Type_specific_fields_Present == present) return;
	sp->REV_PDCH_Capability_Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->REV_PDCH_Capability_Type_specific_fields = (c_REV_PDCH_Type_Specific_Fields*)EDAlloc (sizeof (c_REV_PDCH_Type_Specific_Fields));
		INIT_c_REV_PDCH_Type_Specific_Fields (sp->REV_PDCH_Capability_Type_specific_fields);
	} else {
		FREE_c_REV_PDCH_Type_Specific_Fields (sp->REV_PDCH_Capability_Type_specific_fields);
		EDFree (sp->REV_PDCH_Capability_Type_specific_fields);
	}
}

void SETPRESENT_c_RIR_Channel_Configuration_Capability_Information_FUNDICATED_BCMC_Type_specific_fields (c_RIR_Channel_Configuration_Capability_Information* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FUNDICATED_BCMC_Type_specific_fields_Present == present) return;
	sp->FUNDICATED_BCMC_Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->FUNDICATED_BCMC_Type_specific_fields = (c_FUNDICATED_BCMC_Type_Specific_Fields*)EDAlloc (sizeof (c_FUNDICATED_BCMC_Type_Specific_Fields));
		INIT_c_FUNDICATED_BCMC_Type_Specific_Fields (sp->FUNDICATED_BCMC_Type_specific_fields);
	} else {
		FREE_c_FUNDICATED_BCMC_Type_Specific_Fields (sp->FUNDICATED_BCMC_Type_specific_fields);
		EDFree (sp->FUNDICATED_BCMC_Type_specific_fields);
	}
}

/* Free function for variable-sized sequence c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH (c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Extended_Multiplex_Option_Information_MO_FOR_DCCH */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_DCCH (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_DCCH* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Extended_Multiplex_Option_Information_MO_REV_DCCH */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_DCCH (c_RIR_Extended_Multiplex_Option_Information_MO_REV_DCCH* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH (c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Extended_Multiplex_Option_Information_MO_FOR_PDCH */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_PDCH (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_PDCH* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Extended_Multiplex_Option_Information_MO_REV_PDCH */
void FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_PDCH (c_RIR_Extended_Multiplex_Option_Information_MO_REV_PDCH* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Extended_Multiplex_Option_Information */
void FREE_c_RIR_Extended_Multiplex_Option_Information (c_RIR_Extended_Multiplex_Option_Information* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->MO_FOR_FCH.items; i0++) {
		FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data (sp->MO_FOR_FCH.data[i0]);
		EDFree (sp->MO_FOR_FCH.data[i0]);
	}
	if (sp->MO_FOR_FCH.data) {EDFree (sp->MO_FOR_FCH.data); sp->MO_FOR_FCH.data = NULL; sp->MO_FOR_FCH.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->MO_REV_FCH.items; i0++) {
		FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data (sp->MO_REV_FCH.data[i0]);
		EDFree (sp->MO_REV_FCH.data[i0]);
	}
	if (sp->MO_REV_FCH.data) {EDFree (sp->MO_REV_FCH.data); sp->MO_REV_FCH.data = NULL; sp->MO_REV_FCH.allocatedItems=0; /*FR02A*/}
	if (sp->MO_FOR_DCCH.data) {EDFree (sp->MO_FOR_DCCH.data); sp->MO_FOR_DCCH.data = NULL; sp->MO_FOR_DCCH.allocatedItems=0; /*FR02A*/}
	if (sp->MO_REV_DCCH.data) {EDFree (sp->MO_REV_DCCH.data); sp->MO_REV_DCCH.data = NULL; sp->MO_REV_DCCH.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->MO_FOR_SCH.items; i0++) {
		FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data (sp->MO_FOR_SCH.data[i0]);
		EDFree (sp->MO_FOR_SCH.data[i0]);
	}
	if (sp->MO_FOR_SCH.data) {EDFree (sp->MO_FOR_SCH.data); sp->MO_FOR_SCH.data = NULL; sp->MO_FOR_SCH.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->MO_REV_SCH.items; i0++) {
		FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data (sp->MO_REV_SCH.data[i0]);
		EDFree (sp->MO_REV_SCH.data[i0]);
	}
	if (sp->MO_REV_SCH.data) {EDFree (sp->MO_REV_SCH.data); sp->MO_REV_SCH.data = NULL; sp->MO_REV_SCH.allocatedItems=0; /*FR02A*/}
	if (sp->MO_FOR_PDCH.data) {EDFree (sp->MO_FOR_PDCH.data); sp->MO_FOR_PDCH.data = NULL; sp->MO_FOR_PDCH.allocatedItems=0; /*FR02A*/}
	if (sp->MO_REV_PDCH.data) {EDFree (sp->MO_REV_PDCH.data); sp->MO_REV_PDCH.data = NULL; sp->MO_REV_PDCH.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_RIR_Geo_Location_Capability */
void FREE_c_RIR_Geo_Location_Capability (c_RIR_Geo_Location_Capability* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Band_Subclass_Information */
void FREE_c_RIR_Band_Subclass_Information (c_RIR_Band_Subclass_Information* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->BAND_SUBCLASS_INFO.value != NULL) {
		EDFree (sp->BAND_SUBCLASS_INFO.value);
		sp->BAND_SUBCLASS_INFO.value = NULL;
	}
	sp->BAND_SUBCLASS_INFO.usedBits = 0;

}

/* Free function for variable-sized sequence c_RIR_Global_Emergency_Call_CHARi */
void FREE_c_RIR_Global_Emergency_Call_CHARi (c_RIR_Global_Emergency_Call_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		if (sp->data[i]->value != NULL) {
			EDFree (sp->data[i]->value);
			sp->data[i]->value = NULL;
		}
		sp->data[i]->usedBits = 0;
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Global_Emergency_Call */
void FREE_c_RIR_Global_Emergency_Call (c_RIR_Global_Emergency_Call* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->CHARi.items; i0++) {
		if (sp->CHARi.data[i0]->value != NULL) {
			EDFree (sp->CHARi.data[i0]->value);
			sp->CHARi.data[i0]->value = NULL;
		}
		sp->CHARi.data[i0]->usedBits = 0;
		EDFree (sp->CHARi.data[i0]);
	}
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_RIR_Hook_Status */
void FREE_c_RIR_Hook_Status (c_RIR_Hook_Status* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_QoS_Parameters */
void FREE_c_RIR_QoS_Parameters (c_RIR_QoS_Parameters* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->QoS.value != NULL) {
		EDFree (sp->QoS.value);
		sp->QoS.value = NULL;
	}
	sp->QoS.usedBits = 0;

}

/* Free function for struct c_RIR_Encryption_Capability */
void FREE_c_RIR_Encryption_Capability (c_RIR_Encryption_Capability* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_Signaling_Message_Integrity_Capability */
void FREE_c_RIR_Signaling_Message_Integrity_Capability (c_RIR_Signaling_Message_Integrity_Capability* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_RIR_UIM_ID */
void FREE_c_RIR_UIM_ID (c_RIR_UIM_ID* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->UIM_ID.value != NULL) {
		EDFree (sp->UIM_ID.value);
		sp->UIM_ID.value = NULL;
	}
	sp->UIM_ID.usedBits = 0;

}

/* Free function for struct c_RIR_ESN_ME */
void FREE_c_RIR_ESN_ME (c_RIR_ESN_ME* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->ESN_ME.value != NULL) {
		EDFree (sp->ESN_ME.value);
		sp->ESN_ME.value = NULL;
	}
	sp->ESN_ME.usedBits = 0;

}

/* Free function for struct c_RIR_MEID */
void FREE_c_RIR_MEID (c_RIR_MEID* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->MEID.value != NULL) {
		EDFree (sp->MEID.value);
		sp->MEID.value = NULL;
	}
	sp->MEID.usedBits = 0;

}

/* Free function for variable-sized sequence c_RIR_Extended_Keypad_Facility_CHARi */
void FREE_c_RIR_Extended_Keypad_Facility_CHARi (c_RIR_Extended_Keypad_Facility_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Extended_Keypad_Facility */
void FREE_c_RIR_Extended_Keypad_Facility (c_RIR_Extended_Keypad_Facility* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_SYNC_ID_SYNC_IDs */
void FREE_c_RIR_SYNC_ID_SYNC_IDs (c_RIR_SYNC_ID_SYNC_IDs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_SYNC_ID_SYNC_IDs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_SYNC_ID */
void FREE_c_RIR_SYNC_ID (c_RIR_SYNC_ID* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SYNC_IDs.items; i0++) {
		FREE_c_RIR_SYNC_ID_SYNC_IDs_data (sp->SYNC_IDs.data[i0]);
		EDFree (sp->SYNC_IDs.data[i0]);
	}
	if (sp->SYNC_IDs.data) {EDFree (sp->SYNC_IDs.data); sp->SYNC_IDs.data = NULL; sp->SYNC_IDs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Extended_Terminal_Information_SERVICE_OPTION */
void FREE_c_RIR_Extended_Terminal_Information_SERVICE_OPTION (c_RIR_Extended_Terminal_Information_SERVICE_OPTION* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Extended_Terminal_Information_SO_GROUP */
void FREE_c_RIR_Extended_Terminal_Information_SO_GROUP (c_RIR_Extended_Terminal_Information_SO_GROUP* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Extended_Terminal_Information_SO_GROUP_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Extended_Terminal_Information */
void FREE_c_RIR_Extended_Terminal_Information (c_RIR_Extended_Terminal_Information* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->SERVICE_OPTION.data) {EDFree (sp->SERVICE_OPTION.data); sp->SERVICE_OPTION.data = NULL; sp->SERVICE_OPTION.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->SO_GROUP.items; i0++) {
		FREE_c_RIR_Extended_Terminal_Information_SO_GROUP_data (sp->SO_GROUP.data[i0]);
		EDFree (sp->SO_GROUP.data[i0]);
	}
	if (sp->SO_GROUP.data) {EDFree (sp->SO_GROUP.data); sp->SO_GROUP.data = NULL; sp->SO_GROUP.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Extended_Service_Option_Information_SO_GROUP */
void FREE_c_RIR_Extended_Service_Option_Information_SO_GROUP (c_RIR_Extended_Service_Option_Information_SO_GROUP* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Extended_Service_Option_Information_SO_GROUP_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Extended_Service_Option_Information_supports */
void FREE_c_RIR_Extended_Service_Option_Information_supports (c_RIR_Extended_Service_Option_Information_supports* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Extended_Service_Option_Information_supports_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Extended_Service_Option_Information */
void FREE_c_RIR_Extended_Service_Option_Information (c_RIR_Extended_Service_Option_Information* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SO_GROUP.items; i0++) {
		FREE_c_RIR_Extended_Service_Option_Information_SO_GROUP_data (sp->SO_GROUP.data[i0]);
		EDFree (sp->SO_GROUP.data[i0]);
	}
	if (sp->SO_GROUP.data) {EDFree (sp->SO_GROUP.data); sp->SO_GROUP.data = NULL; sp->SO_GROUP.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->supports.items; i0++) {
		FREE_c_RIR_Extended_Service_Option_Information_supports_data (sp->supports.data[i0]);
		EDFree (sp->supports.data[i0]);
	}
	if (sp->supports.data) {EDFree (sp->supports.data); sp->supports.data = NULL; sp->supports.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS */
void FREE_c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS (c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Band_Class_and_Band_Subclass_Information */
void FREE_c_RIR_Band_Class_and_Band_Subclass_Information (c_RIR_Band_Class_and_Band_Subclass_Information* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->BAND_CLASS.items; i0++) {
		FREE_c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data (sp->BAND_CLASS.data[i0]);
		EDFree (sp->BAND_CLASS.data[i0]);
	}
	if (sp->BAND_CLASS.data) {EDFree (sp->BAND_CLASS.data); sp->BAND_CLASS.data = NULL; sp->BAND_CLASS.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_RIR_EXT_UIM_ID */
void FREE_c_RIR_EXT_UIM_ID (c_RIR_EXT_UIM_ID* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->EXT_UIM_ID.value != NULL) {
		EDFree (sp->EXT_UIM_ID.value);
		sp->EXT_UIM_ID.value = NULL;
	}
	sp->EXT_UIM_ID.usedBits = 0;

}

/* Free function for struct c_RIR_MEID_ME */
void FREE_c_RIR_MEID_ME (c_RIR_MEID_ME* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->MEID_ME.value != NULL) {
		EDFree (sp->MEID_ME.value);
		sp->MEID_ME.value = NULL;
	}
	sp->MEID_ME.usedBits = 0;

}

/* Free function for struct c_RIR_Additional_Geo_Location_Capability */
void FREE_c_RIR_Additional_Geo_Location_Capability (c_RIR_Additional_Geo_Location_Capability* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->ADD_GEO_LOC.value != NULL) {
		EDFree (sp->ADD_GEO_LOC.value);
		sp->ADD_GEO_LOC.value = NULL;
	}
	sp->ADD_GEO_LOC.usedBits = 0;

}

/* Free function for struct c_RIR_MEID_SIG */
void FREE_c_RIR_MEID_SIG (c_RIR_MEID_SIG* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->MEID_SIG_VAL.value != NULL) {
		EDFree (sp->MEID_SIG_VAL.value);
		sp->MEID_SIG_VAL.value = NULL;
	}
	sp->MEID_SIG_VAL.usedBits = 0;

}

/* Free function for struct c_InfoRec_Connected_Number_data_01_data */
void FREE_c_InfoRec_Connected_Number_data_01_data (c_InfoRec_Connected_Number_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Service_Configuration_CON_RECs_data */
void FREE_c_InfoRec_Service_Configuration_CON_RECs_data (c_InfoRec_Service_Configuration_CON_RECs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->RLP_BLOB_Present) {
		if (sp->RLP_BLOB->value != NULL) {
			EDFree (sp->RLP_BLOB->value);
			sp->RLP_BLOB->value = NULL;
		}
		sp->RLP_BLOB->usedBits = 0;
		EDFree (sp->RLP_BLOB);
		sp->RLP_BLOB_Present = ED_FALSE;
	}
	if (sp->QOS_PARMS_Present) {
		if (sp->QOS_PARMS->value != NULL) {
			EDFree (sp->QOS_PARMS->value);
			sp->QOS_PARMS->value = NULL;
		}
		sp->QOS_PARMS->usedBits = 0;
		EDFree (sp->QOS_PARMS);
		sp->QOS_PARMS_Present = ED_FALSE;
	}

}

void SETPRESENT_c_InfoRec_Service_Configuration_CON_RECs_data_RLP_BLOB (c_InfoRec_Service_Configuration_CON_RECs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->RLP_BLOB_Present == present) return;
	sp->RLP_BLOB_Present = present;
	if (present) {
		/*-->*/sp->RLP_BLOB = (c_InfoRec_Service_Configuration_CON_RECs_data_RLP_BLOB*)EDAlloc (sizeof (c_InfoRec_Service_Configuration_CON_RECs_data_RLP_BLOB));
		sp->RLP_BLOB->value = NULL;
		sp->RLP_BLOB->usedBits = 0;
	} else {
		if (sp->RLP_BLOB->value != NULL) {
			EDFree (sp->RLP_BLOB->value);
			sp->RLP_BLOB->value = NULL;
		}
		sp->RLP_BLOB->usedBits = 0;
		EDFree (sp->RLP_BLOB);
	}
}

void SETPRESENT_c_InfoRec_Service_Configuration_CON_RECs_data_QOS_PARMS (c_InfoRec_Service_Configuration_CON_RECs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->QOS_PARMS_Present == present) return;
	sp->QOS_PARMS_Present = present;
	if (present) {
		/*-->*/sp->QOS_PARMS = (c_InfoRec_Service_Configuration_CON_RECs_data_QOS_PARMS*)EDAlloc (sizeof (c_InfoRec_Service_Configuration_CON_RECs_data_QOS_PARMS));
		sp->QOS_PARMS->value = NULL;
		sp->QOS_PARMS->usedBits = 0;
	} else {
		if (sp->QOS_PARMS->value != NULL) {
			EDFree (sp->QOS_PARMS->value);
			sp->QOS_PARMS->value = NULL;
		}
		sp->QOS_PARMS->usedBits = 0;
		EDFree (sp->QOS_PARMS);
	}
}

/* Free function for struct c_InfoRec_Service_Configuration_FOR_SCHs_data */
void FREE_c_InfoRec_Service_Configuration_FOR_SCHs_data (c_InfoRec_Service_Configuration_FOR_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_Channel_Configuration_for_the_Supplemental_Channel (&(sp->SCH_CC_Type_specific_field));

}

/* Free function for struct c_InfoRec_Service_Configuration_REV_SCHs_data */
void FREE_c_InfoRec_Service_Configuration_REV_SCHs_data (c_InfoRec_Service_Configuration_REV_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_Channel_Configuration_for_the_Supplemental_Channel (&(sp->SCH_CC_Type_specific_field2));

}

/* Free function for struct c_InfoRec_Called_Party_Subaddress_data_01_data */
void FREE_c_InfoRec_Called_Party_Subaddress_data_01_data (c_InfoRec_Called_Party_Subaddress_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Calling_Party_Subaddress_data_01_data */
void FREE_c_InfoRec_Calling_Party_Subaddress_data_01_data (c_InfoRec_Calling_Party_Subaddress_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Connected_Subaddress_data_01_data */
void FREE_c_InfoRec_Connected_Subaddress_data_01_data (c_InfoRec_Connected_Subaddress_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Redirecting_Number_data_01_data */
void FREE_c_InfoRec_Redirecting_Number_data_01_data (c_InfoRec_Redirecting_Number_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Redirecting_Subaddress_data_01_data */
void FREE_c_InfoRec_Redirecting_Subaddress_data_01_data (c_InfoRec_Redirecting_Subaddress_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Parametric_Alerting_GROUPSs_data */
void FREE_c_InfoRec_Parametric_Alerting_GROUPSs_data (c_InfoRec_Parametric_Alerting_GROUPSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_InfoRec_Extended_Display_data_01_data_data_02 */
void FREE_c_InfoRec_Extended_Display_data_01_data_data_02 (c_InfoRec_Extended_Display_data_01_data_data_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Extended_Display_data_01_data_data_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Extended_Display_data_01_data */
void FREE_c_InfoRec_Extended_Display_data_01_data (c_InfoRec_Extended_Display_data_01_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_02.items; i0++) {
		FREE_c_InfoRec_Extended_Display_data_01_data_data_02_data (sp->data_02.data[i0]);
		EDFree (sp->data_02.data[i0]);
	}
	if (sp->data_02.data) {EDFree (sp->data_02.data); sp->data_02.data = NULL; sp->data_02.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data (c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05 (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_05.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data (sp->data_05.data[i0]);
		EDFree (sp->data_05.data[i0]);
	}
	if (sp->data_05.data) {EDFree (sp->data_05.data); sp->data_05.data = NULL; sp->data_05.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07 (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_07.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data (sp->data_07.data[i0]);
		EDFree (sp->data_07.data[i0]);
	}
	if (sp->data_07.data) {EDFree (sp->data_07.data); sp->data_07.data = NULL; sp->data_07.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->ROWSs.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data (sp->ROWSs.data[i0]);
		EDFree (sp->ROWSs.data[i0]);
	}
	if (sp->ROWSs.data) {EDFree (sp->ROWSs.data); sp->ROWSs.data = NULL; sp->ROWSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->ROWS.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data (sp->ROWS.data[i0]);
		EDFree (sp->ROWS.data[i0]);
	}
	if (sp->ROWS.data) {EDFree (sp->ROWS.data); sp->ROWS.data = NULL; sp->ROWS.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_PROGRAM_ID.value != NULL) {
		EDFree (sp->BCMC_PROGRAM_ID.value);
		sp->BCMC_PROGRAM_ID.value = NULL;
	}
	sp->BCMC_PROGRAM_ID.usedBits = 0;
	if (sp->NUM_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->NUM_FLOW_DISCRIMINATOR.value);
		sp->NUM_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->NUM_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->FLOW_DISCRIMINATORs.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->FLOW_DISCRIMINATORs.data[i0]);
		EDFree (sp->FLOW_DISCRIMINATORs.data[i0]);
	}
	if (sp->FLOW_DISCRIMINATORs.data) {EDFree (sp->FLOW_DISCRIMINATORs.data); sp->FLOW_DISCRIMINATORs.data = NULL; sp->FLOW_DISCRIMINATORs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data (c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs */
void FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Multiple_Character_Extended_Display_data_01_data */
void FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data (c_InfoRec_Multiple_Character_Extended_Display_data_01_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RECORDs.items; i0++) {
		FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data (sp->RECORDs.data[i0]);
		EDFree (sp->RECORDs.data[i0]);
	}
	if (sp->RECORDs.data) {EDFree (sp->RECORDs.data); sp->RECORDs.data = NULL; sp->RECORDs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs */
void FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data */
void FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RECORDs.items; i0++) {
		FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data (sp->RECORDs.data[i0]);
		EDFree (sp->RECORDs.data[i0]);
	}
	if (sp->RECORDs.data) {EDFree (sp->RECORDs.data); sp->RECORDs.data = NULL; sp->RECORDs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records_Type_specific_fields (&(sp->Type_specific_fields));

}

/* Free function for struct c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data */
void FREE_c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data (c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data */
void FREE_c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data (c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields (&(sp->Type_specific_fields));

}

/* Free function for struct c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data */
void FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data */
void FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data */
void FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data */
void FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.value != NULL) {
		EDFree (sp->CHARi.value);
		sp->CHARi.value = NULL;
	}
	sp->CHARi.usedBits = 0;

}

/* Free function for struct c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data */
void FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data */
void FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data (c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data */
void FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.value != NULL) {
		EDFree (sp->CHARi.value);
		sp->CHARi.value = NULL;
	}
	sp->CHARi.usedBits = 0;

}

/* Free function for struct c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data */
void FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_SCHs */
void FREE_c_f_dsch_UHDM_SCHs (c_f_dsch_UHDM_SCHs* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data */
void FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data (c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MUHDM_SCHs */
void FREE_c_f_dsch_MUHDM_SCHs (c_f_dsch_MUHDM_SCHs* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data */
void FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data (c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data */
void FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data (c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_Record_data_11 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_Record_data_11 (c_InfoRec_Non_Negotiable_Service_Configuration_Record_data_11* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration_Record */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_Record (c_InfoRec_Non_Negotiable_Service_Configuration_Record* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_11.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data (sp->data_11.data[i0]);
		EDFree (sp->data_11.data[i0]);
	}
	if (sp->data_11.data) {EDFree (sp->data_11.data); sp->data_11.data = NULL; sp->data_11.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c01 */
void FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c01 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c01* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c02 */
void FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c02 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c02* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH.value);
		sp->AUX_PILOT_WALSH.value = NULL;
	}
	sp->AUX_PILOT_WALSH.usedBits = 0;

}

/* Free function for struct c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c03 */
void FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c03 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c03* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_WALSH.value != NULL) {
		EDFree (sp->AUX_WALSH.value);
		sp->AUX_WALSH.value = NULL;
	}
	sp->AUX_WALSH.usedBits = 0;

}

/* Free function for struct c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c04 */
void FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c04 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c04* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05 */
void FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH___1.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH___1.value);
		sp->AUX_PILOT_WALSH___1.value = NULL;
	}
	sp->AUX_PILOT_WALSH___1.usedBits = 0;
	if (sp->AUX_PILOT_WALSH1_Present) {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
		sp->AUX_PILOT_WALSH1_Present = ED_FALSE;
	}
	if (sp->AUX_PILOT_WALSH2_Present) {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
		sp->AUX_PILOT_WALSH2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH1_Present == present) return;
	sp->AUX_PILOT_WALSH1_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH1 = (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1));
		sp->AUX_PILOT_WALSH1->value = NULL;
		sp->AUX_PILOT_WALSH1->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
	}
}

void SETPRESENT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH2_Present == present) return;
	sp->AUX_PILOT_WALSH2_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH2 = (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2));
		sp->AUX_PILOT_WALSH2->value = NULL;
		sp->AUX_PILOT_WALSH2->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
	}
}

/* Free function for variable-sized sequence c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs */
void FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data */
void FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->TDM_SUPER_PERIOD_MASK.value != NULL) {
		EDFree (sp->TDM_SUPER_PERIOD_MASK.value);
		sp->TDM_SUPER_PERIOD_MASK.value = NULL;
	}
	sp->TDM_SUPER_PERIOD_MASK.usedBits = 0;
	if (sp->TDM_MEGA_PERIOD_MASK.value != NULL) {
		EDFree (sp->TDM_MEGA_PERIOD_MASK.value);
		sp->TDM_MEGA_PERIOD_MASK.value = NULL;
	}
	sp->TDM_MEGA_PERIOD_MASK.usedBits = 0;
	for (i0=0; i0<sp->NGHBRs.items; i0++) {
		FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data (sp->NGHBRs.data[i0]);
		EDFree (sp->NGHBRs.data[i0]);
	}
	if (sp->NGHBRs.data) {EDFree (sp->NGHBRs.data); sp->NGHBRs.data = NULL; sp->NGHBRs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data */
void FREE_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c01 */
void FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c01 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c01* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c02 */
void FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c02 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c02* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH.value);
		sp->AUX_PILOT_WALSH.value = NULL;
	}
	sp->AUX_PILOT_WALSH.usedBits = 0;

}

/* Free function for struct c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c03 */
void FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c03 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c03* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_WALSH.value != NULL) {
		EDFree (sp->AUX_WALSH.value);
		sp->AUX_WALSH.value = NULL;
	}
	sp->AUX_WALSH.usedBits = 0;

}

/* Free function for struct c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c04 */
void FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c04 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c04* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05 */
void FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AUX_PILOT_WALSH___1.value != NULL) {
		EDFree (sp->AUX_PILOT_WALSH___1.value);
		sp->AUX_PILOT_WALSH___1.value = NULL;
	}
	sp->AUX_PILOT_WALSH___1.usedBits = 0;
	if (sp->AUX_PILOT_WALSH1_Present) {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
		sp->AUX_PILOT_WALSH1_Present = ED_FALSE;
	}
	if (sp->AUX_PILOT_WALSH2_Present) {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
		sp->AUX_PILOT_WALSH2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH1_Present == present) return;
	sp->AUX_PILOT_WALSH1_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH1 = (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1*)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH1));
		sp->AUX_PILOT_WALSH1->value = NULL;
		sp->AUX_PILOT_WALSH1->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH1->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH1->value);
			sp->AUX_PILOT_WALSH1->value = NULL;
		}
		sp->AUX_PILOT_WALSH1->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH1);
	}
}

void SETPRESENT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AUX_PILOT_WALSH2_Present == present) return;
	sp->AUX_PILOT_WALSH2_Present = present;
	if (present) {
		/*-->*/sp->AUX_PILOT_WALSH2 = (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2*)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05_AUX_PILOT_WALSH2));
		sp->AUX_PILOT_WALSH2->value = NULL;
		sp->AUX_PILOT_WALSH2->usedBits = 0;
	} else {
		if (sp->AUX_PILOT_WALSH2->value != NULL) {
			EDFree (sp->AUX_PILOT_WALSH2->value);
			sp->AUX_PILOT_WALSH2->value = NULL;
		}
		sp->AUX_PILOT_WALSH2->usedBits = 0;
		EDFree (sp->AUX_PILOT_WALSH2);
	}
}

/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c00_Present) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c00 (sp->c00);
		EDFree (sp->c00);
		sp->c00_Present = ED_FALSE;
	}
	if (sp->c01_Present) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}
	if (sp->c04_Present) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
		sp->c04_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c00 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c00_Present == present) return;
	sp->c00_Present = present;
	if (present) {
		/*-->*/sp->c00 = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c00*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c00));
		INIT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c00 (sp->c00);
	} else {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c00 (sp->c00);
		EDFree (sp->c00);
	}
}

void SETPRESENT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c01 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c01*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c01));
		INIT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c01 (sp->c01);
	} else {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c02 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c02*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c02));
		INIT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c02 (sp->c02);
	} else {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c03 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c03*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c03));
		INIT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c03 (sp->c03);
	} else {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

void SETPRESENT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c04_Present == present) return;
	sp->c04_Present = present;
	if (present) {
		/*-->*/sp->c04 = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04));
		INIT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04 (sp->c04);
	} else {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
	}
}

/* Free function for struct c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data */
void FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data */
void FREE_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for union c_R_InformationRecords */
void FREE_c_R_InformationRecords (c_R_InformationRecords* sp) {
	switch (sp->Present) {
		case U_c_R_InformationRecords_p_generic: {
			if (sp->u.p_generic->FieldData.value != NULL) {
				EDFree (sp->u.p_generic->FieldData.value);
				sp->u.p_generic->FieldData.value = NULL;
			}
			sp->u.p_generic->FieldData.usedBits = 0;
			EDFree (sp->u.p_generic);
			sp->u.p_generic = NULL;

			break;
		}
		case U_c_R_InformationRecords_p_Keypad_Facility: {
			FREE_c_RIR_Keypad_Facility (sp->u.p_Keypad_Facility);
			EDFree (sp->u.p_Keypad_Facility);

			break;
		}
		case U_c_R_InformationRecords_p_Called_Party_Number: {
			FREE_c_RIR_Called_Party_Number (sp->u.p_Called_Party_Number);
			EDFree (sp->u.p_Called_Party_Number);

			break;
		}
		case U_c_R_InformationRecords_p_Calling_Party_Number: {
			FREE_c_RIR_Calling_Party_Number (sp->u.p_Calling_Party_Number);
			EDFree (sp->u.p_Calling_Party_Number);

			break;
		}
		case U_c_R_InformationRecords_p_Call_Mode: {
			FREE_c_RIR_Call_Mode (sp->u.p_Call_Mode);
			EDFree (sp->u.p_Call_Mode);

			break;
		}
		case U_c_R_InformationRecords_p_Terminal_Information: {
			FREE_c_RIR_Terminal_Information (sp->u.p_Terminal_Information);
			EDFree (sp->u.p_Terminal_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Roaming_Information: {
			FREE_c_RIR_Roaming_Information (sp->u.p_Roaming_Information);
			EDFree (sp->u.p_Roaming_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Security_Status: {
			FREE_c_RIR_Security_Status (sp->u.p_Security_Status);
			EDFree (sp->u.p_Security_Status);

			break;
		}
		case U_c_R_InformationRecords_p_Connected_Number: {
			FREE_c_RIR_Connected_Number (sp->u.p_Connected_Number);
			EDFree (sp->u.p_Connected_Number);

			break;
		}
		case U_c_R_InformationRecords_p_IMSI: {
			FREE_c_RIR_IMSI (sp->u.p_IMSI);
			EDFree (sp->u.p_IMSI);

			break;
		}
		case U_c_R_InformationRecords_p_ESN: {
			FREE_c_RIR_ESN (sp->u.p_ESN);
			EDFree (sp->u.p_ESN);

			break;
		}
		case U_c_R_InformationRecords_p_Band_Class_Information: {
			FREE_c_RIR_Band_Class_Information (sp->u.p_Band_Class_Information);
			EDFree (sp->u.p_Band_Class_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Power_Class_Information: {
			FREE_c_RIR_Power_Class_Information (sp->u.p_Power_Class_Information);
			EDFree (sp->u.p_Power_Class_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Operating_Mode_Information: {
			FREE_c_RIR_Operating_Mode_Information (sp->u.p_Operating_Mode_Information);
			EDFree (sp->u.p_Operating_Mode_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Service_Option_Information: {
			FREE_c_RIR_Service_Option_Information (sp->u.p_Service_Option_Information);
			EDFree (sp->u.p_Service_Option_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Multiplex_Option_Information: {
			FREE_c_RIR_Multiplex_Option_Information (sp->u.p_Multiplex_Option_Information);
			EDFree (sp->u.p_Multiplex_Option_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Service_Configuration_Information: {
			FREE_c_RIR_Service_Configuration_Information (sp->u.p_Service_Configuration_Information);
			EDFree (sp->u.p_Service_Configuration_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Called_Party_Subaddress: {
			FREE_c_RIR_Called_Party_Subaddress (sp->u.p_Called_Party_Subaddress);
			EDFree (sp->u.p_Called_Party_Subaddress);

			break;
		}
		case U_c_R_InformationRecords_p_Calling_Party_Subaddress: {
			FREE_c_RIR_Calling_Party_Subaddress (sp->u.p_Calling_Party_Subaddress);
			EDFree (sp->u.p_Calling_Party_Subaddress);

			break;
		}
		case U_c_R_InformationRecords_p_Connected_Subaddress: {
			FREE_c_RIR_Connected_Subaddress (sp->u.p_Connected_Subaddress);
			EDFree (sp->u.p_Connected_Subaddress);

			break;
		}
		case U_c_R_InformationRecords_p_Power_Control_Information: {
			FREE_c_RIR_Power_Control_Information (sp->u.p_Power_Control_Information);
			EDFree (sp->u.p_Power_Control_Information);

			break;
		}
		case U_c_R_InformationRecords_p_IMSI_M: {
			FREE_c_RIR_IMSI_M (sp->u.p_IMSI_M);
			EDFree (sp->u.p_IMSI_M);

			break;
		}
		case U_c_R_InformationRecords_p_IMSI_T: {
			FREE_c_RIR_IMSI_T (sp->u.p_IMSI_T);
			EDFree (sp->u.p_IMSI_T);

			break;
		}
		case U_c_R_InformationRecords_p_Capability_Information: {
			FREE_c_RIR_Capability_Information (sp->u.p_Capability_Information);
			EDFree (sp->u.p_Capability_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Extended_Multiplex_Option_Information: {
			FREE_c_RIR_Extended_Multiplex_Option_Information (sp->u.p_Extended_Multiplex_Option_Information);
			EDFree (sp->u.p_Extended_Multiplex_Option_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Channel_Configuration_Capability_Information: {
			FREE_c_RIR_Channel_Configuration_Capability_Information (sp->u.p_Channel_Configuration_Capability_Information);
			EDFree (sp->u.p_Channel_Configuration_Capability_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Geo_Location_Capability: {
			FREE_c_RIR_Geo_Location_Capability (sp->u.p_Geo_Location_Capability);
			EDFree (sp->u.p_Geo_Location_Capability);

			break;
		}
		case U_c_R_InformationRecords_p_Band_Subclass_Information: {
			FREE_c_RIR_Band_Subclass_Information (sp->u.p_Band_Subclass_Information);
			EDFree (sp->u.p_Band_Subclass_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Global_Emergency_Call: {
			FREE_c_RIR_Global_Emergency_Call (sp->u.p_Global_Emergency_Call);
			EDFree (sp->u.p_Global_Emergency_Call);

			break;
		}
		case U_c_R_InformationRecords_p_Hook_Status: {
			FREE_c_RIR_Hook_Status (sp->u.p_Hook_Status);
			EDFree (sp->u.p_Hook_Status);

			break;
		}
		case U_c_R_InformationRecords_p_QoS_Parameters: {
			FREE_c_RIR_QoS_Parameters (sp->u.p_QoS_Parameters);
			EDFree (sp->u.p_QoS_Parameters);

			break;
		}
		case U_c_R_InformationRecords_p_Encryption_Capability: {
			FREE_c_RIR_Encryption_Capability (sp->u.p_Encryption_Capability);
			EDFree (sp->u.p_Encryption_Capability);

			break;
		}
		case U_c_R_InformationRecords_p_Signaling_Message_Integrity_Capability: {
			FREE_c_RIR_Signaling_Message_Integrity_Capability (sp->u.p_Signaling_Message_Integrity_Capability);
			EDFree (sp->u.p_Signaling_Message_Integrity_Capability);

			break;
		}
		case U_c_R_InformationRecords_p_UIM_ID: {
			FREE_c_RIR_UIM_ID (sp->u.p_UIM_ID);
			EDFree (sp->u.p_UIM_ID);

			break;
		}
		case U_c_R_InformationRecords_p_ESN_ME: {
			FREE_c_RIR_ESN_ME (sp->u.p_ESN_ME);
			EDFree (sp->u.p_ESN_ME);

			break;
		}
		case U_c_R_InformationRecords_p_MEID: {
			FREE_c_RIR_MEID (sp->u.p_MEID);
			EDFree (sp->u.p_MEID);

			break;
		}
		case U_c_R_InformationRecords_p_Extended_Keypad_Facility: {
			FREE_c_RIR_Extended_Keypad_Facility (sp->u.p_Extended_Keypad_Facility);
			EDFree (sp->u.p_Extended_Keypad_Facility);

			break;
		}
		case U_c_R_InformationRecords_p_SYNC_ID: {
			FREE_c_RIR_SYNC_ID (sp->u.p_SYNC_ID);
			EDFree (sp->u.p_SYNC_ID);

			break;
		}
		case U_c_R_InformationRecords_p_Extended_Terminal_Information: {
			FREE_c_RIR_Extended_Terminal_Information (sp->u.p_Extended_Terminal_Information);
			EDFree (sp->u.p_Extended_Terminal_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Extended_Service_Option_Information: {
			FREE_c_RIR_Extended_Service_Option_Information (sp->u.p_Extended_Service_Option_Information);
			EDFree (sp->u.p_Extended_Service_Option_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Band_Class_and_Band_Subclass_Information: {
			FREE_c_RIR_Band_Class_and_Band_Subclass_Information (sp->u.p_Band_Class_and_Band_Subclass_Information);
			EDFree (sp->u.p_Band_Class_and_Band_Subclass_Information);

			break;
		}
		case U_c_R_InformationRecords_p_EXT_UIM_ID: {
			FREE_c_RIR_EXT_UIM_ID (sp->u.p_EXT_UIM_ID);
			EDFree (sp->u.p_EXT_UIM_ID);

			break;
		}
		case U_c_R_InformationRecords_p_MEID_ME: {
			FREE_c_RIR_MEID_ME (sp->u.p_MEID_ME);
			EDFree (sp->u.p_MEID_ME);

			break;
		}
		case U_c_R_InformationRecords_p_Additional_Geo_Location_Capability: {
			FREE_c_RIR_Additional_Geo_Location_Capability (sp->u.p_Additional_Geo_Location_Capability);
			EDFree (sp->u.p_Additional_Geo_Location_Capability);

			break;
		}
		case U_c_R_InformationRecords_p_MEID_SIG: {
			FREE_c_RIR_MEID_SIG (sp->u.p_MEID_SIG);
			EDFree (sp->u.p_MEID_SIG);

			break;
		}
		case U_c_R_InformationRecords_p_Extended_Record_Type_International: {
			FREE_c_RIR_Extended_Record_Type_International (sp->u.p_Extended_Record_Type_International);
			EDFree (sp->u.p_Extended_Record_Type_International);

			break;
		}

		default:;
	}
	sp->Present = U_c_R_InformationRecords_NONE;
}

/* Set-present function for c_R_InformationRecords LEVEL = -1 */
void GLOB_SETPRESENT_c_R_InformationRecords (c_R_InformationRecords* sp, TPRESENT_c_R_InformationRecords toBeSetPresent) 
{
	FREE_c_R_InformationRecords (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_R_InformationRecords_p_generic: {
			sp->u.p_generic = (c_R_InformationRecords_p_generic*)EDAlloc (sizeof (c_R_InformationRecords_p_generic));
			ED_RESET_MEM (sp->u.p_generic, sizeof (c_R_InformationRecords_p_generic));
			(sp->u.p_generic->RECORD_TYPE) = 0;
			sp->u.p_generic->FieldData.value = NULL;
			sp->u.p_generic->FieldData.usedBits = 0;
			

			break;
		}
		case U_c_R_InformationRecords_p_Keypad_Facility: {
			sp->u.p_Keypad_Facility = (c_RIR_Keypad_Facility*)EDAlloc (sizeof (c_RIR_Keypad_Facility));
			INIT_c_RIR_Keypad_Facility (sp->u.p_Keypad_Facility);

			break;
		}
		case U_c_R_InformationRecords_p_Called_Party_Number: {
			sp->u.p_Called_Party_Number = (c_RIR_Called_Party_Number*)EDAlloc (sizeof (c_RIR_Called_Party_Number));
			INIT_c_RIR_Called_Party_Number (sp->u.p_Called_Party_Number);

			break;
		}
		case U_c_R_InformationRecords_p_Calling_Party_Number: {
			sp->u.p_Calling_Party_Number = (c_RIR_Calling_Party_Number*)EDAlloc (sizeof (c_RIR_Calling_Party_Number));
			INIT_c_RIR_Calling_Party_Number (sp->u.p_Calling_Party_Number);

			break;
		}
		case U_c_R_InformationRecords_p_Call_Mode: {
			sp->u.p_Call_Mode = (c_RIR_Call_Mode*)EDAlloc (sizeof (c_RIR_Call_Mode));
			INIT_c_RIR_Call_Mode (sp->u.p_Call_Mode);

			break;
		}
		case U_c_R_InformationRecords_p_Terminal_Information: {
			sp->u.p_Terminal_Information = (c_RIR_Terminal_Information*)EDAlloc (sizeof (c_RIR_Terminal_Information));
			INIT_c_RIR_Terminal_Information (sp->u.p_Terminal_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Roaming_Information: {
			sp->u.p_Roaming_Information = (c_RIR_Roaming_Information*)EDAlloc (sizeof (c_RIR_Roaming_Information));
			INIT_c_RIR_Roaming_Information (sp->u.p_Roaming_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Security_Status: {
			sp->u.p_Security_Status = (c_RIR_Security_Status*)EDAlloc (sizeof (c_RIR_Security_Status));
			INIT_c_RIR_Security_Status (sp->u.p_Security_Status);

			break;
		}
		case U_c_R_InformationRecords_p_Connected_Number: {
			sp->u.p_Connected_Number = (c_RIR_Connected_Number*)EDAlloc (sizeof (c_RIR_Connected_Number));
			INIT_c_RIR_Connected_Number (sp->u.p_Connected_Number);

			break;
		}
		case U_c_R_InformationRecords_p_IMSI: {
			sp->u.p_IMSI = (c_RIR_IMSI*)EDAlloc (sizeof (c_RIR_IMSI));
			INIT_c_RIR_IMSI (sp->u.p_IMSI);

			break;
		}
		case U_c_R_InformationRecords_p_ESN: {
			sp->u.p_ESN = (c_RIR_ESN*)EDAlloc (sizeof (c_RIR_ESN));
			INIT_c_RIR_ESN (sp->u.p_ESN);

			break;
		}
		case U_c_R_InformationRecords_p_Band_Class_Information: {
			sp->u.p_Band_Class_Information = (c_RIR_Band_Class_Information*)EDAlloc (sizeof (c_RIR_Band_Class_Information));
			INIT_c_RIR_Band_Class_Information (sp->u.p_Band_Class_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Power_Class_Information: {
			sp->u.p_Power_Class_Information = (c_RIR_Power_Class_Information*)EDAlloc (sizeof (c_RIR_Power_Class_Information));
			INIT_c_RIR_Power_Class_Information (sp->u.p_Power_Class_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Operating_Mode_Information: {
			sp->u.p_Operating_Mode_Information = (c_RIR_Operating_Mode_Information*)EDAlloc (sizeof (c_RIR_Operating_Mode_Information));
			INIT_c_RIR_Operating_Mode_Information (sp->u.p_Operating_Mode_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Service_Option_Information: {
			sp->u.p_Service_Option_Information = (c_RIR_Service_Option_Information*)EDAlloc (sizeof (c_RIR_Service_Option_Information));
			INIT_c_RIR_Service_Option_Information (sp->u.p_Service_Option_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Multiplex_Option_Information: {
			sp->u.p_Multiplex_Option_Information = (c_RIR_Multiplex_Option_Information*)EDAlloc (sizeof (c_RIR_Multiplex_Option_Information));
			INIT_c_RIR_Multiplex_Option_Information (sp->u.p_Multiplex_Option_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Service_Configuration_Information: {
			sp->u.p_Service_Configuration_Information = (c_RIR_Service_Configuration_Information*)EDAlloc (sizeof (c_RIR_Service_Configuration_Information));
			INIT_c_RIR_Service_Configuration_Information (sp->u.p_Service_Configuration_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Called_Party_Subaddress: {
			sp->u.p_Called_Party_Subaddress = (c_RIR_Called_Party_Subaddress*)EDAlloc (sizeof (c_RIR_Called_Party_Subaddress));
			INIT_c_RIR_Called_Party_Subaddress (sp->u.p_Called_Party_Subaddress);

			break;
		}
		case U_c_R_InformationRecords_p_Calling_Party_Subaddress: {
			sp->u.p_Calling_Party_Subaddress = (c_RIR_Calling_Party_Subaddress*)EDAlloc (sizeof (c_RIR_Calling_Party_Subaddress));
			INIT_c_RIR_Calling_Party_Subaddress (sp->u.p_Calling_Party_Subaddress);

			break;
		}
		case U_c_R_InformationRecords_p_Connected_Subaddress: {
			sp->u.p_Connected_Subaddress = (c_RIR_Connected_Subaddress*)EDAlloc (sizeof (c_RIR_Connected_Subaddress));
			INIT_c_RIR_Connected_Subaddress (sp->u.p_Connected_Subaddress);

			break;
		}
		case U_c_R_InformationRecords_p_Power_Control_Information: {
			sp->u.p_Power_Control_Information = (c_RIR_Power_Control_Information*)EDAlloc (sizeof (c_RIR_Power_Control_Information));
			INIT_c_RIR_Power_Control_Information (sp->u.p_Power_Control_Information);

			break;
		}
		case U_c_R_InformationRecords_p_IMSI_M: {
			sp->u.p_IMSI_M = (c_RIR_IMSI_M*)EDAlloc (sizeof (c_RIR_IMSI_M));
			INIT_c_RIR_IMSI_M (sp->u.p_IMSI_M);

			break;
		}
		case U_c_R_InformationRecords_p_IMSI_T: {
			sp->u.p_IMSI_T = (c_RIR_IMSI_T*)EDAlloc (sizeof (c_RIR_IMSI_T));
			INIT_c_RIR_IMSI_T (sp->u.p_IMSI_T);

			break;
		}
		case U_c_R_InformationRecords_p_Capability_Information: {
			sp->u.p_Capability_Information = (c_RIR_Capability_Information*)EDAlloc (sizeof (c_RIR_Capability_Information));
			INIT_c_RIR_Capability_Information (sp->u.p_Capability_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Extended_Multiplex_Option_Information: {
			sp->u.p_Extended_Multiplex_Option_Information = (c_RIR_Extended_Multiplex_Option_Information*)EDAlloc (sizeof (c_RIR_Extended_Multiplex_Option_Information));
			INIT_c_RIR_Extended_Multiplex_Option_Information (sp->u.p_Extended_Multiplex_Option_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Channel_Configuration_Capability_Information: {
			sp->u.p_Channel_Configuration_Capability_Information = (c_RIR_Channel_Configuration_Capability_Information*)EDAlloc (sizeof (c_RIR_Channel_Configuration_Capability_Information));
			INIT_c_RIR_Channel_Configuration_Capability_Information (sp->u.p_Channel_Configuration_Capability_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Geo_Location_Capability: {
			sp->u.p_Geo_Location_Capability = (c_RIR_Geo_Location_Capability*)EDAlloc (sizeof (c_RIR_Geo_Location_Capability));
			INIT_c_RIR_Geo_Location_Capability (sp->u.p_Geo_Location_Capability);

			break;
		}
		case U_c_R_InformationRecords_p_Band_Subclass_Information: {
			sp->u.p_Band_Subclass_Information = (c_RIR_Band_Subclass_Information*)EDAlloc (sizeof (c_RIR_Band_Subclass_Information));
			INIT_c_RIR_Band_Subclass_Information (sp->u.p_Band_Subclass_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Global_Emergency_Call: {
			sp->u.p_Global_Emergency_Call = (c_RIR_Global_Emergency_Call*)EDAlloc (sizeof (c_RIR_Global_Emergency_Call));
			INIT_c_RIR_Global_Emergency_Call (sp->u.p_Global_Emergency_Call);

			break;
		}
		case U_c_R_InformationRecords_p_Hook_Status: {
			sp->u.p_Hook_Status = (c_RIR_Hook_Status*)EDAlloc (sizeof (c_RIR_Hook_Status));
			INIT_c_RIR_Hook_Status (sp->u.p_Hook_Status);

			break;
		}
		case U_c_R_InformationRecords_p_QoS_Parameters: {
			sp->u.p_QoS_Parameters = (c_RIR_QoS_Parameters*)EDAlloc (sizeof (c_RIR_QoS_Parameters));
			INIT_c_RIR_QoS_Parameters (sp->u.p_QoS_Parameters);

			break;
		}
		case U_c_R_InformationRecords_p_Encryption_Capability: {
			sp->u.p_Encryption_Capability = (c_RIR_Encryption_Capability*)EDAlloc (sizeof (c_RIR_Encryption_Capability));
			INIT_c_RIR_Encryption_Capability (sp->u.p_Encryption_Capability);

			break;
		}
		case U_c_R_InformationRecords_p_Signaling_Message_Integrity_Capability: {
			sp->u.p_Signaling_Message_Integrity_Capability = (c_RIR_Signaling_Message_Integrity_Capability*)EDAlloc (sizeof (c_RIR_Signaling_Message_Integrity_Capability));
			INIT_c_RIR_Signaling_Message_Integrity_Capability (sp->u.p_Signaling_Message_Integrity_Capability);

			break;
		}
		case U_c_R_InformationRecords_p_UIM_ID: {
			sp->u.p_UIM_ID = (c_RIR_UIM_ID*)EDAlloc (sizeof (c_RIR_UIM_ID));
			INIT_c_RIR_UIM_ID (sp->u.p_UIM_ID);

			break;
		}
		case U_c_R_InformationRecords_p_ESN_ME: {
			sp->u.p_ESN_ME = (c_RIR_ESN_ME*)EDAlloc (sizeof (c_RIR_ESN_ME));
			INIT_c_RIR_ESN_ME (sp->u.p_ESN_ME);

			break;
		}
		case U_c_R_InformationRecords_p_MEID: {
			sp->u.p_MEID = (c_RIR_MEID*)EDAlloc (sizeof (c_RIR_MEID));
			INIT_c_RIR_MEID (sp->u.p_MEID);

			break;
		}
		case U_c_R_InformationRecords_p_Extended_Keypad_Facility: {
			sp->u.p_Extended_Keypad_Facility = (c_RIR_Extended_Keypad_Facility*)EDAlloc (sizeof (c_RIR_Extended_Keypad_Facility));
			INIT_c_RIR_Extended_Keypad_Facility (sp->u.p_Extended_Keypad_Facility);

			break;
		}
		case U_c_R_InformationRecords_p_SYNC_ID: {
			sp->u.p_SYNC_ID = (c_RIR_SYNC_ID*)EDAlloc (sizeof (c_RIR_SYNC_ID));
			INIT_c_RIR_SYNC_ID (sp->u.p_SYNC_ID);

			break;
		}
		case U_c_R_InformationRecords_p_Extended_Terminal_Information: {
			sp->u.p_Extended_Terminal_Information = (c_RIR_Extended_Terminal_Information*)EDAlloc (sizeof (c_RIR_Extended_Terminal_Information));
			INIT_c_RIR_Extended_Terminal_Information (sp->u.p_Extended_Terminal_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Extended_Service_Option_Information: {
			sp->u.p_Extended_Service_Option_Information = (c_RIR_Extended_Service_Option_Information*)EDAlloc (sizeof (c_RIR_Extended_Service_Option_Information));
			INIT_c_RIR_Extended_Service_Option_Information (sp->u.p_Extended_Service_Option_Information);

			break;
		}
		case U_c_R_InformationRecords_p_Band_Class_and_Band_Subclass_Information: {
			sp->u.p_Band_Class_and_Band_Subclass_Information = (c_RIR_Band_Class_and_Band_Subclass_Information*)EDAlloc (sizeof (c_RIR_Band_Class_and_Band_Subclass_Information));
			INIT_c_RIR_Band_Class_and_Band_Subclass_Information (sp->u.p_Band_Class_and_Band_Subclass_Information);

			break;
		}
		case U_c_R_InformationRecords_p_EXT_UIM_ID: {
			sp->u.p_EXT_UIM_ID = (c_RIR_EXT_UIM_ID*)EDAlloc (sizeof (c_RIR_EXT_UIM_ID));
			INIT_c_RIR_EXT_UIM_ID (sp->u.p_EXT_UIM_ID);

			break;
		}
		case U_c_R_InformationRecords_p_MEID_ME: {
			sp->u.p_MEID_ME = (c_RIR_MEID_ME*)EDAlloc (sizeof (c_RIR_MEID_ME));
			INIT_c_RIR_MEID_ME (sp->u.p_MEID_ME);

			break;
		}
		case U_c_R_InformationRecords_p_Additional_Geo_Location_Capability: {
			sp->u.p_Additional_Geo_Location_Capability = (c_RIR_Additional_Geo_Location_Capability*)EDAlloc (sizeof (c_RIR_Additional_Geo_Location_Capability));
			INIT_c_RIR_Additional_Geo_Location_Capability (sp->u.p_Additional_Geo_Location_Capability);

			break;
		}
		case U_c_R_InformationRecords_p_MEID_SIG: {
			sp->u.p_MEID_SIG = (c_RIR_MEID_SIG*)EDAlloc (sizeof (c_RIR_MEID_SIG));
			INIT_c_RIR_MEID_SIG (sp->u.p_MEID_SIG);

			break;
		}
		case U_c_R_InformationRecords_p_Extended_Record_Type_International: {
			sp->u.p_Extended_Record_Type_International = (c_RIR_Extended_Record_Type_International*)EDAlloc (sizeof (c_RIR_Extended_Record_Type_International));
			INIT_c_RIR_Extended_Record_Type_International (sp->u.p_Extended_Record_Type_International);

			break;
		}
		default:;	}
}

/* Free function for variable-sized sequence c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs */
void FREE_c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_F_BCMC_Order_BCMC_PROGRAMSs_data */
void FREE_c_F_BCMC_Order_BCMC_PROGRAMSs_data (c_F_BCMC_Order_BCMC_PROGRAMSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_PROGRAM_ID.value != NULL) {
		EDFree (sp->BCMC_PROGRAM_ID.value);
		sp->BCMC_PROGRAM_ID.value = NULL;
	}
	sp->BCMC_PROGRAM_ID.usedBits = 0;
	if (sp->NUM_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->NUM_FLOW_DISCRIMINATOR.value);
		sp->NUM_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->NUM_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->FLOW_DISCRIMINATORs.items; i0++) {
		FREE_c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->FLOW_DISCRIMINATORs.data[i0]);
		EDFree (sp->FLOW_DISCRIMINATORs.data[i0]);
	}
	if (sp->FLOW_DISCRIMINATORs.data) {EDFree (sp->FLOW_DISCRIMINATORs.data); sp->FLOW_DISCRIMINATORs.data = NULL; sp->FLOW_DISCRIMINATORs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_F_Service_Status_Order_data_01_data */
void FREE_c_F_Service_Status_Order_data_01_data (c_F_Service_Status_Order_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs_1.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data (sp->PILOTSs_1.data[i0]);
		EDFree (sp->PILOTSs_1.data[i0]);
	}
	if (sp->PILOTSs_1.data) {EDFree (sp->PILOTSs_1.data); sp->PILOTSs_1.data = NULL; sp->PILOTSs_1.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_2.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data (sp->PILOTSs_2.data[i0]);
		EDFree (sp->PILOTSs_2.data[i0]);
	}
	if (sp->PILOTSs_2.data) {EDFree (sp->PILOTSs_2.data); sp->PILOTSs_2.data = NULL; sp->PILOTSs_2.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs_1.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data (sp->PILOTSs_1.data[i0]);
		EDFree (sp->PILOTSs_1.data[i0]);
	}
	if (sp->PILOTSs_1.data) {EDFree (sp->PILOTSs_1.data); sp->PILOTSs_1.data = NULL; sp->PILOTSs_1.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_2.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data (sp->PILOTSs_2.data[i0]);
		EDFree (sp->PILOTSs_2.data[i0]);
	}
	if (sp->PILOTSs_2.data) {EDFree (sp->PILOTSs_2.data); sp->PILOTSs_2.data = NULL; sp->PILOTSs_2.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_3.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data (sp->PILOTSs_3.data[i0]);
		EDFree (sp->PILOTSs_3.data[i0]);
	}
	if (sp->PILOTSs_3.data) {EDFree (sp->PILOTSs_3.data); sp->PILOTSs_3.data = NULL; sp->PILOTSs_3.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs_1.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data (sp->PILOTSs_1.data[i0]);
		EDFree (sp->PILOTSs_1.data[i0]);
	}
	if (sp->PILOTSs_1.data) {EDFree (sp->PILOTSs_1.data); sp->PILOTSs_1.data = NULL; sp->PILOTSs_1.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_2.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data (sp->PILOTSs_2.data[i0]);
		EDFree (sp->PILOTSs_2.data[i0]);
	}
	if (sp->PILOTSs_2.data) {EDFree (sp->PILOTSs_2.data); sp->PILOTSs_2.data = NULL; sp->PILOTSs_2.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_3.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data (sp->PILOTSs_3.data[i0]);
		EDFree (sp->PILOTSs_3.data[i0]);
	}
	if (sp->PILOTSs_3.data) {EDFree (sp->PILOTSs_3.data); sp->PILOTSs_3.data = NULL; sp->PILOTSs_3.allocatedItems=0; /*FR02A*/}
	if (sp->ADD_PLCM_FOR_FCH_39_Present) {
		EDFree (sp->ADD_PLCM_FOR_FCH_39);
		sp->ADD_PLCM_FOR_FCH_39_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->PILOTSs_4.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data (sp->PILOTSs_4.data[i0]);
		EDFree (sp->PILOTSs_4.data[i0]);
	}
	if (sp->PILOTSs_4.data) {EDFree (sp->PILOTSs_4.data); sp->PILOTSs_4.data = NULL; sp->PILOTSs_4.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_ADD_PLCM_FOR_FCH_39 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->ADD_PLCM_FOR_FCH_39_Present == present) return;
	sp->ADD_PLCM_FOR_FCH_39_Present = present;
	if (present) {
		/*-->*/sp->ADD_PLCM_FOR_FCH_39 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->ADD_PLCM_FOR_FCH_39, 5);
	} else {
		EDFree (sp->ADD_PLCM_FOR_FCH_39);
	}
}

/* Free function for variable-sized sequence c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->records_Present) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records (sp->records);
		EDFree (sp->records);
		sp->records_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->UZIDs.items; i0++) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data (sp->UZIDs.data[i0]);
		EDFree (sp->UZIDs.data[i0]);
	}
	if (sp->UZIDs.data) {EDFree (sp->UZIDs.data); sp->UZIDs.data = NULL; sp->UZIDs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->records_Present == present) return;
	sp->records_Present = present;
	if (present) {
		/*-->*/sp->records = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records));
		INIT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records (sp->records);
	} else {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_records (sp->records);
		EDFree (sp->records);
	}
}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->records_Present) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records (sp->records);
		EDFree (sp->records);
		sp->records_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->records_Present == present) return;
	sp->records_Present = present;
	if (present) {
		/*-->*/sp->records = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records));
		INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records (sp->records);
	} else {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records (sp->records);
		EDFree (sp->records);
	}
}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_BAND */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_BAND (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_BAND* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_FREQ */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_FREQ (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_FREQ* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->NGHBR_BAND.data) {EDFree (sp->NGHBR_BAND.data); sp->NGHBR_BAND.data = NULL; sp->NGHBR_BAND.allocatedItems=0; /*FR02A*/}
	if (sp->NGHBR_FREQ.data) {EDFree (sp->NGHBR_FREQ.data); sp->NGHBR_FREQ.data = NULL; sp->NGHBR_FREQ.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data */
void FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data (c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs_1.items; i0++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data (sp->PILOTSs_1.data[i0]);
		EDFree (sp->PILOTSs_1.data[i0]);
	}
	if (sp->PILOTSs_1.data) {EDFree (sp->PILOTSs_1.data); sp->PILOTSs_1.data = NULL; sp->PILOTSs_1.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_2.items; i0++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data (sp->PILOTSs_2.data[i0]);
		EDFree (sp->PILOTSs_2.data[i0]);
	}
	if (sp->PILOTSs_2.data) {EDFree (sp->PILOTSs_2.data); sp->PILOTSs_2.data = NULL; sp->PILOTSs_2.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs_1.items; i0++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data (sp->PILOTSs_1.data[i0]);
		EDFree (sp->PILOTSs_1.data[i0]);
	}
	if (sp->PILOTSs_1.data) {EDFree (sp->PILOTSs_1.data); sp->PILOTSs_1.data = NULL; sp->PILOTSs_1.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_2.items; i0++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data (sp->PILOTSs_2.data[i0]);
		EDFree (sp->PILOTSs_2.data[i0]);
	}
	if (sp->PILOTSs_2.data) {EDFree (sp->PILOTSs_2.data); sp->PILOTSs_2.data = NULL; sp->PILOTSs_2.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_3.items; i0++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data (sp->PILOTSs_3.data[i0]);
		EDFree (sp->PILOTSs_3.data[i0]);
	}
	if (sp->PILOTSs_3.data) {EDFree (sp->PILOTSs_3.data); sp->PILOTSs_3.data = NULL; sp->PILOTSs_3.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs_1.items; i0++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data (sp->PILOTSs_1.data[i0]);
		EDFree (sp->PILOTSs_1.data[i0]);
	}
	if (sp->PILOTSs_1.data) {EDFree (sp->PILOTSs_1.data); sp->PILOTSs_1.data = NULL; sp->PILOTSs_1.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_2.items; i0++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data (sp->PILOTSs_2.data[i0]);
		EDFree (sp->PILOTSs_2.data[i0]);
	}
	if (sp->PILOTSs_2.data) {EDFree (sp->PILOTSs_2.data); sp->PILOTSs_2.data = NULL; sp->PILOTSs_2.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_3.items; i0++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data (sp->PILOTSs_3.data[i0]);
		EDFree (sp->PILOTSs_3.data[i0]);
	}
	if (sp->PILOTSs_3.data) {EDFree (sp->PILOTSs_3.data); sp->PILOTSs_3.data = NULL; sp->PILOTSs_3.allocatedItems=0; /*FR02A*/}
	if (sp->ADD_PLCM_FOR_FCH_39_Present) {
		EDFree (sp->ADD_PLCM_FOR_FCH_39);
		sp->ADD_PLCM_FOR_FCH_39_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->PILOTSs_4.items; i0++) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data (sp->PILOTSs_4.data[i0]);
		EDFree (sp->PILOTSs_4.data[i0]);
	}
	if (sp->PILOTSs_4.data) {EDFree (sp->PILOTSs_4.data); sp->PILOTSs_4.data = NULL; sp->PILOTSs_4.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_ADD_PLCM_FOR_FCH_39 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->ADD_PLCM_FOR_FCH_39_Present == present) return;
	sp->ADD_PLCM_FOR_FCH_39_Present = present;
	if (present) {
		/*-->*/sp->ADD_PLCM_FOR_FCH_39 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->ADD_PLCM_FOR_FCH_39, 5);
	} else {
		EDFree (sp->ADD_PLCM_FOR_FCH_39);
	}
}

/* Free function for struct c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data */
void FREE_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data (c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data */
void FREE_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data (c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data */
void FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data (c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data */
void FREE_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data (c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_InfoRec_Display_CHARi */
void FREE_c_InfoRec_Display_CHARi (c_InfoRec_Display_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Display */
void FREE_c_InfoRec_Display (c_InfoRec_Display* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Called_Party_Number_CHARi */
void FREE_c_InfoRec_Called_Party_Number_CHARi (c_InfoRec_Called_Party_Number_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Called_Party_Number */
void FREE_c_InfoRec_Called_Party_Number (c_InfoRec_Called_Party_Number* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Calling_Party_Number_CHARi */
void FREE_c_InfoRec_Calling_Party_Number_CHARi (c_InfoRec_Calling_Party_Number_CHARi* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Calling_Party_Number */
void FREE_c_InfoRec_Calling_Party_Number (c_InfoRec_Calling_Party_Number* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.data) {EDFree (sp->CHARi.data); sp->CHARi.data = NULL; sp->CHARi.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Connected_Number_data_01 */
void FREE_c_InfoRec_Connected_Number_data_01 (c_InfoRec_Connected_Number_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Connected_Number_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Connected_Number */
void FREE_c_InfoRec_Connected_Number (c_InfoRec_Connected_Number* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_InfoRec_Connected_Number_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_InfoRec_Signal */
void FREE_c_InfoRec_Signal (c_InfoRec_Signal* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_InfoRec_Message_Waiting */
void FREE_c_InfoRec_Message_Waiting (c_InfoRec_Message_Waiting* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_InfoRec_Service_Configuration_CON_RECs */
void FREE_c_InfoRec_Service_Configuration_CON_RECs (c_InfoRec_Service_Configuration_CON_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Service_Configuration_CON_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Service_Configuration_FOR_SCHs */
void FREE_c_InfoRec_Service_Configuration_FOR_SCHs (c_InfoRec_Service_Configuration_FOR_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Service_Configuration_FOR_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Service_Configuration_REV_SCHs */
void FREE_c_InfoRec_Service_Configuration_REV_SCHs (c_InfoRec_Service_Configuration_REV_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Service_Configuration_REV_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Service_Configuration */
void FREE_c_InfoRec_Service_Configuration (c_InfoRec_Service_Configuration* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->CON_RECs.items; i0++) {
		FREE_c_InfoRec_Service_Configuration_CON_RECs_data (sp->CON_RECs.data[i0]);
		EDFree (sp->CON_RECs.data[i0]);
	}
	if (sp->CON_RECs.data) {EDFree (sp->CON_RECs.data); sp->CON_RECs.data = NULL; sp->CON_RECs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FOR_SCHs.items; i0++) {
		FREE_c_InfoRec_Service_Configuration_FOR_SCHs_data (sp->FOR_SCHs.data[i0]);
		EDFree (sp->FOR_SCHs.data[i0]);
	}
	if (sp->FOR_SCHs.data) {EDFree (sp->FOR_SCHs.data); sp->FOR_SCHs.data = NULL; sp->FOR_SCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs.items; i0++) {
		FREE_c_InfoRec_Service_Configuration_REV_SCHs_data (sp->REV_SCHs.data[i0]);
		EDFree (sp->REV_SCHs.data[i0]);
	}
	if (sp->REV_SCHs.data) {EDFree (sp->REV_SCHs.data); sp->REV_SCHs.data = NULL; sp->REV_SCHs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Called_Party_Subaddress_data_01 */
void FREE_c_InfoRec_Called_Party_Subaddress_data_01 (c_InfoRec_Called_Party_Subaddress_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Called_Party_Subaddress_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Called_Party_Subaddress */
void FREE_c_InfoRec_Called_Party_Subaddress (c_InfoRec_Called_Party_Subaddress* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_InfoRec_Called_Party_Subaddress_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Calling_Party_Subaddress_data_01 */
void FREE_c_InfoRec_Calling_Party_Subaddress_data_01 (c_InfoRec_Calling_Party_Subaddress_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Calling_Party_Subaddress_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Calling_Party_Subaddress */
void FREE_c_InfoRec_Calling_Party_Subaddress (c_InfoRec_Calling_Party_Subaddress* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_InfoRec_Calling_Party_Subaddress_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Connected_Subaddress_data_01 */
void FREE_c_InfoRec_Connected_Subaddress_data_01 (c_InfoRec_Connected_Subaddress_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Connected_Subaddress_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Connected_Subaddress */
void FREE_c_InfoRec_Connected_Subaddress (c_InfoRec_Connected_Subaddress* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_InfoRec_Connected_Subaddress_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Redirecting_Number_data_01 */
void FREE_c_InfoRec_Redirecting_Number_data_01 (c_InfoRec_Redirecting_Number_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Redirecting_Number_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Redirecting_Number */
void FREE_c_InfoRec_Redirecting_Number (c_InfoRec_Redirecting_Number* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_InfoRec_Redirecting_Number_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_InfoRec_Redirecting_Subaddress_data_01 */
void FREE_c_InfoRec_Redirecting_Subaddress_data_01 (c_InfoRec_Redirecting_Subaddress_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Redirecting_Subaddress_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Redirecting_Subaddress */
void FREE_c_InfoRec_Redirecting_Subaddress (c_InfoRec_Redirecting_Subaddress* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_InfoRec_Redirecting_Subaddress_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_InfoRec_Meter_Pulses */
void FREE_c_InfoRec_Meter_Pulses (c_InfoRec_Meter_Pulses* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_InfoRec_Parametric_Alerting_GROUPSs */
void FREE_c_InfoRec_Parametric_Alerting_GROUPSs (c_InfoRec_Parametric_Alerting_GROUPSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Parametric_Alerting_GROUPSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Parametric_Alerting */
void FREE_c_InfoRec_Parametric_Alerting (c_InfoRec_Parametric_Alerting* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->GROUPSs.items; i0++) {
		FREE_c_InfoRec_Parametric_Alerting_GROUPSs_data (sp->GROUPSs.data[i0]);
		EDFree (sp->GROUPSs.data[i0]);
	}
	if (sp->GROUPSs.data) {EDFree (sp->GROUPSs.data); sp->GROUPSs.data = NULL; sp->GROUPSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_InfoRec_Line_Control */
void FREE_c_InfoRec_Line_Control (c_InfoRec_Line_Control* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_InfoRec_Extended_Display_data_01 */
void FREE_c_InfoRec_Extended_Display_data_01 (c_InfoRec_Extended_Display_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Extended_Display_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Extended_Display */
void FREE_c_InfoRec_Extended_Display (c_InfoRec_Extended_Display* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_InfoRec_Extended_Display_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_InfoRec_Extended_Record_Type_International */
void FREE_c_InfoRec_Extended_Record_Type_International (c_InfoRec_Extended_Record_Type_International* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->RECORD.value != NULL) {
		EDFree (sp->RECORD.value);
		sp->RECORD.value = NULL;
	}
	sp->RECORD.usedBits = 0;

}

/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_01 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_01 (c_InfoRec_Non_Negotiable_Service_Configuration_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_02 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_02 (c_InfoRec_Non_Negotiable_Service_Configuration_data_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_03 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_03 (c_InfoRec_Non_Negotiable_Service_Configuration_data_03* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_RECs */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_RECs (c_InfoRec_Non_Negotiable_Service_Configuration_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_04 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_04 (c_InfoRec_Non_Negotiable_Service_Configuration_data_04* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_06 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_06 (c_InfoRec_Non_Negotiable_Service_Configuration_data_06* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_10 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_10 (c_InfoRec_Non_Negotiable_Service_Configuration_data_10* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_12 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_12 (c_InfoRec_Non_Negotiable_Service_Configuration_data_12* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_13 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_13 (c_InfoRec_Non_Negotiable_Service_Configuration_data_13* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_InfoRec_Non_Negotiable_Service_Configuration_data_14 */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_14 (c_InfoRec_Non_Negotiable_Service_Configuration_data_14* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Non_Negotiable_Service_Configuration */
void FREE_c_InfoRec_Non_Negotiable_Service_Configuration (c_InfoRec_Non_Negotiable_Service_Configuration* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_02.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data (sp->data_02.data[i0]);
		EDFree (sp->data_02.data[i0]);
	}
	if (sp->data_02.data) {EDFree (sp->data_02.data); sp->data_02.data = NULL; sp->data_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_03.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data (sp->data_03.data[i0]);
		EDFree (sp->data_03.data[i0]);
	}
	if (sp->data_03.data) {EDFree (sp->data_03.data); sp->data_03.data = NULL; sp->data_03.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->RECs.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data (sp->RECs.data[i0]);
		EDFree (sp->RECs.data[i0]);
	}
	if (sp->RECs.data) {EDFree (sp->RECs.data); sp->RECs.data = NULL; sp->RECs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_04.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data (sp->data_04.data[i0]);
		EDFree (sp->data_04.data[i0]);
	}
	if (sp->data_04.data) {EDFree (sp->data_04.data); sp->data_04.data = NULL; sp->data_04.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_06.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data (sp->data_06.data[i0]);
		EDFree (sp->data_06.data[i0]);
	}
	if (sp->data_06.data) {EDFree (sp->data_06.data); sp->data_06.data = NULL; sp->data_06.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_10.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data (sp->data_10.data[i0]);
		EDFree (sp->data_10.data[i0]);
	}
	if (sp->data_10.data) {EDFree (sp->data_10.data); sp->data_10.data = NULL; sp->data_10.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PARTITION_TABLESs.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data (sp->PARTITION_TABLESs.data[i0]);
		EDFree (sp->PARTITION_TABLESs.data[i0]);
	}
	if (sp->PARTITION_TABLESs.data) {EDFree (sp->PARTITION_TABLESs.data); sp->PARTITION_TABLESs.data = NULL; sp->PARTITION_TABLESs.allocatedItems=0; /*FR02A*/}
	if (sp->rec_Present) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_Record (sp->rec);
		EDFree (sp->rec);
		sp->rec_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->BCMC_PROGRAMSs.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data (sp->BCMC_PROGRAMSs.data[i0]);
		EDFree (sp->BCMC_PROGRAMSs.data[i0]);
	}
	if (sp->BCMC_PROGRAMSs.data) {EDFree (sp->BCMC_PROGRAMSs.data); sp->BCMC_PROGRAMSs.data = NULL; sp->BCMC_PROGRAMSs.allocatedItems=0; /*FR02A*/}
	if (sp->REV_PDCH_EP_MAP_Present) {
		if (sp->REV_PDCH_EP_MAP->value != NULL) {
			EDFree (sp->REV_PDCH_EP_MAP->value);
			sp->REV_PDCH_EP_MAP->value = NULL;
		}
		sp->REV_PDCH_EP_MAP->usedBits = 0;
		EDFree (sp->REV_PDCH_EP_MAP);
		sp->REV_PDCH_EP_MAP_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->data_12.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data (sp->data_12.data[i0]);
		EDFree (sp->data_12.data[i0]);
	}
	if (sp->data_12.data) {EDFree (sp->data_12.data); sp->data_12.data = NULL; sp->data_12.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_13.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data (sp->data_13.data[i0]);
		EDFree (sp->data_13.data[i0]);
	}
	if (sp->data_13.data) {EDFree (sp->data_13.data); sp->data_13.data = NULL; sp->data_13.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_14.items; i0++) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data (sp->data_14.data[i0]);
		EDFree (sp->data_14.data[i0]);
	}
	if (sp->data_14.data) {EDFree (sp->data_14.data); sp->data_14.data = NULL; sp->data_14.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_InfoRec_Non_Negotiable_Service_Configuration_rec (c_InfoRec_Non_Negotiable_Service_Configuration* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->rec_Present == present) return;
	sp->rec_Present = present;
	if (present) {
		/*-->*/sp->rec = (c_InfoRec_Non_Negotiable_Service_Configuration_Record*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_Record));
		INIT_c_InfoRec_Non_Negotiable_Service_Configuration_Record (sp->rec);
	} else {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_Record (sp->rec);
		EDFree (sp->rec);
	}
}

void SETPRESENT_c_InfoRec_Non_Negotiable_Service_Configuration_REV_PDCH_EP_MAP (c_InfoRec_Non_Negotiable_Service_Configuration* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->REV_PDCH_EP_MAP_Present == present) return;
	sp->REV_PDCH_EP_MAP_Present = present;
	if (present) {
		/*-->*/sp->REV_PDCH_EP_MAP = (c_InfoRec_Non_Negotiable_Service_Configuration_REV_PDCH_EP_MAP*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_REV_PDCH_EP_MAP));
		sp->REV_PDCH_EP_MAP->value = NULL;
		sp->REV_PDCH_EP_MAP->usedBits = 0;
	} else {
		if (sp->REV_PDCH_EP_MAP->value != NULL) {
			EDFree (sp->REV_PDCH_EP_MAP->value);
			sp->REV_PDCH_EP_MAP->value = NULL;
		}
		sp->REV_PDCH_EP_MAP->usedBits = 0;
		EDFree (sp->REV_PDCH_EP_MAP);
	}
}

/* Free function for variable-sized sequence c_InfoRec_Multiple_Character_Extended_Display_data_01 */
void FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01 (c_InfoRec_Multiple_Character_Extended_Display_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Multiple_Character_Extended_Display */
void FREE_c_InfoRec_Multiple_Character_Extended_Display (c_InfoRec_Multiple_Character_Extended_Display* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_InfoRec_Call_Waiting_Indicator */
void FREE_c_InfoRec_Call_Waiting_Indicator (c_InfoRec_Call_Waiting_Indicator* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs */
void FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_InfoRec_Enhanced_Multiple_Character_Extended_Display */
void FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display (c_InfoRec_Enhanced_Multiple_Character_Extended_Display* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->DISPLAYSs.items; i0++) {
		FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data (sp->DISPLAYSs.data[i0]);
		EDFree (sp->DISPLAYSs.data[i0]);
	}
	if (sp->DISPLAYSs.data) {EDFree (sp->DISPLAYSs.data); sp->DISPLAYSs.data = NULL; sp->DISPLAYSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_CAM_Additional_record_fields_c02_data_03_data */
void FREE_c_f_csch_CAM_Additional_record_fields_c02_data_03_data (c_f_csch_CAM_Additional_record_fields_c02_data_03_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_CAM_Additional_record_fields_c04_data_06_data */
void FREE_c_f_csch_CAM_Additional_record_fields_c04_data_06_data (c_f_csch_CAM_Additional_record_fields_c04_data_06_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data (c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data (c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs */
void FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs */
void FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data */
void FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->PDCCHs.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data (sp->PDCCHs.data[i0]);
		EDFree (sp->PDCCHs.data[i0]);
	}
	if (sp->PDCCHs.data) {EDFree (sp->PDCCHs.data); sp->PDCCHs.data = NULL; sp->PDCCHs.allocatedItems=0; /*FR02A*/}
	if (sp->FOR_CPCSCH_Present) {
		if (sp->FOR_CPCSCH->value != NULL) {
			EDFree (sp->FOR_CPCSCH->value);
			sp->FOR_CPCSCH->value = NULL;
		}
		sp->FOR_CPCSCH->usedBits = 0;
		EDFree (sp->FOR_CPCSCH);
		sp->FOR_CPCSCH_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->FOR_GCHs.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data (sp->FOR_GCHs.data[i0]);
		EDFree (sp->FOR_GCHs.data[i0]);
	}
	if (sp->FOR_GCHs.data) {EDFree (sp->FOR_GCHs.data); sp->FOR_GCHs.data = NULL; sp->FOR_GCHs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_Type_specific_fields (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_CPCSCH (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FOR_CPCSCH_Present == present) return;
	sp->FOR_CPCSCH_Present = present;
	if (present) {
		/*-->*/sp->FOR_CPCSCH = (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_CPCSCH*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_CPCSCH));
		sp->FOR_CPCSCH->value = NULL;
		sp->FOR_CPCSCH->usedBits = 0;
	} else {
		if (sp->FOR_CPCSCH->value != NULL) {
			EDFree (sp->FOR_CPCSCH->value);
			sp->FOR_CPCSCH->value = NULL;
		}
		sp->FOR_CPCSCH->usedBits = 0;
		EDFree (sp->FOR_CPCSCH);
	}
}

/* Free function for struct c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields */
void FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}
	if (sp->c04_Present) {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
		sp->c04_Present = ED_FALSE;
	}
	if (sp->c05_Present) {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05 (sp->c05);
		EDFree (sp->c05);
		sp->c05_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c01 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c01*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c01));
		INIT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c01 (sp->c01);
	} else {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c02 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c02*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c02));
		INIT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c02 (sp->c02);
	} else {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c03 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c03*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c03));
		INIT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c03 (sp->c03);
	} else {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

void SETPRESENT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c04 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c04_Present == present) return;
	sp->c04_Present = present;
	if (present) {
		/*-->*/sp->c04 = (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c04*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c04));
		INIT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c04 (sp->c04);
	} else {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
	}
}

void SETPRESENT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05 (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c05_Present == present) return;
	sp->c05_Present = present;
	if (present) {
		/*-->*/sp->c05 = (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05));
		INIT_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05 (sp->c05);
	} else {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields_c05 (sp->c05);
		EDFree (sp->c05);
	}
}

/* Free function for struct c_f_csch_EGSRDM_Type_Specific_Fields_1 */
void FREE_c_f_csch_EGSRDM_Type_Specific_Fields_1 (c_f_csch_EGSRDM_Type_Specific_Fields_1* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_EGSRDM_Type_Specific_Fields_2_CHANSs */
void FREE_c_f_csch_EGSRDM_Type_Specific_Fields_2_CHANSs (c_f_csch_EGSRDM_Type_Specific_Fields_2_CHANSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_EGSRDM_Type_Specific_Fields_2_data_02 */
void FREE_c_f_csch_EGSRDM_Type_Specific_Fields_2_data_02 (c_f_csch_EGSRDM_Type_Specific_Fields_2_data_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_EGSRDM_Type_Specific_Fields_2 */
void FREE_c_f_csch_EGSRDM_Type_Specific_Fields_2 (c_f_csch_EGSRDM_Type_Specific_Fields_2* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->CHANSs.items; i0++) {
		FREE_c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data (sp->CHANSs.data[i0]);
		EDFree (sp->CHANSs.data[i0]);
	}
	if (sp->CHANSs.data) {EDFree (sp->CHANSs.data); sp->CHANSs.data = NULL; sp->CHANSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_02.items; i0++) {
		FREE_c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data (sp->data_02.data[i0]);
		EDFree (sp->data_02.data[i0]);
	}
	if (sp->data_02.data) {EDFree (sp->data_02.data); sp->data_02.data = NULL; sp->data_02.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c01 */
void FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c01 (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c01* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06 */
void FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06 (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02 */
void FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02 (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_06.items; i0++) {
		FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data (sp->data_06.data[i0]);
		EDFree (sp->data_06.data[i0]);
	}
	if (sp->data_06.data) {EDFree (sp->data_06.data); sp->data_06.data = NULL; sp->data_06.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08 */
void FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08 (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03 */
void FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03 (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_08.items; i0++) {
		FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data (sp->data_08.data[i0]);
		EDFree (sp->data_08.data[i0]);
	}
	if (sp->data_08.data) {EDFree (sp->data_08.data); sp->data_08.data = NULL; sp->data_08.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data */
void FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01 */
void FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01 (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data.items; i0++) {
		FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data (sp->data.data[i0]);
		EDFree (sp->data.data[i0]);
	}
	if (sp->data.data) {EDFree (sp->data.data); sp->data.data = NULL; sp->data.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c02 */
void FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c02 (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c02* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs */
void FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01 */
void FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01 (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs */
void FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02 */
void FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02 (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->NETWORKSs.items; i0++) {
		FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data (sp->NETWORKSs.data[i0]);
		EDFree (sp->NETWORKSs.data[i0]);
	}
	if (sp->NETWORKSs.data) {EDFree (sp->NETWORKSs.data); sp->NETWORKSs.data = NULL; sp->NETWORKSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_GEM_GE_RECs_data_GE_REC_c00 */
void FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c00 (c_f_csch_GEM_GE_RECs_data_GE_REC_c00* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GEM_GE_RECs_data_GE_REC_c02 */
void FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c02 (c_f_csch_GEM_GE_RECs_data_GE_REC_c02* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs */
void FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs (c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_GEM_GE_RECs_data_GE_REC_c01 */
void FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c01 (c_f_csch_GEM_GE_RECs_data_GE_REC_c01* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RC_PARAMS_RECORDSs.items; i0++) {
		FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data (sp->RC_PARAMS_RECORDSs.data[i0]);
		EDFree (sp->RC_PARAMS_RECORDSs.data[i0]);
	}
	if (sp->RC_PARAMS_RECORDSs.data) {EDFree (sp->RC_PARAMS_RECORDSs.data); sp->RC_PARAMS_RECORDSs.data = NULL; sp->RC_PARAMS_RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs */
void FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01 */
void FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01 (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs */
void FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02 */
void FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02 (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->NETWORKSs.items; i0++) {
		FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data (sp->NETWORKSs.data[i0]);
		EDFree (sp->NETWORKSs.data[i0]);
	}
	if (sp->NETWORKSs.data) {EDFree (sp->NETWORKSs.data); sp->NETWORKSs.data = NULL; sp->NETWORKSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields */
void FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}
	if (sp->c04_Present) {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
		sp->c04_Present = ED_FALSE;
	}
	if (sp->c05_Present) {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05 (sp->c05);
		EDFree (sp->c05);
		sp->c05_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c01 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c01*)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c01));
		INIT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c01 (sp->c01);
	} else {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c02 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c02*)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c02));
		INIT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c02 (sp->c02);
	} else {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c03 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c03*)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c03));
		INIT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c03 (sp->c03);
	} else {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

void SETPRESENT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c04 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c04_Present == present) return;
	sp->c04_Present = present;
	if (present) {
		/*-->*/sp->c04 = (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c04*)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c04));
		INIT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c04 (sp->c04);
	} else {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c04 (sp->c04);
		EDFree (sp->c04);
	}
}

void SETPRESENT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05 (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c05_Present == present) return;
	sp->c05_Present = present;
	if (present) {
		/*-->*/sp->c05 = (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05*)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05));
		INIT_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05 (sp->c05);
	} else {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields_c05 (sp->c05);
		EDFree (sp->c05);
	}
}

/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records_Type_specific_fields (&(sp->Type_specific_fields));

}

/* Free function for struct c_f_dsch_UHDM_FOR_SCHs */
void FREE_c_f_dsch_UHDM_FOR_SCHs (c_f_dsch_UHDM_FOR_SCHs* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_REV_SCHs */
void FREE_c_f_dsch_UHDM_REV_SCHs (c_f_dsch_UHDM_REV_SCHs* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_Type_specific_fields (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_UHDM_SCHs (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_CCSH_ENCODER_TYPE_BY_PILOT_SCHs */
void FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_SCHs (c_CCSH_ENCODER_TYPE_BY_PILOT_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_CCSH_ENCODER_TYPE_BY_PILOT */
void FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (c_CCSH_ENCODER_TYPE_BY_PILOT* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_Type_specific_fields (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_UHDM_SCHs (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->data_06.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data (sp->data_06.data[i0]);
		EDFree (sp->data_06.data[i0]);
	}
	if (sp->data_06.data) {EDFree (sp->data_06.data); sp->data_06.data = NULL; sp->data_06.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_Type_specific_fields (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_UHDM_SCHs (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->FOR_PDCCH_WALSHs.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data (sp->FOR_PDCCH_WALSHs.data[i0]);
		EDFree (sp->FOR_PDCCH_WALSHs.data[i0]);
	}
	if (sp->FOR_PDCCH_WALSHs.data) {EDFree (sp->FOR_PDCCH_WALSHs.data); sp->FOR_PDCCH_WALSHs.data = NULL; sp->FOR_PDCCH_WALSHs.allocatedItems=0; /*FR02A*/}
	if (sp->FOR_CPCSCH_Present) {
		if (sp->FOR_CPCSCH->value != NULL) {
			EDFree (sp->FOR_CPCSCH->value);
			sp->FOR_CPCSCH->value = NULL;
		}
		sp->FOR_CPCSCH->usedBits = 0;
		EDFree (sp->FOR_CPCSCH);
		sp->FOR_CPCSCH_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->FOR_GCHs.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data (sp->FOR_GCHs.data[i0]);
		EDFree (sp->FOR_GCHs.data[i0]);
	}
	if (sp->FOR_GCHs.data) {EDFree (sp->FOR_GCHs.data); sp->FOR_GCHs.data = NULL; sp->FOR_GCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_Type_specific_fields (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

void SETPRESENT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_CPCSCH (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FOR_CPCSCH_Present == present) return;
	sp->FOR_CPCSCH_Present = present;
	if (present) {
		/*-->*/sp->FOR_CPCSCH = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_CPCSCH*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_CPCSCH));
		sp->FOR_CPCSCH->value = NULL;
		sp->FOR_CPCSCH->usedBits = 0;
	} else {
		if (sp->FOR_CPCSCH->value != NULL) {
			EDFree (sp->FOR_CPCSCH->value);
			sp->FOR_CPCSCH->value = NULL;
		}
		sp->FOR_CPCSCH->usedBits = 0;
		EDFree (sp->FOR_CPCSCH);
	}
}

/* Free function for struct c_f_dsch_GEM_GE_RECs_data_GE_REC_c00 */
void FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c00 (c_f_dsch_GEM_GE_RECs_data_GE_REC_c00* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_GEM_GE_RECs_data_GE_REC_c02 */
void FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c02 (c_f_dsch_GEM_GE_RECs_data_GE_REC_c02* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs */
void FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs (c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_GEM_GE_RECs_data_GE_REC_c01 */
void FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01 (c_f_dsch_GEM_GE_RECs_data_GE_REC_c01* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RC_PARAMS_RECORDSs.items; i0++) {
		FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data (sp->RC_PARAMS_RECORDSs.data[i0]);
		EDFree (sp->RC_PARAMS_RECORDSs.data[i0]);
	}
	if (sp->RC_PARAMS_RECORDSs.data) {EDFree (sp->RC_PARAMS_RECORDSs.data); sp->RC_PARAMS_RECORDSs.data = NULL; sp->RC_PARAMS_RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_MUHDM_FOR_SCHs */
void FREE_c_f_dsch_MUHDM_FOR_SCHs (c_f_dsch_MUHDM_FOR_SCHs* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MUHDM_REV_SCHs */
void FREE_c_f_dsch_MUHDM_REV_SCHs (c_f_dsch_MUHDM_REV_SCHs* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_Type_specific_fields (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_MUHDM_SCHs (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM_SCHs */
void FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM_SCHs (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM */
void FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_Type_specific_fields (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_MUHDM_SCHs (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->data_06.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data (sp->data_06.data[i0]);
		EDFree (sp->data_06.data[i0]);
	}
	if (sp->data_06.data) {EDFree (sp->data_06.data); sp->data_06.data = NULL; sp->data_06.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_Type_specific_fields (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_MUHDM_SCHs (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->FOR_PDCCH_WALSHs.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data (sp->FOR_PDCCH_WALSHs.data[i0]);
		EDFree (sp->FOR_PDCCH_WALSHs.data[i0]);
	}
	if (sp->FOR_PDCCH_WALSHs.data) {EDFree (sp->FOR_PDCCH_WALSHs.data); sp->FOR_PDCCH_WALSHs.data = NULL; sp->FOR_PDCCH_WALSHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->SCHs.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data (sp->SCHs.data[i0]);
		EDFree (sp->SCHs.data[i0]);
	}
	if (sp->SCHs.data) {EDFree (sp->SCHs.data); sp->SCHs.data = NULL; sp->SCHs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_Type_specific_fields (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for variable-sized sequence c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs */
void FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs (c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_GEM_GE_RECs_data_GE_REC_c01 */
void FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c01 (c_r_csch_GEM_GE_RECs_data_GE_REC_c01* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->BAND_SUBCLASSs.items; i0++) {
		FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data (sp->BAND_SUBCLASSs.data[i0]);
		EDFree (sp->BAND_SUBCLASSs.data[i0]);
	}
	if (sp->BAND_SUBCLASSs.data) {EDFree (sp->BAND_SUBCLASSs.data); sp->BAND_SUBCLASSs.data = NULL; sp->BAND_SUBCLASSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_csch_GEM_GE_RECs_data_GE_REC_c02 */
void FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c02 (c_r_csch_GEM_GE_RECs_data_GE_REC_c02* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->ADD_GEO_LOC_TYPE.value != NULL) {
		EDFree (sp->ADD_GEO_LOC_TYPE.value);
		sp->ADD_GEO_LOC_TYPE.value = NULL;
	}
	sp->ADD_GEO_LOC_TYPE.usedBits = 0;

}

/* Free function for struct c_r_csch_GEM_GE_RECs_data_GE_REC_c03 */
void FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c03 (c_r_csch_GEM_GE_RECs_data_GE_REC_c03* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_GEM_GE_RECs_data_GE_REC_c04 */
void FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c04 (c_r_csch_GEM_GE_RECs_data_GE_REC_c04* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->REQ_BLOB.value != NULL) {
		EDFree (sp->REQ_BLOB.value);
		sp->REQ_BLOB.value = NULL;
	}
	sp->REQ_BLOB.usedBits = 0;

}

/* Free function for struct c_r_dsch_PPSMM_PILOTs_01_data_records_Type_specific_fields_000 */
void FREE_c_r_dsch_PPSMM_PILOTs_01_data_records_Type_specific_fields_000 (c_r_dsch_PPSMM_PILOTs_01_data_records_Type_specific_fields_000* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->PILOT_WALSH.value != NULL) {
		EDFree (sp->PILOT_WALSH.value);
		sp->PILOT_WALSH.value = NULL;
	}
	sp->PILOT_WALSH.usedBits = 0;

}

/* Free function for struct c_r_dsch_EHOCM_PILOTSs_data_records_Type_specific_fields_000 */
void FREE_c_r_dsch_EHOCM_PILOTSs_data_records_Type_specific_fields_000 (c_r_dsch_EHOCM_PILOTSs_data_records_Type_specific_fields_000* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->PILOT_WALSH.value != NULL) {
		EDFree (sp->PILOT_WALSH.value);
		sp->PILOT_WALSH.value = NULL;
	}
	sp->PILOT_WALSH.usedBits = 0;

}

/* Free function for variable-sized sequence c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs */
void FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs (c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_GEM_GE_RECs_data_GE_REC_c01 */
void FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01 (c_r_dsch_GEM_GE_RECs_data_GE_REC_c01* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->BAND_SUBCLASSs.items; i0++) {
		FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data (sp->BAND_SUBCLASSs.data[i0]);
		EDFree (sp->BAND_SUBCLASSs.data[i0]);
	}
	if (sp->BAND_SUBCLASSs.data) {EDFree (sp->BAND_SUBCLASSs.data); sp->BAND_SUBCLASSs.data = NULL; sp->BAND_SUBCLASSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_GEM_GE_RECs_data_GE_REC_c02 */
void FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c02 (c_r_dsch_GEM_GE_RECs_data_GE_REC_c02* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->ADD_GEO_LOC_TYPE.value != NULL) {
		EDFree (sp->ADD_GEO_LOC_TYPE.value);
		sp->ADD_GEO_LOC_TYPE.value = NULL;
	}
	sp->ADD_GEO_LOC_TYPE.usedBits = 0;

}

/* Free function for struct c_r_dsch_GEM_GE_RECs_data_GE_REC_c03 */
void FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c03 (c_r_dsch_GEM_GE_RECs_data_GE_REC_c03* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_GEM_GE_RECs_data_GE_REC_c04 */
void FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c04 (c_r_dsch_GEM_GE_RECs_data_GE_REC_c04* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->REQ_BLOB.value != NULL) {
		EDFree (sp->REQ_BLOB.value);
		sp->REQ_BLOB.value = NULL;
	}
	sp->REQ_BLOB.usedBits = 0;

}

/* Free function for struct c_InfoRec_2_7_4_encrypted */
void FREE_c_InfoRec_2_7_4_encrypted (c_InfoRec_2_7_4_encrypted* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_R_InformationRecords (&(sp->Type_specific_fields));

}

/* Free function for struct c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data */
void FREE_c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data (c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01));
		INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 (sp->c01);
	} else {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02));
		INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 (sp->c02);
	} else {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03));
		INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 (sp->c03);
	} else {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

/* Free function for struct c_f_csch_ECCLM_BANDs_data_data_02_data */
void FREE_c_f_csch_ECCLM_BANDs_data_data_02_data (c_f_csch_ECCLM_BANDs_data_data_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data */
void FREE_c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data (c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data */
void FREE_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs */
void FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data */
void FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->BCMC_FLOW_DISCRIMINATOR.value);
		sp->BCMC_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->BCMC_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->LPM_ENTRIESs.items; i0++) {
		FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data (sp->LPM_ENTRIESs.data[i0]);
		EDFree (sp->LPM_ENTRIESs.data[i0]);
	}
	if (sp->LPM_ENTRIESs.data) {EDFree (sp->LPM_ENTRIESs.data); sp->LPM_ENTRIESs.data = NULL; sp->LPM_ENTRIESs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data */
void FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data */
void FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01));
		INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 (sp->c01);
	} else {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02));
		INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 (sp->c02);
	} else {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03));
		INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 (sp->c03);
	} else {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

/* Free function for variable-sized sequence c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs */
void FREE_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data */
void FREE_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FOR_SUPs.items; i0++) {
		FREE_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data (sp->FOR_SUPs.data[i0]);
		EDFree (sp->FOR_SUPs.data[i0]);
	}
	if (sp->FOR_SUPs.data) {EDFree (sp->FOR_SUPs.data); sp->FOR_SUPs.data = NULL; sp->FOR_SUPs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data */
void FREE_c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data (c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs */
void FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data */
void FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->BCMC_FLOW_DISCRIMINATOR.value);
		sp->BCMC_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->BCMC_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->LPM_ENTRIESs.items; i0++) {
		FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data (sp->LPM_ENTRIESs.data[i0]);
		EDFree (sp->LPM_ENTRIESs.data[i0]);
	}
	if (sp->LPM_ENTRIESs.data) {EDFree (sp->LPM_ENTRIESs.data); sp->LPM_ENTRIESs.data = NULL; sp->LPM_ENTRIESs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data */
void FREE_c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data (c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
		sp->Type_specific_fields_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data_Type_specific_fields (c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_Present == present) return;
	sp->Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields);
		EDFree (sp->Type_specific_fields);
	}
}

/* Free function for variable-sized sequence c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs */
void FREE_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data */
void FREE_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SUP_SHOs.items; i0++) {
		FREE_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data (sp->SUP_SHOs.data[i0]);
		EDFree (sp->SUP_SHOs.data[i0]);
	}
	if (sp->SUP_SHOs.data) {EDFree (sp->SUP_SHOs.data); sp->SUP_SHOs.data = NULL; sp->SUP_SHOs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data */
void FREE_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->BCMC_FLOW_DISCRIMINATOR.value);
		sp->BCMC_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->BCMC_FLOW_DISCRIMINATOR.usedBits = 0;

}

/* Free function for struct c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data */
void FREE_c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->BCMC_FLOW_DISCRIMINATOR.value);
		sp->BCMC_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->BCMC_FLOW_DISCRIMINATOR.usedBits = 0;

}

/* Free function for struct c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data */
void FREE_c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->BCMC_FLOW_DISCRIMINATOR.value);
		sp->BCMC_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->BCMC_FLOW_DISCRIMINATOR.usedBits = 0;

}

/* Free function for struct c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data */
void FREE_c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->BCMC_FLOW_DISCRIMINATOR.value);
		sp->BCMC_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->BCMC_FLOW_DISCRIMINATOR.usedBits = 0;

}

/* Free function for struct c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data */
void FREE_c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->BCMC_FLOW_DISCRIMINATOR.value);
		sp->BCMC_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->BCMC_FLOW_DISCRIMINATOR.usedBits = 0;

}

/* Free function for struct c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data */
void FREE_c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->BCMC_FLOW_DISCRIMINATOR.value);
		sp->BCMC_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->BCMC_FLOW_DISCRIMINATOR.usedBits = 0;

}

/* Free function for struct c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data */
void FREE_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->BCMC_FLOW_DISCRIMINATOR.value);
		sp->BCMC_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->BCMC_FLOW_DISCRIMINATOR.usedBits = 0;

}

/* Free function for struct c_R_Base_Station_Challenge_Order */
void FREE_c_R_Base_Station_Challenge_Order (c_R_Base_Station_Challenge_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_R_Service_Option_Request_Order */
void FREE_c_R_Service_Option_Request_Order (c_R_Service_Option_Request_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_R_Service_Option_Response_Order */
void FREE_c_R_Service_Option_Response_Order (c_R_Service_Option_Response_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_R_Mobile_Station_Reject_Order */
void FREE_c_R_Mobile_Station_Reject_Order (c_R_Mobile_Station_Reject_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_R_Release_Order */
void FREE_c_R_Release_Order (c_R_Release_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_R_Fast_Call_Setup_Order */
void FREE_c_R_Fast_Call_Setup_Order (c_R_Fast_Call_Setup_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_F_Base_Station_Challenge_Confirmation_Order */
void FREE_c_F_Base_Station_Challenge_Confirmation_Order (c_F_Base_Station_Challenge_Confirmation_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_F_Service_Option_Request_Order */
void FREE_c_F_Service_Option_Request_Order (c_F_Service_Option_Request_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_F_Service_Option_Response_Order */
void FREE_c_F_Service_Option_Response_Order (c_F_Service_Option_Response_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_F_Status_Request_Order */
void FREE_c_F_Status_Request_Order (c_F_Status_Request_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_F_Registration_Accepted_Order */
void FREE_c_F_Registration_Accepted_Order (c_F_Registration_Accepted_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_F_Periodic_Pilot_Measurement_Request_Order */
void FREE_c_F_Periodic_Pilot_Measurement_Request_Order (c_F_Periodic_Pilot_Measurement_Request_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_F_Retry_Order */
void FREE_c_F_Retry_Order (c_F_Retry_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_F_Base_Station_Reject_Order */
void FREE_c_F_Base_Station_Reject_Order (c_F_Base_Station_Reject_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_F_BCMC_Order_BCMC_PROGRAMSs */
void FREE_c_F_BCMC_Order_BCMC_PROGRAMSs (c_F_BCMC_Order_BCMC_PROGRAMSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_F_BCMC_Order_BCMC_PROGRAMSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_F_BCMC_Order */
void FREE_c_F_BCMC_Order (c_F_BCMC_Order* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->BCMC_PROGRAMSs.items; i0++) {
		FREE_c_F_BCMC_Order_BCMC_PROGRAMSs_data (sp->BCMC_PROGRAMSs.data[i0]);
		EDFree (sp->BCMC_PROGRAMSs.data[i0]);
	}
	if (sp->BCMC_PROGRAMSs.data) {EDFree (sp->BCMC_PROGRAMSs.data); sp->BCMC_PROGRAMSs.data = NULL; sp->BCMC_PROGRAMSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_F_Fast_Call_Setup_Order */
void FREE_c_F_Fast_Call_Setup_Order (c_F_Fast_Call_Setup_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_F_Service_Status_Order_data_01 */
void FREE_c_F_Service_Status_Order_data_01 (c_F_Service_Status_Order_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_F_Service_Status_Order_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_F_Service_Status_Order */
void FREE_c_F_Service_Status_Order (c_F_Service_Status_Order* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_F_Service_Status_Order_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_F_Location_Services_Order */
void FREE_c_F_Location_Services_Order (c_F_Location_Services_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_F_EACH_Selective_Acknowledgment_Order */
void FREE_c_F_EACH_Selective_Acknowledgment_Order (c_F_EACH_Selective_Acknowledgment_Order* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->SELECTIVE_ACK.value != NULL) {
		EDFree (sp->SELECTIVE_ACK.value);
		sp->SELECTIVE_ACK.value = NULL;
	}
	sp->SELECTIVE_ACK.usedBits = 0;

}

/* Free function for union c_F_InformationRecords */
void FREE_c_F_InformationRecords (c_F_InformationRecords* sp) {
	switch (sp->Present) {
		case U_c_F_InformationRecords_p_generic: {
			if (sp->u.p_generic->FieldData.value != NULL) {
				EDFree (sp->u.p_generic->FieldData.value);
				sp->u.p_generic->FieldData.value = NULL;
			}
			sp->u.p_generic->FieldData.usedBits = 0;
			EDFree (sp->u.p_generic);
			sp->u.p_generic = NULL;

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Display: {
			FREE_c_InfoRec_Display (sp->u.p_InfoRec_Display);
			EDFree (sp->u.p_InfoRec_Display);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Called_Party_Number: {
			FREE_c_InfoRec_Called_Party_Number (sp->u.p_InfoRec_Called_Party_Number);
			EDFree (sp->u.p_InfoRec_Called_Party_Number);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Calling_Party_Number: {
			FREE_c_InfoRec_Calling_Party_Number (sp->u.p_InfoRec_Calling_Party_Number);
			EDFree (sp->u.p_InfoRec_Calling_Party_Number);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Connected_Number: {
			FREE_c_InfoRec_Connected_Number (sp->u.p_InfoRec_Connected_Number);
			EDFree (sp->u.p_InfoRec_Connected_Number);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Signal: {
			FREE_c_InfoRec_Signal (sp->u.p_InfoRec_Signal);
			EDFree (sp->u.p_InfoRec_Signal);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Message_Waiting: {
			FREE_c_InfoRec_Message_Waiting (sp->u.p_InfoRec_Message_Waiting);
			EDFree (sp->u.p_InfoRec_Message_Waiting);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Service_Configuration: {
			FREE_c_InfoRec_Service_Configuration (sp->u.p_InfoRec_Service_Configuration);
			EDFree (sp->u.p_InfoRec_Service_Configuration);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Called_Party_Subaddress: {
			FREE_c_InfoRec_Called_Party_Subaddress (sp->u.p_InfoRec_Called_Party_Subaddress);
			EDFree (sp->u.p_InfoRec_Called_Party_Subaddress);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Calling_Party_Subaddress: {
			FREE_c_InfoRec_Calling_Party_Subaddress (sp->u.p_InfoRec_Calling_Party_Subaddress);
			EDFree (sp->u.p_InfoRec_Calling_Party_Subaddress);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Connected_Subaddress: {
			FREE_c_InfoRec_Connected_Subaddress (sp->u.p_InfoRec_Connected_Subaddress);
			EDFree (sp->u.p_InfoRec_Connected_Subaddress);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Redirecting_Number: {
			FREE_c_InfoRec_Redirecting_Number (sp->u.p_InfoRec_Redirecting_Number);
			EDFree (sp->u.p_InfoRec_Redirecting_Number);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Redirecting_Subaddress: {
			FREE_c_InfoRec_Redirecting_Subaddress (sp->u.p_InfoRec_Redirecting_Subaddress);
			EDFree (sp->u.p_InfoRec_Redirecting_Subaddress);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Meter_Pulses: {
			FREE_c_InfoRec_Meter_Pulses (sp->u.p_InfoRec_Meter_Pulses);
			EDFree (sp->u.p_InfoRec_Meter_Pulses);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Parametric_Alerting: {
			FREE_c_InfoRec_Parametric_Alerting (sp->u.p_InfoRec_Parametric_Alerting);
			EDFree (sp->u.p_InfoRec_Parametric_Alerting);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Line_Control: {
			FREE_c_InfoRec_Line_Control (sp->u.p_InfoRec_Line_Control);
			EDFree (sp->u.p_InfoRec_Line_Control);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Extended_Display: {
			FREE_c_InfoRec_Extended_Display (sp->u.p_InfoRec_Extended_Display);
			EDFree (sp->u.p_InfoRec_Extended_Display);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Non_Negotiable_Service_Configuration: {
			FREE_c_InfoRec_Non_Negotiable_Service_Configuration (sp->u.p_InfoRec_Non_Negotiable_Service_Configuration);
			EDFree (sp->u.p_InfoRec_Non_Negotiable_Service_Configuration);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Multiple_Character_Extended_Display: {
			FREE_c_InfoRec_Multiple_Character_Extended_Display (sp->u.p_InfoRec_Multiple_Character_Extended_Display);
			EDFree (sp->u.p_InfoRec_Multiple_Character_Extended_Display);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Call_Waiting_Indicator: {
			FREE_c_InfoRec_Call_Waiting_Indicator (sp->u.p_InfoRec_Call_Waiting_Indicator);
			EDFree (sp->u.p_InfoRec_Call_Waiting_Indicator);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Enhanced_Multiple_Character_Extended_Display: {
			FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display (sp->u.p_InfoRec_Enhanced_Multiple_Character_Extended_Display);
			EDFree (sp->u.p_InfoRec_Enhanced_Multiple_Character_Extended_Display);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Extended_Record_Type_International: {
			FREE_c_InfoRec_Extended_Record_Type_International (sp->u.p_InfoRec_Extended_Record_Type_International);
			EDFree (sp->u.p_InfoRec_Extended_Record_Type_International);

			break;
		}

		default:;
	}
	sp->Present = U_c_F_InformationRecords_NONE;
}

/* Set-present function for c_F_InformationRecords LEVEL = -1 */
void GLOB_SETPRESENT_c_F_InformationRecords (c_F_InformationRecords* sp, TPRESENT_c_F_InformationRecords toBeSetPresent) 
{
	FREE_c_F_InformationRecords (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_F_InformationRecords_p_generic: {
			sp->u.p_generic = (c_F_InformationRecords_p_generic*)EDAlloc (sizeof (c_F_InformationRecords_p_generic));
			ED_RESET_MEM (sp->u.p_generic, sizeof (c_F_InformationRecords_p_generic));
			(sp->u.p_generic->RECORD_TYPE) = 0;
			sp->u.p_generic->FieldData.value = NULL;
			sp->u.p_generic->FieldData.usedBits = 0;
			

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Display: {
			sp->u.p_InfoRec_Display = (c_InfoRec_Display*)EDAlloc (sizeof (c_InfoRec_Display));
			INIT_c_InfoRec_Display (sp->u.p_InfoRec_Display);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Called_Party_Number: {
			sp->u.p_InfoRec_Called_Party_Number = (c_InfoRec_Called_Party_Number*)EDAlloc (sizeof (c_InfoRec_Called_Party_Number));
			INIT_c_InfoRec_Called_Party_Number (sp->u.p_InfoRec_Called_Party_Number);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Calling_Party_Number: {
			sp->u.p_InfoRec_Calling_Party_Number = (c_InfoRec_Calling_Party_Number*)EDAlloc (sizeof (c_InfoRec_Calling_Party_Number));
			INIT_c_InfoRec_Calling_Party_Number (sp->u.p_InfoRec_Calling_Party_Number);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Connected_Number: {
			sp->u.p_InfoRec_Connected_Number = (c_InfoRec_Connected_Number*)EDAlloc (sizeof (c_InfoRec_Connected_Number));
			INIT_c_InfoRec_Connected_Number (sp->u.p_InfoRec_Connected_Number);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Signal: {
			sp->u.p_InfoRec_Signal = (c_InfoRec_Signal*)EDAlloc (sizeof (c_InfoRec_Signal));
			INIT_c_InfoRec_Signal (sp->u.p_InfoRec_Signal);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Message_Waiting: {
			sp->u.p_InfoRec_Message_Waiting = (c_InfoRec_Message_Waiting*)EDAlloc (sizeof (c_InfoRec_Message_Waiting));
			INIT_c_InfoRec_Message_Waiting (sp->u.p_InfoRec_Message_Waiting);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Service_Configuration: {
			sp->u.p_InfoRec_Service_Configuration = (c_InfoRec_Service_Configuration*)EDAlloc (sizeof (c_InfoRec_Service_Configuration));
			INIT_c_InfoRec_Service_Configuration (sp->u.p_InfoRec_Service_Configuration);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Called_Party_Subaddress: {
			sp->u.p_InfoRec_Called_Party_Subaddress = (c_InfoRec_Called_Party_Subaddress*)EDAlloc (sizeof (c_InfoRec_Called_Party_Subaddress));
			INIT_c_InfoRec_Called_Party_Subaddress (sp->u.p_InfoRec_Called_Party_Subaddress);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Calling_Party_Subaddress: {
			sp->u.p_InfoRec_Calling_Party_Subaddress = (c_InfoRec_Calling_Party_Subaddress*)EDAlloc (sizeof (c_InfoRec_Calling_Party_Subaddress));
			INIT_c_InfoRec_Calling_Party_Subaddress (sp->u.p_InfoRec_Calling_Party_Subaddress);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Connected_Subaddress: {
			sp->u.p_InfoRec_Connected_Subaddress = (c_InfoRec_Connected_Subaddress*)EDAlloc (sizeof (c_InfoRec_Connected_Subaddress));
			INIT_c_InfoRec_Connected_Subaddress (sp->u.p_InfoRec_Connected_Subaddress);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Redirecting_Number: {
			sp->u.p_InfoRec_Redirecting_Number = (c_InfoRec_Redirecting_Number*)EDAlloc (sizeof (c_InfoRec_Redirecting_Number));
			INIT_c_InfoRec_Redirecting_Number (sp->u.p_InfoRec_Redirecting_Number);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Redirecting_Subaddress: {
			sp->u.p_InfoRec_Redirecting_Subaddress = (c_InfoRec_Redirecting_Subaddress*)EDAlloc (sizeof (c_InfoRec_Redirecting_Subaddress));
			INIT_c_InfoRec_Redirecting_Subaddress (sp->u.p_InfoRec_Redirecting_Subaddress);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Meter_Pulses: {
			sp->u.p_InfoRec_Meter_Pulses = (c_InfoRec_Meter_Pulses*)EDAlloc (sizeof (c_InfoRec_Meter_Pulses));
			INIT_c_InfoRec_Meter_Pulses (sp->u.p_InfoRec_Meter_Pulses);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Parametric_Alerting: {
			sp->u.p_InfoRec_Parametric_Alerting = (c_InfoRec_Parametric_Alerting*)EDAlloc (sizeof (c_InfoRec_Parametric_Alerting));
			INIT_c_InfoRec_Parametric_Alerting (sp->u.p_InfoRec_Parametric_Alerting);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Line_Control: {
			sp->u.p_InfoRec_Line_Control = (c_InfoRec_Line_Control*)EDAlloc (sizeof (c_InfoRec_Line_Control));
			INIT_c_InfoRec_Line_Control (sp->u.p_InfoRec_Line_Control);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Extended_Display: {
			sp->u.p_InfoRec_Extended_Display = (c_InfoRec_Extended_Display*)EDAlloc (sizeof (c_InfoRec_Extended_Display));
			INIT_c_InfoRec_Extended_Display (sp->u.p_InfoRec_Extended_Display);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Non_Negotiable_Service_Configuration: {
			sp->u.p_InfoRec_Non_Negotiable_Service_Configuration = (c_InfoRec_Non_Negotiable_Service_Configuration*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration (sp->u.p_InfoRec_Non_Negotiable_Service_Configuration);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Multiple_Character_Extended_Display: {
			sp->u.p_InfoRec_Multiple_Character_Extended_Display = (c_InfoRec_Multiple_Character_Extended_Display*)EDAlloc (sizeof (c_InfoRec_Multiple_Character_Extended_Display));
			INIT_c_InfoRec_Multiple_Character_Extended_Display (sp->u.p_InfoRec_Multiple_Character_Extended_Display);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Call_Waiting_Indicator: {
			sp->u.p_InfoRec_Call_Waiting_Indicator = (c_InfoRec_Call_Waiting_Indicator*)EDAlloc (sizeof (c_InfoRec_Call_Waiting_Indicator));
			INIT_c_InfoRec_Call_Waiting_Indicator (sp->u.p_InfoRec_Call_Waiting_Indicator);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Enhanced_Multiple_Character_Extended_Display: {
			sp->u.p_InfoRec_Enhanced_Multiple_Character_Extended_Display = (c_InfoRec_Enhanced_Multiple_Character_Extended_Display*)EDAlloc (sizeof (c_InfoRec_Enhanced_Multiple_Character_Extended_Display));
			INIT_c_InfoRec_Enhanced_Multiple_Character_Extended_Display (sp->u.p_InfoRec_Enhanced_Multiple_Character_Extended_Display);

			break;
		}
		case U_c_F_InformationRecords_p_InfoRec_Extended_Record_Type_International: {
			sp->u.p_InfoRec_Extended_Record_Type_International = (c_InfoRec_Extended_Record_Type_International*)EDAlloc (sizeof (c_InfoRec_Extended_Record_Type_International));
			INIT_c_InfoRec_Extended_Record_Type_International (sp->u.p_InfoRec_Extended_Record_Type_International);

			break;
		}
		default:;	}
}

/* Free function for struct c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data */
void FREE_c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data (c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data */
void FREE_c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data (c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GNLM_NGHBRs_01_data_records */
void FREE_c_f_csch_GNLM_NGHBRs_01_data_records (c_f_csch_GNLM_NGHBRs_01_data_records* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_GNLM_NGHBRs_01_data_records_Type_specific_fields (&(sp->Type_specific_fields));

}

/* Free function for variable-sized sequence c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PRI_NGHBRs.items; i0++) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data (sp->PRI_NGHBRs.data[i0]);
		EDFree (sp->PRI_NGHBRs.data[i0]);
	}
	if (sp->PRI_NGHBRs.data) {EDFree (sp->PRI_NGHBRs.data); sp->PRI_NGHBRs.data = NULL; sp->PRI_NGHBRs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_EGSRDM_ADD_RECORDs_data_typeSpecificFields */
void FREE_c_f_csch_EGSRDM_ADD_RECORDs_data_typeSpecificFields (c_f_csch_EGSRDM_ADD_RECORDs_data_typeSpecificFields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->field_1_Present) {
		FREE_c_f_csch_EGSRDM_Type_Specific_Fields_1 (sp->field_1);
		EDFree (sp->field_1);
		sp->field_1_Present = ED_FALSE;
	}
	if (sp->field_2_Present) {
		FREE_c_f_csch_EGSRDM_Type_Specific_Fields_2 (sp->field_2);
		EDFree (sp->field_2);
		sp->field_2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_EGSRDM_ADD_RECORDs_data_typeSpecificFields_field_1 (c_f_csch_EGSRDM_ADD_RECORDs_data_typeSpecificFields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->field_1_Present == present) return;
	sp->field_1_Present = present;
	if (present) {
		/*-->*/sp->field_1 = (c_f_csch_EGSRDM_Type_Specific_Fields_1*)EDAlloc (sizeof (c_f_csch_EGSRDM_Type_Specific_Fields_1));
		INIT_c_f_csch_EGSRDM_Type_Specific_Fields_1 (sp->field_1);
	} else {
		FREE_c_f_csch_EGSRDM_Type_Specific_Fields_1 (sp->field_1);
		EDFree (sp->field_1);
	}
}

void SETPRESENT_c_f_csch_EGSRDM_ADD_RECORDs_data_typeSpecificFields_field_2 (c_f_csch_EGSRDM_ADD_RECORDs_data_typeSpecificFields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->field_2_Present == present) return;
	sp->field_2_Present = present;
	if (present) {
		/*-->*/sp->field_2 = (c_f_csch_EGSRDM_Type_Specific_Fields_2*)EDAlloc (sizeof (c_f_csch_EGSRDM_Type_Specific_Fields_2));
		INIT_c_f_csch_EGSRDM_Type_Specific_Fields_2 (sp->field_2);
	} else {
		FREE_c_f_csch_EGSRDM_Type_Specific_Fields_2 (sp->field_2);
		EDFree (sp->field_2);
	}
}

/* Free function for variable-sized sequence c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01 */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02 */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->NGHBRs.items; i0++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data (sp->NGHBRs.data[i0]);
		EDFree (sp->NGHBRs.data[i0]);
	}
	if (sp->NGHBRs.data) {EDFree (sp->NGHBRs.data); sp->NGHBRs.data = NULL; sp->NGHBRs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->NGHBRs_01.items; i0++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data (sp->NGHBRs_01.data[i0]);
		EDFree (sp->NGHBRs_01.data[i0]);
	}
	if (sp->NGHBRs_01.data) {EDFree (sp->NGHBRs_01.data); sp->NGHBRs_01.data = NULL; sp->NGHBRs_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_02.items; i0++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data (sp->data_02.data[i0]);
		EDFree (sp->data_02.data[i0]);
	}
	if (sp->data_02.data) {EDFree (sp->data_02.data); sp->data_02.data = NULL; sp->data_02.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->ANALOG_NGHBR.items; i0++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data (sp->ANALOG_NGHBR.data[i0]);
		EDFree (sp->ANALOG_NGHBR.data[i0]);
	}
	if (sp->ANALOG_NGHBR.data) {EDFree (sp->ANALOG_NGHBR.data); sp->ANALOG_NGHBR.data = NULL; sp->ANALOG_NGHBR.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->HRPD_NGHBR.items; i0++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data (sp->HRPD_NGHBR.data[i0]);
		EDFree (sp->HRPD_NGHBR.data[i0]);
	}
	if (sp->HRPD_NGHBR.data) {EDFree (sp->HRPD_NGHBR.data); sp->HRPD_NGHBR.data = NULL; sp->HRPD_NGHBR.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_APIDM_data_01_data_HO_INFO_VALUE */
void FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c01 (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c01*)EDAlloc (sizeof (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c01));
		INIT_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c01 (sp->c01);
	} else {
		FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02 (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02*)EDAlloc (sizeof (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02));
		INIT_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02 (sp->c02);
	} else {
		FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03 (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03*)EDAlloc (sizeof (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03));
		INIT_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03 (sp->c03);
	} else {
		FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

/* Free function for struct c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC */
void FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01 (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01*)EDAlloc (sizeof (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01));
		INIT_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01 (sp->c01);
	} else {
		FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c02 (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c02*)EDAlloc (sizeof (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c02));
		INIT_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c02 (sp->c02);
	} else {
		FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

/* Free function for struct c_f_csch_GOIM_GOI_RECs_data_GOI_REC */
void FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC (c_f_csch_GOIM_GOI_RECs_data_GOI_REC* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01 (c_f_csch_GOIM_GOI_RECs_data_GOI_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01*)EDAlloc (sizeof (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01));
		INIT_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01 (sp->c01);
	} else {
		FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02 (c_f_csch_GOIM_GOI_RECs_data_GOI_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02*)EDAlloc (sizeof (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02));
		INIT_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02 (sp->c02);
	} else {
		FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

/* Free function for struct c_f_csch_GEM_GE_RECs_data_GE_REC */
void FREE_c_f_csch_GEM_GE_RECs_data_GE_REC (c_f_csch_GEM_GE_RECs_data_GE_REC* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c00_Present) {
		FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c00 (sp->c00);
		EDFree (sp->c00);
		sp->c00_Present = ED_FALSE;
	}
	if (sp->c01_Present) {
		FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_GEM_GE_RECs_data_GE_REC_c00 (c_f_csch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c00_Present == present) return;
	sp->c00_Present = present;
	if (present) {
		/*-->*/sp->c00 = (c_f_csch_GEM_GE_RECs_data_GE_REC_c00*)EDAlloc (sizeof (c_f_csch_GEM_GE_RECs_data_GE_REC_c00));
		INIT_c_f_csch_GEM_GE_RECs_data_GE_REC_c00 (sp->c00);
	} else {
		FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c00 (sp->c00);
		EDFree (sp->c00);
	}
}

void SETPRESENT_c_f_csch_GEM_GE_RECs_data_GE_REC_c01 (c_f_csch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_csch_GEM_GE_RECs_data_GE_REC_c01*)EDAlloc (sizeof (c_f_csch_GEM_GE_RECs_data_GE_REC_c01));
		INIT_c_f_csch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
	} else {
		FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_csch_GEM_GE_RECs_data_GE_REC_c02 (c_f_csch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_csch_GEM_GE_RECs_data_GE_REC_c02*)EDAlloc (sizeof (c_f_csch_GEM_GE_RECs_data_GE_REC_c02));
		INIT_c_f_csch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
	} else {
		FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

/* Free function for variable-sized sequence c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs */
void FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs (c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 */
void FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 (c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->HRPD_NGHBRs.items; i0++) {
		FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data (sp->HRPD_NGHBRs.data[i0]);
		EDFree (sp->HRPD_NGHBRs.data[i0]);
	}
	if (sp->HRPD_NGHBRs.data) {EDFree (sp->HRPD_NGHBRs.data); sp->HRPD_NGHBRs.data = NULL; sp->HRPD_NGHBRs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_FGOIM_GOI_RECs_data_GOI_REC */
void FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01 (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01*)EDAlloc (sizeof (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01));
		INIT_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01 (sp->c01);
	} else {
		FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02 (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02*)EDAlloc (sizeof (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02));
		INIT_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02 (sp->c02);
	} else {
		FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

/* Free function for struct c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data */
void FREE_c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data (c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ENLUM_NGHBRs_01_data_records */
void FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records (c_f_dsch_ENLUM_NGHBRs_01_data_records* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records_Type_specific_fields (&(sp->Type_specific_fields));

}

/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data (c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->records_Present) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records (sp->records);
		EDFree (sp->records);
		sp->records_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->records_Present == present) return;
	sp->records_Present = present;
	if (present) {
		/*-->*/sp->records = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records));
		INIT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records (sp->records);
	} else {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data_records (sp->records);
		EDFree (sp->records);
	}
}

/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data (c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs */
void FREE_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs (c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record */
void FREE_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record (c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FOR_SUPs.items; i0++) {
		FREE_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data (sp->FOR_SUPs.data[i0]);
		EDFree (sp->FOR_SUPs.data[i0]);
	}
	if (sp->FOR_SUPs.data) {EDFree (sp->FOR_SUPs.data); sp->FOR_SUPs.data = NULL; sp->FOR_SUPs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs */
void FREE_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs (c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields */
void FREE_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields (c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_GEM_GE_RECs_data_GE_REC */
void FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC (c_f_dsch_GEM_GE_RECs_data_GE_REC* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c00_Present) {
		FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c00 (sp->c00);
		EDFree (sp->c00);
		sp->c00_Present = ED_FALSE;
	}
	if (sp->c01_Present) {
		FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_GEM_GE_RECs_data_GE_REC_c00 (c_f_dsch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c00_Present == present) return;
	sp->c00_Present = present;
	if (present) {
		/*-->*/sp->c00 = (c_f_dsch_GEM_GE_RECs_data_GE_REC_c00*)EDAlloc (sizeof (c_f_dsch_GEM_GE_RECs_data_GE_REC_c00));
		INIT_c_f_dsch_GEM_GE_RECs_data_GE_REC_c00 (sp->c00);
	} else {
		FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c00 (sp->c00);
		EDFree (sp->c00);
	}
}

void SETPRESENT_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01 (c_f_dsch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_dsch_GEM_GE_RECs_data_GE_REC_c01*)EDAlloc (sizeof (c_f_dsch_GEM_GE_RECs_data_GE_REC_c01));
		INIT_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
	} else {
		FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_dsch_GEM_GE_RECs_data_GE_REC_c02 (c_f_dsch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_dsch_GEM_GE_RECs_data_GE_REC_c02*)EDAlloc (sizeof (c_f_dsch_GEM_GE_RECs_data_GE_REC_c02));
		INIT_c_f_dsch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
	} else {
		FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

/* Free function for variable-sized sequence c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs */
void FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs (c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 */
void FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 (c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->HRPD_NGHBRs.items; i0++) {
		FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data (sp->HRPD_NGHBRs.data[i0]);
		EDFree (sp->HRPD_NGHBRs.data[i0]);
	}
	if (sp->HRPD_NGHBRs.data) {EDFree (sp->HRPD_NGHBRs.data); sp->HRPD_NGHBRs.data = NULL; sp->HRPD_NGHBRs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_csch_GEM_GE_RECs_data_GE_REC */
void FREE_c_r_csch_GEM_GE_RECs_data_GE_REC (c_r_csch_GEM_GE_RECs_data_GE_REC* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}
	if (sp->c04_Present) {
		FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c04 (sp->c04);
		EDFree (sp->c04);
		sp->c04_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_csch_GEM_GE_RECs_data_GE_REC_c01 (c_r_csch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_r_csch_GEM_GE_RECs_data_GE_REC_c01*)EDAlloc (sizeof (c_r_csch_GEM_GE_RECs_data_GE_REC_c01));
		INIT_c_r_csch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
	} else {
		FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_r_csch_GEM_GE_RECs_data_GE_REC_c02 (c_r_csch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_r_csch_GEM_GE_RECs_data_GE_REC_c02*)EDAlloc (sizeof (c_r_csch_GEM_GE_RECs_data_GE_REC_c02));
		INIT_c_r_csch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
	} else {
		FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_r_csch_GEM_GE_RECs_data_GE_REC_c03 (c_r_csch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_r_csch_GEM_GE_RECs_data_GE_REC_c03*)EDAlloc (sizeof (c_r_csch_GEM_GE_RECs_data_GE_REC_c03));
		INIT_c_r_csch_GEM_GE_RECs_data_GE_REC_c03 (sp->c03);
	} else {
		FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

void SETPRESENT_c_r_csch_GEM_GE_RECs_data_GE_REC_c04 (c_r_csch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c04_Present == present) return;
	sp->c04_Present = present;
	if (present) {
		/*-->*/sp->c04 = (c_r_csch_GEM_GE_RECs_data_GE_REC_c04*)EDAlloc (sizeof (c_r_csch_GEM_GE_RECs_data_GE_REC_c04));
		INIT_c_r_csch_GEM_GE_RECs_data_GE_REC_c04 (sp->c04);
	} else {
		FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c04 (sp->c04);
		EDFree (sp->c04);
	}
}

/* Free function for struct c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data */
void FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data */
void FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->PILOT_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->PILOT);
		EDFree (sp->PILOT);
		sp->PILOT_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data_PILOT (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->PILOT_Present == present) return;
	sp->PILOT_Present = present;
	if (present) {
		/*-->*/sp->PILOT = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->PILOT);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->PILOT);
		EDFree (sp->PILOT);
	}
}

/* Free function for struct c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data */
void FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data (c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_PPSMM_PILOTs_01_data_records */
void FREE_c_r_dsch_PPSMM_PILOTs_01_data_records (c_r_dsch_PPSMM_PILOTs_01_data_records* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_000_Present) {
		FREE_c_r_dsch_PPSMM_PILOTs_01_data_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
		EDFree (sp->Type_specific_fields_000);
		sp->Type_specific_fields_000_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_PPSMM_PILOTs_01_data_records_Type_specific_fields_000 (c_r_dsch_PPSMM_PILOTs_01_data_records* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_000_Present == present) return;
	sp->Type_specific_fields_000_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields_000 = (c_r_dsch_PPSMM_PILOTs_01_data_records_Type_specific_fields_000*)EDAlloc (sizeof (c_r_dsch_PPSMM_PILOTs_01_data_records_Type_specific_fields_000));
		INIT_c_r_dsch_PPSMM_PILOTs_01_data_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
	} else {
		FREE_c_r_dsch_PPSMM_PILOTs_01_data_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
		EDFree (sp->Type_specific_fields_000);
	}
}

/* Free function for struct c_r_dsch_EHOCM_PILOTSs_data_records */
void FREE_c_r_dsch_EHOCM_PILOTSs_data_records (c_r_dsch_EHOCM_PILOTSs_data_records* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_000_Present) {
		FREE_c_r_dsch_EHOCM_PILOTSs_data_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
		EDFree (sp->Type_specific_fields_000);
		sp->Type_specific_fields_000_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_EHOCM_PILOTSs_data_records_Type_specific_fields_000 (c_r_dsch_EHOCM_PILOTSs_data_records* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_000_Present == present) return;
	sp->Type_specific_fields_000_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields_000 = (c_r_dsch_EHOCM_PILOTSs_data_records_Type_specific_fields_000*)EDAlloc (sizeof (c_r_dsch_EHOCM_PILOTSs_data_records_Type_specific_fields_000));
		INIT_c_r_dsch_EHOCM_PILOTSs_data_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
	} else {
		FREE_c_r_dsch_EHOCM_PILOTSs_data_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
		EDFree (sp->Type_specific_fields_000);
	}
}

/* Free function for variable-sized sequence c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs */
void FREE_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs (c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields */
void FREE_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields (c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_HOSINM_HO_SUP_RECs_data_HO_SUP_REC */
void FREE_c_r_dsch_HOSINM_HO_SUP_RECs_data_HO_SUP_REC (c_r_dsch_HOSINM_HO_SUP_RECs_data_HO_SUP_REC* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AP_ID.value != NULL) {
		EDFree (sp->AP_ID.value);
		sp->AP_ID.value = NULL;
	}
	sp->AP_ID.usedBits = 0;

}

/* Free function for struct c_r_dsch_GEM_GE_RECs_data_GE_REC */
void FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC (c_r_dsch_GEM_GE_RECs_data_GE_REC* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}
	if (sp->c04_Present) {
		FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c04 (sp->c04);
		EDFree (sp->c04);
		sp->c04_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01 (c_r_dsch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_r_dsch_GEM_GE_RECs_data_GE_REC_c01*)EDAlloc (sizeof (c_r_dsch_GEM_GE_RECs_data_GE_REC_c01));
		INIT_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
	} else {
		FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_r_dsch_GEM_GE_RECs_data_GE_REC_c02 (c_r_dsch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_r_dsch_GEM_GE_RECs_data_GE_REC_c02*)EDAlloc (sizeof (c_r_dsch_GEM_GE_RECs_data_GE_REC_c02));
		INIT_c_r_dsch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
	} else {
		FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_r_dsch_GEM_GE_RECs_data_GE_REC_c03 (c_r_dsch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_r_dsch_GEM_GE_RECs_data_GE_REC_c03*)EDAlloc (sizeof (c_r_dsch_GEM_GE_RECs_data_GE_REC_c03));
		INIT_c_r_dsch_GEM_GE_RECs_data_GE_REC_c03 (sp->c03);
	} else {
		FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

void SETPRESENT_c_r_dsch_GEM_GE_RECs_data_GE_REC_c04 (c_r_dsch_GEM_GE_RECs_data_GE_REC* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c04_Present == present) return;
	sp->c04_Present = present;
	if (present) {
		/*-->*/sp->c04 = (c_r_dsch_GEM_GE_RECs_data_GE_REC_c04*)EDAlloc (sizeof (c_r_dsch_GEM_GE_RECs_data_GE_REC_c04));
		INIT_c_r_dsch_GEM_GE_RECs_data_GE_REC_c04 (sp->c04);
	} else {
		FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c04 (sp->c04);
		EDFree (sp->c04);
	}
}

/* Free function for struct c_STR_Type_specific_fields_BAND_CLASS */
void FREE_c_STR_Type_specific_fields_BAND_CLASS (c_STR_Type_specific_fields_BAND_CLASS* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_STR_Type_specific_fields_BAND_CLASS_and_OP_MODE */
void FREE_c_STR_Type_specific_fields_BAND_CLASS_and_OP_MODE (c_STR_Type_specific_fields_BAND_CLASS_and_OP_MODE* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_CAM_Additional_record_fields_c01 */
void FREE_c_f_csch_CAM_Additional_record_fields_c01 (c_f_csch_CAM_Additional_record_fields_c01* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_CAM_Additional_record_fields_c02_data_03 */
void FREE_c_f_csch_CAM_Additional_record_fields_c02_data_03 (c_f_csch_CAM_Additional_record_fields_c02_data_03* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_CAM_Additional_record_fields_c02_data_03_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_CAM_Additional_record_fields_c02 */
void FREE_c_f_csch_CAM_Additional_record_fields_c02 (c_f_csch_CAM_Additional_record_fields_c02* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_03.items; i0++) {
		FREE_c_f_csch_CAM_Additional_record_fields_c02_data_03_data (sp->data_03.data[i0]);
		EDFree (sp->data_03.data[i0]);
	}
	if (sp->data_03.data) {EDFree (sp->data_03.data); sp->data_03.data = NULL; sp->data_03.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_CAM_Additional_record_fields_c05 */
void FREE_c_f_csch_CAM_Additional_record_fields_c05 (c_f_csch_CAM_Additional_record_fields_c05* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_CAM_Additional_record_fields_c06 */
void FREE_c_f_csch_CAM_Additional_record_fields_c06 (c_f_csch_CAM_Additional_record_fields_c06* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_CAM_Additional_record_fields_c03 */
void FREE_c_f_csch_CAM_Additional_record_fields_c03 (c_f_csch_CAM_Additional_record_fields_c03* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_CAM_Additional_record_fields_c04_data_06 */
void FREE_c_f_csch_CAM_Additional_record_fields_c04_data_06 (c_f_csch_CAM_Additional_record_fields_c04_data_06* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_CAM_Additional_record_fields_c04_data_06_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_CAM_Additional_record_fields_c04 */
void FREE_c_f_csch_CAM_Additional_record_fields_c04 (c_f_csch_CAM_Additional_record_fields_c04* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_06.items; i0++) {
		FREE_c_f_csch_CAM_Additional_record_fields_c04_data_06_data (sp->data_06.data[i0]);
		EDFree (sp->data_06.data[i0]);
	}
	if (sp->data_06.data) {EDFree (sp->data_06.data); sp->data_06.data = NULL; sp->data_06.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs */
void FREE_c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs (c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c000 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c000 (c_f_csch_ECAM_Additional_record_fields_c000* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}
	if (sp->PLCM_39_Present) {
		EDFree (sp->PLCM_39);
		sp->PLCM_39_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c000_PLCM_39 (c_f_csch_ECAM_Additional_record_fields_c000* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->PLCM_39_Present == present) return;
	sp->PLCM_39_Present = present;
	if (present) {
		/*-->*/sp->PLCM_39 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->PLCM_39, 5);
	} else {
		EDFree (sp->PLCM_39);
	}
}

/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs */
void FREE_c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs (c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c001 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c001 (c_f_csch_ECAM_Additional_record_fields_c001* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c100 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c100 (c_f_csch_ECAM_Additional_record_fields_c100* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS (&(sp->CH_RECORD_FIELDS));
	if (sp->PLCM_39_Present) {
		EDFree (sp->PLCM_39);
		sp->PLCM_39_Present = ED_FALSE;
	}
	if (sp->SYNC_ID.value != NULL) {
		EDFree (sp->SYNC_ID.value);
		sp->SYNC_ID.value = NULL;
	}
	sp->SYNC_ID.usedBits = 0;

}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c100_PLCM_39 (c_f_csch_ECAM_Additional_record_fields_c100* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->PLCM_39_Present == present) return;
	sp->PLCM_39_Present = present;
	if (present) {
		/*-->*/sp->PLCM_39 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->PLCM_39, 5);
	} else {
		EDFree (sp->PLCM_39);
	}
}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c010 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c010 (c_f_csch_ECAM_Additional_record_fields_c010* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c011 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c011 (c_f_csch_ECAM_Additional_record_fields_c011* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs */
void FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ECAM_Additional_record_fields_c101 */
void FREE_c_f_csch_ECAM_Additional_record_fields_c101 (c_f_csch_ECAM_Additional_record_fields_c101* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->PLCM_39_Present) {
		EDFree (sp->PLCM_39);
		sp->PLCM_39_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}
	if (sp->SYNC_ID.value != NULL) {
		EDFree (sp->SYNC_ID.value);
		sp->SYNC_ID.value = NULL;
	}
	sp->SYNC_ID.usedBits = 0;
	if (sp->ADD_PLCM_FOR_FCH_39_Present) {
		EDFree (sp->ADD_PLCM_FOR_FCH_39);
		sp->ADD_PLCM_FOR_FCH_39_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c101_PLCM_39 (c_f_csch_ECAM_Additional_record_fields_c101* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->PLCM_39_Present == present) return;
	sp->PLCM_39_Present = present;
	if (present) {
		/*-->*/sp->PLCM_39 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->PLCM_39, 5);
	} else {
		EDFree (sp->PLCM_39);
	}
}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c101_ADD_PLCM_FOR_FCH_39 (c_f_csch_ECAM_Additional_record_fields_c101* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->ADD_PLCM_FOR_FCH_39_Present == present) return;
	sp->ADD_PLCM_FOR_FCH_39_Present = present;
	if (present) {
		/*-->*/sp->ADD_PLCM_FOR_FCH_39 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->ADD_PLCM_FOR_FCH_39, 5);
	} else {
		EDFree (sp->ADD_PLCM_FOR_FCH_39);
	}
}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields_c100 */
void FREE_c_f_csch_MECAM_Additional_record_fields_c100 (c_f_csch_MECAM_Additional_record_fields_c100* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS (&(sp->CH_RECORD_FIELDS));
	if (sp->PLCM_39_Present) {
		EDFree (sp->PLCM_39);
		sp->PLCM_39_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_MECAM_Additional_record_fields_c100_PLCM_39 (c_f_csch_MECAM_Additional_record_fields_c100* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->PLCM_39_Present == present) return;
	sp->PLCM_39_Present = present;
	if (present) {
		/*-->*/sp->PLCM_39 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->PLCM_39, 5);
	} else {
		EDFree (sp->PLCM_39);
	}
}

/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_FOR_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_REV_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FOR_SCHs.items; i0++) {
		FREE_c_f_dsch_UHDM_FOR_SCHs (sp->FOR_SCHs.data[i0]);
		EDFree (sp->FOR_SCHs.data[i0]);
	}
	if (sp->FOR_SCHs.data) {EDFree (sp->FOR_SCHs.data); sp->FOR_SCHs.data = NULL; sp->FOR_SCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs.items; i0++) {
		FREE_c_f_dsch_UHDM_REV_SCHs (sp->REV_SCHs.data[i0]);
		EDFree (sp->REV_SCHs.data[i0]);
	}
	if (sp->REV_SCHs.data) {EDFree (sp->REV_SCHs.data); sp->REV_SCHs.data = NULL; sp->REV_SCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs1.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data (sp->PILOTSs1.data[i0]);
		EDFree (sp->PILOTSs1.data[i0]);
	}
	if (sp->PILOTSs1.data) {EDFree (sp->PILOTSs1.data); sp->PILOTSs1.data = NULL; sp->PILOTSs1.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs2.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data (sp->PILOTSs2.data[i0]);
		EDFree (sp->PILOTSs2.data[i0]);
	}
	if (sp->PILOTSs2.data) {EDFree (sp->PILOTSs2.data); sp->PILOTSs2.data = NULL; sp->PILOTSs2.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->CCSH_ENCODER_TYPEs.items; i0++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sp->CCSH_ENCODER_TYPEs.data[i0]);
		EDFree (sp->CCSH_ENCODER_TYPEs.data[i0]);
	}
	if (sp->CCSH_ENCODER_TYPEs.data) {EDFree (sp->CCSH_ENCODER_TYPEs.data); sp->CCSH_ENCODER_TYPEs.data = NULL; sp->CCSH_ENCODER_TYPEs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_FOR_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_REV_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FOR_SCHs_01.items; i0++) {
		FREE_c_f_dsch_UHDM_FOR_SCHs (sp->FOR_SCHs_01.data[i0]);
		EDFree (sp->FOR_SCHs_01.data[i0]);
	}
	if (sp->FOR_SCHs_01.data) {EDFree (sp->FOR_SCHs_01.data); sp->FOR_SCHs_01.data = NULL; sp->FOR_SCHs_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs_01.items; i0++) {
		FREE_c_f_dsch_UHDM_REV_SCHs (sp->REV_SCHs_01.data[i0]);
		EDFree (sp->REV_SCHs_01.data[i0]);
	}
	if (sp->REV_SCHs_01.data) {EDFree (sp->REV_SCHs_01.data); sp->REV_SCHs_01.data = NULL; sp->REV_SCHs_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_02.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data (sp->PILOTSs_02.data[i0]);
		EDFree (sp->PILOTSs_02.data[i0]);
	}
	if (sp->PILOTSs_02.data) {EDFree (sp->PILOTSs_02.data); sp->PILOTSs_02.data = NULL; sp->PILOTSs_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->CCSH_ENCODER_TYPEs.items; i0++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sp->CCSH_ENCODER_TYPEs.data[i0]);
		EDFree (sp->CCSH_ENCODER_TYPEs.data[i0]);
	}
	if (sp->CCSH_ENCODER_TYPEs.data) {EDFree (sp->CCSH_ENCODER_TYPEs.data); sp->CCSH_ENCODER_TYPEs.data = NULL; sp->CCSH_ENCODER_TYPEs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTS3.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data (sp->PILOTS3.data[i0]);
		EDFree (sp->PILOTS3.data[i0]);
	}
	if (sp->PILOTS3.data) {EDFree (sp->PILOTS3.data); sp->PILOTS3.data = NULL; sp->PILOTS3.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_REV_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FOR_SCHs_02.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data (sp->FOR_SCHs_02.data[i0]);
		EDFree (sp->FOR_SCHs_02.data[i0]);
	}
	if (sp->FOR_SCHs_02.data) {EDFree (sp->FOR_SCHs_02.data); sp->FOR_SCHs_02.data = NULL; sp->FOR_SCHs_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs_02.items; i0++) {
		FREE_c_f_dsch_UHDM_REV_SCHs (sp->REV_SCHs_02.data[i0]);
		EDFree (sp->REV_SCHs_02.data[i0]);
	}
	if (sp->REV_SCHs_02.data) {EDFree (sp->REV_SCHs_02.data); sp->REV_SCHs_02.data = NULL; sp->REV_SCHs_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_04.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data (sp->PILOTSs_04.data[i0]);
		EDFree (sp->PILOTSs_04.data[i0]);
	}
	if (sp->PILOTSs_04.data) {EDFree (sp->PILOTSs_04.data); sp->PILOTSs_04.data = NULL; sp->PILOTSs_04.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_05.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data (sp->PILOTSs_05.data[i0]);
		EDFree (sp->PILOTSs_05.data[i0]);
	}
	if (sp->PILOTSs_05.data) {EDFree (sp->PILOTSs_05.data); sp->PILOTSs_05.data = NULL; sp->PILOTSs_05.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->CCSH_ENCODER_TYPEs.items; i0++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sp->CCSH_ENCODER_TYPEs.data[i0]);
		EDFree (sp->CCSH_ENCODER_TYPEs.data[i0]);
	}
	if (sp->CCSH_ENCODER_TYPEs.data) {EDFree (sp->CCSH_ENCODER_TYPEs.data); sp->CCSH_ENCODER_TYPEs.data = NULL; sp->CCSH_ENCODER_TYPEs.allocatedItems=0; /*FR02A*/}
	if (sp->ADD_PLCM_FOR_FCH_39_Present) {
		EDFree (sp->ADD_PLCM_FOR_FCH_39);
		sp->ADD_PLCM_FOR_FCH_39_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->PILOTS3.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data (sp->PILOTS3.data[i0]);
		EDFree (sp->PILOTS3.data[i0]);
	}
	if (sp->PILOTS3.data) {EDFree (sp->PILOTS3.data); sp->PILOTS3.data = NULL; sp->PILOTS3.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_ADD_PLCM_FOR_FCH_39 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->ADD_PLCM_FOR_FCH_39_Present == present) return;
	sp->ADD_PLCM_FOR_FCH_39_Present = present;
	if (present) {
		/*-->*/sp->ADD_PLCM_FOR_FCH_39 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->ADD_PLCM_FOR_FCH_39, 5);
	} else {
		EDFree (sp->ADD_PLCM_FOR_FCH_39);
	}
}

/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_FOR_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_REV_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04 */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FOR_SCHs_03.items; i0++) {
		FREE_c_f_dsch_UHDM_FOR_SCHs (sp->FOR_SCHs_03.data[i0]);
		EDFree (sp->FOR_SCHs_03.data[i0]);
	}
	if (sp->FOR_SCHs_03.data) {EDFree (sp->FOR_SCHs_03.data); sp->FOR_SCHs_03.data = NULL; sp->FOR_SCHs_03.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs_03.items; i0++) {
		FREE_c_f_dsch_UHDM_REV_SCHs (sp->REV_SCHs_03.data[i0]);
		EDFree (sp->REV_SCHs_03.data[i0]);
	}
	if (sp->REV_SCHs_03.data) {EDFree (sp->REV_SCHs_03.data); sp->REV_SCHs_03.data = NULL; sp->REV_SCHs_03.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PDCCHs.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data (sp->PDCCHs.data[i0]);
		EDFree (sp->PDCCHs.data[i0]);
	}
	if (sp->PDCCHs.data) {EDFree (sp->PDCCHs.data); sp->PDCCHs.data = NULL; sp->PDCCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTS1.items; i0++) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data (sp->PILOTS1.data[i0]);
		EDFree (sp->PILOTS1.data[i0]);
	}
	if (sp->PILOTS1.data) {EDFree (sp->PILOTS1.data); sp->PILOTS1.data = NULL; sp->PILOTS1.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTS2.items; i0++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sp->PILOTS2.data[i0]);
		EDFree (sp->PILOTS2.data[i0]);
	}
	if (sp->PILOTS2.data) {EDFree (sp->PILOTS2.data); sp->PILOTS2.data = NULL; sp->PILOTS2.allocatedItems=0; /*FR02A*/}
	if (sp->ADD_PLCM_FOR_FCH_39_Present) {
		EDFree (sp->ADD_PLCM_FOR_FCH_39);
		sp->ADD_PLCM_FOR_FCH_39_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_ADD_PLCM_FOR_FCH_39 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->ADD_PLCM_FOR_FCH_39_Present == present) return;
	sp->ADD_PLCM_FOR_FCH_39_Present = present;
	if (present) {
		/*-->*/sp->ADD_PLCM_FOR_FCH_39 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->ADD_PLCM_FOR_FCH_39, 5);
	} else {
		EDFree (sp->ADD_PLCM_FOR_FCH_39);
	}
}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_FOR_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_REV_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FOR_SCHs.items; i0++) {
		FREE_c_f_dsch_MUHDM_FOR_SCHs (sp->FOR_SCHs.data[i0]);
		EDFree (sp->FOR_SCHs.data[i0]);
	}
	if (sp->FOR_SCHs.data) {EDFree (sp->FOR_SCHs.data); sp->FOR_SCHs.data = NULL; sp->FOR_SCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs.items; i0++) {
		FREE_c_f_dsch_MUHDM_REV_SCHs (sp->REV_SCHs.data[i0]);
		EDFree (sp->REV_SCHs.data[i0]);
	}
	if (sp->REV_SCHs.data) {EDFree (sp->REV_SCHs.data); sp->REV_SCHs.data = NULL; sp->REV_SCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs1.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data (sp->PILOTSs1.data[i0]);
		EDFree (sp->PILOTSs1.data[i0]);
	}
	if (sp->PILOTSs1.data) {EDFree (sp->PILOTSs1.data); sp->PILOTSs1.data = NULL; sp->PILOTSs1.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs2.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data (sp->PILOTSs2.data[i0]);
		EDFree (sp->PILOTSs2.data[i0]);
	}
	if (sp->PILOTSs2.data) {EDFree (sp->PILOTSs2.data); sp->PILOTSs2.data = NULL; sp->PILOTSs2.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->CCSH_ENCODER_TYPEs.items; i0++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sp->CCSH_ENCODER_TYPEs.data[i0]);
		EDFree (sp->CCSH_ENCODER_TYPEs.data[i0]);
	}
	if (sp->CCSH_ENCODER_TYPEs.data) {EDFree (sp->CCSH_ENCODER_TYPEs.data); sp->CCSH_ENCODER_TYPEs.data = NULL; sp->CCSH_ENCODER_TYPEs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_FOR_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_REV_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FOR_SCHs_01.items; i0++) {
		FREE_c_f_dsch_MUHDM_FOR_SCHs (sp->FOR_SCHs_01.data[i0]);
		EDFree (sp->FOR_SCHs_01.data[i0]);
	}
	if (sp->FOR_SCHs_01.data) {EDFree (sp->FOR_SCHs_01.data); sp->FOR_SCHs_01.data = NULL; sp->FOR_SCHs_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs_01.items; i0++) {
		FREE_c_f_dsch_MUHDM_REV_SCHs (sp->REV_SCHs_01.data[i0]);
		EDFree (sp->REV_SCHs_01.data[i0]);
	}
	if (sp->REV_SCHs_01.data) {EDFree (sp->REV_SCHs_01.data); sp->REV_SCHs_01.data = NULL; sp->REV_SCHs_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_02.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data (sp->PILOTSs_02.data[i0]);
		EDFree (sp->PILOTSs_02.data[i0]);
	}
	if (sp->PILOTSs_02.data) {EDFree (sp->PILOTSs_02.data); sp->PILOTSs_02.data = NULL; sp->PILOTSs_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->CCSH_ENCODER_TYPEs.items; i0++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sp->CCSH_ENCODER_TYPEs.data[i0]);
		EDFree (sp->CCSH_ENCODER_TYPEs.data[i0]);
	}
	if (sp->CCSH_ENCODER_TYPEs.data) {EDFree (sp->CCSH_ENCODER_TYPEs.data); sp->CCSH_ENCODER_TYPEs.data = NULL; sp->CCSH_ENCODER_TYPEs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_REV_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FOR_SCHs_02.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data (sp->FOR_SCHs_02.data[i0]);
		EDFree (sp->FOR_SCHs_02.data[i0]);
	}
	if (sp->FOR_SCHs_02.data) {EDFree (sp->FOR_SCHs_02.data); sp->FOR_SCHs_02.data = NULL; sp->FOR_SCHs_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs_02.items; i0++) {
		FREE_c_f_dsch_MUHDM_REV_SCHs (sp->REV_SCHs_02.data[i0]);
		EDFree (sp->REV_SCHs_02.data[i0]);
	}
	if (sp->REV_SCHs_02.data) {EDFree (sp->REV_SCHs_02.data); sp->REV_SCHs_02.data = NULL; sp->REV_SCHs_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_04.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data (sp->PILOTSs_04.data[i0]);
		EDFree (sp->PILOTSs_04.data[i0]);
	}
	if (sp->PILOTSs_04.data) {EDFree (sp->PILOTSs_04.data); sp->PILOTSs_04.data = NULL; sp->PILOTSs_04.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_05.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data (sp->PILOTSs_05.data[i0]);
		EDFree (sp->PILOTSs_05.data[i0]);
	}
	if (sp->PILOTSs_05.data) {EDFree (sp->PILOTSs_05.data); sp->PILOTSs_05.data = NULL; sp->PILOTSs_05.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->CCSH_ENCODER_TYPEs.items; i0++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sp->CCSH_ENCODER_TYPEs.data[i0]);
		EDFree (sp->CCSH_ENCODER_TYPEs.data[i0]);
	}
	if (sp->CCSH_ENCODER_TYPEs.data) {EDFree (sp->CCSH_ENCODER_TYPEs.data); sp->CCSH_ENCODER_TYPEs.data = NULL; sp->CCSH_ENCODER_TYPEs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_FOR_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_REV_SCHs (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04 */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FOR_SCHs_03.items; i0++) {
		FREE_c_f_dsch_MUHDM_FOR_SCHs (sp->FOR_SCHs_03.data[i0]);
		EDFree (sp->FOR_SCHs_03.data[i0]);
	}
	if (sp->FOR_SCHs_03.data) {EDFree (sp->FOR_SCHs_03.data); sp->FOR_SCHs_03.data = NULL; sp->FOR_SCHs_03.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs_03.items; i0++) {
		FREE_c_f_dsch_MUHDM_REV_SCHs (sp->REV_SCHs_03.data[i0]);
		EDFree (sp->REV_SCHs_03.data[i0]);
	}
	if (sp->REV_SCHs_03.data) {EDFree (sp->REV_SCHs_03.data); sp->REV_SCHs_03.data = NULL; sp->REV_SCHs_03.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PDCCHs.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data (sp->PDCCHs.data[i0]);
		EDFree (sp->PDCCHs.data[i0]);
	}
	if (sp->PDCCHs.data) {EDFree (sp->PDCCHs.data); sp->PDCCHs.data = NULL; sp->PDCCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTS1.items; i0++) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data (sp->PILOTS1.data[i0]);
		EDFree (sp->PILOTS1.data[i0]);
	}
	if (sp->PILOTS1.data) {EDFree (sp->PILOTS1.data); sp->PILOTS1.data = NULL; sp->PILOTS1.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTS2.items; i0++) {
		FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sp->PILOTS2.data[i0]);
		EDFree (sp->PILOTS2.data[i0]);
	}
	if (sp->PILOTS2.data) {EDFree (sp->PILOTS2.data); sp->PILOTS2.data = NULL; sp->PILOTS2.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_SCRM_records_Type_specific_fields_000 */
void FREE_c_r_dsch_SCRM_records_Type_specific_fields_000 (c_r_dsch_SCRM_records_Type_specific_fields_000* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->PILOT_WALSH.value != NULL) {
		EDFree (sp->PILOT_WALSH.value);
		sp->PILOT_WALSH.value = NULL;
	}
	sp->PILOT_WALSH.usedBits = 0;

}

/* Free function for struct c_r_dsch_EPSMM_records_Type_specific_fields_000 */
void FREE_c_r_dsch_EPSMM_records_Type_specific_fields_000 (c_r_dsch_EPSMM_records_Type_specific_fields_000* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->PILOT_WALSH.value != NULL) {
		EDFree (sp->PILOT_WALSH.value);
		sp->PILOT_WALSH.value = NULL;
	}
	sp->PILOT_WALSH.usedBits = 0;

}

/* Free function for struct c_InfoRec_2_7_4 */
void FREE_c_InfoRec_2_7_4 (c_InfoRec_2_7_4* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_R_InformationRecords (&(sp->Type_specific_fields));

}

/* Free function for struct c_InfoRec_3_7_5 */
void FREE_c_InfoRec_3_7_5 (c_InfoRec_3_7_5* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_F_InformationRecords (&(sp->Type_specific_fields));

}

/* Free function for struct c_InfoRec_3_7_5_encrypted */
void FREE_c_InfoRec_3_7_5_encrypted (c_InfoRec_3_7_5_encrypted* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_F_InformationRecords (&(sp->Type_specific_fields));

}

/* Free function for struct c_f_csch_APM_ACCT_SOs_data */
void FREE_c_f_csch_APM_ACCT_SOs_data (c_f_csch_APM_ACCT_SOs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_APM_ACCT_SO_GRPs_data */
void FREE_c_f_csch_APM_ACCT_SO_GRPs_data (c_f_csch_APM_ACCT_SO_GRPs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_NLM_data_01_data */
void FREE_c_f_csch_NLM_data_01_data (c_f_csch_NLM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_CCLM_data_01_data */
void FREE_c_f_csch_CCLM_data_01_data (c_f_csch_CCLM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_DBM_FIELDSs_data */
void FREE_c_f_csch_DBM_FIELDSs_data (c_f_csch_DBM_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ESPM_data_01_data */
void FREE_c_f_csch_ESPM_data_01_data (c_f_csch_ESPM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ESPM_data_02_data */
void FREE_c_f_csch_ESPM_data_02_data (c_f_csch_ESPM_data_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ESPM_PDCCHs_data */
void FREE_c_f_csch_ESPM_PDCCHs_data (c_f_csch_ESPM_PDCCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03 */
void FREE_c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03 (c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ESPM_CAND_BAND_CLASSs_data */
void FREE_c_f_csch_ESPM_CAND_BAND_CLASSs_data (c_f_csch_ESPM_CAND_BAND_CLASSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_03.items; i0++) {
		FREE_c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data (sp->data_03.data[i0]);
		EDFree (sp->data_03.data[i0]);
	}
	if (sp->data_03.data) {EDFree (sp->data_03.data); sp->data_03.data = NULL; sp->data_03.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_ENLM_data_01_data */
void FREE_c_f_csch_ENLM_data_01_data (c_f_csch_ENLM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_STRQM_FIELDSs_data */
void FREE_c_f_csch_STRQM_FIELDSs_data (c_f_csch_STRQM_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GNLM_NGHBRs_data */
void FREE_c_f_csch_GNLM_NGHBRs_data (c_f_csch_GNLM_NGHBRs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GNLM_ANALOG_NGHBRs_data */
void FREE_c_f_csch_GNLM_ANALOG_NGHBRs_data (c_f_csch_GNLM_ANALOG_NGHBRs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GNLM_NGHBRs_01_data */
void FREE_c_f_csch_GNLM_NGHBRs_01_data (c_f_csch_GNLM_NGHBRs_01_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->records_Present) {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records (sp->records);
		EDFree (sp->records);
		sp->records_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_GNLM_NGHBRs_01_data_records (c_f_csch_GNLM_NGHBRs_01_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->records_Present == present) return;
	sp->records_Present = present;
	if (present) {
		/*-->*/sp->records = (c_f_csch_GNLM_NGHBRs_01_data_records*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_01_data_records));
		INIT_c_f_csch_GNLM_NGHBRs_01_data_records (sp->records);
	} else {
		FREE_c_f_csch_GNLM_NGHBRs_01_data_records (sp->records);
		EDFree (sp->records);
	}
}

/* Free function for struct c_f_csch_GNLM_NGHBRs_02_data */
void FREE_c_f_csch_GNLM_NGHBRs_02_data (c_f_csch_GNLM_NGHBRs_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GNLM_NGHBRs_03_data */
void FREE_c_f_csch_GNLM_NGHBRs_03_data (c_f_csch_GNLM_NGHBRs_03_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GNLM_NGHBRs_04_data */
void FREE_c_f_csch_GNLM_NGHBRs_04_data (c_f_csch_GNLM_NGHBRs_04_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GNLM_HRPD_NGHBRs_data */
void FREE_c_f_csch_GNLM_HRPD_NGHBRs_data (c_f_csch_GNLM_HRPD_NGHBRs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_UZIM_UZIDs_data */
void FREE_c_f_csch_UZIM_UZIDs_data (c_f_csch_UZIM_UZIDs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_PNLM_RADIO_INTERFACEs_data */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data (c_f_csch_PNLM_RADIO_INTERFACEs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Radio_Interface_Type_Specific_Fields_Present) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields (sp->Radio_Interface_Type_Specific_Fields);
		EDFree (sp->Radio_Interface_Type_Specific_Fields);
		sp->Radio_Interface_Type_Specific_Fields_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields (c_f_csch_PNLM_RADIO_INTERFACEs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Radio_Interface_Type_Specific_Fields_Present == present) return;
	sp->Radio_Interface_Type_Specific_Fields_Present = present;
	if (present) {
		/*-->*/sp->Radio_Interface_Type_Specific_Fields = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields));
		INIT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields (sp->Radio_Interface_Type_Specific_Fields);
	} else {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields (sp->Radio_Interface_Type_Specific_Fields);
		EDFree (sp->Radio_Interface_Type_Specific_Fields);
	}
}

/* Free function for struct c_f_csch_EGSRDM_ADD_RECORDs_data */
void FREE_c_f_csch_EGSRDM_ADD_RECORDs_data (c_f_csch_EGSRDM_ADD_RECORDs_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_EGSRDM_ADD_RECORDs_data_typeSpecificFields (&(sp->typeSpecificFields));

}

/* Free function for struct c_f_csch_ECCLM_FREQs_data */
void FREE_c_f_csch_ECCLM_FREQs_data (c_f_csch_ECCLM_FREQs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECCLM_FREQs_01_data */
void FREE_c_f_csch_ECCLM_FREQs_01_data (c_f_csch_ECCLM_FREQs_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECCLM_FREQs_02_data */
void FREE_c_f_csch_ECCLM_FREQs_02_data (c_f_csch_ECCLM_FREQs_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECCLM_data_01_data */
void FREE_c_f_csch_ECCLM_data_01_data (c_f_csch_ECCLM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECCLM_FREQs_03_data */
void FREE_c_f_csch_ECCLM_FREQs_03_data (c_f_csch_ECCLM_FREQs_03_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_ECCLM_BANDs_data_data_02 */
void FREE_c_f_csch_ECCLM_BANDs_data_data_02 (c_f_csch_ECCLM_BANDs_data_data_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECCLM_BANDs_data_data_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECCLM_BANDs_data_ADD_FREQs */
void FREE_c_f_csch_ECCLM_BANDs_data_ADD_FREQs (c_f_csch_ECCLM_BANDs_data_ADD_FREQs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ECCLM_BANDs_data */
void FREE_c_f_csch_ECCLM_BANDs_data (c_f_csch_ECCLM_BANDs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_02.items; i0++) {
		FREE_c_f_csch_ECCLM_BANDs_data_data_02_data (sp->data_02.data[i0]);
		EDFree (sp->data_02.data[i0]);
	}
	if (sp->data_02.data) {EDFree (sp->data_02.data); sp->data_02.data = NULL; sp->data_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->ADD_FREQs.items; i0++) {
		FREE_c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data (sp->ADD_FREQs.data[i0]);
		EDFree (sp->ADD_FREQs.data[i0]);
	}
	if (sp->ADD_FREQs.data) {EDFree (sp->ADD_FREQs.data); sp->ADD_FREQs.data = NULL; sp->ADD_FREQs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_MCRRPM_FCCCHs_data */
void FREE_c_f_csch_MCRRPM_FCCCHs_data (c_f_csch_MCRRPM_FCCCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MCRRPM_BCCH_BCASTs_data */
void FREE_c_f_csch_MCRRPM_BCCH_BCASTs_data (c_f_csch_MCRRPM_BCCH_BCASTs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MCRRPM_QPCHs_data */
void FREE_c_f_csch_MCRRPM_QPCHs_data (c_f_csch_MCRRPM_QPCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MCRRPM_PDCCHs_data */
void FREE_c_f_csch_MCRRPM_PDCCHs_data (c_f_csch_MCRRPM_PDCCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01 */
void FREE_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01 (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_MCRRPM_CAND_BAND_CLASSs_data */
void FREE_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data */
void FREE_c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data (c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_EAPM_MODE_PARM_RECs_data */
void FREE_c_f_csch_EAPM_MODE_PARM_RECs_data (c_f_csch_EAPM_MODE_PARM_RECs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_EAPM_CACHs_data */
void FREE_c_f_csch_EAPM_CACHs_data (c_f_csch_EAPM_CACHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_EAPM_CPCCHs_data */
void FREE_c_f_csch_EAPM_CPCCHs_data (c_f_csch_EAPM_CPCCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_EAPM_ACCT_SOs_data */
void FREE_c_f_csch_EAPM_ACCT_SOs_data (c_f_csch_EAPM_ACCT_SOs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_EAPM_ACCT_SO_GRPs_data */
void FREE_c_f_csch_EAPM_ACCT_SO_GRPs_data (c_f_csch_EAPM_ACCT_SO_GRPs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_UNLM_RADIO_INTERFACEs_data */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data (c_f_csch_UNLM_RADIO_INTERFACEs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Radio_Interface_Type_Specific_Fields_Present) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields (sp->Radio_Interface_Type_Specific_Fields);
		EDFree (sp->Radio_Interface_Type_Specific_Fields);
		sp->Radio_Interface_Type_Specific_Fields_Present = ED_FALSE;
	}
	if (sp->Radio_Interface_Type_Specific_Fields_analog_Present) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog (sp->Radio_Interface_Type_Specific_Fields_analog);
		EDFree (sp->Radio_Interface_Type_Specific_Fields_analog);
		sp->Radio_Interface_Type_Specific_Fields_analog_Present = ED_FALSE;
	}
	if (sp->Radio_Interface_Type_Specific_Fields_HRPD_Present) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD (sp->Radio_Interface_Type_Specific_Fields_HRPD);
		EDFree (sp->Radio_Interface_Type_Specific_Fields_HRPD);
		sp->Radio_Interface_Type_Specific_Fields_HRPD_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields (c_f_csch_UNLM_RADIO_INTERFACEs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Radio_Interface_Type_Specific_Fields_Present == present) return;
	sp->Radio_Interface_Type_Specific_Fields_Present = present;
	if (present) {
		/*-->*/sp->Radio_Interface_Type_Specific_Fields = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields));
		INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields (sp->Radio_Interface_Type_Specific_Fields);
	} else {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields (sp->Radio_Interface_Type_Specific_Fields);
		EDFree (sp->Radio_Interface_Type_Specific_Fields);
	}
}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog (c_f_csch_UNLM_RADIO_INTERFACEs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Radio_Interface_Type_Specific_Fields_analog_Present == present) return;
	sp->Radio_Interface_Type_Specific_Fields_analog_Present = present;
	if (present) {
		/*-->*/sp->Radio_Interface_Type_Specific_Fields_analog = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog));
		INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog (sp->Radio_Interface_Type_Specific_Fields_analog);
	} else {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog (sp->Radio_Interface_Type_Specific_Fields_analog);
		EDFree (sp->Radio_Interface_Type_Specific_Fields_analog);
	}
}

void SETPRESENT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD (c_f_csch_UNLM_RADIO_INTERFACEs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Radio_Interface_Type_Specific_Fields_HRPD_Present == present) return;
	sp->Radio_Interface_Type_Specific_Fields_HRPD_Present = present;
	if (present) {
		/*-->*/sp->Radio_Interface_Type_Specific_Fields_HRPD = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD));
		INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD (sp->Radio_Interface_Type_Specific_Fields_HRPD);
	} else {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD (sp->Radio_Interface_Type_Specific_Fields_HRPD);
		EDFree (sp->Radio_Interface_Type_Specific_Fields_HRPD);
	}
}

/* Free function for struct c_f_csch_BSPM_FSCHs_data */
void FREE_c_f_csch_BSPM_FSCHs_data (c_f_csch_BSPM_FSCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs */
void FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_BSPM_BCMC_PROGRAMSs_data */
void FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data (c_f_csch_BSPM_BCMC_PROGRAMSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_PROGRAM_ID.value != NULL) {
		EDFree (sp->BCMC_PROGRAM_ID.value);
		sp->BCMC_PROGRAM_ID.value = NULL;
	}
	sp->BCMC_PROGRAM_ID.usedBits = 0;
	if (sp->NUM_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->NUM_FLOW_DISCRIMINATOR.value);
		sp->NUM_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->NUM_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->FLOW_DISCRIMINATORs.items; i0++) {
		FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->FLOW_DISCRIMINATORs.data[i0]);
		EDFree (sp->FLOW_DISCRIMINATORs.data[i0]);
	}
	if (sp->FLOW_DISCRIMINATORs.data) {EDFree (sp->FLOW_DISCRIMINATORs.data); sp->FLOW_DISCRIMINATORs.data = NULL; sp->FLOW_DISCRIMINATORs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_BSPM_data_01_data */
void FREE_c_f_csch_BSPM_data_01_data (c_f_csch_BSPM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_APIDM_data_01_data */
void FREE_c_f_csch_APIDM_data_01_data (c_f_csch_APIDM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE (&(sp->HO_INFO_VALUE));

}

/* Free function for struct c_f_csch_APIDTM_FIELDSs_data */
void FREE_c_f_csch_APIDTM_FIELDSs_data (c_f_csch_APIDTM_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.value != NULL) {
		EDFree (sp->CHARi.value);
		sp->CHARi.value = NULL;
	}
	sp->CHARi.usedBits = 0;

}

/* Free function for struct c_f_csch_APPIM_APPI_RECs_data */
void FREE_c_f_csch_APPIM_APPI_RECs_data (c_f_csch_APPIM_APPI_RECs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->AP_PN_REC_Present) {
		FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC (sp->AP_PN_REC);
		EDFree (sp->AP_PN_REC);
		sp->AP_PN_REC_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC (c_f_csch_APPIM_APPI_RECs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->AP_PN_REC_Present == present) return;
	sp->AP_PN_REC_Present = present;
	if (present) {
		/*-->*/sp->AP_PN_REC = (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC*)EDAlloc (sizeof (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC));
		INIT_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC (sp->AP_PN_REC);
	} else {
		FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC (sp->AP_PN_REC);
		EDFree (sp->AP_PN_REC);
	}
}

/* Free function for struct c_f_csch_GOIM_GOI_RECs_data */
void FREE_c_f_csch_GOIM_GOI_RECs_data (c_f_csch_GOIM_GOI_RECs_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC (&(sp->GOI_REC));

}

/* Free function for variable-sized sequence c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01 */
void FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01 (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs */
void FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_FDCCLM_CAND_BAND_CLASSs_data */
void FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FREQs.items; i0++) {
		FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data (sp->FREQs.data[i0]);
		EDFree (sp->FREQs.data[i0]);
	}
	if (sp->FREQs.data) {EDFree (sp->FREQs.data); sp->FREQs.data = NULL; sp->FREQs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_GEM_GE_RECs_data */
void FREE_c_f_csch_GEM_GE_RECs_data (c_f_csch_GEM_GE_RECs_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_GEM_GE_RECs_data_GE_REC (&(sp->GE_REC));

}

/* Free function for struct c_f_csch_ATIM_RADIO_INTERFACEs_data */
void FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data (c_f_csch_ATIM_RADIO_INTERFACEs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Radio_Interface_Type_Specific_Fields_010_Present) {
		FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 (sp->Radio_Interface_Type_Specific_Fields_010);
		EDFree (sp->Radio_Interface_Type_Specific_Fields_010);
		sp->Radio_Interface_Type_Specific_Fields_010_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 (c_f_csch_ATIM_RADIO_INTERFACEs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Radio_Interface_Type_Specific_Fields_010_Present == present) return;
	sp->Radio_Interface_Type_Specific_Fields_010_Present = present;
	if (present) {
		/*-->*/sp->Radio_Interface_Type_Specific_Fields_010 = (c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010*)EDAlloc (sizeof (c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010));
		INIT_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 (sp->Radio_Interface_Type_Specific_Fields_010);
	} else {
		FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 (sp->Radio_Interface_Type_Specific_Fields_010);
		EDFree (sp->Radio_Interface_Type_Specific_Fields_010);
	}
}

/* Free function for struct c_f_csch_FGOIM_GOI_RECs_data */
void FREE_c_f_csch_FGOIM_GOI_RECs_data (c_f_csch_FGOIM_GOI_RECs_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC (&(sp->GOI_REC));

}

/* Free function for struct c_f_csch_HDPPM_records_data */
void FREE_c_f_csch_HDPPM_records_data (c_f_csch_HDPPM_records_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_DBM_FIELDSs_data */
void FREE_c_f_dsch_DBM_FIELDSs_data (c_f_dsch_DBM_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_NLUM_data_01_data */
void FREE_c_f_dsch_NLUM_data_01_data (c_f_dsch_NLUM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_BDTMFM_DIGITSs_data */
void FREE_c_f_dsch_BDTMFM_DIGITSs_data (c_f_dsch_BDTMFM_DIGITSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_RTPM_data_01_data */
void FREE_c_f_dsch_RTPM_data_01_data (c_f_dsch_RTPM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_STPM_data_01_data */
void FREE_c_f_dsch_STPM_data_01_data (c_f_dsch_STPM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->PARAMETER.value != NULL) {
		EDFree (sp->PARAMETER.value);
		sp->PARAMETER.value = NULL;
	}
	sp->PARAMETER.usedBits = 0;

}

/* Free function for struct c_f_dsch_STRQM_FIELDSs_data */
void FREE_c_f_dsch_STRQM_FIELDSs_data (c_f_dsch_STRQM_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_EHDM_data_01_data */
void FREE_c_f_dsch_EHDM_data_01_data (c_f_dsch_EHDM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_SCM_CALLS_ASSIGNs_data */
void FREE_c_f_dsch_SCM_CALLS_ASSIGNs_data (c_f_dsch_SCM_CALLS_ASSIGNs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_SCAM_data_01_data */
void FREE_c_f_dsch_SCAM_data_01_data (c_f_dsch_SCAM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_SCAM_data_02_data_SUP_PILOTSs */
void FREE_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_SCAM_data_02_data */
void FREE_c_f_dsch_SCAM_data_02_data (c_f_dsch_SCAM_data_02_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SUP_PILOTSs.items; i0++) {
		FREE_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data (sp->SUP_PILOTSs.data[i0]);
		EDFree (sp->SUP_PILOTSs.data[i0]);
	}
	if (sp->SUP_PILOTSs.data) {EDFree (sp->SUP_PILOTSs.data); sp->SUP_PILOTSs.data = NULL; sp->SUP_PILOTSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_PCNM_SUPs_data */
void FREE_c_f_dsch_PCNM_SUPs_data (c_f_dsch_PCNM_SUPs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ENLUM_NGHBRs_02_data */
void FREE_c_f_dsch_ENLUM_NGHBRs_02_data (c_f_dsch_ENLUM_NGHBRs_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ENLUM_NGHBRs_data */
void FREE_c_f_dsch_ENLUM_NGHBRs_data (c_f_dsch_ENLUM_NGHBRs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ENLUM_NGHBRs_01_data */
void FREE_c_f_dsch_ENLUM_NGHBRs_01_data (c_f_dsch_ENLUM_NGHBRs_01_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->records_Present) {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records (sp->records);
		EDFree (sp->records);
		sp->records_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_ENLUM_NGHBRs_01_data_records (c_f_dsch_ENLUM_NGHBRs_01_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->records_Present == present) return;
	sp->records_Present = present;
	if (present) {
		/*-->*/sp->records = (c_f_dsch_ENLUM_NGHBRs_01_data_records*)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_01_data_records));
		INIT_c_f_dsch_ENLUM_NGHBRs_01_data_records (sp->records);
	} else {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data_records (sp->records);
		EDFree (sp->records);
	}
}

/* Free function for struct c_f_dsch_GHDM_PILOTSs_data */
void FREE_c_f_dsch_GHDM_PILOTSs_data (c_f_dsch_GHDM_PILOTSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->FOR_SUP_CHAN_REC_Record_Present) {
		FREE_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record (sp->FOR_SUP_CHAN_REC_Record);
		EDFree (sp->FOR_SUP_CHAN_REC_Record);
		sp->FOR_SUP_CHAN_REC_Record_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record (c_f_dsch_GHDM_PILOTSs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FOR_SUP_CHAN_REC_Record_Present == present) return;
	sp->FOR_SUP_CHAN_REC_Record_Present = present;
	if (present) {
		/*-->*/sp->FOR_SUP_CHAN_REC_Record = (c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record*)EDAlloc (sizeof (c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record));
		INIT_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record (sp->FOR_SUP_CHAN_REC_Record);
	} else {
		FREE_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record (sp->FOR_SUP_CHAN_REC_Record);
		EDFree (sp->FOR_SUP_CHAN_REC_Record);
	}
}

/* Free function for variable-sized sequence c_f_dsch_ERM_data_01_data_PILOTS_D2Is */
void FREE_c_f_dsch_ERM_data_01_data_PILOTS_D2Is (c_f_dsch_ERM_data_01_data_PILOTS_D2Is* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ERM_data_01_data */
void FREE_c_f_dsch_ERM_data_01_data (c_f_dsch_ERM_data_01_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTS_D2Is.items; i0++) {
		FREE_c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data (sp->PILOTS_D2Is.data[i0]);
		EDFree (sp->PILOTS_D2Is.data[i0]);
	}
	if (sp->PILOTS_D2Is.data) {EDFree (sp->PILOTS_D2Is.data); sp->PILOTS_D2Is.data = NULL; sp->PILOTS_D2Is.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_ERM_FSCHs_data */
void FREE_c_f_dsch_ERM_FSCHs_data (c_f_dsch_ERM_FSCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs */
void FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ERM_BCMC_PROGRAMSs_data */
void FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data (c_f_dsch_ERM_BCMC_PROGRAMSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_PROGRAM_ID.value != NULL) {
		EDFree (sp->BCMC_PROGRAM_ID.value);
		sp->BCMC_PROGRAM_ID.value = NULL;
	}
	sp->BCMC_PROGRAM_ID.usedBits = 0;
	if (sp->NUM_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->NUM_FLOW_DISCRIMINATOR.value);
		sp->NUM_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->NUM_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->FLOW_DISCRIMINATORs.items; i0++) {
		FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->FLOW_DISCRIMINATORs.data[i0]);
		EDFree (sp->FLOW_DISCRIMINATORs.data[i0]);
	}
	if (sp->FLOW_DISCRIMINATORs.data) {EDFree (sp->FLOW_DISCRIMINATORs.data); sp->FLOW_DISCRIMINATORs.data = NULL; sp->FLOW_DISCRIMINATORs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_UHDM_FOR_ASSIGNs_data */
void FREE_c_f_dsch_UHDM_FOR_ASSIGNs_data (c_f_dsch_UHDM_FOR_ASSIGNs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_REV_ASSIGNs_data */
void FREE_c_f_dsch_UHDM_REV_ASSIGNs_data (c_f_dsch_UHDM_REV_ASSIGNs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_CALLS_ASSIGNs_data */
void FREE_c_f_dsch_UHDM_CALLS_ASSIGNs_data (c_f_dsch_UHDM_CALLS_ASSIGNs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UHDM_FOR_ASSIGNs_01_data */
void FREE_c_f_dsch_UHDM_FOR_ASSIGNs_01_data (c_f_dsch_UHDM_FOR_ASSIGNs_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ESCAM_REV_CFG_RECSs_data */
void FREE_c_f_dsch_ESCAM_REV_CFG_RECSs_data (c_f_dsch_ESCAM_REV_CFG_RECSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ESCAM_REV_SCHs_data */
void FREE_c_f_dsch_ESCAM_REV_SCHs_data (c_f_dsch_ESCAM_REV_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs */
void FREE_c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs (c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ESCAM_FOR_CFG_RECSs_data */
void FREE_c_f_dsch_ESCAM_FOR_CFG_RECSs_data (c_f_dsch_ESCAM_FOR_CFG_RECSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SUP_SHOs.items; i0++) {
		FREE_c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data (sp->SUP_SHOs.data[i0]);
		EDFree (sp->SUP_SHOs.data[i0]);
	}
	if (sp->SUP_SHOs.data) {EDFree (sp->SUP_SHOs.data); sp->SUP_SHOs.data = NULL; sp->SUP_SHOs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_ESCAM_FOR_SCHs_data */
void FREE_c_f_dsch_ESCAM_FOR_SCHs_data (c_f_dsch_ESCAM_FOR_SCHs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ESCAM_SUPs_data */
void FREE_c_f_dsch_ESCAM_SUPs_data (c_f_dsch_ESCAM_SUPs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ESCAM_data_01_data */
void FREE_c_f_dsch_ESCAM_data_01_data (c_f_dsch_ESCAM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs */
void FREE_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ESCAM_P3X_CFGs_data */
void FREE_c_f_dsch_ESCAM_P3X_CFGs_data (c_f_dsch_ESCAM_P3X_CFGs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->P3X_RECs.items; i0++) {
		FREE_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data (sp->P3X_RECs.data[i0]);
		EDFree (sp->P3X_RECs.data[i0]);
	}
	if (sp->P3X_RECs.data) {EDFree (sp->P3X_RECs.data); sp->P3X_RECs.data = NULL; sp->P3X_RECs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_ESCAM_data_02_data */
void FREE_c_f_dsch_ESCAM_data_02_data (c_f_dsch_ESCAM_data_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ESCAM_FOR_SCHs_01_data */
void FREE_c_f_dsch_ESCAM_FOR_SCHs_01_data (c_f_dsch_ESCAM_FOR_SCHs_01_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_Channel_Configuration_for_the_Supplemental_Channel (&(sp->SCH_CC_Type_specific_field));

}

/* Free function for struct c_f_dsch_ESCAM_REV_SCHs_01_data */
void FREE_c_f_dsch_ESCAM_REV_SCHs_01_data (c_f_dsch_ESCAM_REV_SCHs_01_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_Channel_Configuration_for_the_Supplemental_Channel (&(sp->SCH_CC_Type_specific_field2));

}

/* Free function for struct c_f_dsch_ESCAM_FOR_SCHs_02_data */
void FREE_c_f_dsch_ESCAM_FOR_SCHs_02_data (c_f_dsch_ESCAM_FOR_SCHs_02_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_CLAM_ADD_CALL_RECORDSs_data */
void FREE_c_f_dsch_CLAM_ADD_CALL_RECORDSs_data (c_f_dsch_CLAM_ADD_CALL_RECORDSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_SMCM_RECSs_data */
void FREE_c_f_dsch_SMCM_RECSs_data (c_f_dsch_SMCM_RECSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_BSSRSPM_RECORDSs_data */
void FREE_c_f_dsch_BSSRSPM_RECORDSs_data (c_f_dsch_BSSRSPM_RECORDSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Record_type_specific_fields_Present) {
		FREE_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields (sp->Record_type_specific_fields);
		EDFree (sp->Record_type_specific_fields);
		sp->Record_type_specific_fields_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields (c_f_dsch_BSSRSPM_RECORDSs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Record_type_specific_fields_Present == present) return;
	sp->Record_type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->Record_type_specific_fields = (c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields*)EDAlloc (sizeof (c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields));
		INIT_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields (sp->Record_type_specific_fields);
	} else {
		FREE_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields (sp->Record_type_specific_fields);
		EDFree (sp->Record_type_specific_fields);
	}
}

/* Free function for struct c_f_dsch_RATCHGM_RECSs_data */
void FREE_c_f_dsch_RATCHGM_RECSs_data (c_f_dsch_RATCHGM_RECSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs */
void FREE_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data */
void FREE_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_PROGRAM_ID.value != NULL) {
		EDFree (sp->BCMC_PROGRAM_ID.value);
		sp->BCMC_PROGRAM_ID.value = NULL;
	}
	sp->BCMC_PROGRAM_ID.usedBits = 0;
	if (sp->NUM_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->NUM_FLOW_DISCRIMINATOR.value);
		sp->NUM_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->NUM_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->FLOW_DISCRIMINATORs.items; i0++) {
		FREE_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->FLOW_DISCRIMINATORs.data[i0]);
		EDFree (sp->FLOW_DISCRIMINATORs.data[i0]);
	}
	if (sp->FLOW_DISCRIMINATORs.data) {EDFree (sp->FLOW_DISCRIMINATORs.data); sp->FLOW_DISCRIMINATORs.data = NULL; sp->FLOW_DISCRIMINATORs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data */
void FREE_c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data (c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_HOSISM_PILOT_PNs_data */
void FREE_c_f_dsch_HOSISM_PILOT_PNs_data (c_f_dsch_HOSISM_PILOT_PNs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_GEM_GE_RECs_data */
void FREE_c_f_dsch_GEM_GE_RECs_data (c_f_dsch_GEM_GE_RECs_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC (&(sp->GE_REC));

}

/* Free function for struct c_f_dsch_ATIM_RADIO_INTERFACEs_data */
void FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data (c_f_dsch_ATIM_RADIO_INTERFACEs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Radio_Interface_Type_Specific_Fields_010_Present) {
		FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 (sp->Radio_Interface_Type_Specific_Fields_010);
		EDFree (sp->Radio_Interface_Type_Specific_Fields_010);
		sp->Radio_Interface_Type_Specific_Fields_010_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 (c_f_dsch_ATIM_RADIO_INTERFACEs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Radio_Interface_Type_Specific_Fields_010_Present == present) return;
	sp->Radio_Interface_Type_Specific_Fields_010_Present = present;
	if (present) {
		/*-->*/sp->Radio_Interface_Type_Specific_Fields_010 = (c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010*)EDAlloc (sizeof (c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010));
		INIT_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 (sp->Radio_Interface_Type_Specific_Fields_010);
	} else {
		FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010 (sp->Radio_Interface_Type_Specific_Fields_010);
		EDFree (sp->Radio_Interface_Type_Specific_Fields_010);
	}
}

/* Free function for struct c_f_dsch_MUHDM_FOR_ASSIGNs_data */
void FREE_c_f_dsch_MUHDM_FOR_ASSIGNs_data (c_f_dsch_MUHDM_FOR_ASSIGNs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MUHDM_REV_ASSIGNs_data */
void FREE_c_f_dsch_MUHDM_REV_ASSIGNs_data (c_f_dsch_MUHDM_REV_ASSIGNs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MUHDM_CALLS_ASSIGNs_data */
void FREE_c_f_dsch_MUHDM_CALLS_ASSIGNs_data (c_f_dsch_MUHDM_CALLS_ASSIGNs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs */
void FREE_c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_RGM_BCMC_PROGRAMSs_data */
void FREE_c_r_csch_RGM_BCMC_PROGRAMSs_data (c_r_csch_RGM_BCMC_PROGRAMSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_PROGRAM_ID.value != NULL) {
		EDFree (sp->BCMC_PROGRAM_ID.value);
		sp->BCMC_PROGRAM_ID.value = NULL;
	}
	sp->BCMC_PROGRAM_ID.usedBits = 0;
	if (sp->NUM_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->NUM_FLOW_DISCRIMINATOR.value);
		sp->NUM_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->NUM_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->FLOW_DISCRIMINATORs.items; i0++) {
		FREE_c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->FLOW_DISCRIMINATORs.data[i0]);
		EDFree (sp->FLOW_DISCRIMINATORs.data[i0]);
	}
	if (sp->FLOW_DISCRIMINATORs.data) {EDFree (sp->FLOW_DISCRIMINATORs.data); sp->FLOW_DISCRIMINATORs.data = NULL; sp->FLOW_DISCRIMINATORs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_csch_RGM_BAND_SUBCLASSs_data */
void FREE_c_r_csch_RGM_BAND_SUBCLASSs_data (c_r_csch_RGM_BAND_SUBCLASSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_DBM_FIELDSs_data */
void FREE_c_r_csch_DBM_FIELDSs_data (c_r_csch_DBM_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_ORM_FIELDSs_data */
void FREE_c_r_csch_ORM_FIELDSs_data (c_r_csch_ORM_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.value != NULL) {
		EDFree (sp->CHARi.value);
		sp->CHARi.value = NULL;
	}
	sp->CHARi.usedBits = 0;

}

/* Free function for struct c_r_csch_ORM_ALT_SOs_data */
void FREE_c_r_csch_ORM_ALT_SOs_data (c_r_csch_ORM_ALT_SOs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_ORM_ADD_SERV_INSTANCEs_data */
void FREE_c_r_csch_ORM_ADD_SERV_INSTANCEs_data (c_r_csch_ORM_ADD_SERV_INSTANCEs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->ADD_QOS_PARMS.value != NULL) {
		EDFree (sp->ADD_QOS_PARMS.value);
		sp->ADD_QOS_PARMS.value = NULL;
	}
	sp->ADD_QOS_PARMS.usedBits = 0;

}

/* Free function for variable-sized sequence c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs */
void FREE_c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_ORM_BCMC_PROGRAMSs_data */
void FREE_c_r_csch_ORM_BCMC_PROGRAMSs_data (c_r_csch_ORM_BCMC_PROGRAMSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_PROGRAM_ID.value != NULL) {
		EDFree (sp->BCMC_PROGRAM_ID.value);
		sp->BCMC_PROGRAM_ID.value = NULL;
	}
	sp->BCMC_PROGRAM_ID.usedBits = 0;
	if (sp->NUM_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->NUM_FLOW_DISCRIMINATOR.value);
		sp->NUM_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->NUM_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->FLOW_DISCRIMINATORs.items; i0++) {
		FREE_c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->FLOW_DISCRIMINATORs.data[i0]);
		EDFree (sp->FLOW_DISCRIMINATORs.data[i0]);
	}
	if (sp->FLOW_DISCRIMINATORs.data) {EDFree (sp->FLOW_DISCRIMINATORs.data); sp->FLOW_DISCRIMINATORs.data = NULL; sp->FLOW_DISCRIMINATORs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_csch_ORM_BAND_SUBCLASSs_data */
void FREE_c_r_csch_ORM_BAND_SUBCLASSs_data (c_r_csch_ORM_BAND_SUBCLASSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_PRM_ALT_SOs_data */
void FREE_c_r_csch_PRM_ALT_SOs_data (c_r_csch_PRM_ALT_SOs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs */
void FREE_c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_PRM_BCMC_PROGRAMSs_data */
void FREE_c_r_csch_PRM_BCMC_PROGRAMSs_data (c_r_csch_PRM_BCMC_PROGRAMSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_PROGRAM_ID.value != NULL) {
		EDFree (sp->BCMC_PROGRAM_ID.value);
		sp->BCMC_PROGRAM_ID.value = NULL;
	}
	sp->BCMC_PROGRAM_ID.usedBits = 0;
	if (sp->NUM_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->NUM_FLOW_DISCRIMINATOR.value);
		sp->NUM_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->NUM_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->FLOW_DISCRIMINATORs.items; i0++) {
		FREE_c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->FLOW_DISCRIMINATORs.data[i0]);
		EDFree (sp->FLOW_DISCRIMINATORs.data[i0]);
	}
	if (sp->FLOW_DISCRIMINATORs.data) {EDFree (sp->FLOW_DISCRIMINATORs.data); sp->FLOW_DISCRIMINATORs.data = NULL; sp->FLOW_DISCRIMINATORs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_csch_PRM_BAND_SUBCLASSs_data */
void FREE_c_r_csch_PRM_BAND_SUBCLASSs_data (c_r_csch_PRM_BAND_SUBCLASSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_RCNM_ADD_SERV_INSTANCEs_data */
void FREE_c_r_csch_RCNM_ADD_SERV_INSTANCEs_data (c_r_csch_RCNM_ADD_SERV_INSTANCEs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_RCNM_FIELDSs_data */
void FREE_c_r_csch_RCNM_FIELDSs_data (c_r_csch_RCNM_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_GEM_GE_RECs_data */
void FREE_c_r_csch_GEM_GE_RECs_data (c_r_csch_GEM_GE_RECs_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_r_csch_GEM_GE_RECs_data_GE_REC (&(sp->GE_REC));

}

/* Free function for struct c_r_dsch_DBM_FIELDSs_data */
void FREE_c_r_dsch_DBM_FIELDSs_data (c_r_dsch_DBM_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_PSMM_data_01_data */
void FREE_c_r_dsch_PSMM_data_01_data (c_r_dsch_PSMM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_PMRM_PILOTSs_data */
void FREE_c_r_dsch_PMRM_PILOTSs_data (c_r_dsch_PMRM_PILOTSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_BDTMFM_DIGITSs_data */
void FREE_c_r_dsch_BDTMFM_DIGITSs_data (c_r_dsch_BDTMFM_DIGITSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_ORCM_FIELDSs_data */
void FREE_c_r_dsch_ORCM_FIELDSs_data (c_r_dsch_ORCM_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.value != NULL) {
		EDFree (sp->CHARi.value);
		sp->CHARi.value = NULL;
	}
	sp->CHARi.usedBits = 0;

}

/* Free function for struct c_r_dsch_HOCM_data_01_data */
void FREE_c_r_dsch_HOCM_data_01_data (c_r_dsch_HOCM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_PRSM_data_01_data */
void FREE_c_r_dsch_PRSM_data_01_data (c_r_dsch_PRSM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->PARAMETER.value != NULL) {
		EDFree (sp->PARAMETER.value);
		sp->PARAMETER.value = NULL;
	}
	sp->PARAMETER.usedBits = 0;

}

/* Free function for struct c_r_dsch_SCRM_ACT_PNs_data */
void FREE_c_r_dsch_SCRM_ACT_PNs_data (c_r_dsch_SCRM_ACT_PNs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_SCRM_NGHBR_PNs_data */
void FREE_c_r_dsch_SCRM_NGHBR_PNs_data (c_r_dsch_SCRM_NGHBR_PNs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_SCRM_ACT_PNs_01_data */
void FREE_c_r_dsch_SCRM_ACT_PNs_01_data (c_r_dsch_SCRM_ACT_PNs_01_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields2_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields2);
		EDFree (sp->Type_specific_fields2);
		sp->Type_specific_fields2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_SCRM_ACT_PNs_01_data_Type_specific_fields2 (c_r_dsch_SCRM_ACT_PNs_01_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields2_Present == present) return;
	sp->Type_specific_fields2_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields2 = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields2);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->Type_specific_fields2);
		EDFree (sp->Type_specific_fields2);
	}
}

/* Free function for struct c_r_dsch_PPSMM_PILOTs_data */
void FREE_c_r_dsch_PPSMM_PILOTs_data (c_r_dsch_PPSMM_PILOTs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_PPSMM_PILOTs_01_data */
void FREE_c_r_dsch_PPSMM_PILOTs_01_data (c_r_dsch_PPSMM_PILOTs_01_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->records_Present) {
		FREE_c_r_dsch_PPSMM_PILOTs_01_data_records (sp->records);
		EDFree (sp->records);
		sp->records_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_PPSMM_PILOTs_01_data_records (c_r_dsch_PPSMM_PILOTs_01_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->records_Present == present) return;
	sp->records_Present = present;
	if (present) {
		/*-->*/sp->records = (c_r_dsch_PPSMM_PILOTs_01_data_records*)EDAlloc (sizeof (c_r_dsch_PPSMM_PILOTs_01_data_records));
		INIT_c_r_dsch_PPSMM_PILOTs_01_data_records (sp->records);
	} else {
		FREE_c_r_dsch_PPSMM_PILOTs_01_data_records (sp->records);
		EDFree (sp->records);
	}
}

/* Free function for struct c_r_dsch_PPSMM_data_01_data */
void FREE_c_r_dsch_PPSMM_data_01_data (c_r_dsch_PPSMM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_OLRM_data_01_data */
void FREE_c_r_dsch_OLRM_data_01_data (c_r_dsch_OLRM_data_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_RRRM_ADD_CON_REFs_data */
void FREE_c_r_dsch_RRRM_ADD_CON_REFs_data (c_r_dsch_RRRM_ADD_CON_REFs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs */
void FREE_c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_RRRM_BCMC_PROGRAMSs_data */
void FREE_c_r_dsch_RRRM_BCMC_PROGRAMSs_data (c_r_dsch_RRRM_BCMC_PROGRAMSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_PROGRAM_ID.value != NULL) {
		EDFree (sp->BCMC_PROGRAM_ID.value);
		sp->BCMC_PROGRAM_ID.value = NULL;
	}
	sp->BCMC_PROGRAM_ID.usedBits = 0;
	if (sp->NUM_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->NUM_FLOW_DISCRIMINATOR.value);
		sp->NUM_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->NUM_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->FLOW_DISCRIMINATORs.items; i0++) {
		FREE_c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->FLOW_DISCRIMINATORs.data[i0]);
		EDFree (sp->FLOW_DISCRIMINATORs.data[i0]);
	}
	if (sp->FLOW_DISCRIMINATORs.data) {EDFree (sp->FLOW_DISCRIMINATORs.data); sp->FLOW_DISCRIMINATORs.data = NULL; sp->FLOW_DISCRIMINATORs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_EOM_ALT_SOs_data */
void FREE_c_r_dsch_EOM_ALT_SOs_data (c_r_dsch_EOM_ALT_SOs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_EOM_FIELDSs_data */
void FREE_c_r_dsch_EOM_FIELDSs_data (c_r_dsch_EOM_FIELDSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->CHARi.value != NULL) {
		EDFree (sp->CHARi.value);
		sp->CHARi.value = NULL;
	}
	sp->CHARi.usedBits = 0;

}

/* Free function for variable-sized sequence c_r_dsch_EOM_ADD_SERV_INSTANCEs_data_RECS */
void FREE_c_r_dsch_EOM_ADD_SERV_INSTANCEs_data_RECS (c_r_dsch_EOM_ADD_SERV_INSTANCEs_data_RECS* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_2_7_4_encrypted (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_EOM_ADD_SERV_INSTANCEs_data */
void FREE_c_r_dsch_EOM_ADD_SERV_INSTANCEs_data (c_r_dsch_EOM_ADD_SERV_INSTANCEs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RECS.items; i0++) {
		FREE_c_InfoRec_2_7_4_encrypted (sp->RECS.data[i0]);
		EDFree (sp->RECS.data[i0]);
	}
	if (sp->RECS.data) {EDFree (sp->RECS.data); sp->RECS.data = NULL; sp->RECS.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs */
void FREE_c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_EOM_BCMC_PROGRAMSs_data */
void FREE_c_r_dsch_EOM_BCMC_PROGRAMSs_data (c_r_dsch_EOM_BCMC_PROGRAMSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_PROGRAM_ID.value != NULL) {
		EDFree (sp->BCMC_PROGRAM_ID.value);
		sp->BCMC_PROGRAM_ID.value = NULL;
	}
	sp->BCMC_PROGRAM_ID.usedBits = 0;
	if (sp->NUM_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->NUM_FLOW_DISCRIMINATOR.value);
		sp->NUM_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->NUM_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->FLOW_DISCRIMINATORs.items; i0++) {
		FREE_c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->FLOW_DISCRIMINATORs.data[i0]);
		EDFree (sp->FLOW_DISCRIMINATORs.data[i0]);
	}
	if (sp->FLOW_DISCRIMINATORs.data) {EDFree (sp->FLOW_DISCRIMINATORs.data); sp->FLOW_DISCRIMINATORs.data = NULL; sp->FLOW_DISCRIMINATORs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_EPSMM_PILOTSs_data */
void FREE_c_r_dsch_EPSMM_PILOTSs_data (c_r_dsch_EPSMM_PILOTSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->pilotRecord_Present) {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->pilotRecord);
		EDFree (sp->pilotRecord);
		sp->pilotRecord_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_EPSMM_PILOTSs_data_pilotRecord (c_r_dsch_EPSMM_PILOTSs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->pilotRecord_Present == present) return;
	sp->pilotRecord_Present = present;
	if (present) {
		/*-->*/sp->pilotRecord = (c_PilotRecord_Pilot_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_PilotRecord_Pilot_Record_Type_Specific_Fields));
		INIT_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->pilotRecord);
	} else {
		FREE_c_PilotRecord_Pilot_Record_Type_Specific_Fields (sp->pilotRecord);
		EDFree (sp->pilotRecord);
	}
}

/* Free function for struct c_r_dsch_EPSMM_PILOTSs_01_data */
void FREE_c_r_dsch_EPSMM_PILOTSs_01_data (c_r_dsch_EPSMM_PILOTSs_01_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_EHOCM_PILOTSs_data */
void FREE_c_r_dsch_EHOCM_PILOTSs_data (c_r_dsch_EHOCM_PILOTSs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->records_Present) {
		FREE_c_r_dsch_EHOCM_PILOTSs_data_records (sp->records);
		EDFree (sp->records);
		sp->records_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_EHOCM_PILOTSs_data_records (c_r_dsch_EHOCM_PILOTSs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->records_Present == present) return;
	sp->records_Present = present;
	if (present) {
		/*-->*/sp->records = (c_r_dsch_EHOCM_PILOTSs_data_records*)EDAlloc (sizeof (c_r_dsch_EHOCM_PILOTSs_data_records));
		INIT_c_r_dsch_EHOCM_PILOTSs_data_records (sp->records);
	} else {
		FREE_c_r_dsch_EHOCM_PILOTSs_data_records (sp->records);
		EDFree (sp->records);
	}
}

/* Free function for struct c_r_dsch_SMRM_RECSs_data */
void FREE_c_r_dsch_SMRM_RECSs_data (c_r_dsch_SMRM_RECSs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_CLCM_ADD_TAGs_data */
void FREE_c_r_dsch_CLCM_ADD_TAGs_data (c_r_dsch_CLCM_ADD_TAGs_data* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_BSSREQM_RECORDs_data */
void FREE_c_r_dsch_BSSREQM_RECORDs_data (c_r_dsch_BSSREQM_RECORDs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Record_Type_Specific_Fields_Present) {
		FREE_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields (sp->Record_Type_Specific_Fields);
		EDFree (sp->Record_Type_Specific_Fields);
		sp->Record_Type_Specific_Fields_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields (c_r_dsch_BSSREQM_RECORDs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Record_Type_Specific_Fields_Present == present) return;
	sp->Record_Type_Specific_Fields_Present = present;
	if (present) {
		/*-->*/sp->Record_Type_Specific_Fields = (c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields*)EDAlloc (sizeof (c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields));
		INIT_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields (sp->Record_Type_Specific_Fields);
	} else {
		FREE_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields (sp->Record_Type_Specific_Fields);
		EDFree (sp->Record_Type_Specific_Fields);
	}
}

/* Free function for variable-sized sequence c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs */
void FREE_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data */
void FREE_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->BCMC_PROGRAM_ID.value != NULL) {
		EDFree (sp->BCMC_PROGRAM_ID.value);
		sp->BCMC_PROGRAM_ID.value = NULL;
	}
	sp->BCMC_PROGRAM_ID.usedBits = 0;
	if (sp->NUM_FLOW_DISCRIMINATOR.value != NULL) {
		EDFree (sp->NUM_FLOW_DISCRIMINATOR.value);
		sp->NUM_FLOW_DISCRIMINATOR.value = NULL;
	}
	sp->NUM_FLOW_DISCRIMINATOR.usedBits = 0;
	for (i0=0; i0<sp->FLOW_DISCRIMINATORs.items; i0++) {
		FREE_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sp->FLOW_DISCRIMINATORs.data[i0]);
		EDFree (sp->FLOW_DISCRIMINATORs.data[i0]);
	}
	if (sp->FLOW_DISCRIMINATORs.data) {EDFree (sp->FLOW_DISCRIMINATORs.data); sp->FLOW_DISCRIMINATORs.data = NULL; sp->FLOW_DISCRIMINATORs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_HOSINM_HO_SUP_RECs_data */
void FREE_c_r_dsch_HOSINM_HO_SUP_RECs_data (c_r_dsch_HOSINM_HO_SUP_RECs_data* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->HO_SUP_REC_Present) {
		FREE_c_r_dsch_HOSINM_HO_SUP_RECs_data_HO_SUP_REC (sp->HO_SUP_REC);
		EDFree (sp->HO_SUP_REC);
		sp->HO_SUP_REC_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_HOSINM_HO_SUP_RECs_data_HO_SUP_REC (c_r_dsch_HOSINM_HO_SUP_RECs_data* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->HO_SUP_REC_Present == present) return;
	sp->HO_SUP_REC_Present = present;
	if (present) {
		/*-->*/sp->HO_SUP_REC = (c_r_dsch_HOSINM_HO_SUP_RECs_data_HO_SUP_REC*)EDAlloc (sizeof (c_r_dsch_HOSINM_HO_SUP_RECs_data_HO_SUP_REC));
		INIT_c_r_dsch_HOSINM_HO_SUP_RECs_data_HO_SUP_REC (sp->HO_SUP_REC);
	} else {
		FREE_c_r_dsch_HOSINM_HO_SUP_RECs_data_HO_SUP_REC (sp->HO_SUP_REC);
		EDFree (sp->HO_SUP_REC);
	}
}

/* Free function for struct c_r_dsch_GEM_GE_RECs_data */
void FREE_c_r_dsch_GEM_GE_RECs_data (c_r_dsch_GEM_GE_RECs_data* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC (&(sp->GE_REC));

}

/* Free function for union c_R_Orders_parameters */
void FREE_c_R_Orders_parameters (c_R_Orders_parameters* sp) {
	switch (sp->Present) {
		case U_c_R_Orders_parameters_p_R_Base_Station_Challenge_Order: {
			FREE_c_R_Base_Station_Challenge_Order (sp->u.p_R_Base_Station_Challenge_Order);
			EDFree (sp->u.p_R_Base_Station_Challenge_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Service_Option_Request_Order: {
			FREE_c_R_Service_Option_Request_Order (sp->u.p_R_Service_Option_Request_Order);
			EDFree (sp->u.p_R_Service_Option_Request_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Service_Option_Response_Order: {
			FREE_c_R_Service_Option_Response_Order (sp->u.p_R_Service_Option_Response_Order);
			EDFree (sp->u.p_R_Service_Option_Response_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Mobile_Station_Reject_Order: {
			FREE_c_R_Mobile_Station_Reject_Order (sp->u.p_R_Mobile_Station_Reject_Order);
			EDFree (sp->u.p_R_Mobile_Station_Reject_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Release_Order: {
			FREE_c_R_Release_Order (sp->u.p_R_Release_Order);
			EDFree (sp->u.p_R_Release_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Fast_Call_Setup_Order: {
			FREE_c_R_Fast_Call_Setup_Order (sp->u.p_R_Fast_Call_Setup_Order);
			EDFree (sp->u.p_R_Fast_Call_Setup_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Generic_Order: {
			EDFree (sp->u.p_R_Generic_Order);
			sp->u.p_R_Generic_Order = NULL;

			break;
		}

		default:;
	}
	sp->Present = U_c_R_Orders_parameters_NONE;
}

/* Free function for struct c_R_Orders */
void FREE_c_R_Orders (c_R_Orders* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_R_Orders_parameters (&(sp->parameters));

}

/* Free function for union c_F_Orders_parameters */
void FREE_c_F_Orders_parameters (c_F_Orders_parameters* sp) {
	switch (sp->Present) {
		case U_c_F_Orders_parameters_p_F_Base_Station_Challenge_Confirmation_Order: {
			FREE_c_F_Base_Station_Challenge_Confirmation_Order (sp->u.p_F_Base_Station_Challenge_Confirmation_Order);
			EDFree (sp->u.p_F_Base_Station_Challenge_Confirmation_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Service_Option_Request_Order: {
			FREE_c_F_Service_Option_Request_Order (sp->u.p_F_Service_Option_Request_Order);
			EDFree (sp->u.p_F_Service_Option_Request_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Service_Option_Response_Order: {
			FREE_c_F_Service_Option_Response_Order (sp->u.p_F_Service_Option_Response_Order);
			EDFree (sp->u.p_F_Service_Option_Response_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Status_Request_Order: {
			FREE_c_F_Status_Request_Order (sp->u.p_F_Status_Request_Order);
			EDFree (sp->u.p_F_Status_Request_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Registration_Accepted_Order: {
			FREE_c_F_Registration_Accepted_Order (sp->u.p_F_Registration_Accepted_Order);
			EDFree (sp->u.p_F_Registration_Accepted_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Periodic_Pilot_Measurement_Request_Order: {
			FREE_c_F_Periodic_Pilot_Measurement_Request_Order (sp->u.p_F_Periodic_Pilot_Measurement_Request_Order);
			EDFree (sp->u.p_F_Periodic_Pilot_Measurement_Request_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Retry_Order: {
			FREE_c_F_Retry_Order (sp->u.p_F_Retry_Order);
			EDFree (sp->u.p_F_Retry_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Base_Station_Reject_Order: {
			FREE_c_F_Base_Station_Reject_Order (sp->u.p_F_Base_Station_Reject_Order);
			EDFree (sp->u.p_F_Base_Station_Reject_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_BCMC_Order: {
			FREE_c_F_BCMC_Order (sp->u.p_F_BCMC_Order);
			EDFree (sp->u.p_F_BCMC_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Fast_Call_Setup_Order: {
			FREE_c_F_Fast_Call_Setup_Order (sp->u.p_F_Fast_Call_Setup_Order);
			EDFree (sp->u.p_F_Fast_Call_Setup_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Service_Status_Order: {
			FREE_c_F_Service_Status_Order (sp->u.p_F_Service_Status_Order);
			EDFree (sp->u.p_F_Service_Status_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Location_Services_Order: {
			FREE_c_F_Location_Services_Order (sp->u.p_F_Location_Services_Order);
			EDFree (sp->u.p_F_Location_Services_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_EACH_Selective_Acknowledgment_Order: {
			FREE_c_F_EACH_Selective_Acknowledgment_Order (sp->u.p_F_EACH_Selective_Acknowledgment_Order);
			EDFree (sp->u.p_F_EACH_Selective_Acknowledgment_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_R_Generic_Order: {
			EDFree (sp->u.p_R_Generic_Order);
			sp->u.p_R_Generic_Order = NULL;

			break;
		}

		default:;
	}
	sp->Present = U_c_F_Orders_parameters_NONE;
}

/* Free function for struct c_F_Orders */
void FREE_c_F_Orders (c_F_Orders* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_F_Orders_parameters (&(sp->parameters));

}

/* Free function for struct c_STR_Type_specific_fields */
void FREE_c_STR_Type_specific_fields (c_STR_Type_specific_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->BAND_CLASS_Present) {
		FREE_c_STR_Type_specific_fields_BAND_CLASS (sp->BAND_CLASS);
		EDFree (sp->BAND_CLASS);
		sp->BAND_CLASS_Present = ED_FALSE;
	}
	if (sp->BAND_CLASS_and_OP_MODE_Present) {
		FREE_c_STR_Type_specific_fields_BAND_CLASS_and_OP_MODE (sp->BAND_CLASS_and_OP_MODE);
		EDFree (sp->BAND_CLASS_and_OP_MODE);
		sp->BAND_CLASS_and_OP_MODE_Present = ED_FALSE;
	}

}

void SETPRESENT_c_STR_Type_specific_fields_BAND_CLASS (c_STR_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->BAND_CLASS_Present == present) return;
	sp->BAND_CLASS_Present = present;
	if (present) {
		/*-->*/sp->BAND_CLASS = (c_STR_Type_specific_fields_BAND_CLASS*)EDAlloc (sizeof (c_STR_Type_specific_fields_BAND_CLASS));
		INIT_c_STR_Type_specific_fields_BAND_CLASS (sp->BAND_CLASS);
	} else {
		FREE_c_STR_Type_specific_fields_BAND_CLASS (sp->BAND_CLASS);
		EDFree (sp->BAND_CLASS);
	}
}

void SETPRESENT_c_STR_Type_specific_fields_BAND_CLASS_and_OP_MODE (c_STR_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->BAND_CLASS_and_OP_MODE_Present == present) return;
	sp->BAND_CLASS_and_OP_MODE_Present = present;
	if (present) {
		/*-->*/sp->BAND_CLASS_and_OP_MODE = (c_STR_Type_specific_fields_BAND_CLASS_and_OP_MODE*)EDAlloc (sizeof (c_STR_Type_specific_fields_BAND_CLASS_and_OP_MODE));
		INIT_c_STR_Type_specific_fields_BAND_CLASS_and_OP_MODE (sp->BAND_CLASS_and_OP_MODE);
	} else {
		FREE_c_STR_Type_specific_fields_BAND_CLASS_and_OP_MODE (sp->BAND_CLASS_and_OP_MODE);
		EDFree (sp->BAND_CLASS_and_OP_MODE);
	}
}

/* Free function for struct c_f_csch_CAM_Additional_record_fields */
void FREE_c_f_csch_CAM_Additional_record_fields (c_f_csch_CAM_Additional_record_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_csch_CAM_Additional_record_fields_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_csch_CAM_Additional_record_fields_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c05_Present) {
		FREE_c_f_csch_CAM_Additional_record_fields_c05 (sp->c05);
		EDFree (sp->c05);
		sp->c05_Present = ED_FALSE;
	}
	if (sp->c06_Present) {
		FREE_c_f_csch_CAM_Additional_record_fields_c06 (sp->c06);
		EDFree (sp->c06);
		sp->c06_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_f_csch_CAM_Additional_record_fields_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}
	if (sp->c04_Present) {
		FREE_c_f_csch_CAM_Additional_record_fields_c04 (sp->c04);
		EDFree (sp->c04);
		sp->c04_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_CAM_Additional_record_fields_c01 (c_f_csch_CAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_csch_CAM_Additional_record_fields_c01*)EDAlloc (sizeof (c_f_csch_CAM_Additional_record_fields_c01));
		INIT_c_f_csch_CAM_Additional_record_fields_c01 (sp->c01);
	} else {
		FREE_c_f_csch_CAM_Additional_record_fields_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_csch_CAM_Additional_record_fields_c02 (c_f_csch_CAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_csch_CAM_Additional_record_fields_c02*)EDAlloc (sizeof (c_f_csch_CAM_Additional_record_fields_c02));
		INIT_c_f_csch_CAM_Additional_record_fields_c02 (sp->c02);
	} else {
		FREE_c_f_csch_CAM_Additional_record_fields_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_f_csch_CAM_Additional_record_fields_c05 (c_f_csch_CAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c05_Present == present) return;
	sp->c05_Present = present;
	if (present) {
		/*-->*/sp->c05 = (c_f_csch_CAM_Additional_record_fields_c05*)EDAlloc (sizeof (c_f_csch_CAM_Additional_record_fields_c05));
		INIT_c_f_csch_CAM_Additional_record_fields_c05 (sp->c05);
	} else {
		FREE_c_f_csch_CAM_Additional_record_fields_c05 (sp->c05);
		EDFree (sp->c05);
	}
}

void SETPRESENT_c_f_csch_CAM_Additional_record_fields_c06 (c_f_csch_CAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c06_Present == present) return;
	sp->c06_Present = present;
	if (present) {
		/*-->*/sp->c06 = (c_f_csch_CAM_Additional_record_fields_c06*)EDAlloc (sizeof (c_f_csch_CAM_Additional_record_fields_c06));
		INIT_c_f_csch_CAM_Additional_record_fields_c06 (sp->c06);
	} else {
		FREE_c_f_csch_CAM_Additional_record_fields_c06 (sp->c06);
		EDFree (sp->c06);
	}
}

void SETPRESENT_c_f_csch_CAM_Additional_record_fields_c03 (c_f_csch_CAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_f_csch_CAM_Additional_record_fields_c03*)EDAlloc (sizeof (c_f_csch_CAM_Additional_record_fields_c03));
		INIT_c_f_csch_CAM_Additional_record_fields_c03 (sp->c03);
	} else {
		FREE_c_f_csch_CAM_Additional_record_fields_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

void SETPRESENT_c_f_csch_CAM_Additional_record_fields_c04 (c_f_csch_CAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c04_Present == present) return;
	sp->c04_Present = present;
	if (present) {
		/*-->*/sp->c04 = (c_f_csch_CAM_Additional_record_fields_c04*)EDAlloc (sizeof (c_f_csch_CAM_Additional_record_fields_c04));
		INIT_c_f_csch_CAM_Additional_record_fields_c04 (sp->c04);
	} else {
		FREE_c_f_csch_CAM_Additional_record_fields_c04 (sp->c04);
		EDFree (sp->c04);
	}
}

/* Free function for struct c_f_csch_SRDM_Type_specific_fields_1 */
void FREE_c_f_csch_SRDM_Type_specific_fields_1 (c_f_csch_SRDM_Type_specific_fields_1* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_SRDM_Type_specific_fields_2_CHANSs */
void FREE_c_f_csch_SRDM_Type_specific_fields_2_CHANSs (c_f_csch_SRDM_Type_specific_fields_2_CHANSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_SRDM_Type_specific_fields_2 */
void FREE_c_f_csch_SRDM_Type_specific_fields_2 (c_f_csch_SRDM_Type_specific_fields_2* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->CHANSs.items; i0++) {
		FREE_c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data (sp->CHANSs.data[i0]);
		EDFree (sp->CHANSs.data[i0]);
	}
	if (sp->CHANSs.data) {EDFree (sp->CHANSs.data); sp->CHANSs.data = NULL; sp->CHANSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_GSRDM_Type_specific_fields_1 */
void FREE_c_f_csch_GSRDM_Type_specific_fields_1 (c_f_csch_GSRDM_Type_specific_fields_1* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_GSRDM_Type_specific_fields_2_CHANSs */
void FREE_c_f_csch_GSRDM_Type_specific_fields_2_CHANSs (c_f_csch_GSRDM_Type_specific_fields_2_CHANSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_GSRDM_Type_specific_fields_2 */
void FREE_c_f_csch_GSRDM_Type_specific_fields_2 (c_f_csch_GSRDM_Type_specific_fields_2* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->CHANSs.items; i0++) {
		FREE_c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data (sp->CHANSs.data[i0]);
		EDFree (sp->CHANSs.data[i0]);
	}
	if (sp->CHANSs.data) {EDFree (sp->CHANSs.data); sp->CHANSs.data = NULL; sp->CHANSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_ECAM_Additional_record_fields */
void FREE_c_f_csch_ECAM_Additional_record_fields (c_f_csch_ECAM_Additional_record_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c000_Present) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c000 (sp->c000);
		EDFree (sp->c000);
		sp->c000_Present = ED_FALSE;
	}
	if (sp->c001_Present) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c001 (sp->c001);
		EDFree (sp->c001);
		sp->c001_Present = ED_FALSE;
	}
	if (sp->c010_Present) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c010 (sp->c010);
		EDFree (sp->c010);
		sp->c010_Present = ED_FALSE;
	}
	if (sp->c011_Present) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c011 (sp->c011);
		EDFree (sp->c011);
		sp->c011_Present = ED_FALSE;
	}
	if (sp->c100_Present) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100 (sp->c100);
		EDFree (sp->c100);
		sp->c100_Present = ED_FALSE;
	}
	if (sp->c101_Present) {
		FREE_c_f_csch_ECAM_Additional_record_fields_c101 (sp->c101);
		EDFree (sp->c101);
		sp->c101_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c000 (c_f_csch_ECAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c000_Present == present) return;
	sp->c000_Present = present;
	if (present) {
		/*-->*/sp->c000 = (c_f_csch_ECAM_Additional_record_fields_c000*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c000));
		INIT_c_f_csch_ECAM_Additional_record_fields_c000 (sp->c000);
	} else {
		FREE_c_f_csch_ECAM_Additional_record_fields_c000 (sp->c000);
		EDFree (sp->c000);
	}
}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c001 (c_f_csch_ECAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c001_Present == present) return;
	sp->c001_Present = present;
	if (present) {
		/*-->*/sp->c001 = (c_f_csch_ECAM_Additional_record_fields_c001*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c001));
		INIT_c_f_csch_ECAM_Additional_record_fields_c001 (sp->c001);
	} else {
		FREE_c_f_csch_ECAM_Additional_record_fields_c001 (sp->c001);
		EDFree (sp->c001);
	}
}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c010 (c_f_csch_ECAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c010_Present == present) return;
	sp->c010_Present = present;
	if (present) {
		/*-->*/sp->c010 = (c_f_csch_ECAM_Additional_record_fields_c010*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c010));
		INIT_c_f_csch_ECAM_Additional_record_fields_c010 (sp->c010);
	} else {
		FREE_c_f_csch_ECAM_Additional_record_fields_c010 (sp->c010);
		EDFree (sp->c010);
	}
}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c011 (c_f_csch_ECAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c011_Present == present) return;
	sp->c011_Present = present;
	if (present) {
		/*-->*/sp->c011 = (c_f_csch_ECAM_Additional_record_fields_c011*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c011));
		INIT_c_f_csch_ECAM_Additional_record_fields_c011 (sp->c011);
	} else {
		FREE_c_f_csch_ECAM_Additional_record_fields_c011 (sp->c011);
		EDFree (sp->c011);
	}
}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c100 (c_f_csch_ECAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c100_Present == present) return;
	sp->c100_Present = present;
	if (present) {
		/*-->*/sp->c100 = (c_f_csch_ECAM_Additional_record_fields_c100*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100));
		INIT_c_f_csch_ECAM_Additional_record_fields_c100 (sp->c100);
	} else {
		FREE_c_f_csch_ECAM_Additional_record_fields_c100 (sp->c100);
		EDFree (sp->c100);
	}
}

void SETPRESENT_c_f_csch_ECAM_Additional_record_fields_c101 (c_f_csch_ECAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c101_Present == present) return;
	sp->c101_Present = present;
	if (present) {
		/*-->*/sp->c101 = (c_f_csch_ECAM_Additional_record_fields_c101*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c101));
		INIT_c_f_csch_ECAM_Additional_record_fields_c101 (sp->c101);
	} else {
		FREE_c_f_csch_ECAM_Additional_record_fields_c101 (sp->c101);
		EDFree (sp->c101);
	}
}

/* Free function for struct c_f_csch_EGSRDM_Type_specific_fields */
void FREE_c_f_csch_EGSRDM_Type_specific_fields (c_f_csch_EGSRDM_Type_specific_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->field_1_Present) {
		FREE_c_f_csch_EGSRDM_Type_Specific_Fields_1 (sp->field_1);
		EDFree (sp->field_1);
		sp->field_1_Present = ED_FALSE;
	}
	if (sp->field_2_Present) {
		FREE_c_f_csch_EGSRDM_Type_Specific_Fields_2 (sp->field_2);
		EDFree (sp->field_2);
		sp->field_2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_EGSRDM_Type_specific_fields_field_1 (c_f_csch_EGSRDM_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->field_1_Present == present) return;
	sp->field_1_Present = present;
	if (present) {
		/*-->*/sp->field_1 = (c_f_csch_EGSRDM_Type_Specific_Fields_1*)EDAlloc (sizeof (c_f_csch_EGSRDM_Type_Specific_Fields_1));
		INIT_c_f_csch_EGSRDM_Type_Specific_Fields_1 (sp->field_1);
	} else {
		FREE_c_f_csch_EGSRDM_Type_Specific_Fields_1 (sp->field_1);
		EDFree (sp->field_1);
	}
}

void SETPRESENT_c_f_csch_EGSRDM_Type_specific_fields_field_2 (c_f_csch_EGSRDM_Type_specific_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->field_2_Present == present) return;
	sp->field_2_Present = present;
	if (present) {
		/*-->*/sp->field_2 = (c_f_csch_EGSRDM_Type_Specific_Fields_2*)EDAlloc (sizeof (c_f_csch_EGSRDM_Type_Specific_Fields_2));
		INIT_c_f_csch_EGSRDM_Type_Specific_Fields_2 (sp->field_2);
	} else {
		FREE_c_f_csch_EGSRDM_Type_Specific_Fields_2 (sp->field_2);
		EDFree (sp->field_2);
	}
}

/* Free function for struct c_f_csch_APIDM_LOC_REC_0001 */
void FREE_c_f_csch_APIDM_LOC_REC_0001 (c_f_csch_APIDM_LOC_REC_0001* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_MECAM_Additional_record_fields */
void FREE_c_f_csch_MECAM_Additional_record_fields (c_f_csch_MECAM_Additional_record_fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c100_Present) {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100 (sp->c100);
		EDFree (sp->c100);
		sp->c100_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_MECAM_Additional_record_fields_c100 (c_f_csch_MECAM_Additional_record_fields* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c100_Present == present) return;
	sp->c100_Present = present;
	if (present) {
		/*-->*/sp->c100 = (c_f_csch_MECAM_Additional_record_fields_c100*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100));
		INIT_c_f_csch_MECAM_Additional_record_fields_c100 (sp->c100);
	} else {
		FREE_c_f_csch_MECAM_Additional_record_fields_c100 (sp->c100);
		EDFree (sp->c100);
	}
}

/* Free function for struct c_f_dsch_EHDM_Additional_fields */
void FREE_c_f_dsch_EHDM_Additional_fields (c_f_dsch_EHDM_Additional_fields* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_SRDM_Type_specific_fields_1 */
void FREE_c_f_dsch_SRDM_Type_specific_fields_1 (c_f_dsch_SRDM_Type_specific_fields_1* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_SRDM_Type_specific_fields_2_CHANSs */
void FREE_c_f_dsch_SRDM_Type_specific_fields_2_CHANSs (c_f_dsch_SRDM_Type_specific_fields_2_CHANSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_SRDM_Type_specific_fields_2 */
void FREE_c_f_dsch_SRDM_Type_specific_fields_2 (c_f_dsch_SRDM_Type_specific_fields_2* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->CHANSs.items; i0++) {
		FREE_c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data (sp->CHANSs.data[i0]);
		EDFree (sp->CHANSs.data[i0]);
	}
	if (sp->CHANSs.data) {EDFree (sp->CHANSs.data); sp->CHANSs.data = NULL; sp->CHANSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs (c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_1 */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_1 (c_f_dsch_CFSRQM_Mode_specific_fields_1* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->ANALOG_FREQs.items; i0++) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data (sp->ANALOG_FREQs.data[i0]);
		EDFree (sp->ANALOG_FREQs.data[i0]);
	}
	if (sp->ANALOG_FREQs.data) {EDFree (sp->ANALOG_FREQs.data); sp->ANALOG_FREQs.data = NULL; sp->ANALOG_FREQs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs (c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02 */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_CFSRQM_Mode_specific_fields_0 */
void FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0 (c_f_dsch_CFSRQM_Mode_specific_fields_0* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->CF_SRCH_OFFSET_INCLs.items; i0++) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data (sp->CF_SRCH_OFFSET_INCLs.data[i0]);
		EDFree (sp->CF_SRCH_OFFSET_INCLs.data[i0]);
	}
	if (sp->CF_SRCH_OFFSET_INCLs.data) {EDFree (sp->CF_SRCH_OFFSET_INCLs.data); sp->CF_SRCH_OFFSET_INCLs.data = NULL; sp->CF_SRCH_OFFSET_INCLs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_02.items; i0++) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data (sp->PILOTSs_02.data[i0]);
		EDFree (sp->PILOTSs_02.data[i0]);
	}
	if (sp->PILOTSs_02.data) {EDFree (sp->PILOTSs_02.data); sp->PILOTSs_02.data = NULL; sp->PILOTSs_02.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS */
void FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}
	if (sp->c04_Present) {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04 (sp->c04);
		EDFree (sp->c04);
		sp->c04_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01));
		INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01 (sp->c01);
	} else {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02));
		INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02 (sp->c02);
	} else {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03));
		INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03 (sp->c03);
	} else {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

void SETPRESENT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c04_Present == present) return;
	sp->c04_Present = present;
	if (present) {
		/*-->*/sp->c04 = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04));
		INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04 (sp->c04);
	} else {
		FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04 (sp->c04);
		EDFree (sp->c04);
	}
}

/* Free function for struct c_f_dsch_MABOPM_data_01 */
void FREE_c_f_dsch_MABOPM_data_01 (c_f_dsch_MABOPM_data_01* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MABOPM_data_02 */
void FREE_c_f_dsch_MABOPM_data_02 (c_f_dsch_MABOPM_data_02* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MABOPM_data_03 */
void FREE_c_f_dsch_MABOPM_data_03 (c_f_dsch_MABOPM_data_03* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS */
void FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->c01_Present) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01 (sp->c01);
		EDFree (sp->c01);
		sp->c01_Present = ED_FALSE;
	}
	if (sp->c02_Present) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02 (sp->c02);
		EDFree (sp->c02);
		sp->c02_Present = ED_FALSE;
	}
	if (sp->c03_Present) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03 (sp->c03);
		EDFree (sp->c03);
		sp->c03_Present = ED_FALSE;
	}
	if (sp->c04_Present) {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04 (sp->c04);
		EDFree (sp->c04);
		sp->c04_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c01_Present == present) return;
	sp->c01_Present = present;
	if (present) {
		/*-->*/sp->c01 = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01));
		INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01 (sp->c01);
	} else {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01 (sp->c01);
		EDFree (sp->c01);
	}
}

void SETPRESENT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c02_Present == present) return;
	sp->c02_Present = present;
	if (present) {
		/*-->*/sp->c02 = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02));
		INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02 (sp->c02);
	} else {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02 (sp->c02);
		EDFree (sp->c02);
	}
}

void SETPRESENT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c03_Present == present) return;
	sp->c03_Present = present;
	if (present) {
		/*-->*/sp->c03 = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03));
		INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03 (sp->c03);
	} else {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03 (sp->c03);
		EDFree (sp->c03);
	}
}

void SETPRESENT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->c04_Present == present) return;
	sp->c04_Present = present;
	if (present) {
		/*-->*/sp->c04 = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04));
		INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04 (sp->c04);
	} else {
		FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04 (sp->c04);
		EDFree (sp->c04);
	}
}

/* Free function for struct c_r_dsch_SCRM_records */
void FREE_c_r_dsch_SCRM_records (c_r_dsch_SCRM_records* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_000_Present) {
		FREE_c_r_dsch_SCRM_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
		EDFree (sp->Type_specific_fields_000);
		sp->Type_specific_fields_000_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_SCRM_records_Type_specific_fields_000 (c_r_dsch_SCRM_records* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_000_Present == present) return;
	sp->Type_specific_fields_000_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields_000 = (c_r_dsch_SCRM_records_Type_specific_fields_000*)EDAlloc (sizeof (c_r_dsch_SCRM_records_Type_specific_fields_000));
		INIT_c_r_dsch_SCRM_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
	} else {
		FREE_c_r_dsch_SCRM_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
		EDFree (sp->Type_specific_fields_000);
	}
}

/* Free function for variable-sized sequence c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs */
void FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01 */
void FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01 (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_CFSRPM_Mode_specific_fields_0000 */
void FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000 (c_r_dsch_CFSRPM_Mode_specific_fields_0000* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_01.items; i0++) {
		FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data (sp->PILOTSs_01.data[i0]);
		EDFree (sp->PILOTSs_01.data[i0]);
	}
	if (sp->PILOTSs_01.data) {EDFree (sp->PILOTSs_01.data); sp->PILOTSs_01.data = NULL; sp->PILOTSs_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS */
void FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS (c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_CFSRPM_Mode_specific_fields_0001 */
void FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0001 (c_r_dsch_CFSRPM_Mode_specific_fields_0001* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->ANALOG_FREQS.items; i0++) {
		FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data (sp->ANALOG_FREQS.data[i0]);
		EDFree (sp->ANALOG_FREQS.data[i0]);
	}
	if (sp->ANALOG_FREQS.data) {EDFree (sp->ANALOG_FREQS.data); sp->ANALOG_FREQS.data = NULL; sp->ANALOG_FREQS.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_EPSMM_records */
void FREE_c_r_dsch_EPSMM_records (c_r_dsch_EPSMM_records* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_000_Present) {
		FREE_c_r_dsch_EPSMM_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
		EDFree (sp->Type_specific_fields_000);
		sp->Type_specific_fields_000_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_EPSMM_records_Type_specific_fields_000 (c_r_dsch_EPSMM_records* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_000_Present == present) return;
	sp->Type_specific_fields_000_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields_000 = (c_r_dsch_EPSMM_records_Type_specific_fields_000*)EDAlloc (sizeof (c_r_dsch_EPSMM_records_Type_specific_fields_000));
		INIT_c_r_dsch_EPSMM_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
	} else {
		FREE_c_r_dsch_EPSMM_records_Type_specific_fields_000 (sp->Type_specific_fields_000);
		EDFree (sp->Type_specific_fields_000);
	}
}

/* Free function for struct c_f_csch_SPM */
void FREE_c_f_csch_SPM (c_f_csch_SPM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_APM_ACCT_SOs */
void FREE_c_f_csch_APM_ACCT_SOs (c_f_csch_APM_ACCT_SOs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_APM_ACCT_SOs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_APM_ACCT_SO_GRPs */
void FREE_c_f_csch_APM_ACCT_SO_GRPs (c_f_csch_APM_ACCT_SO_GRPs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_APM_ACCT_SO_GRPs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_APM */
void FREE_c_f_csch_APM (c_f_csch_APM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->ACCT_SOs.items; i0++) {
		FREE_c_f_csch_APM_ACCT_SOs_data (sp->ACCT_SOs.data[i0]);
		EDFree (sp->ACCT_SOs.data[i0]);
	}
	if (sp->ACCT_SOs.data) {EDFree (sp->ACCT_SOs.data); sp->ACCT_SOs.data = NULL; sp->ACCT_SOs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->ACCT_SO_GRPs.items; i0++) {
		FREE_c_f_csch_APM_ACCT_SO_GRPs_data (sp->ACCT_SO_GRPs.data[i0]);
		EDFree (sp->ACCT_SO_GRPs.data[i0]);
	}
	if (sp->ACCT_SO_GRPs.data) {EDFree (sp->ACCT_SO_GRPs.data); sp->ACCT_SO_GRPs.data = NULL; sp->ACCT_SO_GRPs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_NLM_data_01 */
void FREE_c_f_csch_NLM_data_01 (c_f_csch_NLM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_NLM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_NLM */
void FREE_c_f_csch_NLM (c_f_csch_NLM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_csch_NLM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_CCLM_data_01 */
void FREE_c_f_csch_CCLM_data_01 (c_f_csch_CCLM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_CCLM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_CCLM */
void FREE_c_f_csch_CCLM (c_f_csch_CCLM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_csch_CCLM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_ORDM */
void FREE_c_f_csch_ORDM (c_f_csch_ORDM* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_F_Orders (&(sp->order));

}

/* Free function for struct c_f_csch_CAM */
void FREE_c_f_csch_CAM (c_f_csch_CAM* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_CAM_Additional_record_fields (&(sp->Additional_record_fields));

}

/* Free function for variable-sized sequence c_f_csch_DBM_FIELDSs */
void FREE_c_f_csch_DBM_FIELDSs (c_f_csch_DBM_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_DBM_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_DBM */
void FREE_c_f_csch_DBM (c_f_csch_DBM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_f_csch_DBM_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_AUCM */
void FREE_c_f_csch_AUCM (c_f_csch_AUCM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_SSDUM */
void FREE_c_f_csch_SSDUM (c_f_csch_SSDUM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_FNM_INFO_RECORDs */
void FREE_c_f_csch_FNM_INFO_RECORDs (c_f_csch_FNM_INFO_RECORDs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_3_7_5 (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_FNM */
void FREE_c_f_csch_FNM (c_f_csch_FNM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->INFO_RECORDs.items; i0++) {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORDs.data[i0]);
		EDFree (sp->INFO_RECORDs.data[i0]);
	}
	if (sp->INFO_RECORDs.data) {EDFree (sp->INFO_RECORDs.data); sp->INFO_RECORDs.data = NULL; sp->INFO_RECORDs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_ESPM_data_01 */
void FREE_c_f_csch_ESPM_data_01 (c_f_csch_ESPM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ESPM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ESPM_data_02 */
void FREE_c_f_csch_ESPM_data_02 (c_f_csch_ESPM_data_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ESPM_data_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ESPM_PDCCHs */
void FREE_c_f_csch_ESPM_PDCCHs (c_f_csch_ESPM_PDCCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ESPM_PDCCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ESPM_CAND_BAND_CLASSs */
void FREE_c_f_csch_ESPM_CAND_BAND_CLASSs (c_f_csch_ESPM_CAND_BAND_CLASSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ESPM_CAND_BAND_CLASSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ESPM */
void FREE_c_f_csch_ESPM (c_f_csch_ESPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->TMSI_ZONE.value != NULL) {
		EDFree (sp->TMSI_ZONE.value);
		sp->TMSI_ZONE.value = NULL;
	}
	sp->TMSI_ZONE.usedBits = 0;
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_csch_ESPM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_02.items; i0++) {
		FREE_c_f_csch_ESPM_data_02_data (sp->data_02.data[i0]);
		EDFree (sp->data_02.data[i0]);
	}
	if (sp->data_02.data) {EDFree (sp->data_02.data); sp->data_02.data = NULL; sp->data_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PDCCHs.items; i0++) {
		FREE_c_f_csch_ESPM_PDCCHs_data (sp->PDCCHs.data[i0]);
		EDFree (sp->PDCCHs.data[i0]);
	}
	if (sp->PDCCHs.data) {EDFree (sp->PDCCHs.data); sp->PDCCHs.data = NULL; sp->PDCCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->CAND_BAND_CLASSs.items; i0++) {
		FREE_c_f_csch_ESPM_CAND_BAND_CLASSs_data (sp->CAND_BAND_CLASSs.data[i0]);
		EDFree (sp->CAND_BAND_CLASSs.data[i0]);
	}
	if (sp->CAND_BAND_CLASSs.data) {EDFree (sp->CAND_BAND_CLASSs.data); sp->CAND_BAND_CLASSs.data = NULL; sp->CAND_BAND_CLASSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_ENLM_data_01 */
void FREE_c_f_csch_ENLM_data_01 (c_f_csch_ENLM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ENLM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ENLM */
void FREE_c_f_csch_ENLM (c_f_csch_ENLM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_csch_ENLM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_STRQM_FIELDSs */
void FREE_c_f_csch_STRQM_FIELDSs (c_f_csch_STRQM_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_STRQM_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_STRQM */
void FREE_c_f_csch_STRQM (c_f_csch_STRQM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	FREE_c_STR_Type_specific_fields (&(sp->Type_specific_fields));
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_f_csch_STRQM_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_SRDM */
void FREE_c_f_csch_SRDM (c_f_csch_SRDM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_1_Present) {
		FREE_c_f_csch_SRDM_Type_specific_fields_1 (sp->Type_specific_fields_1);
		EDFree (sp->Type_specific_fields_1);
		sp->Type_specific_fields_1_Present = ED_FALSE;
	}
	if (sp->Type_specific_fields_2_Present) {
		FREE_c_f_csch_SRDM_Type_specific_fields_2 (sp->Type_specific_fields_2);
		EDFree (sp->Type_specific_fields_2);
		sp->Type_specific_fields_2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_SRDM_Type_specific_fields_1 (c_f_csch_SRDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_1_Present == present) return;
	sp->Type_specific_fields_1_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields_1 = (c_f_csch_SRDM_Type_specific_fields_1*)EDAlloc (sizeof (c_f_csch_SRDM_Type_specific_fields_1));
		INIT_c_f_csch_SRDM_Type_specific_fields_1 (sp->Type_specific_fields_1);
	} else {
		FREE_c_f_csch_SRDM_Type_specific_fields_1 (sp->Type_specific_fields_1);
		EDFree (sp->Type_specific_fields_1);
	}
}

void SETPRESENT_c_f_csch_SRDM_Type_specific_fields_2 (c_f_csch_SRDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_2_Present == present) return;
	sp->Type_specific_fields_2_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields_2 = (c_f_csch_SRDM_Type_specific_fields_2*)EDAlloc (sizeof (c_f_csch_SRDM_Type_specific_fields_2));
		INIT_c_f_csch_SRDM_Type_specific_fields_2 (sp->Type_specific_fields_2);
	} else {
		FREE_c_f_csch_SRDM_Type_specific_fields_2 (sp->Type_specific_fields_2);
		EDFree (sp->Type_specific_fields_2);
	}
}

/* Free function for struct c_f_csch_GPM */
void FREE_c_f_csch_GPM (c_f_csch_GPM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_GSRDM */
void FREE_c_f_csch_GSRDM (c_f_csch_GSRDM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_1_Present) {
		FREE_c_f_csch_GSRDM_Type_specific_fields_1 (sp->Type_specific_fields_1);
		EDFree (sp->Type_specific_fields_1);
		sp->Type_specific_fields_1_Present = ED_FALSE;
	}
	if (sp->Type_specific_fields_2_Present) {
		FREE_c_f_csch_GSRDM_Type_specific_fields_2 (sp->Type_specific_fields_2);
		EDFree (sp->Type_specific_fields_2);
		sp->Type_specific_fields_2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_csch_GSRDM_Type_specific_fields_1 (c_f_csch_GSRDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_1_Present == present) return;
	sp->Type_specific_fields_1_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields_1 = (c_f_csch_GSRDM_Type_specific_fields_1*)EDAlloc (sizeof (c_f_csch_GSRDM_Type_specific_fields_1));
		INIT_c_f_csch_GSRDM_Type_specific_fields_1 (sp->Type_specific_fields_1);
	} else {
		FREE_c_f_csch_GSRDM_Type_specific_fields_1 (sp->Type_specific_fields_1);
		EDFree (sp->Type_specific_fields_1);
	}
}

void SETPRESENT_c_f_csch_GSRDM_Type_specific_fields_2 (c_f_csch_GSRDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_2_Present == present) return;
	sp->Type_specific_fields_2_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields_2 = (c_f_csch_GSRDM_Type_specific_fields_2*)EDAlloc (sizeof (c_f_csch_GSRDM_Type_specific_fields_2));
		INIT_c_f_csch_GSRDM_Type_specific_fields_2 (sp->Type_specific_fields_2);
	} else {
		FREE_c_f_csch_GSRDM_Type_specific_fields_2 (sp->Type_specific_fields_2);
		EDFree (sp->Type_specific_fields_2);
	}
}

/* Free function for struct c_f_csch_TASM */
void FREE_c_f_csch_TASM (c_f_csch_TASM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->TMSI_ZONE.value != NULL) {
		EDFree (sp->TMSI_ZONE.value);
		sp->TMSI_ZONE.value = NULL;
	}
	sp->TMSI_ZONE.usedBits = 0;

}

/* Free function for struct c_f_csch_PACAM */
void FREE_c_f_csch_PACAM (c_f_csch_PACAM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_ECAM */
void FREE_c_f_csch_ECAM (c_f_csch_ECAM* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_ECAM_Additional_record_fields (&(sp->Additional_record_fields));

}

/* Free function for variable-sized sequence c_f_csch_GNLM_NGHBRs */
void FREE_c_f_csch_GNLM_NGHBRs (c_f_csch_GNLM_NGHBRs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GNLM_NGHBRs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_GNLM_ANALOG_NGHBRs */
void FREE_c_f_csch_GNLM_ANALOG_NGHBRs (c_f_csch_GNLM_ANALOG_NGHBRs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GNLM_ANALOG_NGHBRs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_GNLM_NGHBRs_01 */
void FREE_c_f_csch_GNLM_NGHBRs_01 (c_f_csch_GNLM_NGHBRs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GNLM_NGHBRs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_GNLM_NGHBRs_02 */
void FREE_c_f_csch_GNLM_NGHBRs_02 (c_f_csch_GNLM_NGHBRs_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GNLM_NGHBRs_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_GNLM_NGHBRs_03 */
void FREE_c_f_csch_GNLM_NGHBRs_03 (c_f_csch_GNLM_NGHBRs_03* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GNLM_NGHBRs_03_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_GNLM_NGHBRs_04 */
void FREE_c_f_csch_GNLM_NGHBRs_04 (c_f_csch_GNLM_NGHBRs_04* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GNLM_NGHBRs_04_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_GNLM_HRPD_NGHBRs */
void FREE_c_f_csch_GNLM_HRPD_NGHBRs (c_f_csch_GNLM_HRPD_NGHBRs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GNLM_HRPD_NGHBRs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_GNLM */
void FREE_c_f_csch_GNLM (c_f_csch_GNLM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->NGHBRs.items; i0++) {
		FREE_c_f_csch_GNLM_NGHBRs_data (sp->NGHBRs.data[i0]);
		EDFree (sp->NGHBRs.data[i0]);
	}
	if (sp->NGHBRs.data) {EDFree (sp->NGHBRs.data); sp->NGHBRs.data = NULL; sp->NGHBRs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->ANALOG_NGHBRs.items; i0++) {
		FREE_c_f_csch_GNLM_ANALOG_NGHBRs_data (sp->ANALOG_NGHBRs.data[i0]);
		EDFree (sp->ANALOG_NGHBRs.data[i0]);
	}
	if (sp->ANALOG_NGHBRs.data) {EDFree (sp->ANALOG_NGHBRs.data); sp->ANALOG_NGHBRs.data = NULL; sp->ANALOG_NGHBRs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->NGHBRs_01.items; i0++) {
		FREE_c_f_csch_GNLM_NGHBRs_01_data (sp->NGHBRs_01.data[i0]);
		EDFree (sp->NGHBRs_01.data[i0]);
	}
	if (sp->NGHBRs_01.data) {EDFree (sp->NGHBRs_01.data); sp->NGHBRs_01.data = NULL; sp->NGHBRs_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->NGHBRs_02.items; i0++) {
		FREE_c_f_csch_GNLM_NGHBRs_02_data (sp->NGHBRs_02.data[i0]);
		EDFree (sp->NGHBRs_02.data[i0]);
	}
	if (sp->NGHBRs_02.data) {EDFree (sp->NGHBRs_02.data); sp->NGHBRs_02.data = NULL; sp->NGHBRs_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->NGHBRs_03.items; i0++) {
		FREE_c_f_csch_GNLM_NGHBRs_03_data (sp->NGHBRs_03.data[i0]);
		EDFree (sp->NGHBRs_03.data[i0]);
	}
	if (sp->NGHBRs_03.data) {EDFree (sp->NGHBRs_03.data); sp->NGHBRs_03.data = NULL; sp->NGHBRs_03.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->NGHBRs_04.items; i0++) {
		FREE_c_f_csch_GNLM_NGHBRs_04_data (sp->NGHBRs_04.data[i0]);
		EDFree (sp->NGHBRs_04.data[i0]);
	}
	if (sp->NGHBRs_04.data) {EDFree (sp->NGHBRs_04.data); sp->NGHBRs_04.data = NULL; sp->NGHBRs_04.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->HRPD_NGHBRs.items; i0++) {
		FREE_c_f_csch_GNLM_HRPD_NGHBRs_data (sp->HRPD_NGHBRs.data[i0]);
		EDFree (sp->HRPD_NGHBRs.data[i0]);
	}
	if (sp->HRPD_NGHBRs.data) {EDFree (sp->HRPD_NGHBRs.data); sp->HRPD_NGHBRs.data = NULL; sp->HRPD_NGHBRs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_UZIM_UZIDs */
void FREE_c_f_csch_UZIM_UZIDs (c_f_csch_UZIM_UZIDs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_UZIM_UZIDs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_UZIM */
void FREE_c_f_csch_UZIM (c_f_csch_UZIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->UZIDs.items; i0++) {
		FREE_c_f_csch_UZIM_UZIDs_data (sp->UZIDs.data[i0]);
		EDFree (sp->UZIDs.data[i0]);
	}
	if (sp->UZIDs.data) {EDFree (sp->UZIDs.data); sp->UZIDs.data = NULL; sp->UZIDs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_PNLM_RADIO_INTERFACEs */
void FREE_c_f_csch_PNLM_RADIO_INTERFACEs (c_f_csch_PNLM_RADIO_INTERFACEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_PNLM */
void FREE_c_f_csch_PNLM (c_f_csch_PNLM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RADIO_INTERFACEs.items; i0++) {
		FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data (sp->RADIO_INTERFACEs.data[i0]);
		EDFree (sp->RADIO_INTERFACEs.data[i0]);
	}
	if (sp->RADIO_INTERFACEs.data) {EDFree (sp->RADIO_INTERFACEs.data); sp->RADIO_INTERFACEs.data = NULL; sp->RADIO_INTERFACEs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_SCHM */
void FREE_c_f_csch_SCHM (c_f_csch_SCHM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_EGSRDM_ADD_RECORDs */
void FREE_c_f_csch_EGSRDM_ADD_RECORDs (c_f_csch_EGSRDM_ADD_RECORDs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_EGSRDM_ADD_RECORDs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_EGSRDM */
void FREE_c_f_csch_EGSRDM (c_f_csch_EGSRDM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_EGSRDM_Type_specific_fields (&(sp->Type_specific_fields));
	for (i0=0; i0<sp->ADD_RECORDs.items; i0++) {
		FREE_c_f_csch_EGSRDM_ADD_RECORDs_data (sp->ADD_RECORDs.data[i0]);
		EDFree (sp->ADD_RECORDs.data[i0]);
	}
	if (sp->ADD_RECORDs.data) {EDFree (sp->ADD_RECORDs.data); sp->ADD_RECORDs.data = NULL; sp->ADD_RECORDs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_ECCLM_FREQs */
void FREE_c_f_csch_ECCLM_FREQs (c_f_csch_ECCLM_FREQs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECCLM_FREQs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECCLM_FREQs_01 */
void FREE_c_f_csch_ECCLM_FREQs_01 (c_f_csch_ECCLM_FREQs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECCLM_FREQs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECCLM_FREQs_02 */
void FREE_c_f_csch_ECCLM_FREQs_02 (c_f_csch_ECCLM_FREQs_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECCLM_FREQs_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECCLM_data_01 */
void FREE_c_f_csch_ECCLM_data_01 (c_f_csch_ECCLM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECCLM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECCLM_FREQs_03 */
void FREE_c_f_csch_ECCLM_FREQs_03 (c_f_csch_ECCLM_FREQs_03* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECCLM_FREQs_03_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_ECCLM_BANDs */
void FREE_c_f_csch_ECCLM_BANDs (c_f_csch_ECCLM_BANDs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ECCLM_BANDs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ECCLM */
void FREE_c_f_csch_ECCLM (c_f_csch_ECCLM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FREQs.items; i0++) {
		FREE_c_f_csch_ECCLM_FREQs_data (sp->FREQs.data[i0]);
		EDFree (sp->FREQs.data[i0]);
	}
	if (sp->FREQs.data) {EDFree (sp->FREQs.data); sp->FREQs.data = NULL; sp->FREQs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FREQs_01.items; i0++) {
		FREE_c_f_csch_ECCLM_FREQs_01_data (sp->FREQs_01.data[i0]);
		EDFree (sp->FREQs_01.data[i0]);
	}
	if (sp->FREQs_01.data) {EDFree (sp->FREQs_01.data); sp->FREQs_01.data = NULL; sp->FREQs_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FREQs_02.items; i0++) {
		FREE_c_f_csch_ECCLM_FREQs_02_data (sp->FREQs_02.data[i0]);
		EDFree (sp->FREQs_02.data[i0]);
	}
	if (sp->FREQs_02.data) {EDFree (sp->FREQs_02.data); sp->FREQs_02.data = NULL; sp->FREQs_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_csch_ECCLM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FREQs_03.items; i0++) {
		FREE_c_f_csch_ECCLM_FREQs_03_data (sp->FREQs_03.data[i0]);
		EDFree (sp->FREQs_03.data[i0]);
	}
	if (sp->FREQs_03.data) {EDFree (sp->FREQs_03.data); sp->FREQs_03.data = NULL; sp->FREQs_03.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->BANDs.items; i0++) {
		FREE_c_f_csch_ECCLM_BANDs_data (sp->BANDs.data[i0]);
		EDFree (sp->BANDs.data[i0]);
	}
	if (sp->BANDs.data) {EDFree (sp->BANDs.data); sp->BANDs.data = NULL; sp->BANDs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_UZRM_ASSIGN_UZID */
void FREE_c_f_csch_UZRM_ASSIGN_UZID (c_f_csch_UZRM_ASSIGN_UZID* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_UZRM */
void FREE_c_f_csch_UZRM (c_f_csch_UZRM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->ASSIGN_UZID.data) {EDFree (sp->ASSIGN_UZID.data); sp->ASSIGN_UZID.data = NULL; sp->ASSIGN_UZID.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_A41SPM */
void FREE_c_f_csch_A41SPM (c_f_csch_A41SPM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->TMSI_ZONE.value != NULL) {
		EDFree (sp->TMSI_ZONE.value);
		sp->TMSI_ZONE.value = NULL;
	}
	sp->TMSI_ZONE.usedBits = 0;

}

/* Free function for variable-sized sequence c_f_csch_MCRRPM_FCCCHs */
void FREE_c_f_csch_MCRRPM_FCCCHs (c_f_csch_MCRRPM_FCCCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MCRRPM_FCCCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_MCRRPM_BCCH_BCASTs */
void FREE_c_f_csch_MCRRPM_BCCH_BCASTs (c_f_csch_MCRRPM_BCCH_BCASTs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MCRRPM_BCCH_BCASTs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_MCRRPM_QPCHs */
void FREE_c_f_csch_MCRRPM_QPCHs (c_f_csch_MCRRPM_QPCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MCRRPM_QPCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_MCRRPM_PDCCHs */
void FREE_c_f_csch_MCRRPM_PDCCHs (c_f_csch_MCRRPM_PDCCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MCRRPM_PDCCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_MCRRPM_CAND_BAND_CLASSs */
void FREE_c_f_csch_MCRRPM_CAND_BAND_CLASSs (c_f_csch_MCRRPM_CAND_BAND_CLASSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_MCRRPM */
void FREE_c_f_csch_MCRRPM (c_f_csch_MCRRPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->ADD_FIELDS.value != NULL) {
		EDFree (sp->ADD_FIELDS.value);
		sp->ADD_FIELDS.value = NULL;
	}
	sp->ADD_FIELDS.usedBits = 0;
	for (i0=0; i0<sp->FCCCHs.items; i0++) {
		FREE_c_f_csch_MCRRPM_FCCCHs_data (sp->FCCCHs.data[i0]);
		EDFree (sp->FCCCHs.data[i0]);
	}
	if (sp->FCCCHs.data) {EDFree (sp->FCCCHs.data); sp->FCCCHs.data = NULL; sp->FCCCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->BCCH_BCASTs.items; i0++) {
		FREE_c_f_csch_MCRRPM_BCCH_BCASTs_data (sp->BCCH_BCASTs.data[i0]);
		EDFree (sp->BCCH_BCASTs.data[i0]);
	}
	if (sp->BCCH_BCASTs.data) {EDFree (sp->BCCH_BCASTs.data); sp->BCCH_BCASTs.data = NULL; sp->BCCH_BCASTs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->QPCHs.items; i0++) {
		FREE_c_f_csch_MCRRPM_QPCHs_data (sp->QPCHs.data[i0]);
		EDFree (sp->QPCHs.data[i0]);
	}
	if (sp->QPCHs.data) {EDFree (sp->QPCHs.data); sp->QPCHs.data = NULL; sp->QPCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PDCCHs.items; i0++) {
		FREE_c_f_csch_MCRRPM_PDCCHs_data (sp->PDCCHs.data[i0]);
		EDFree (sp->PDCCHs.data[i0]);
	}
	if (sp->PDCCHs.data) {EDFree (sp->PDCCHs.data); sp->PDCCHs.data = NULL; sp->PDCCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->CAND_BAND_CLASSs.items; i0++) {
		FREE_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data (sp->CAND_BAND_CLASSs.data[i0]);
		EDFree (sp->CAND_BAND_CLASSs.data[i0]);
	}
	if (sp->CAND_BAND_CLASSs.data) {EDFree (sp->CAND_BAND_CLASSs.data); sp->CAND_BAND_CLASSs.data = NULL; sp->CAND_BAND_CLASSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_A41RANDM */
void FREE_c_f_csch_A41RANDM (c_f_csch_A41RANDM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_EAPM_MODE_SELECTION_ENTRIESs */
void FREE_c_f_csch_EAPM_MODE_SELECTION_ENTRIESs (c_f_csch_EAPM_MODE_SELECTION_ENTRIESs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_EAPM_MODE_PARM_RECs */
void FREE_c_f_csch_EAPM_MODE_PARM_RECs (c_f_csch_EAPM_MODE_PARM_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_EAPM_MODE_PARM_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_EAPM_CACHs */
void FREE_c_f_csch_EAPM_CACHs (c_f_csch_EAPM_CACHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_EAPM_CACHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_EAPM_CPCCHs */
void FREE_c_f_csch_EAPM_CPCCHs (c_f_csch_EAPM_CPCCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_EAPM_CPCCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_EAPM_ACCT_SOs */
void FREE_c_f_csch_EAPM_ACCT_SOs (c_f_csch_EAPM_ACCT_SOs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_EAPM_ACCT_SOs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_EAPM_ACCT_SO_GRPs */
void FREE_c_f_csch_EAPM_ACCT_SO_GRPs (c_f_csch_EAPM_ACCT_SO_GRPs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_EAPM_ACCT_SO_GRPs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_EAPM */
void FREE_c_f_csch_EAPM (c_f_csch_EAPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->MODE_SELECTION_ENTRIESs.items; i0++) {
		FREE_c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data (sp->MODE_SELECTION_ENTRIESs.data[i0]);
		EDFree (sp->MODE_SELECTION_ENTRIESs.data[i0]);
	}
	if (sp->MODE_SELECTION_ENTRIESs.data) {EDFree (sp->MODE_SELECTION_ENTRIESs.data); sp->MODE_SELECTION_ENTRIESs.data = NULL; sp->MODE_SELECTION_ENTRIESs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->MODE_PARM_RECs.items; i0++) {
		FREE_c_f_csch_EAPM_MODE_PARM_RECs_data (sp->MODE_PARM_RECs.data[i0]);
		EDFree (sp->MODE_PARM_RECs.data[i0]);
	}
	if (sp->MODE_PARM_RECs.data) {EDFree (sp->MODE_PARM_RECs.data); sp->MODE_PARM_RECs.data = NULL; sp->MODE_PARM_RECs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->CACHs.items; i0++) {
		FREE_c_f_csch_EAPM_CACHs_data (sp->CACHs.data[i0]);
		EDFree (sp->CACHs.data[i0]);
	}
	if (sp->CACHs.data) {EDFree (sp->CACHs.data); sp->CACHs.data = NULL; sp->CACHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->CPCCHs.items; i0++) {
		FREE_c_f_csch_EAPM_CPCCHs_data (sp->CPCCHs.data[i0]);
		EDFree (sp->CPCCHs.data[i0]);
	}
	if (sp->CPCCHs.data) {EDFree (sp->CPCCHs.data); sp->CPCCHs.data = NULL; sp->CPCCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->ACCT_SOs.items; i0++) {
		FREE_c_f_csch_EAPM_ACCT_SOs_data (sp->ACCT_SOs.data[i0]);
		EDFree (sp->ACCT_SOs.data[i0]);
	}
	if (sp->ACCT_SOs.data) {EDFree (sp->ACCT_SOs.data); sp->ACCT_SOs.data = NULL; sp->ACCT_SOs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->ACCT_SO_GRPs.items; i0++) {
		FREE_c_f_csch_EAPM_ACCT_SO_GRPs_data (sp->ACCT_SO_GRPs.data[i0]);
		EDFree (sp->ACCT_SO_GRPs.data[i0]);
	}
	if (sp->ACCT_SO_GRPs.data) {EDFree (sp->ACCT_SO_GRPs.data); sp->ACCT_SO_GRPs.data = NULL; sp->ACCT_SO_GRPs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_UNLM_RADIO_INTERFACEs */
void FREE_c_f_csch_UNLM_RADIO_INTERFACEs (c_f_csch_UNLM_RADIO_INTERFACEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_UNLM */
void FREE_c_f_csch_UNLM (c_f_csch_UNLM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RADIO_INTERFACEs.items; i0++) {
		FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data (sp->RADIO_INTERFACEs.data[i0]);
		EDFree (sp->RADIO_INTERFACEs.data[i0]);
	}
	if (sp->RADIO_INTERFACEs.data) {EDFree (sp->RADIO_INTERFACEs.data); sp->RADIO_INTERFACEs.data = NULL; sp->RADIO_INTERFACEs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_SMCM */
void FREE_c_f_csch_SMCM (c_f_csch_SMCM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_csch_AUREQM */
void FREE_c_f_csch_AUREQM (c_f_csch_AUREQM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_BSPM_FSCHs */
void FREE_c_f_csch_BSPM_FSCHs (c_f_csch_BSPM_FSCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_BSPM_FSCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_BSPM_BCMC_PROGRAMSs */
void FREE_c_f_csch_BSPM_BCMC_PROGRAMSs (c_f_csch_BSPM_BCMC_PROGRAMSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_csch_BSPM_data_01 */
void FREE_c_f_csch_BSPM_data_01 (c_f_csch_BSPM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_BSPM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_BSPM */
void FREE_c_f_csch_BSPM (c_f_csch_BSPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FSCHs.items; i0++) {
		FREE_c_f_csch_BSPM_FSCHs_data (sp->FSCHs.data[i0]);
		EDFree (sp->FSCHs.data[i0]);
	}
	if (sp->FSCHs.data) {EDFree (sp->FSCHs.data); sp->FSCHs.data = NULL; sp->FSCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->BCMC_PROGRAMSs.items; i0++) {
		FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data (sp->BCMC_PROGRAMSs.data[i0]);
		EDFree (sp->BCMC_PROGRAMSs.data[i0]);
	}
	if (sp->BCMC_PROGRAMSs.data) {EDFree (sp->BCMC_PROGRAMSs.data); sp->BCMC_PROGRAMSs.data = NULL; sp->BCMC_PROGRAMSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_csch_BSPM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_APIDM_data_01 */
void FREE_c_f_csch_APIDM_data_01 (c_f_csch_APIDM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_APIDM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_APIDM */
void FREE_c_f_csch_APIDM (c_f_csch_APIDM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->AP_ID.value != NULL) {
		EDFree (sp->AP_ID.value);
		sp->AP_ID.value = NULL;
	}
	sp->AP_ID.usedBits = 0;
	if (sp->LOC_REC_0001_Present) {
		FREE_c_f_csch_APIDM_LOC_REC_0001 (sp->LOC_REC_0001);
		EDFree (sp->LOC_REC_0001);
		sp->LOC_REC_0001_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_csch_APIDM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_csch_APIDM_LOC_REC_0001 (c_f_csch_APIDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->LOC_REC_0001_Present == present) return;
	sp->LOC_REC_0001_Present = present;
	if (present) {
		/*-->*/sp->LOC_REC_0001 = (c_f_csch_APIDM_LOC_REC_0001*)EDAlloc (sizeof (c_f_csch_APIDM_LOC_REC_0001));
		INIT_c_f_csch_APIDM_LOC_REC_0001 (sp->LOC_REC_0001);
	} else {
		FREE_c_f_csch_APIDM_LOC_REC_0001 (sp->LOC_REC_0001);
		EDFree (sp->LOC_REC_0001);
	}
}

/* Free function for variable-sized sequence c_f_csch_APIDTM_FIELDSs */
void FREE_c_f_csch_APIDTM_FIELDSs (c_f_csch_APIDTM_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_APIDTM_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_APIDTM */
void FREE_c_f_csch_APIDTM (c_f_csch_APIDTM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_f_csch_APIDTM_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_APPIM_APPI_RECs */
void FREE_c_f_csch_APPIM_APPI_RECs (c_f_csch_APPIM_APPI_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_APPIM_APPI_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_APPIM */
void FREE_c_f_csch_APPIM (c_f_csch_APPIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->APPI_RECs.items; i0++) {
		FREE_c_f_csch_APPIM_APPI_RECs_data (sp->APPI_RECs.data[i0]);
		EDFree (sp->APPI_RECs.data[i0]);
	}
	if (sp->APPI_RECs.data) {EDFree (sp->APPI_RECs.data); sp->APPI_RECs.data = NULL; sp->APPI_RECs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_GOIM_GOI_RECs */
void FREE_c_f_csch_GOIM_GOI_RECs (c_f_csch_GOIM_GOI_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GOIM_GOI_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_GOIM */
void FREE_c_f_csch_GOIM (c_f_csch_GOIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->GOI_RECs.items; i0++) {
		FREE_c_f_csch_GOIM_GOI_RECs_data (sp->GOI_RECs.data[i0]);
		EDFree (sp->GOI_RECs.data[i0]);
	}
	if (sp->GOI_RECs.data) {EDFree (sp->GOI_RECs.data); sp->GOI_RECs.data = NULL; sp->GOI_RECs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_FDCCLM_CAND_BAND_CLASSs */
void FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs (c_f_csch_FDCCLM_CAND_BAND_CLASSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_FDCCLM */
void FREE_c_f_csch_FDCCLM (c_f_csch_FDCCLM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->CAND_BAND_CLASSs.items; i0++) {
		FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data (sp->CAND_BAND_CLASSs.data[i0]);
		EDFree (sp->CAND_BAND_CLASSs.data[i0]);
	}
	if (sp->CAND_BAND_CLASSs.data) {EDFree (sp->CAND_BAND_CLASSs.data); sp->CAND_BAND_CLASSs.data = NULL; sp->CAND_BAND_CLASSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_GEM_GE_RECs */
void FREE_c_f_csch_GEM_GE_RECs (c_f_csch_GEM_GE_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_GEM_GE_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_GEM */
void FREE_c_f_csch_GEM (c_f_csch_GEM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->GE_RECs.items; i0++) {
		FREE_c_f_csch_GEM_GE_RECs_data (sp->GE_RECs.data[i0]);
		EDFree (sp->GE_RECs.data[i0]);
	}
	if (sp->GE_RECs.data) {EDFree (sp->GE_RECs.data); sp->GE_RECs.data = NULL; sp->GE_RECs.allocatedItems=0; /*FR02A*/}
	if (sp->MESSAGE_REC.value != NULL) {
		EDFree (sp->MESSAGE_REC.value);
		sp->MESSAGE_REC.value = NULL;
	}
	sp->MESSAGE_REC.usedBits = 0;

}

/* Free function for variable-sized sequence c_f_csch_ATIM_RADIO_INTERFACEs */
void FREE_c_f_csch_ATIM_RADIO_INTERFACEs (c_f_csch_ATIM_RADIO_INTERFACEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_ATIM */
void FREE_c_f_csch_ATIM (c_f_csch_ATIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RADIO_INTERFACEs.items; i0++) {
		FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data (sp->RADIO_INTERFACEs.data[i0]);
		EDFree (sp->RADIO_INTERFACEs.data[i0]);
	}
	if (sp->RADIO_INTERFACEs.data) {EDFree (sp->RADIO_INTERFACEs.data); sp->RADIO_INTERFACEs.data = NULL; sp->RADIO_INTERFACEs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_csch_FGOIM_GOI_RECs */
void FREE_c_f_csch_FGOIM_GOI_RECs (c_f_csch_FGOIM_GOI_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_FGOIM_GOI_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_FGOIM */
void FREE_c_f_csch_FGOIM (c_f_csch_FGOIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->GOI_RECs.items; i0++) {
		FREE_c_f_csch_FGOIM_GOI_RECs_data (sp->GOI_RECs.data[i0]);
		EDFree (sp->GOI_RECs.data[i0]);
	}
	if (sp->GOI_RECs.data) {EDFree (sp->GOI_RECs.data); sp->GOI_RECs.data = NULL; sp->GOI_RECs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_GAPM */
void FREE_c_f_csch_GAPM (c_f_csch_GAPM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_csch_HDPPM_records */
void FREE_c_f_csch_HDPPM_records (c_f_csch_HDPPM_records* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_csch_HDPPM_records_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_csch_HDPPM */
void FREE_c_f_csch_HDPPM (c_f_csch_HDPPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->records.items; i0++) {
		FREE_c_f_csch_HDPPM_records_data (sp->records.data[i0]);
		EDFree (sp->records.data[i0]);
	}
	if (sp->records.data) {EDFree (sp->records.data); sp->records.data = NULL; sp->records.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_csch_MECAM */
void FREE_c_f_csch_MECAM (c_f_csch_MECAM* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_f_csch_MECAM_Additional_record_fields (&(sp->Additional_record_fields));

}

/* Free function for struct c_f_dsch_ORDRM */
void FREE_c_f_dsch_ORDRM (c_f_dsch_ORDRM* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_F_Orders (&(sp->order));

}

/* Free function for struct c_f_dsch_AUCM */
void FREE_c_f_dsch_AUCM (c_f_dsch_AUCM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_AWIM_INFO_RECORDs */
void FREE_c_f_dsch_AWIM_INFO_RECORDs (c_f_dsch_AWIM_INFO_RECORDs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_3_7_5_encrypted (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_AWIM */
void FREE_c_f_dsch_AWIM (c_f_dsch_AWIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->INFO_RECORDs.items; i0++) {
		FREE_c_InfoRec_3_7_5_encrypted (sp->INFO_RECORDs.data[i0]);
		EDFree (sp->INFO_RECORDs.data[i0]);
	}
	if (sp->INFO_RECORDs.data) {EDFree (sp->INFO_RECORDs.data); sp->INFO_RECORDs.data = NULL; sp->INFO_RECORDs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_DBM_MOBILE_COUNTRY_CODE */
void FREE_c_f_dsch_DBM_MOBILE_COUNTRY_CODE (c_f_dsch_DBM_MOBILE_COUNTRY_CODE* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_DBM_COUNTRY_BURST_TYPE */
void FREE_c_f_dsch_DBM_COUNTRY_BURST_TYPE (c_f_dsch_DBM_COUNTRY_BURST_TYPE* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_DBM_EXTENDED_BURST_TYPE */
void FREE_c_f_dsch_DBM_EXTENDED_BURST_TYPE (c_f_dsch_DBM_EXTENDED_BURST_TYPE* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_DBM_FIELDSs */
void FREE_c_f_dsch_DBM_FIELDSs (c_f_dsch_DBM_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_DBM_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_DBM */
void FREE_c_f_dsch_DBM (c_f_dsch_DBM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->MOBILE_COUNTRY_CODE.data) {EDFree (sp->MOBILE_COUNTRY_CODE.data); sp->MOBILE_COUNTRY_CODE.data = NULL; sp->MOBILE_COUNTRY_CODE.allocatedItems=0; /*FR02A*/}
	if (sp->COUNTRY_BURST_TYPE.data) {EDFree (sp->COUNTRY_BURST_TYPE.data); sp->COUNTRY_BURST_TYPE.data = NULL; sp->COUNTRY_BURST_TYPE.allocatedItems=0; /*FR02A*/}
	if (sp->EXTENDED_BURST_TYPE.data) {EDFree (sp->EXTENDED_BURST_TYPE.data); sp->EXTENDED_BURST_TYPE.data = NULL; sp->EXTENDED_BURST_TYPE.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_f_dsch_DBM_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_AHDM */
void FREE_c_f_dsch_AHDM (c_f_dsch_AHDM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_ITSPM */
void FREE_c_f_dsch_ITSPM (c_f_dsch_ITSPM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_NLUM_data_01 */
void FREE_c_f_dsch_NLUM_data_01 (c_f_dsch_NLUM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_NLUM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_NLUM */
void FREE_c_f_dsch_NLUM (c_f_dsch_NLUM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_dsch_NLUM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_BDTMFM_DIGITSs */
void FREE_c_f_dsch_BDTMFM_DIGITSs (c_f_dsch_BDTMFM_DIGITSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_BDTMFM_DIGITSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_BDTMFM */
void FREE_c_f_dsch_BDTMFM (c_f_dsch_BDTMFM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->DIGITSs.items; i0++) {
		FREE_c_f_dsch_BDTMFM_DIGITSs_data (sp->DIGITSs.data[i0]);
		EDFree (sp->DIGITSs.data[i0]);
	}
	if (sp->DIGITSs.data) {EDFree (sp->DIGITSs.data); sp->DIGITSs.data = NULL; sp->DIGITSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_PCNPM */
void FREE_c_f_dsch_PCNPM (c_f_dsch_PCNPM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_RTPM_data_01 */
void FREE_c_f_dsch_RTPM_data_01 (c_f_dsch_RTPM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_RTPM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_RTPM */
void FREE_c_f_dsch_RTPM (c_f_dsch_RTPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_dsch_RTPM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_STPM_data_01 */
void FREE_c_f_dsch_STPM_data_01 (c_f_dsch_STPM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_STPM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_STPM */
void FREE_c_f_dsch_STPM (c_f_dsch_STPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_dsch_STPM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_SSDUM */
void FREE_c_f_dsch_SSDUM (c_f_dsch_SSDUM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_FWIM_INFO_RECORDs */
void FREE_c_f_dsch_FWIM_INFO_RECORDs (c_f_dsch_FWIM_INFO_RECORDs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_3_7_5_encrypted (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_FWIM */
void FREE_c_f_dsch_FWIM (c_f_dsch_FWIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->INFO_RECORDs.items; i0++) {
		FREE_c_InfoRec_3_7_5_encrypted (sp->INFO_RECORDs.data[i0]);
		EDFree (sp->INFO_RECORDs.data[i0]);
	}
	if (sp->INFO_RECORDs.data) {EDFree (sp->INFO_RECORDs.data); sp->INFO_RECORDs.data = NULL; sp->INFO_RECORDs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_MSRM */
void FREE_c_f_dsch_MSRM (c_f_dsch_MSRM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_STRQM_FIELDSs */
void FREE_c_f_dsch_STRQM_FIELDSs (c_f_dsch_STRQM_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_STRQM_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_STRQM */
void FREE_c_f_dsch_STRQM (c_f_dsch_STRQM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	FREE_c_STR_Type_specific_fields (&(sp->Type_specific_fields));
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_f_dsch_STRQM_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_EHDM_data_01 */
void FREE_c_f_dsch_EHDM_data_01 (c_f_dsch_EHDM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_EHDM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_EHDM */
void FREE_c_f_dsch_EHDM (c_f_dsch_EHDM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->Additional_fields_Present) {
		FREE_c_f_dsch_EHDM_Additional_fields (sp->Additional_fields);
		EDFree (sp->Additional_fields);
		sp->Additional_fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_dsch_EHDM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_EHDM_Additional_fields (c_f_dsch_EHDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Additional_fields_Present == present) return;
	sp->Additional_fields_Present = present;
	if (present) {
		/*-->*/sp->Additional_fields = (c_f_dsch_EHDM_Additional_fields*)EDAlloc (sizeof (c_f_dsch_EHDM_Additional_fields));
		INIT_c_f_dsch_EHDM_Additional_fields (sp->Additional_fields);
	} else {
		FREE_c_f_dsch_EHDM_Additional_fields (sp->Additional_fields);
		EDFree (sp->Additional_fields);
	}
}

/* Free function for struct c_f_dsch_SRQM */
void FREE_c_f_dsch_SRQM (c_f_dsch_SRQM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->record_Present) {
		FREE_c_InfoRec_3_7_5 (sp->record);
		EDFree (sp->record);
		sp->record_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_SRQM_record (c_f_dsch_SRQM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->record_Present == present) return;
	sp->record_Present = present;
	if (present) {
		/*-->*/sp->record = (c_InfoRec_3_7_5*)EDAlloc (sizeof (c_InfoRec_3_7_5));
		INIT_c_InfoRec_3_7_5 (sp->record);
	} else {
		FREE_c_InfoRec_3_7_5 (sp->record);
		EDFree (sp->record);
	}
}

/* Free function for struct c_f_dsch_SRPM */
void FREE_c_f_dsch_SRPM (c_f_dsch_SRPM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->record_Present) {
		FREE_c_InfoRec_3_7_5 (sp->record);
		EDFree (sp->record);
		sp->record_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_SRPM_record (c_f_dsch_SRPM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->record_Present == present) return;
	sp->record_Present = present;
	if (present) {
		/*-->*/sp->record = (c_InfoRec_3_7_5*)EDAlloc (sizeof (c_InfoRec_3_7_5));
		INIT_c_InfoRec_3_7_5 (sp->record);
	} else {
		FREE_c_InfoRec_3_7_5 (sp->record);
		EDFree (sp->record);
	}
}

/* Free function for variable-sized sequence c_f_dsch_SCM_CALLS_ASSIGNs */
void FREE_c_f_dsch_SCM_CALLS_ASSIGNs (c_f_dsch_SCM_CALLS_ASSIGNs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_SCM_CALLS_ASSIGNs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_SCM */
void FREE_c_f_dsch_SCM (c_f_dsch_SCM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->RESERVED.value != NULL) {
		EDFree (sp->RESERVED.value);
		sp->RESERVED.value = NULL;
	}
	sp->RESERVED.usedBits = 0;
	if (sp->SYNC_ID_Present) {
		if (sp->SYNC_ID->value != NULL) {
			EDFree (sp->SYNC_ID->value);
			sp->SYNC_ID->value = NULL;
		}
		sp->SYNC_ID->usedBits = 0;
		EDFree (sp->SYNC_ID);
		sp->SYNC_ID_Present = ED_FALSE;
	}
	if (sp->INFO_RECORD1_Present) {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD1);
		EDFree (sp->INFO_RECORD1);
		sp->INFO_RECORD1_Present = ED_FALSE;
	}
	if (sp->INFO_RECORD2_Present) {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD2);
		EDFree (sp->INFO_RECORD2);
		sp->INFO_RECORD2_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->CALLS_ASSIGNs.items; i0++) {
		FREE_c_f_dsch_SCM_CALLS_ASSIGNs_data (sp->CALLS_ASSIGNs.data[i0]);
		EDFree (sp->CALLS_ASSIGNs.data[i0]);
	}
	if (sp->CALLS_ASSIGNs.data) {EDFree (sp->CALLS_ASSIGNs.data); sp->CALLS_ASSIGNs.data = NULL; sp->CALLS_ASSIGNs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_SCM_SYNC_ID (c_f_dsch_SCM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->SYNC_ID_Present == present) return;
	sp->SYNC_ID_Present = present;
	if (present) {
		/*-->*/sp->SYNC_ID = (c_f_dsch_SCM_SYNC_ID*)EDAlloc (sizeof (c_f_dsch_SCM_SYNC_ID));
		sp->SYNC_ID->value = NULL;
		sp->SYNC_ID->usedBits = 0;
	} else {
		if (sp->SYNC_ID->value != NULL) {
			EDFree (sp->SYNC_ID->value);
			sp->SYNC_ID->value = NULL;
		}
		sp->SYNC_ID->usedBits = 0;
		EDFree (sp->SYNC_ID);
	}
}

void SETPRESENT_c_f_dsch_SCM_INFO_RECORD1 (c_f_dsch_SCM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->INFO_RECORD1_Present == present) return;
	sp->INFO_RECORD1_Present = present;
	if (present) {
		/*-->*/sp->INFO_RECORD1 = (c_InfoRec_3_7_5*)EDAlloc (sizeof (c_InfoRec_3_7_5));
		INIT_c_InfoRec_3_7_5 (sp->INFO_RECORD1);
	} else {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD1);
		EDFree (sp->INFO_RECORD1);
	}
}

void SETPRESENT_c_f_dsch_SCM_INFO_RECORD2 (c_f_dsch_SCM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->INFO_RECORD2_Present == present) return;
	sp->INFO_RECORD2_Present = present;
	if (present) {
		/*-->*/sp->INFO_RECORD2 = (c_InfoRec_3_7_5*)EDAlloc (sizeof (c_InfoRec_3_7_5));
		INIT_c_InfoRec_3_7_5 (sp->INFO_RECORD2);
	} else {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD2);
		EDFree (sp->INFO_RECORD2);
	}
}

/* Free function for struct c_f_dsch_SOCM */
void FREE_c_f_dsch_SOCM (c_f_dsch_SOCM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields.value != NULL) {
		EDFree (sp->Type_specific_fields.value);
		sp->Type_specific_fields.value = NULL;
	}
	sp->Type_specific_fields.usedBits = 0;

}

/* Free function for struct c_f_dsch_TASM */
void FREE_c_f_dsch_TASM (c_f_dsch_TASM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->TMSI_ZONE.value != NULL) {
		EDFree (sp->TMSI_ZONE.value);
		sp->TMSI_ZONE.value = NULL;
	}
	sp->TMSI_ZONE.usedBits = 0;

}

/* Free function for struct c_f_dsch_SRDM */
void FREE_c_f_dsch_SRDM (c_f_dsch_SRDM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields_1_Present) {
		FREE_c_f_dsch_SRDM_Type_specific_fields_1 (sp->Type_specific_fields_1);
		EDFree (sp->Type_specific_fields_1);
		sp->Type_specific_fields_1_Present = ED_FALSE;
	}
	if (sp->Type_specific_fields_2_Present) {
		FREE_c_f_dsch_SRDM_Type_specific_fields_2 (sp->Type_specific_fields_2);
		EDFree (sp->Type_specific_fields_2);
		sp->Type_specific_fields_2_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_SRDM_Type_specific_fields_1 (c_f_dsch_SRDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_1_Present == present) return;
	sp->Type_specific_fields_1_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields_1 = (c_f_dsch_SRDM_Type_specific_fields_1*)EDAlloc (sizeof (c_f_dsch_SRDM_Type_specific_fields_1));
		INIT_c_f_dsch_SRDM_Type_specific_fields_1 (sp->Type_specific_fields_1);
	} else {
		FREE_c_f_dsch_SRDM_Type_specific_fields_1 (sp->Type_specific_fields_1);
		EDFree (sp->Type_specific_fields_1);
	}
}

void SETPRESENT_c_f_dsch_SRDM_Type_specific_fields_2 (c_f_dsch_SRDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Type_specific_fields_2_Present == present) return;
	sp->Type_specific_fields_2_Present = present;
	if (present) {
		/*-->*/sp->Type_specific_fields_2 = (c_f_dsch_SRDM_Type_specific_fields_2*)EDAlloc (sizeof (c_f_dsch_SRDM_Type_specific_fields_2));
		INIT_c_f_dsch_SRDM_Type_specific_fields_2 (sp->Type_specific_fields_2);
	} else {
		FREE_c_f_dsch_SRDM_Type_specific_fields_2 (sp->Type_specific_fields_2);
		EDFree (sp->Type_specific_fields_2);
	}
}

/* Free function for variable-sized sequence c_f_dsch_SCAM_data_01 */
void FREE_c_f_dsch_SCAM_data_01 (c_f_dsch_SCAM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_SCAM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_SCAM_data_02 */
void FREE_c_f_dsch_SCAM_data_02 (c_f_dsch_SCAM_data_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_SCAM_data_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_SCAM */
void FREE_c_f_dsch_SCAM (c_f_dsch_SCAM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_dsch_SCAM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_02.items; i0++) {
		FREE_c_f_dsch_SCAM_data_02_data (sp->data_02.data[i0]);
		EDFree (sp->data_02.data[i0]);
	}
	if (sp->data_02.data) {EDFree (sp->data_02.data); sp->data_02.data = NULL; sp->data_02.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_PCNM_SUPs */
void FREE_c_f_dsch_PCNM_SUPs (c_f_dsch_PCNM_SUPs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_PCNM_SUPs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_PCNM */
void FREE_c_f_dsch_PCNM (c_f_dsch_PCNM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->SUPs.items; i0++) {
		FREE_c_f_dsch_PCNM_SUPs_data (sp->SUPs.data[i0]);
		EDFree (sp->SUPs.data[i0]);
	}
	if (sp->SUPs.data) {EDFree (sp->SUPs.data); sp->SUPs.data = NULL; sp->SUPs.allocatedItems=0; /*FR02A*/}
	if (sp->rec_Present) {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_Record (sp->rec);
		EDFree (sp->rec);
		sp->rec_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_PCNM_rec (c_f_dsch_PCNM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->rec_Present == present) return;
	sp->rec_Present = present;
	if (present) {
		/*-->*/sp->rec = (c_InfoRec_Non_Negotiable_Service_Configuration_Record*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_Record));
		INIT_c_InfoRec_Non_Negotiable_Service_Configuration_Record (sp->rec);
	} else {
		FREE_c_InfoRec_Non_Negotiable_Service_Configuration_Record (sp->rec);
		EDFree (sp->rec);
	}
}

/* Free function for variable-sized sequence c_f_dsch_ENLUM_NGHBRs */
void FREE_c_f_dsch_ENLUM_NGHBRs (c_f_dsch_ENLUM_NGHBRs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ENLUM_NGHBRs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ENLUM_NGHBRs_01 */
void FREE_c_f_dsch_ENLUM_NGHBRs_01 (c_f_dsch_ENLUM_NGHBRs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ENLUM_NGHBRs_02 */
void FREE_c_f_dsch_ENLUM_NGHBRs_02 (c_f_dsch_ENLUM_NGHBRs_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ENLUM_NGHBRs_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ENLUM */
void FREE_c_f_dsch_ENLUM (c_f_dsch_ENLUM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->NGHBRs.items; i0++) {
		FREE_c_f_dsch_ENLUM_NGHBRs_data (sp->NGHBRs.data[i0]);
		EDFree (sp->NGHBRs.data[i0]);
	}
	if (sp->NGHBRs.data) {EDFree (sp->NGHBRs.data); sp->NGHBRs.data = NULL; sp->NGHBRs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->NGHBRs_01.items; i0++) {
		FREE_c_f_dsch_ENLUM_NGHBRs_01_data (sp->NGHBRs_01.data[i0]);
		EDFree (sp->NGHBRs_01.data[i0]);
	}
	if (sp->NGHBRs_01.data) {EDFree (sp->NGHBRs_01.data); sp->NGHBRs_01.data = NULL; sp->NGHBRs_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->NGHBRs_02.items; i0++) {
		FREE_c_f_dsch_ENLUM_NGHBRs_02_data (sp->NGHBRs_02.data[i0]);
		EDFree (sp->NGHBRs_02.data[i0]);
	}
	if (sp->NGHBRs_02.data) {EDFree (sp->NGHBRs_02.data); sp->NGHBRs_02.data = NULL; sp->NGHBRs_02.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_CFSRQM */
void FREE_c_f_dsch_CFSRQM (c_f_dsch_CFSRQM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Mode_specific_fields_0_Present) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0 (sp->Mode_specific_fields_0);
		EDFree (sp->Mode_specific_fields_0);
		sp->Mode_specific_fields_0_Present = ED_FALSE;
	}
	if (sp->Mode_specific_fields_1_Present) {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_1 (sp->Mode_specific_fields_1);
		EDFree (sp->Mode_specific_fields_1);
		sp->Mode_specific_fields_1_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_CFSRQM_Mode_specific_fields_0 (c_f_dsch_CFSRQM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Mode_specific_fields_0_Present == present) return;
	sp->Mode_specific_fields_0_Present = present;
	if (present) {
		/*-->*/sp->Mode_specific_fields_0 = (c_f_dsch_CFSRQM_Mode_specific_fields_0*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0));
		INIT_c_f_dsch_CFSRQM_Mode_specific_fields_0 (sp->Mode_specific_fields_0);
	} else {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0 (sp->Mode_specific_fields_0);
		EDFree (sp->Mode_specific_fields_0);
	}
}

void SETPRESENT_c_f_dsch_CFSRQM_Mode_specific_fields_1 (c_f_dsch_CFSRQM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Mode_specific_fields_1_Present == present) return;
	sp->Mode_specific_fields_1_Present = present;
	if (present) {
		/*-->*/sp->Mode_specific_fields_1 = (c_f_dsch_CFSRQM_Mode_specific_fields_1*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_1));
		INIT_c_f_dsch_CFSRQM_Mode_specific_fields_1 (sp->Mode_specific_fields_1);
	} else {
		FREE_c_f_dsch_CFSRQM_Mode_specific_fields_1 (sp->Mode_specific_fields_1);
		EDFree (sp->Mode_specific_fields_1);
	}
}

/* Free function for struct c_f_dsch_CFSCNM */
void FREE_c_f_dsch_CFSCNM (c_f_dsch_CFSCNM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_PUFM */
void FREE_c_f_dsch_PUFM (c_f_dsch_PUFM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_PUFCM */
void FREE_c_f_dsch_PUFCM (c_f_dsch_PUFCM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_GHDM_PILOTSs */
void FREE_c_f_dsch_GHDM_PILOTSs (c_f_dsch_GHDM_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_GHDM_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_GHDM */
void FREE_c_f_dsch_GHDM (c_f_dsch_GHDM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->INFO_RECORDs_Present) {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORDs);
		EDFree (sp->INFO_RECORDs);
		sp->INFO_RECORDs_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_f_dsch_GHDM_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}
	if (sp->NN_INFO_RECORDs_Present) {
		FREE_c_InfoRec_3_7_5 (sp->NN_INFO_RECORDs);
		EDFree (sp->NN_INFO_RECORDs);
		sp->NN_INFO_RECORDs_Present = ED_FALSE;
	}
	if (sp->SYNC_ID.value != NULL) {
		EDFree (sp->SYNC_ID.value);
		sp->SYNC_ID.value = NULL;
	}
	sp->SYNC_ID.usedBits = 0;

}

void SETPRESENT_c_f_dsch_GHDM_INFO_RECORDs (c_f_dsch_GHDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->INFO_RECORDs_Present == present) return;
	sp->INFO_RECORDs_Present = present;
	if (present) {
		/*-->*/sp->INFO_RECORDs = (c_InfoRec_3_7_5*)EDAlloc (sizeof (c_InfoRec_3_7_5));
		INIT_c_InfoRec_3_7_5 (sp->INFO_RECORDs);
	} else {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORDs);
		EDFree (sp->INFO_RECORDs);
	}
}

void SETPRESENT_c_f_dsch_GHDM_NN_INFO_RECORDs (c_f_dsch_GHDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->NN_INFO_RECORDs_Present == present) return;
	sp->NN_INFO_RECORDs_Present = present;
	if (present) {
		/*-->*/sp->NN_INFO_RECORDs = (c_InfoRec_3_7_5*)EDAlloc (sizeof (c_InfoRec_3_7_5));
		INIT_c_InfoRec_3_7_5 (sp->NN_INFO_RECORDs);
	} else {
		FREE_c_InfoRec_3_7_5 (sp->NN_INFO_RECORDs);
		EDFree (sp->NN_INFO_RECORDs);
	}
}

/* Free function for struct c_f_dsch_RAM */
void FREE_c_f_dsch_RAM (c_f_dsch_RAM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_RAMM */
void FREE_c_f_dsch_RAMM (c_f_dsch_RAMM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_ERM_data_01 */
void FREE_c_f_dsch_ERM_data_01 (c_f_dsch_ERM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ERM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ERM_FSCHs */
void FREE_c_f_dsch_ERM_FSCHs (c_f_dsch_ERM_FSCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ERM_FSCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ERM_BCMC_PROGRAMSs */
void FREE_c_f_dsch_ERM_BCMC_PROGRAMSs (c_f_dsch_ERM_BCMC_PROGRAMSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ERM */
void FREE_c_f_dsch_ERM (c_f_dsch_ERM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_dsch_ERM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FSCHs.items; i0++) {
		FREE_c_f_dsch_ERM_FSCHs_data (sp->FSCHs.data[i0]);
		EDFree (sp->FSCHs.data[i0]);
	}
	if (sp->FSCHs.data) {EDFree (sp->FSCHs.data); sp->FSCHs.data = NULL; sp->FSCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->BCMC_PROGRAMSs.items; i0++) {
		FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data (sp->BCMC_PROGRAMSs.data[i0]);
		EDFree (sp->BCMC_PROGRAMSs.data[i0]);
	}
	if (sp->BCMC_PROGRAMSs.data) {EDFree (sp->BCMC_PROGRAMSs.data); sp->BCMC_PROGRAMSs.data = NULL; sp->BCMC_PROGRAMSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_ERMM */
void FREE_c_f_dsch_ERMM (c_f_dsch_ERMM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_UHDM_FOR_ASSIGNs */
void FREE_c_f_dsch_UHDM_FOR_ASSIGNs (c_f_dsch_UHDM_FOR_ASSIGNs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_FOR_ASSIGNs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_REV_ASSIGNs */
void FREE_c_f_dsch_UHDM_REV_ASSIGNs (c_f_dsch_UHDM_REV_ASSIGNs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_REV_ASSIGNs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_CALLS_ASSIGNs */
void FREE_c_f_dsch_UHDM_CALLS_ASSIGNs (c_f_dsch_UHDM_CALLS_ASSIGNs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_CALLS_ASSIGNs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_UHDM_FOR_ASSIGNs_01 */
void FREE_c_f_dsch_UHDM_FOR_ASSIGNs_01 (c_f_dsch_UHDM_FOR_ASSIGNs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_UHDM_FOR_ASSIGNs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_UHDM */
void FREE_c_f_dsch_UHDM (c_f_dsch_UHDM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->INFO_RECORD1_Present) {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD1);
		EDFree (sp->INFO_RECORD1);
		sp->INFO_RECORD1_Present = ED_FALSE;
	}
	if (sp->INFO_RECORD2_Present) {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD2);
		EDFree (sp->INFO_RECORD2);
		sp->INFO_RECORD2_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->FOR_ASSIGNs.items; i0++) {
		FREE_c_f_dsch_UHDM_FOR_ASSIGNs_data (sp->FOR_ASSIGNs.data[i0]);
		EDFree (sp->FOR_ASSIGNs.data[i0]);
	}
	if (sp->FOR_ASSIGNs.data) {EDFree (sp->FOR_ASSIGNs.data); sp->FOR_ASSIGNs.data = NULL; sp->FOR_ASSIGNs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_ASSIGNs.items; i0++) {
		FREE_c_f_dsch_UHDM_REV_ASSIGNs_data (sp->REV_ASSIGNs.data[i0]);
		EDFree (sp->REV_ASSIGNs.data[i0]);
	}
	if (sp->REV_ASSIGNs.data) {EDFree (sp->REV_ASSIGNs.data); sp->REV_ASSIGNs.data = NULL; sp->REV_ASSIGNs.allocatedItems=0; /*FR02A*/}
	FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS (&(sp->ACTIVE_SET_REC_FIELDS));
	if (sp->SYNC_ID.value != NULL) {
		EDFree (sp->SYNC_ID.value);
		sp->SYNC_ID.value = NULL;
	}
	sp->SYNC_ID.usedBits = 0;
	for (i0=0; i0<sp->CALLS_ASSIGNs.items; i0++) {
		FREE_c_f_dsch_UHDM_CALLS_ASSIGNs_data (sp->CALLS_ASSIGNs.data[i0]);
		EDFree (sp->CALLS_ASSIGNs.data[i0]);
	}
	if (sp->CALLS_ASSIGNs.data) {EDFree (sp->CALLS_ASSIGNs.data); sp->CALLS_ASSIGNs.data = NULL; sp->CALLS_ASSIGNs.allocatedItems=0; /*FR02A*/}
	if (sp->PLCM_39_Present) {
		EDFree (sp->PLCM_39);
		sp->PLCM_39_Present = ED_FALSE;
	}
	if (sp->ADD_PLCM_FOR_SCH_35_Present) {
		EDFree (sp->ADD_PLCM_FOR_SCH_35);
		sp->ADD_PLCM_FOR_SCH_35_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->FOR_ASSIGNs_01.items; i0++) {
		FREE_c_f_dsch_UHDM_FOR_ASSIGNs_01_data (sp->FOR_ASSIGNs_01.data[i0]);
		EDFree (sp->FOR_ASSIGNs_01.data[i0]);
	}
	if (sp->FOR_ASSIGNs_01.data) {EDFree (sp->FOR_ASSIGNs_01.data); sp->FOR_ASSIGNs_01.data = NULL; sp->FOR_ASSIGNs_01.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_UHDM_INFO_RECORD1 (c_f_dsch_UHDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->INFO_RECORD1_Present == present) return;
	sp->INFO_RECORD1_Present = present;
	if (present) {
		/*-->*/sp->INFO_RECORD1 = (c_InfoRec_3_7_5*)EDAlloc (sizeof (c_InfoRec_3_7_5));
		INIT_c_InfoRec_3_7_5 (sp->INFO_RECORD1);
	} else {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD1);
		EDFree (sp->INFO_RECORD1);
	}
}

void SETPRESENT_c_f_dsch_UHDM_INFO_RECORD2 (c_f_dsch_UHDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->INFO_RECORD2_Present == present) return;
	sp->INFO_RECORD2_Present = present;
	if (present) {
		/*-->*/sp->INFO_RECORD2 = (c_InfoRec_3_7_5*)EDAlloc (sizeof (c_InfoRec_3_7_5));
		INIT_c_InfoRec_3_7_5 (sp->INFO_RECORD2);
	} else {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD2);
		EDFree (sp->INFO_RECORD2);
	}
}

void SETPRESENT_c_f_dsch_UHDM_PLCM_39 (c_f_dsch_UHDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->PLCM_39_Present == present) return;
	sp->PLCM_39_Present = present;
	if (present) {
		/*-->*/sp->PLCM_39 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->PLCM_39, 5);
	} else {
		EDFree (sp->PLCM_39);
	}
}

void SETPRESENT_c_f_dsch_UHDM_ADD_PLCM_FOR_SCH_35 (c_f_dsch_UHDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->ADD_PLCM_FOR_SCH_35_Present == present) return;
	sp->ADD_PLCM_FOR_SCH_35_Present = present;
	if (present) {
		/*-->*/sp->ADD_PLCM_FOR_SCH_35 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->ADD_PLCM_FOR_SCH_35, 5);
	} else {
		EDFree (sp->ADD_PLCM_FOR_SCH_35);
	}
}

/* Free function for variable-sized sequence c_f_dsch_ESCAM_REV_CFG_RECSs */
void FREE_c_f_dsch_ESCAM_REV_CFG_RECSs (c_f_dsch_ESCAM_REV_CFG_RECSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_REV_CFG_RECSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ESCAM_REV_SCHs */
void FREE_c_f_dsch_ESCAM_REV_SCHs (c_f_dsch_ESCAM_REV_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_REV_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ESCAM_FOR_CFG_RECSs */
void FREE_c_f_dsch_ESCAM_FOR_CFG_RECSs (c_f_dsch_ESCAM_FOR_CFG_RECSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_FOR_CFG_RECSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ESCAM_FOR_SCHs */
void FREE_c_f_dsch_ESCAM_FOR_SCHs (c_f_dsch_ESCAM_FOR_SCHs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_FOR_SCHs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ESCAM_SUPs */
void FREE_c_f_dsch_ESCAM_SUPs (c_f_dsch_ESCAM_SUPs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_SUPs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ESCAM_data_01 */
void FREE_c_f_dsch_ESCAM_data_01 (c_f_dsch_ESCAM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ESCAM_P3X_CFGs */
void FREE_c_f_dsch_ESCAM_P3X_CFGs (c_f_dsch_ESCAM_P3X_CFGs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_P3X_CFGs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ESCAM_data_02 */
void FREE_c_f_dsch_ESCAM_data_02 (c_f_dsch_ESCAM_data_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_data_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ESCAM_FOR_SCHs_01 */
void FREE_c_f_dsch_ESCAM_FOR_SCHs_01 (c_f_dsch_ESCAM_FOR_SCHs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_FOR_SCHs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ESCAM_REV_SCHs_01 */
void FREE_c_f_dsch_ESCAM_REV_SCHs_01 (c_f_dsch_ESCAM_REV_SCHs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_REV_SCHs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_ESCAM_FOR_SCHs_02 */
void FREE_c_f_dsch_ESCAM_FOR_SCHs_02 (c_f_dsch_ESCAM_FOR_SCHs_02* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ESCAM_FOR_SCHs_02_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ESCAM */
void FREE_c_f_dsch_ESCAM (c_f_dsch_ESCAM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->REV_CFG_RECSs.items; i0++) {
		FREE_c_f_dsch_ESCAM_REV_CFG_RECSs_data (sp->REV_CFG_RECSs.data[i0]);
		EDFree (sp->REV_CFG_RECSs.data[i0]);
	}
	if (sp->REV_CFG_RECSs.data) {EDFree (sp->REV_CFG_RECSs.data); sp->REV_CFG_RECSs.data = NULL; sp->REV_CFG_RECSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs.items; i0++) {
		FREE_c_f_dsch_ESCAM_REV_SCHs_data (sp->REV_SCHs.data[i0]);
		EDFree (sp->REV_SCHs.data[i0]);
	}
	if (sp->REV_SCHs.data) {EDFree (sp->REV_SCHs.data); sp->REV_SCHs.data = NULL; sp->REV_SCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FOR_CFG_RECSs.items; i0++) {
		FREE_c_f_dsch_ESCAM_FOR_CFG_RECSs_data (sp->FOR_CFG_RECSs.data[i0]);
		EDFree (sp->FOR_CFG_RECSs.data[i0]);
	}
	if (sp->FOR_CFG_RECSs.data) {EDFree (sp->FOR_CFG_RECSs.data); sp->FOR_CFG_RECSs.data = NULL; sp->FOR_CFG_RECSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FOR_SCHs.items; i0++) {
		FREE_c_f_dsch_ESCAM_FOR_SCHs_data (sp->FOR_SCHs.data[i0]);
		EDFree (sp->FOR_SCHs.data[i0]);
	}
	if (sp->FOR_SCHs.data) {EDFree (sp->FOR_SCHs.data); sp->FOR_SCHs.data = NULL; sp->FOR_SCHs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->SUPs.items; i0++) {
		FREE_c_f_dsch_ESCAM_SUPs_data (sp->SUPs.data[i0]);
		EDFree (sp->SUPs.data[i0]);
	}
	if (sp->SUPs.data) {EDFree (sp->SUPs.data); sp->SUPs.data = NULL; sp->SUPs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_f_dsch_ESCAM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->P3X_CFGs.items; i0++) {
		FREE_c_f_dsch_ESCAM_P3X_CFGs_data (sp->P3X_CFGs.data[i0]);
		EDFree (sp->P3X_CFGs.data[i0]);
	}
	if (sp->P3X_CFGs.data) {EDFree (sp->P3X_CFGs.data); sp->P3X_CFGs.data = NULL; sp->P3X_CFGs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_02.items; i0++) {
		FREE_c_f_dsch_ESCAM_data_02_data (sp->data_02.data[i0]);
		EDFree (sp->data_02.data[i0]);
	}
	if (sp->data_02.data) {EDFree (sp->data_02.data); sp->data_02.data = NULL; sp->data_02.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FOR_SCHs_01.items; i0++) {
		FREE_c_f_dsch_ESCAM_FOR_SCHs_01_data (sp->FOR_SCHs_01.data[i0]);
		EDFree (sp->FOR_SCHs_01.data[i0]);
	}
	if (sp->FOR_SCHs_01.data) {EDFree (sp->FOR_SCHs_01.data); sp->FOR_SCHs_01.data = NULL; sp->FOR_SCHs_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_SCHs_01.items; i0++) {
		FREE_c_f_dsch_ESCAM_REV_SCHs_01_data (sp->REV_SCHs_01.data[i0]);
		EDFree (sp->REV_SCHs_01.data[i0]);
	}
	if (sp->REV_SCHs_01.data) {EDFree (sp->REV_SCHs_01.data); sp->REV_SCHs_01.data = NULL; sp->REV_SCHs_01.allocatedItems=0; /*FR02A*/}
	if (sp->ADD_PLCM_FOR_SCH_35_Present) {
		EDFree (sp->ADD_PLCM_FOR_SCH_35);
		sp->ADD_PLCM_FOR_SCH_35_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->FOR_SCHs_02.items; i0++) {
		FREE_c_f_dsch_ESCAM_FOR_SCHs_02_data (sp->FOR_SCHs_02.data[i0]);
		EDFree (sp->FOR_SCHs_02.data[i0]);
	}
	if (sp->FOR_SCHs_02.data) {EDFree (sp->FOR_SCHs_02.data); sp->FOR_SCHs_02.data = NULL; sp->FOR_SCHs_02.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_f_dsch_ESCAM_ADD_PLCM_FOR_SCH_35 (c_f_dsch_ESCAM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->ADD_PLCM_FOR_SCH_35_Present == present) return;
	sp->ADD_PLCM_FOR_SCH_35_Present = present;
	if (present) {
		/*-->*/sp->ADD_PLCM_FOR_SCH_35 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->ADD_PLCM_FOR_SCH_35, 5);
	} else {
		EDFree (sp->ADD_PLCM_FOR_SCH_35);
	}
}

/* Free function for struct c_f_dsch_FSCAMM */
void FREE_c_f_dsch_FSCAMM (c_f_dsch_FSCAMM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_RSCAMM */
void FREE_c_f_dsch_RSCAMM (c_f_dsch_RSCAMM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_MABOPM */
void FREE_c_f_dsch_MABOPM (c_f_dsch_MABOPM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->data_01_Present) {
		FREE_c_f_dsch_MABOPM_data_01 (sp->data_01);
		EDFree (sp->data_01);
		sp->data_01_Present = ED_FALSE;
	}
	if (sp->data_02_Present) {
		FREE_c_f_dsch_MABOPM_data_02 (sp->data_02);
		EDFree (sp->data_02);
		sp->data_02_Present = ED_FALSE;
	}
	if (sp->data_03_Present) {
		FREE_c_f_dsch_MABOPM_data_03 (sp->data_03);
		EDFree (sp->data_03);
		sp->data_03_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_MABOPM_data_01 (c_f_dsch_MABOPM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->data_01_Present == present) return;
	sp->data_01_Present = present;
	if (present) {
		/*-->*/sp->data_01 = (c_f_dsch_MABOPM_data_01*)EDAlloc (sizeof (c_f_dsch_MABOPM_data_01));
		INIT_c_f_dsch_MABOPM_data_01 (sp->data_01);
	} else {
		FREE_c_f_dsch_MABOPM_data_01 (sp->data_01);
		EDFree (sp->data_01);
	}
}

void SETPRESENT_c_f_dsch_MABOPM_data_02 (c_f_dsch_MABOPM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->data_02_Present == present) return;
	sp->data_02_Present = present;
	if (present) {
		/*-->*/sp->data_02 = (c_f_dsch_MABOPM_data_02*)EDAlloc (sizeof (c_f_dsch_MABOPM_data_02));
		INIT_c_f_dsch_MABOPM_data_02 (sp->data_02);
	} else {
		FREE_c_f_dsch_MABOPM_data_02 (sp->data_02);
		EDFree (sp->data_02);
	}
}

void SETPRESENT_c_f_dsch_MABOPM_data_03 (c_f_dsch_MABOPM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->data_03_Present == present) return;
	sp->data_03_Present = present;
	if (present) {
		/*-->*/sp->data_03 = (c_f_dsch_MABOPM_data_03*)EDAlloc (sizeof (c_f_dsch_MABOPM_data_03));
		INIT_c_f_dsch_MABOPM_data_03 (sp->data_03);
	} else {
		FREE_c_f_dsch_MABOPM_data_03 (sp->data_03);
		EDFree (sp->data_03);
	}
}

/* Free function for struct c_f_dsch_UZRM */
void FREE_c_f_dsch_UZRM (c_f_dsch_UZRM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_f_dsch_UZUM */
void FREE_c_f_dsch_UZUM (c_f_dsch_UZUM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_CLAM_ADD_CALL_RECORDSs */
void FREE_c_f_dsch_CLAM_ADD_CALL_RECORDSs (c_f_dsch_CLAM_ADD_CALL_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_CLAM_ADD_CALL_RECORDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_CLAM */
void FREE_c_f_dsch_CLAM (c_f_dsch_CLAM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->ADD_CALL_RECORDSs.items; i0++) {
		FREE_c_f_dsch_CLAM_ADD_CALL_RECORDSs_data (sp->ADD_CALL_RECORDSs.data[i0]);
		EDFree (sp->ADD_CALL_RECORDSs.data[i0]);
	}
	if (sp->ADD_CALL_RECORDSs.data) {EDFree (sp->ADD_CALL_RECORDSs.data); sp->ADD_CALL_RECORDSs.data = NULL; sp->ADD_CALL_RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_EAWIM_RECs */
void FREE_c_f_dsch_EAWIM_RECs (c_f_dsch_EAWIM_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_3_7_5_encrypted (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_EAWIM */
void FREE_c_f_dsch_EAWIM (c_f_dsch_EAWIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RECs.items; i0++) {
		FREE_c_InfoRec_3_7_5_encrypted (sp->RECs.data[i0]);
		EDFree (sp->RECs.data[i0]);
	}
	if (sp->RECs.data) {EDFree (sp->RECs.data); sp->RECs.data = NULL; sp->RECs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_EFWIM_RECs */
void FREE_c_f_dsch_EFWIM_RECs (c_f_dsch_EFWIM_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_3_7_5_encrypted (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_EFWIM */
void FREE_c_f_dsch_EFWIM (c_f_dsch_EFWIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RECs.items; i0++) {
		FREE_c_InfoRec_3_7_5_encrypted (sp->RECs.data[i0]);
		EDFree (sp->RECs.data[i0]);
	}
	if (sp->RECs.data) {EDFree (sp->RECs.data); sp->RECs.data = NULL; sp->RECs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_SMCM_RECSs */
void FREE_c_f_dsch_SMCM_RECSs (c_f_dsch_SMCM_RECSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_SMCM_RECSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_SMCM */
void FREE_c_f_dsch_SMCM (c_f_dsch_SMCM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RECSs.items; i0++) {
		FREE_c_f_dsch_SMCM_RECSs_data (sp->RECSs.data[i0]);
		EDFree (sp->RECSs.data[i0]);
	}
	if (sp->RECSs.data) {EDFree (sp->RECSs.data); sp->RECSs.data = NULL; sp->RECSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_BSSRSPM_RECORDSs */
void FREE_c_f_dsch_BSSRSPM_RECORDSs (c_f_dsch_BSSRSPM_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_BSSRSPM_RECORDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_BSSRSPM */
void FREE_c_f_dsch_BSSRSPM (c_f_dsch_BSSRSPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RECORDSs.items; i0++) {
		FREE_c_f_dsch_BSSRSPM_RECORDSs_data (sp->RECORDSs.data[i0]);
		EDFree (sp->RECORDSs.data[i0]);
	}
	if (sp->RECORDSs.data) {EDFree (sp->RECORDSs.data); sp->RECORDSs.data = NULL; sp->RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_f_dsch_AUREQM */
void FREE_c_f_dsch_AUREQM (c_f_dsch_AUREQM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_f_dsch_RATCHGM_RECSs */
void FREE_c_f_dsch_RATCHGM_RECSs (c_f_dsch_RATCHGM_RECSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_RATCHGM_RECSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_RATCHGM */
void FREE_c_f_dsch_RATCHGM (c_f_dsch_RATCHGM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RECSs.items; i0++) {
		FREE_c_f_dsch_RATCHGM_RECSs_data (sp->RECSs.data[i0]);
		EDFree (sp->RECSs.data[i0]);
	}
	if (sp->RECSs.data) {EDFree (sp->RECSs.data); sp->RECSs.data = NULL; sp->RECSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_ITBSPM_BCMC_PROGRAMSs */
void FREE_c_f_dsch_ITBSPM_BCMC_PROGRAMSs (c_f_dsch_ITBSPM_BCMC_PROGRAMSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ITBSPM */
void FREE_c_f_dsch_ITBSPM (c_f_dsch_ITBSPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->BCMC_PROGRAMSs.items; i0++) {
		FREE_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data (sp->BCMC_PROGRAMSs.data[i0]);
		EDFree (sp->BCMC_PROGRAMSs.data[i0]);
	}
	if (sp->BCMC_PROGRAMSs.data) {EDFree (sp->BCMC_PROGRAMSs.data); sp->BCMC_PROGRAMSs.data = NULL; sp->BCMC_PROGRAMSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_RCPM_RC_PARAMS_RECORDSs */
void FREE_c_f_dsch_RCPM_RC_PARAMS_RECORDSs (c_f_dsch_RCPM_RC_PARAMS_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_RCPM */
void FREE_c_f_dsch_RCPM (c_f_dsch_RCPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RC_PARAMS_RECORDSs.items; i0++) {
		FREE_c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data (sp->RC_PARAMS_RECORDSs.data[i0]);
		EDFree (sp->RC_PARAMS_RECORDSs.data[i0]);
	}
	if (sp->RC_PARAMS_RECORDSs.data) {EDFree (sp->RC_PARAMS_RECORDSs.data); sp->RC_PARAMS_RECORDSs.data = NULL; sp->RC_PARAMS_RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_HOSISM_PILOT_PNs */
void FREE_c_f_dsch_HOSISM_PILOT_PNs (c_f_dsch_HOSISM_PILOT_PNs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_HOSISM_PILOT_PNs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_HOSISM */
void FREE_c_f_dsch_HOSISM (c_f_dsch_HOSISM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOT_PNs.items; i0++) {
		FREE_c_f_dsch_HOSISM_PILOT_PNs_data (sp->PILOT_PNs.data[i0]);
		EDFree (sp->PILOT_PNs.data[i0]);
	}
	if (sp->PILOT_PNs.data) {EDFree (sp->PILOT_PNs.data); sp->PILOT_PNs.data = NULL; sp->PILOT_PNs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_GEM_GE_RECs */
void FREE_c_f_dsch_GEM_GE_RECs (c_f_dsch_GEM_GE_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_GEM_GE_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_GEM */
void FREE_c_f_dsch_GEM (c_f_dsch_GEM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->GE_RECs.items; i0++) {
		FREE_c_f_dsch_GEM_GE_RECs_data (sp->GE_RECs.data[i0]);
		EDFree (sp->GE_RECs.data[i0]);
	}
	if (sp->GE_RECs.data) {EDFree (sp->GE_RECs.data); sp->GE_RECs.data = NULL; sp->GE_RECs.allocatedItems=0; /*FR02A*/}
	if (sp->MESSAGE_REC.value != NULL) {
		EDFree (sp->MESSAGE_REC.value);
		sp->MESSAGE_REC.value = NULL;
	}
	sp->MESSAGE_REC.usedBits = 0;

}

/* Free function for variable-sized sequence c_f_dsch_ATIM_RADIO_INTERFACEs */
void FREE_c_f_dsch_ATIM_RADIO_INTERFACEs (c_f_dsch_ATIM_RADIO_INTERFACEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_ATIM */
void FREE_c_f_dsch_ATIM (c_f_dsch_ATIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RADIO_INTERFACEs.items; i0++) {
		FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data (sp->RADIO_INTERFACEs.data[i0]);
		EDFree (sp->RADIO_INTERFACEs.data[i0]);
	}
	if (sp->RADIO_INTERFACEs.data) {EDFree (sp->RADIO_INTERFACEs.data); sp->RADIO_INTERFACEs.data = NULL; sp->RADIO_INTERFACEs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_f_dsch_MUHDM_FOR_ASSIGNs */
void FREE_c_f_dsch_MUHDM_FOR_ASSIGNs (c_f_dsch_MUHDM_FOR_ASSIGNs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_FOR_ASSIGNs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_REV_ASSIGNs */
void FREE_c_f_dsch_MUHDM_REV_ASSIGNs (c_f_dsch_MUHDM_REV_ASSIGNs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_REV_ASSIGNs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_f_dsch_MUHDM_CALLS_ASSIGNs */
void FREE_c_f_dsch_MUHDM_CALLS_ASSIGNs (c_f_dsch_MUHDM_CALLS_ASSIGNs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_f_dsch_MUHDM_CALLS_ASSIGNs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_f_dsch_MUHDM */
void FREE_c_f_dsch_MUHDM (c_f_dsch_MUHDM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->INFO_RECORD1_Present) {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD1);
		EDFree (sp->INFO_RECORD1);
		sp->INFO_RECORD1_Present = ED_FALSE;
	}
	if (sp->INFO_RECORD2_Present) {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD2);
		EDFree (sp->INFO_RECORD2);
		sp->INFO_RECORD2_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->FOR_ASSIGNs.items; i0++) {
		FREE_c_f_dsch_MUHDM_FOR_ASSIGNs_data (sp->FOR_ASSIGNs.data[i0]);
		EDFree (sp->FOR_ASSIGNs.data[i0]);
	}
	if (sp->FOR_ASSIGNs.data) {EDFree (sp->FOR_ASSIGNs.data); sp->FOR_ASSIGNs.data = NULL; sp->FOR_ASSIGNs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->REV_ASSIGNs.items; i0++) {
		FREE_c_f_dsch_MUHDM_REV_ASSIGNs_data (sp->REV_ASSIGNs.data[i0]);
		EDFree (sp->REV_ASSIGNs.data[i0]);
	}
	if (sp->REV_ASSIGNs.data) {EDFree (sp->REV_ASSIGNs.data); sp->REV_ASSIGNs.data = NULL; sp->REV_ASSIGNs.allocatedItems=0; /*FR02A*/}
	FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS (&(sp->ACTIVE_SET_REC_FIELDS));
	if (sp->SYNC_ID.value != NULL) {
		EDFree (sp->SYNC_ID.value);
		sp->SYNC_ID.value = NULL;
	}
	sp->SYNC_ID.usedBits = 0;
	for (i0=0; i0<sp->CALLS_ASSIGNs.items; i0++) {
		FREE_c_f_dsch_MUHDM_CALLS_ASSIGNs_data (sp->CALLS_ASSIGNs.data[i0]);
		EDFree (sp->CALLS_ASSIGNs.data[i0]);
	}
	if (sp->CALLS_ASSIGNs.data) {EDFree (sp->CALLS_ASSIGNs.data); sp->CALLS_ASSIGNs.data = NULL; sp->CALLS_ASSIGNs.allocatedItems=0; /*FR02A*/}
	if (sp->PLCM_39_Present) {
		EDFree (sp->PLCM_39);
		sp->PLCM_39_Present = ED_FALSE;
	}

}

void SETPRESENT_c_f_dsch_MUHDM_INFO_RECORD1 (c_f_dsch_MUHDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->INFO_RECORD1_Present == present) return;
	sp->INFO_RECORD1_Present = present;
	if (present) {
		/*-->*/sp->INFO_RECORD1 = (c_InfoRec_3_7_5*)EDAlloc (sizeof (c_InfoRec_3_7_5));
		INIT_c_InfoRec_3_7_5 (sp->INFO_RECORD1);
	} else {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD1);
		EDFree (sp->INFO_RECORD1);
	}
}

void SETPRESENT_c_f_dsch_MUHDM_INFO_RECORD2 (c_f_dsch_MUHDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->INFO_RECORD2_Present == present) return;
	sp->INFO_RECORD2_Present = present;
	if (present) {
		/*-->*/sp->INFO_RECORD2 = (c_InfoRec_3_7_5*)EDAlloc (sizeof (c_InfoRec_3_7_5));
		INIT_c_InfoRec_3_7_5 (sp->INFO_RECORD2);
	} else {
		FREE_c_InfoRec_3_7_5 (sp->INFO_RECORD2);
		EDFree (sp->INFO_RECORD2);
	}
}

void SETPRESENT_c_f_dsch_MUHDM_PLCM_39 (c_f_dsch_MUHDM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->PLCM_39_Present == present) return;
	sp->PLCM_39_Present = present;
	if (present) {
		/*-->*/sp->PLCM_39 = (ED_BYTE*)EDAlloc ((size_t)5);
		ED_RESET_MEM (sp->PLCM_39, 5);
	} else {
		EDFree (sp->PLCM_39);
	}
}

/* Free function for variable-sized sequence c_r_csch_RGM_BCMC_PROGRAMSs */
void FREE_c_r_csch_RGM_BCMC_PROGRAMSs (c_r_csch_RGM_BCMC_PROGRAMSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_RGM_BCMC_PROGRAMSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_csch_RGM_BAND_SUBCLASSs */
void FREE_c_r_csch_RGM_BAND_SUBCLASSs (c_r_csch_RGM_BAND_SUBCLASSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_RGM_BAND_SUBCLASSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_RGM */
void FREE_c_r_csch_RGM (c_r_csch_RGM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->TIME_STAMP_SHORT_Present) {
		if (sp->TIME_STAMP_SHORT->value != NULL) {
			EDFree (sp->TIME_STAMP_SHORT->value);
			sp->TIME_STAMP_SHORT->value = NULL;
		}
		sp->TIME_STAMP_SHORT->usedBits = 0;
		EDFree (sp->TIME_STAMP_SHORT);
		sp->TIME_STAMP_SHORT_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->BCMC_PROGRAMSs.items; i0++) {
		FREE_c_r_csch_RGM_BCMC_PROGRAMSs_data (sp->BCMC_PROGRAMSs.data[i0]);
		EDFree (sp->BCMC_PROGRAMSs.data[i0]);
	}
	if (sp->BCMC_PROGRAMSs.data) {EDFree (sp->BCMC_PROGRAMSs.data); sp->BCMC_PROGRAMSs.data = NULL; sp->BCMC_PROGRAMSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->BAND_SUBCLASSs.items; i0++) {
		FREE_c_r_csch_RGM_BAND_SUBCLASSs_data (sp->BAND_SUBCLASSs.data[i0]);
		EDFree (sp->BAND_SUBCLASSs.data[i0]);
	}
	if (sp->BAND_SUBCLASSs.data) {EDFree (sp->BAND_SUBCLASSs.data); sp->BAND_SUBCLASSs.data = NULL; sp->BAND_SUBCLASSs.allocatedItems=0; /*FR02A*/}
	if (sp->ADD_GEO_LOC_TYPE.value != NULL) {
		EDFree (sp->ADD_GEO_LOC_TYPE.value);
		sp->ADD_GEO_LOC_TYPE.value = NULL;
	}
	sp->ADD_GEO_LOC_TYPE.usedBits = 0;

}

void SETPRESENT_c_r_csch_RGM_TIME_STAMP_SHORT (c_r_csch_RGM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->TIME_STAMP_SHORT_Present == present) return;
	sp->TIME_STAMP_SHORT_Present = present;
	if (present) {
		/*-->*/sp->TIME_STAMP_SHORT = (c_r_csch_RGM_TIME_STAMP_SHORT*)EDAlloc (sizeof (c_r_csch_RGM_TIME_STAMP_SHORT));
		sp->TIME_STAMP_SHORT->value = NULL;
		sp->TIME_STAMP_SHORT->usedBits = 0;
	} else {
		if (sp->TIME_STAMP_SHORT->value != NULL) {
			EDFree (sp->TIME_STAMP_SHORT->value);
			sp->TIME_STAMP_SHORT->value = NULL;
		}
		sp->TIME_STAMP_SHORT->usedBits = 0;
		EDFree (sp->TIME_STAMP_SHORT);
	}
}

/* Free function for struct c_r_csch_ORDM */
void FREE_c_r_csch_ORDM (c_r_csch_ORDM* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_R_Orders (&(sp->order));

}

/* Free function for variable-sized sequence c_r_csch_DBM_FIELDSs */
void FREE_c_r_csch_DBM_FIELDSs (c_r_csch_DBM_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_DBM_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_DBM */
void FREE_c_r_csch_DBM (c_r_csch_DBM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_r_csch_DBM_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_csch_ORM_FIELDSs */
void FREE_c_r_csch_ORM_FIELDSs (c_r_csch_ORM_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_ORM_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_csch_ORM_ALT_SOs */
void FREE_c_r_csch_ORM_ALT_SOs (c_r_csch_ORM_ALT_SOs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_ORM_ALT_SOs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_csch_ORM_ADD_SERV_INSTANCEs */
void FREE_c_r_csch_ORM_ADD_SERV_INSTANCEs (c_r_csch_ORM_ADD_SERV_INSTANCEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_ORM_ADD_SERV_INSTANCEs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_csch_ORM_BCMC_PROGRAMSs */
void FREE_c_r_csch_ORM_BCMC_PROGRAMSs (c_r_csch_ORM_BCMC_PROGRAMSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_ORM_BCMC_PROGRAMSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_csch_ORM_BAND_SUBCLASSs */
void FREE_c_r_csch_ORM_BAND_SUBCLASSs (c_r_csch_ORM_BAND_SUBCLASSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_ORM_BAND_SUBCLASSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_ORM */
void FREE_c_r_csch_ORM (c_r_csch_ORM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_r_csch_ORM_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->ALT_SOs.items; i0++) {
		FREE_c_r_csch_ORM_ALT_SOs_data (sp->ALT_SOs.data[i0]);
		EDFree (sp->ALT_SOs.data[i0]);
	}
	if (sp->ALT_SOs.data) {EDFree (sp->ALT_SOs.data); sp->ALT_SOs.data = NULL; sp->ALT_SOs.allocatedItems=0; /*FR02A*/}
	if (sp->FCH_Capability_Type_Specific_Fields_Present) {
		FREE_c_FCH_Type_Specific_Fields (sp->FCH_Capability_Type_Specific_Fields);
		EDFree (sp->FCH_Capability_Type_Specific_Fields);
		sp->FCH_Capability_Type_Specific_Fields_Present = ED_FALSE;
	}
	if (sp->DCCH_Capability_Type_Specific_Fields_Present) {
		FREE_c_DCCH_Type_Specific_Fields (sp->DCCH_Capability_Type_Specific_Fields);
		EDFree (sp->DCCH_Capability_Type_Specific_Fields);
		sp->DCCH_Capability_Type_Specific_Fields_Present = ED_FALSE;
	}
	if (sp->QOS_PARMS_Present) {
		if (sp->QOS_PARMS->value != NULL) {
			EDFree (sp->QOS_PARMS->value);
			sp->QOS_PARMS->value = NULL;
		}
		sp->QOS_PARMS->usedBits = 0;
		EDFree (sp->QOS_PARMS);
		sp->QOS_PARMS_Present = ED_FALSE;
	}
	if (sp->SYNC_ID.value != NULL) {
		EDFree (sp->SYNC_ID.value);
		sp->SYNC_ID.value = NULL;
	}
	sp->SYNC_ID.usedBits = 0;
	if (sp->SO_BITMAP.value != NULL) {
		EDFree (sp->SO_BITMAP.value);
		sp->SO_BITMAP.value = NULL;
	}
	sp->SO_BITMAP.usedBits = 0;
	if (sp->FOR_PDCH_Capability_Type_Specific_Fields_Present) {
		FREE_c_FOR_PDCH_Type_Specific_Fields (sp->FOR_PDCH_Capability_Type_Specific_Fields);
		EDFree (sp->FOR_PDCH_Capability_Type_Specific_Fields);
		sp->FOR_PDCH_Capability_Type_Specific_Fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->ADD_SERV_INSTANCEs.items; i0++) {
		FREE_c_r_csch_ORM_ADD_SERV_INSTANCEs_data (sp->ADD_SERV_INSTANCEs.data[i0]);
		EDFree (sp->ADD_SERV_INSTANCEs.data[i0]);
	}
	if (sp->ADD_SERV_INSTANCEs.data) {EDFree (sp->ADD_SERV_INSTANCEs.data); sp->ADD_SERV_INSTANCEs.data = NULL; sp->ADD_SERV_INSTANCEs.allocatedItems=0; /*FR02A*/}
	if (sp->FUNDICATED_BCMC_Capability_Type_Specific_Fields_Present) {
		FREE_c_FUNDICATED_BCMC_Type_Specific_Fields (sp->FUNDICATED_BCMC_Capability_Type_Specific_Fields);
		EDFree (sp->FUNDICATED_BCMC_Capability_Type_Specific_Fields);
		sp->FUNDICATED_BCMC_Capability_Type_Specific_Fields_Present = ED_FALSE;
	}
	if (sp->TIME_STAMP_SHORT_Present) {
		if (sp->TIME_STAMP_SHORT->value != NULL) {
			EDFree (sp->TIME_STAMP_SHORT->value);
			sp->TIME_STAMP_SHORT->value = NULL;
		}
		sp->TIME_STAMP_SHORT->usedBits = 0;
		EDFree (sp->TIME_STAMP_SHORT);
		sp->TIME_STAMP_SHORT_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->BCMC_PROGRAMSs.items; i0++) {
		FREE_c_r_csch_ORM_BCMC_PROGRAMSs_data (sp->BCMC_PROGRAMSs.data[i0]);
		EDFree (sp->BCMC_PROGRAMSs.data[i0]);
	}
	if (sp->BCMC_PROGRAMSs.data) {EDFree (sp->BCMC_PROGRAMSs.data); sp->BCMC_PROGRAMSs.data = NULL; sp->BCMC_PROGRAMSs.allocatedItems=0; /*FR02A*/}
	if (sp->REV_PDCH_Capability_Type_Specific_Fields_Present) {
		FREE_c_REV_PDCH_Type_Specific_Fields (sp->REV_PDCH_Capability_Type_Specific_Fields);
		EDFree (sp->REV_PDCH_Capability_Type_Specific_Fields);
		sp->REV_PDCH_Capability_Type_Specific_Fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->BAND_SUBCLASSs.items; i0++) {
		FREE_c_r_csch_ORM_BAND_SUBCLASSs_data (sp->BAND_SUBCLASSs.data[i0]);
		EDFree (sp->BAND_SUBCLASSs.data[i0]);
	}
	if (sp->BAND_SUBCLASSs.data) {EDFree (sp->BAND_SUBCLASSs.data); sp->BAND_SUBCLASSs.data = NULL; sp->BAND_SUBCLASSs.allocatedItems=0; /*FR02A*/}
	if (sp->ADD_GEO_LOC_TYPE.value != NULL) {
		EDFree (sp->ADD_GEO_LOC_TYPE.value);
		sp->ADD_GEO_LOC_TYPE.value = NULL;
	}
	sp->ADD_GEO_LOC_TYPE.usedBits = 0;

}

void SETPRESENT_c_r_csch_ORM_FCH_Capability_Type_Specific_Fields (c_r_csch_ORM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FCH_Capability_Type_Specific_Fields_Present == present) return;
	sp->FCH_Capability_Type_Specific_Fields_Present = present;
	if (present) {
		/*-->*/sp->FCH_Capability_Type_Specific_Fields = (c_FCH_Type_Specific_Fields*)EDAlloc (sizeof (c_FCH_Type_Specific_Fields));
		INIT_c_FCH_Type_Specific_Fields (sp->FCH_Capability_Type_Specific_Fields);
	} else {
		FREE_c_FCH_Type_Specific_Fields (sp->FCH_Capability_Type_Specific_Fields);
		EDFree (sp->FCH_Capability_Type_Specific_Fields);
	}
}

void SETPRESENT_c_r_csch_ORM_DCCH_Capability_Type_Specific_Fields (c_r_csch_ORM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->DCCH_Capability_Type_Specific_Fields_Present == present) return;
	sp->DCCH_Capability_Type_Specific_Fields_Present = present;
	if (present) {
		/*-->*/sp->DCCH_Capability_Type_Specific_Fields = (c_DCCH_Type_Specific_Fields*)EDAlloc (sizeof (c_DCCH_Type_Specific_Fields));
		INIT_c_DCCH_Type_Specific_Fields (sp->DCCH_Capability_Type_Specific_Fields);
	} else {
		FREE_c_DCCH_Type_Specific_Fields (sp->DCCH_Capability_Type_Specific_Fields);
		EDFree (sp->DCCH_Capability_Type_Specific_Fields);
	}
}

void SETPRESENT_c_r_csch_ORM_QOS_PARMS (c_r_csch_ORM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->QOS_PARMS_Present == present) return;
	sp->QOS_PARMS_Present = present;
	if (present) {
		/*-->*/sp->QOS_PARMS = (c_r_csch_ORM_QOS_PARMS*)EDAlloc (sizeof (c_r_csch_ORM_QOS_PARMS));
		sp->QOS_PARMS->value = NULL;
		sp->QOS_PARMS->usedBits = 0;
	} else {
		if (sp->QOS_PARMS->value != NULL) {
			EDFree (sp->QOS_PARMS->value);
			sp->QOS_PARMS->value = NULL;
		}
		sp->QOS_PARMS->usedBits = 0;
		EDFree (sp->QOS_PARMS);
	}
}

void SETPRESENT_c_r_csch_ORM_FOR_PDCH_Capability_Type_Specific_Fields (c_r_csch_ORM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FOR_PDCH_Capability_Type_Specific_Fields_Present == present) return;
	sp->FOR_PDCH_Capability_Type_Specific_Fields_Present = present;
	if (present) {
		/*-->*/sp->FOR_PDCH_Capability_Type_Specific_Fields = (c_FOR_PDCH_Type_Specific_Fields*)EDAlloc (sizeof (c_FOR_PDCH_Type_Specific_Fields));
		INIT_c_FOR_PDCH_Type_Specific_Fields (sp->FOR_PDCH_Capability_Type_Specific_Fields);
	} else {
		FREE_c_FOR_PDCH_Type_Specific_Fields (sp->FOR_PDCH_Capability_Type_Specific_Fields);
		EDFree (sp->FOR_PDCH_Capability_Type_Specific_Fields);
	}
}

void SETPRESENT_c_r_csch_ORM_FUNDICATED_BCMC_Capability_Type_Specific_Fields (c_r_csch_ORM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FUNDICATED_BCMC_Capability_Type_Specific_Fields_Present == present) return;
	sp->FUNDICATED_BCMC_Capability_Type_Specific_Fields_Present = present;
	if (present) {
		/*-->*/sp->FUNDICATED_BCMC_Capability_Type_Specific_Fields = (c_FUNDICATED_BCMC_Type_Specific_Fields*)EDAlloc (sizeof (c_FUNDICATED_BCMC_Type_Specific_Fields));
		INIT_c_FUNDICATED_BCMC_Type_Specific_Fields (sp->FUNDICATED_BCMC_Capability_Type_Specific_Fields);
	} else {
		FREE_c_FUNDICATED_BCMC_Type_Specific_Fields (sp->FUNDICATED_BCMC_Capability_Type_Specific_Fields);
		EDFree (sp->FUNDICATED_BCMC_Capability_Type_Specific_Fields);
	}
}

void SETPRESENT_c_r_csch_ORM_TIME_STAMP_SHORT (c_r_csch_ORM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->TIME_STAMP_SHORT_Present == present) return;
	sp->TIME_STAMP_SHORT_Present = present;
	if (present) {
		/*-->*/sp->TIME_STAMP_SHORT = (c_r_csch_ORM_TIME_STAMP_SHORT*)EDAlloc (sizeof (c_r_csch_ORM_TIME_STAMP_SHORT));
		sp->TIME_STAMP_SHORT->value = NULL;
		sp->TIME_STAMP_SHORT->usedBits = 0;
	} else {
		if (sp->TIME_STAMP_SHORT->value != NULL) {
			EDFree (sp->TIME_STAMP_SHORT->value);
			sp->TIME_STAMP_SHORT->value = NULL;
		}
		sp->TIME_STAMP_SHORT->usedBits = 0;
		EDFree (sp->TIME_STAMP_SHORT);
	}
}

void SETPRESENT_c_r_csch_ORM_REV_PDCH_Capability_Type_Specific_Fields (c_r_csch_ORM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->REV_PDCH_Capability_Type_Specific_Fields_Present == present) return;
	sp->REV_PDCH_Capability_Type_Specific_Fields_Present = present;
	if (present) {
		/*-->*/sp->REV_PDCH_Capability_Type_Specific_Fields = (c_REV_PDCH_Type_Specific_Fields*)EDAlloc (sizeof (c_REV_PDCH_Type_Specific_Fields));
		INIT_c_REV_PDCH_Type_Specific_Fields (sp->REV_PDCH_Capability_Type_Specific_Fields);
	} else {
		FREE_c_REV_PDCH_Type_Specific_Fields (sp->REV_PDCH_Capability_Type_Specific_Fields);
		EDFree (sp->REV_PDCH_Capability_Type_Specific_Fields);
	}
}

/* Free function for variable-sized sequence c_r_csch_PRM_ALT_SOs */
void FREE_c_r_csch_PRM_ALT_SOs (c_r_csch_PRM_ALT_SOs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_PRM_ALT_SOs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_csch_PRM_BCMC_PROGRAMSs */
void FREE_c_r_csch_PRM_BCMC_PROGRAMSs (c_r_csch_PRM_BCMC_PROGRAMSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_PRM_BCMC_PROGRAMSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_csch_PRM_BAND_SUBCLASSs */
void FREE_c_r_csch_PRM_BAND_SUBCLASSs (c_r_csch_PRM_BAND_SUBCLASSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_PRM_BAND_SUBCLASSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_PRM */
void FREE_c_r_csch_PRM (c_r_csch_PRM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->ALT_SOs.items; i0++) {
		FREE_c_r_csch_PRM_ALT_SOs_data (sp->ALT_SOs.data[i0]);
		EDFree (sp->ALT_SOs.data[i0]);
	}
	if (sp->ALT_SOs.data) {EDFree (sp->ALT_SOs.data); sp->ALT_SOs.data = NULL; sp->ALT_SOs.allocatedItems=0; /*FR02A*/}
	if (sp->FCH_Capability_Type_Specific_Fields_Present) {
		FREE_c_FCH_Type_Specific_Fields (sp->FCH_Capability_Type_Specific_Fields);
		EDFree (sp->FCH_Capability_Type_Specific_Fields);
		sp->FCH_Capability_Type_Specific_Fields_Present = ED_FALSE;
	}
	if (sp->DCCH_Capability_Type_Specific_Fields_Present) {
		FREE_c_DCCH_Type_Specific_Fields (sp->DCCH_Capability_Type_Specific_Fields);
		EDFree (sp->DCCH_Capability_Type_Specific_Fields);
		sp->DCCH_Capability_Type_Specific_Fields_Present = ED_FALSE;
	}
	if (sp->SYNC_ID_Present) {
		if (sp->SYNC_ID->value != NULL) {
			EDFree (sp->SYNC_ID->value);
			sp->SYNC_ID->value = NULL;
		}
		sp->SYNC_ID->usedBits = 0;
		EDFree (sp->SYNC_ID);
		sp->SYNC_ID_Present = ED_FALSE;
	}
	if (sp->SO_BITMAP.value != NULL) {
		EDFree (sp->SO_BITMAP.value);
		sp->SO_BITMAP.value = NULL;
	}
	sp->SO_BITMAP.usedBits = 0;
	if (sp->FOR_PDCH_Capability_Type_specific_fields_Present) {
		FREE_c_FOR_PDCH_Type_Specific_Fields (sp->FOR_PDCH_Capability_Type_specific_fields);
		EDFree (sp->FOR_PDCH_Capability_Type_specific_fields);
		sp->FOR_PDCH_Capability_Type_specific_fields_Present = ED_FALSE;
	}
	if (sp->FUNDICATED_BCMC_Capability_Type_specific_fields_Present) {
		FREE_c_FUNDICATED_BCMC_Type_Specific_Fields (sp->FUNDICATED_BCMC_Capability_Type_specific_fields);
		EDFree (sp->FUNDICATED_BCMC_Capability_Type_specific_fields);
		sp->FUNDICATED_BCMC_Capability_Type_specific_fields_Present = ED_FALSE;
	}
	if (sp->TIME_STAMP_SHORT_Present) {
		if (sp->TIME_STAMP_SHORT->value != NULL) {
			EDFree (sp->TIME_STAMP_SHORT->value);
			sp->TIME_STAMP_SHORT->value = NULL;
		}
		sp->TIME_STAMP_SHORT->usedBits = 0;
		EDFree (sp->TIME_STAMP_SHORT);
		sp->TIME_STAMP_SHORT_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->BCMC_PROGRAMSs.items; i0++) {
		FREE_c_r_csch_PRM_BCMC_PROGRAMSs_data (sp->BCMC_PROGRAMSs.data[i0]);
		EDFree (sp->BCMC_PROGRAMSs.data[i0]);
	}
	if (sp->BCMC_PROGRAMSs.data) {EDFree (sp->BCMC_PROGRAMSs.data); sp->BCMC_PROGRAMSs.data = NULL; sp->BCMC_PROGRAMSs.allocatedItems=0; /*FR02A*/}
	if (sp->REV_PDCH_Capability_Type_specific_fields_Present) {
		FREE_c_REV_PDCH_Type_Specific_Fields (sp->REV_PDCH_Capability_Type_specific_fields);
		EDFree (sp->REV_PDCH_Capability_Type_specific_fields);
		sp->REV_PDCH_Capability_Type_specific_fields_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->BAND_SUBCLASSs.items; i0++) {
		FREE_c_r_csch_PRM_BAND_SUBCLASSs_data (sp->BAND_SUBCLASSs.data[i0]);
		EDFree (sp->BAND_SUBCLASSs.data[i0]);
	}
	if (sp->BAND_SUBCLASSs.data) {EDFree (sp->BAND_SUBCLASSs.data); sp->BAND_SUBCLASSs.data = NULL; sp->BAND_SUBCLASSs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_r_csch_PRM_FCH_Capability_Type_Specific_Fields (c_r_csch_PRM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FCH_Capability_Type_Specific_Fields_Present == present) return;
	sp->FCH_Capability_Type_Specific_Fields_Present = present;
	if (present) {
		/*-->*/sp->FCH_Capability_Type_Specific_Fields = (c_FCH_Type_Specific_Fields*)EDAlloc (sizeof (c_FCH_Type_Specific_Fields));
		INIT_c_FCH_Type_Specific_Fields (sp->FCH_Capability_Type_Specific_Fields);
	} else {
		FREE_c_FCH_Type_Specific_Fields (sp->FCH_Capability_Type_Specific_Fields);
		EDFree (sp->FCH_Capability_Type_Specific_Fields);
	}
}

void SETPRESENT_c_r_csch_PRM_DCCH_Capability_Type_Specific_Fields (c_r_csch_PRM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->DCCH_Capability_Type_Specific_Fields_Present == present) return;
	sp->DCCH_Capability_Type_Specific_Fields_Present = present;
	if (present) {
		/*-->*/sp->DCCH_Capability_Type_Specific_Fields = (c_DCCH_Type_Specific_Fields*)EDAlloc (sizeof (c_DCCH_Type_Specific_Fields));
		INIT_c_DCCH_Type_Specific_Fields (sp->DCCH_Capability_Type_Specific_Fields);
	} else {
		FREE_c_DCCH_Type_Specific_Fields (sp->DCCH_Capability_Type_Specific_Fields);
		EDFree (sp->DCCH_Capability_Type_Specific_Fields);
	}
}

void SETPRESENT_c_r_csch_PRM_SYNC_ID (c_r_csch_PRM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->SYNC_ID_Present == present) return;
	sp->SYNC_ID_Present = present;
	if (present) {
		/*-->*/sp->SYNC_ID = (c_r_csch_PRM_SYNC_ID*)EDAlloc (sizeof (c_r_csch_PRM_SYNC_ID));
		sp->SYNC_ID->value = NULL;
		sp->SYNC_ID->usedBits = 0;
	} else {
		if (sp->SYNC_ID->value != NULL) {
			EDFree (sp->SYNC_ID->value);
			sp->SYNC_ID->value = NULL;
		}
		sp->SYNC_ID->usedBits = 0;
		EDFree (sp->SYNC_ID);
	}
}

void SETPRESENT_c_r_csch_PRM_FOR_PDCH_Capability_Type_specific_fields (c_r_csch_PRM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FOR_PDCH_Capability_Type_specific_fields_Present == present) return;
	sp->FOR_PDCH_Capability_Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->FOR_PDCH_Capability_Type_specific_fields = (c_FOR_PDCH_Type_Specific_Fields*)EDAlloc (sizeof (c_FOR_PDCH_Type_Specific_Fields));
		INIT_c_FOR_PDCH_Type_Specific_Fields (sp->FOR_PDCH_Capability_Type_specific_fields);
	} else {
		FREE_c_FOR_PDCH_Type_Specific_Fields (sp->FOR_PDCH_Capability_Type_specific_fields);
		EDFree (sp->FOR_PDCH_Capability_Type_specific_fields);
	}
}

void SETPRESENT_c_r_csch_PRM_FUNDICATED_BCMC_Capability_Type_specific_fields (c_r_csch_PRM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->FUNDICATED_BCMC_Capability_Type_specific_fields_Present == present) return;
	sp->FUNDICATED_BCMC_Capability_Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->FUNDICATED_BCMC_Capability_Type_specific_fields = (c_FUNDICATED_BCMC_Type_Specific_Fields*)EDAlloc (sizeof (c_FUNDICATED_BCMC_Type_Specific_Fields));
		INIT_c_FUNDICATED_BCMC_Type_Specific_Fields (sp->FUNDICATED_BCMC_Capability_Type_specific_fields);
	} else {
		FREE_c_FUNDICATED_BCMC_Type_Specific_Fields (sp->FUNDICATED_BCMC_Capability_Type_specific_fields);
		EDFree (sp->FUNDICATED_BCMC_Capability_Type_specific_fields);
	}
}

void SETPRESENT_c_r_csch_PRM_TIME_STAMP_SHORT (c_r_csch_PRM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->TIME_STAMP_SHORT_Present == present) return;
	sp->TIME_STAMP_SHORT_Present = present;
	if (present) {
		/*-->*/sp->TIME_STAMP_SHORT = (c_r_csch_PRM_TIME_STAMP_SHORT*)EDAlloc (sizeof (c_r_csch_PRM_TIME_STAMP_SHORT));
		sp->TIME_STAMP_SHORT->value = NULL;
		sp->TIME_STAMP_SHORT->usedBits = 0;
	} else {
		if (sp->TIME_STAMP_SHORT->value != NULL) {
			EDFree (sp->TIME_STAMP_SHORT->value);
			sp->TIME_STAMP_SHORT->value = NULL;
		}
		sp->TIME_STAMP_SHORT->usedBits = 0;
		EDFree (sp->TIME_STAMP_SHORT);
	}
}

void SETPRESENT_c_r_csch_PRM_REV_PDCH_Capability_Type_specific_fields (c_r_csch_PRM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->REV_PDCH_Capability_Type_specific_fields_Present == present) return;
	sp->REV_PDCH_Capability_Type_specific_fields_Present = present;
	if (present) {
		/*-->*/sp->REV_PDCH_Capability_Type_specific_fields = (c_REV_PDCH_Type_Specific_Fields*)EDAlloc (sizeof (c_REV_PDCH_Type_Specific_Fields));
		INIT_c_REV_PDCH_Type_Specific_Fields (sp->REV_PDCH_Capability_Type_specific_fields);
	} else {
		FREE_c_REV_PDCH_Type_Specific_Fields (sp->REV_PDCH_Capability_Type_specific_fields);
		EDFree (sp->REV_PDCH_Capability_Type_specific_fields);
	}
}

/* Free function for struct c_r_csch_AUCRM */
void FREE_c_r_csch_AUCRM (c_r_csch_AUCRM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_TACM */
void FREE_c_r_csch_TACM (c_r_csch_TACM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_PACNM */
void FREE_c_r_csch_PACNM (c_r_csch_PACNM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_r_csch_STRPM_INFO_RECORDSs */
void FREE_c_r_csch_STRPM_INFO_RECORDSs (c_r_csch_STRPM_INFO_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_2_7_4 (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_STRPM */
void FREE_c_r_csch_STRPM (c_r_csch_STRPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	FREE_c_STR_Type_specific_fields (&(sp->Type_specific_fields));
	for (i0=0; i0<sp->INFO_RECORDSs.items; i0++) {
		FREE_c_InfoRec_2_7_4 (sp->INFO_RECORDSs.data[i0]);
		EDFree (sp->INFO_RECORDSs.data[i0]);
	}
	if (sp->INFO_RECORDSs.data) {EDFree (sp->INFO_RECORDSs.data); sp->INFO_RECORDSs.data = NULL; sp->INFO_RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_csch_ESTRPM_INFO_RECORDSs */
void FREE_c_r_csch_ESTRPM_INFO_RECORDSs (c_r_csch_ESTRPM_INFO_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_2_7_4 (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_ESTRPM */
void FREE_c_r_csch_ESTRPM (c_r_csch_ESTRPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	FREE_c_STR_Type_specific_fields (&(sp->Type_specific_fields));
	for (i0=0; i0<sp->INFO_RECORDSs.items; i0++) {
		FREE_c_InfoRec_2_7_4 (sp->INFO_RECORDSs.data[i0]);
		EDFree (sp->INFO_RECORDSs.data[i0]);
	}
	if (sp->INFO_RECORDSs.data) {EDFree (sp->INFO_RECORDSs.data); sp->INFO_RECORDSs.data = NULL; sp->INFO_RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_csch_DIM_INFO_RECORDSs */
void FREE_c_r_csch_DIM_INFO_RECORDSs (c_r_csch_DIM_INFO_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_2_7_4 (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_DIM */
void FREE_c_r_csch_DIM (c_r_csch_DIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->INFO_RECORDSs.items; i0++) {
		FREE_c_InfoRec_2_7_4 (sp->INFO_RECORDSs.data[i0]);
		EDFree (sp->INFO_RECORDSs.data[i0]);
	}
	if (sp->INFO_RECORDSs.data) {EDFree (sp->INFO_RECORDSs.data); sp->INFO_RECORDSs.data = NULL; sp->INFO_RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_csch_SMRM */
void FREE_c_r_csch_SMRM (c_r_csch_SMRM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_AURSPM */
void FREE_c_r_csch_AURSPM (c_r_csch_AURSPM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_AURSYNM */
void FREE_c_r_csch_AURSYNM (c_r_csch_AURSYNM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_r_csch_RCNM_ADD_SERV_INSTANCEs */
void FREE_c_r_csch_RCNM_ADD_SERV_INSTANCEs (c_r_csch_RCNM_ADD_SERV_INSTANCEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_RCNM_ADD_SERV_INSTANCEs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_csch_RCNM_FIELDSs */
void FREE_c_r_csch_RCNM_FIELDSs (c_r_csch_RCNM_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_RCNM_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_RCNM */
void FREE_c_r_csch_RCNM (c_r_csch_RCNM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->SYNC_ID_Present) {
		if (sp->SYNC_ID->value != NULL) {
			EDFree (sp->SYNC_ID->value);
			sp->SYNC_ID->value = NULL;
		}
		sp->SYNC_ID->usedBits = 0;
		EDFree (sp->SYNC_ID);
		sp->SYNC_ID_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->ADD_SERV_INSTANCEs.items; i0++) {
		FREE_c_r_csch_RCNM_ADD_SERV_INSTANCEs_data (sp->ADD_SERV_INSTANCEs.data[i0]);
		EDFree (sp->ADD_SERV_INSTANCEs.data[i0]);
	}
	if (sp->ADD_SERV_INSTANCEs.data) {EDFree (sp->ADD_SERV_INSTANCEs.data); sp->ADD_SERV_INSTANCEs.data = NULL; sp->ADD_SERV_INSTANCEs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_r_csch_RCNM_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_r_csch_RCNM_SYNC_ID (c_r_csch_RCNM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->SYNC_ID_Present == present) return;
	sp->SYNC_ID_Present = present;
	if (present) {
		/*-->*/sp->SYNC_ID = (c_r_csch_RCNM_SYNC_ID*)EDAlloc (sizeof (c_r_csch_RCNM_SYNC_ID));
		sp->SYNC_ID->value = NULL;
		sp->SYNC_ID->usedBits = 0;
	} else {
		if (sp->SYNC_ID->value != NULL) {
			EDFree (sp->SYNC_ID->value);
			sp->SYNC_ID->value = NULL;
		}
		sp->SYNC_ID->usedBits = 0;
		EDFree (sp->SYNC_ID);
	}
}

/* Free function for struct c_r_csch_REM */
void FREE_c_r_csch_REM (c_r_csch_REM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_csch_CRRM */
void FREE_c_r_csch_CRRM (c_r_csch_CRRM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_r_csch_GEM_GE_RECs */
void FREE_c_r_csch_GEM_GE_RECs (c_r_csch_GEM_GE_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_csch_GEM_GE_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_csch_GEM */
void FREE_c_r_csch_GEM (c_r_csch_GEM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->GE_RECs.items; i0++) {
		FREE_c_r_csch_GEM_GE_RECs_data (sp->GE_RECs.data[i0]);
		EDFree (sp->GE_RECs.data[i0]);
	}
	if (sp->GE_RECs.data) {EDFree (sp->GE_RECs.data); sp->GE_RECs.data = NULL; sp->GE_RECs.allocatedItems=0; /*FR02A*/}
	if (sp->MESSAGE_REC.value != NULL) {
		EDFree (sp->MESSAGE_REC.value);
		sp->MESSAGE_REC.value = NULL;
	}
	sp->MESSAGE_REC.usedBits = 0;

}

/* Free function for struct c_r_dsch_ORDM */
void FREE_c_r_dsch_ORDM (c_r_dsch_ORDM* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_R_Orders (&(sp->order));

}

/* Free function for struct c_r_dsch_AUCRM */
void FREE_c_r_dsch_AUCRM (c_r_dsch_AUCRM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_r_dsch_FWIM_INFO_RECORDSs */
void FREE_c_r_dsch_FWIM_INFO_RECORDSs (c_r_dsch_FWIM_INFO_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_2_7_4_encrypted (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_FWIM */
void FREE_c_r_dsch_FWIM (c_r_dsch_FWIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->INFO_RECORDSs.items; i0++) {
		FREE_c_InfoRec_2_7_4_encrypted (sp->INFO_RECORDSs.data[i0]);
		EDFree (sp->INFO_RECORDSs.data[i0]);
	}
	if (sp->INFO_RECORDSs.data) {EDFree (sp->INFO_RECORDSs.data); sp->INFO_RECORDSs.data = NULL; sp->INFO_RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_DBM_MOBILE_COUNTRY_CODE */
void FREE_c_r_dsch_DBM_MOBILE_COUNTRY_CODE (c_r_dsch_DBM_MOBILE_COUNTRY_CODE* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_DBM_COUNTRY_BURST_TYPE */
void FREE_c_r_dsch_DBM_COUNTRY_BURST_TYPE (c_r_dsch_DBM_COUNTRY_BURST_TYPE* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_DBM_EXTENDED_BURST_TYPE */
void FREE_c_r_dsch_DBM_EXTENDED_BURST_TYPE (c_r_dsch_DBM_EXTENDED_BURST_TYPE* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_DBM_FIELDSs */
void FREE_c_r_dsch_DBM_FIELDSs (c_r_dsch_DBM_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_DBM_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_DBM */
void FREE_c_r_dsch_DBM (c_r_dsch_DBM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->MOBILE_COUNTRY_CODE.data) {EDFree (sp->MOBILE_COUNTRY_CODE.data); sp->MOBILE_COUNTRY_CODE.data = NULL; sp->MOBILE_COUNTRY_CODE.allocatedItems=0; /*FR02A*/}
	if (sp->COUNTRY_BURST_TYPE.data) {EDFree (sp->COUNTRY_BURST_TYPE.data); sp->COUNTRY_BURST_TYPE.data = NULL; sp->COUNTRY_BURST_TYPE.allocatedItems=0; /*FR02A*/}
	if (sp->EXTENDED_BURST_TYPE.data) {EDFree (sp->EXTENDED_BURST_TYPE.data); sp->EXTENDED_BURST_TYPE.data = NULL; sp->EXTENDED_BURST_TYPE.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_r_dsch_DBM_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_PSMM_data_01 */
void FREE_c_r_dsch_PSMM_data_01 (c_r_dsch_PSMM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_PSMM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_PSMM */
void FREE_c_r_dsch_PSMM (c_r_dsch_PSMM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_r_dsch_PSMM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_PMRM_PILOTSs */
void FREE_c_r_dsch_PMRM_PILOTSs (c_r_dsch_PMRM_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_PMRM_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_PMRM */
void FREE_c_r_dsch_PMRM (c_r_dsch_PMRM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_r_dsch_PMRM_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_BDTMFM_DIGITSs */
void FREE_c_r_dsch_BDTMFM_DIGITSs (c_r_dsch_BDTMFM_DIGITSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_BDTMFM_DIGITSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_BDTMFM */
void FREE_c_r_dsch_BDTMFM (c_r_dsch_BDTMFM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->DIGITSs.items; i0++) {
		FREE_c_r_dsch_BDTMFM_DIGITSs_data (sp->DIGITSs.data[i0]);
		EDFree (sp->DIGITSs.data[i0]);
	}
	if (sp->DIGITSs.data) {EDFree (sp->DIGITSs.data); sp->DIGITSs.data = NULL; sp->DIGITSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_STM */
void FREE_c_r_dsch_STM (c_r_dsch_STM* sp) {
ED_WARNING_REMOVER(sp);
	FREE_c_R_InformationRecords (&(sp->Type_specific_fields));

}

/* Free function for variable-sized sequence c_r_dsch_ORCM_FIELDSs */
void FREE_c_r_dsch_ORCM_FIELDSs (c_r_dsch_ORCM_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_ORCM_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_ORCM_INFO_RECORDSs */
void FREE_c_r_dsch_ORCM_INFO_RECORDSs (c_r_dsch_ORCM_INFO_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_2_7_4_encrypted (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_ORCM */
void FREE_c_r_dsch_ORCM (c_r_dsch_ORCM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_r_dsch_ORCM_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->INFO_RECORDSs.items; i0++) {
		FREE_c_InfoRec_2_7_4_encrypted (sp->INFO_RECORDSs.data[i0]);
		EDFree (sp->INFO_RECORDSs.data[i0]);
	}
	if (sp->INFO_RECORDSs.data) {EDFree (sp->INFO_RECORDSs.data); sp->INFO_RECORDSs.data = NULL; sp->INFO_RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_HOCM_data_01 */
void FREE_c_r_dsch_HOCM_data_01 (c_r_dsch_HOCM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_HOCM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_HOCM */
void FREE_c_r_dsch_HOCM (c_r_dsch_HOCM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_r_dsch_HOCM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_PRSM_data_01 */
void FREE_c_r_dsch_PRSM_data_01 (c_r_dsch_PRSM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_PRSM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_PRSM */
void FREE_c_r_dsch_PRSM (c_r_dsch_PRSM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_r_dsch_PRSM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_SRQM */
void FREE_c_r_dsch_SRQM (c_r_dsch_SRQM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->INFO_RECORDSs_Present) {
		FREE_c_InfoRec_2_7_4 (sp->INFO_RECORDSs);
		EDFree (sp->INFO_RECORDSs);
		sp->INFO_RECORDSs_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_SRQM_INFO_RECORDSs (c_r_dsch_SRQM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->INFO_RECORDSs_Present == present) return;
	sp->INFO_RECORDSs_Present = present;
	if (present) {
		/*-->*/sp->INFO_RECORDSs = (c_InfoRec_2_7_4*)EDAlloc (sizeof (c_InfoRec_2_7_4));
		INIT_c_InfoRec_2_7_4 (sp->INFO_RECORDSs);
	} else {
		FREE_c_InfoRec_2_7_4 (sp->INFO_RECORDSs);
		EDFree (sp->INFO_RECORDSs);
	}
}

/* Free function for struct c_r_dsch_SRPM */
void FREE_c_r_dsch_SRPM (c_r_dsch_SRPM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->INFO_RECORDSs_Present) {
		FREE_c_InfoRec_2_7_4 (sp->INFO_RECORDSs);
		EDFree (sp->INFO_RECORDSs);
		sp->INFO_RECORDSs_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_SRPM_INFO_RECORDSs (c_r_dsch_SRPM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->INFO_RECORDSs_Present == present) return;
	sp->INFO_RECORDSs_Present = present;
	if (present) {
		/*-->*/sp->INFO_RECORDSs = (c_InfoRec_2_7_4*)EDAlloc (sizeof (c_InfoRec_2_7_4));
		INIT_c_InfoRec_2_7_4 (sp->INFO_RECORDSs);
	} else {
		FREE_c_InfoRec_2_7_4 (sp->INFO_RECORDSs);
		EDFree (sp->INFO_RECORDSs);
	}
}

/* Free function for struct c_r_dsch_SCCM */
void FREE_c_r_dsch_SCCM (c_r_dsch_SCCM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_SOCM */
void FREE_c_r_dsch_SOCM (c_r_dsch_SOCM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Type_specific_fields.value != NULL) {
		EDFree (sp->Type_specific_fields.value);
		sp->Type_specific_fields.value = NULL;
	}
	sp->Type_specific_fields.usedBits = 0;

}

/* Free function for variable-sized sequence c_r_dsch_STRPM_INFO_RECORDSs */
void FREE_c_r_dsch_STRPM_INFO_RECORDSs (c_r_dsch_STRPM_INFO_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_2_7_4 (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_STRPM */
void FREE_c_r_dsch_STRPM (c_r_dsch_STRPM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	FREE_c_STR_Type_specific_fields (&(sp->Type_specific_fields));
	for (i0=0; i0<sp->INFO_RECORDSs.items; i0++) {
		FREE_c_InfoRec_2_7_4 (sp->INFO_RECORDSs.data[i0]);
		EDFree (sp->INFO_RECORDSs.data[i0]);
	}
	if (sp->INFO_RECORDSs.data) {EDFree (sp->INFO_RECORDSs.data); sp->INFO_RECORDSs.data = NULL; sp->INFO_RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_TACM */
void FREE_c_r_dsch_TACM (c_r_dsch_TACM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_r_dsch_SCRM_ACT_PNs */
void FREE_c_r_dsch_SCRM_ACT_PNs (c_r_dsch_SCRM_ACT_PNs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_SCRM_ACT_PNs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_SCRM_NGHBR_PNs */
void FREE_c_r_dsch_SCRM_NGHBR_PNs (c_r_dsch_SCRM_NGHBR_PNs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_SCRM_NGHBR_PNs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_SCRM_ACT_PNs_01 */
void FREE_c_r_dsch_SCRM_ACT_PNs_01 (c_r_dsch_SCRM_ACT_PNs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_SCRM_ACT_PNs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_SCRM_NGHBR_PNs_01 */
void FREE_c_r_dsch_SCRM_NGHBR_PNs_01 (c_r_dsch_SCRM_NGHBR_PNs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_SCRM_ACT_PNs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_SCRM */
void FREE_c_r_dsch_SCRM (c_r_dsch_SCRM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->REQ_BLOB.value != NULL) {
		EDFree (sp->REQ_BLOB.value);
		sp->REQ_BLOB.value = NULL;
	}
	sp->REQ_BLOB.usedBits = 0;
	for (i0=0; i0<sp->ACT_PNs.items; i0++) {
		FREE_c_r_dsch_SCRM_ACT_PNs_data (sp->ACT_PNs.data[i0]);
		EDFree (sp->ACT_PNs.data[i0]);
	}
	if (sp->ACT_PNs.data) {EDFree (sp->ACT_PNs.data); sp->ACT_PNs.data = NULL; sp->ACT_PNs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->NGHBR_PNs.items; i0++) {
		FREE_c_r_dsch_SCRM_NGHBR_PNs_data (sp->NGHBR_PNs.data[i0]);
		EDFree (sp->NGHBR_PNs.data[i0]);
	}
	if (sp->NGHBR_PNs.data) {EDFree (sp->NGHBR_PNs.data); sp->NGHBR_PNs.data = NULL; sp->NGHBR_PNs.allocatedItems=0; /*FR02A*/}
	if (sp->records_Present) {
		FREE_c_r_dsch_SCRM_records (sp->records);
		EDFree (sp->records);
		sp->records_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->ACT_PNs_01.items; i0++) {
		FREE_c_r_dsch_SCRM_ACT_PNs_01_data (sp->ACT_PNs_01.data[i0]);
		EDFree (sp->ACT_PNs_01.data[i0]);
	}
	if (sp->ACT_PNs_01.data) {EDFree (sp->ACT_PNs_01.data); sp->ACT_PNs_01.data = NULL; sp->ACT_PNs_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->NGHBR_PNs_01.items; i0++) {
		FREE_c_r_dsch_SCRM_ACT_PNs_01_data (sp->NGHBR_PNs_01.data[i0]);
		EDFree (sp->NGHBR_PNs_01.data[i0]);
	}
	if (sp->NGHBR_PNs_01.data) {EDFree (sp->NGHBR_PNs_01.data); sp->NGHBR_PNs_01.data = NULL; sp->NGHBR_PNs_01.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_r_dsch_SCRM_records (c_r_dsch_SCRM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->records_Present == present) return;
	sp->records_Present = present;
	if (present) {
		/*-->*/sp->records = (c_r_dsch_SCRM_records*)EDAlloc (sizeof (c_r_dsch_SCRM_records));
		INIT_c_r_dsch_SCRM_records (sp->records);
	} else {
		FREE_c_r_dsch_SCRM_records (sp->records);
		EDFree (sp->records);
	}
}

/* Free function for struct c_r_dsch_CFSRSM */
void FREE_c_r_dsch_CFSRSM (c_r_dsch_CFSRSM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_CFSRPM */
void FREE_c_r_dsch_CFSRPM (c_r_dsch_CFSRPM* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->Mode_specific_fields_0000_Present) {
		FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000 (sp->Mode_specific_fields_0000);
		EDFree (sp->Mode_specific_fields_0000);
		sp->Mode_specific_fields_0000_Present = ED_FALSE;
	}
	if (sp->Mode_specific_fields_0001_Present) {
		FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0001 (sp->Mode_specific_fields_0001);
		EDFree (sp->Mode_specific_fields_0001);
		sp->Mode_specific_fields_0001_Present = ED_FALSE;
	}

}

void SETPRESENT_c_r_dsch_CFSRPM_Mode_specific_fields_0000 (c_r_dsch_CFSRPM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Mode_specific_fields_0000_Present == present) return;
	sp->Mode_specific_fields_0000_Present = present;
	if (present) {
		/*-->*/sp->Mode_specific_fields_0000 = (c_r_dsch_CFSRPM_Mode_specific_fields_0000*)EDAlloc (sizeof (c_r_dsch_CFSRPM_Mode_specific_fields_0000));
		INIT_c_r_dsch_CFSRPM_Mode_specific_fields_0000 (sp->Mode_specific_fields_0000);
	} else {
		FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000 (sp->Mode_specific_fields_0000);
		EDFree (sp->Mode_specific_fields_0000);
	}
}

void SETPRESENT_c_r_dsch_CFSRPM_Mode_specific_fields_0001 (c_r_dsch_CFSRPM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->Mode_specific_fields_0001_Present == present) return;
	sp->Mode_specific_fields_0001_Present = present;
	if (present) {
		/*-->*/sp->Mode_specific_fields_0001 = (c_r_dsch_CFSRPM_Mode_specific_fields_0001*)EDAlloc (sizeof (c_r_dsch_CFSRPM_Mode_specific_fields_0001));
		INIT_c_r_dsch_CFSRPM_Mode_specific_fields_0001 (sp->Mode_specific_fields_0001);
	} else {
		FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0001 (sp->Mode_specific_fields_0001);
		EDFree (sp->Mode_specific_fields_0001);
	}
}

/* Free function for variable-sized sequence c_r_dsch_PPSMM_PILOTs */
void FREE_c_r_dsch_PPSMM_PILOTs (c_r_dsch_PPSMM_PILOTs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_PPSMM_PILOTs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_PPSMM_PILOTs_01 */
void FREE_c_r_dsch_PPSMM_PILOTs_01 (c_r_dsch_PPSMM_PILOTs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_PPSMM_PILOTs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_PPSMM_data_01 */
void FREE_c_r_dsch_PPSMM_data_01 (c_r_dsch_PPSMM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_PPSMM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_PPSMM */
void FREE_c_r_dsch_PPSMM (c_r_dsch_PPSMM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTs.items; i0++) {
		FREE_c_r_dsch_PPSMM_PILOTs_data (sp->PILOTs.data[i0]);
		EDFree (sp->PILOTs.data[i0]);
	}
	if (sp->PILOTs.data) {EDFree (sp->PILOTs.data); sp->PILOTs.data = NULL; sp->PILOTs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTs_01.items; i0++) {
		FREE_c_r_dsch_PPSMM_PILOTs_01_data (sp->PILOTs_01.data[i0]);
		EDFree (sp->PILOTs_01.data[i0]);
	}
	if (sp->PILOTs_01.data) {EDFree (sp->PILOTs_01.data); sp->PILOTs_01.data = NULL; sp->PILOTs_01.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_r_dsch_PPSMM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_OLRM_data_01 */
void FREE_c_r_dsch_OLRM_data_01 (c_r_dsch_OLRM_data_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_OLRM_data_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_OLRM */
void FREE_c_r_dsch_OLRM (c_r_dsch_OLRM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->data_01.items; i0++) {
		FREE_c_r_dsch_OLRM_data_01_data (sp->data_01.data[i0]);
		EDFree (sp->data_01.data[i0]);
	}
	if (sp->data_01.data) {EDFree (sp->data_01.data); sp->data_01.data = NULL; sp->data_01.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_RRM */
void FREE_c_r_dsch_RRM (c_r_dsch_RRM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_RRMM */
void FREE_c_r_dsch_RRMM (c_r_dsch_RRMM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_ERRM */
void FREE_c_r_dsch_ERRM (c_r_dsch_ERRM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_ERRMM */
void FREE_c_r_dsch_ERRMM (c_r_dsch_ERRMM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_PSMMM */
void FREE_c_r_dsch_PSMMM (c_r_dsch_PSMMM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_SCRMM */
void FREE_c_r_dsch_SCRMM (c_r_dsch_SCRMM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_r_dsch_RRRM_ADD_CON_REFs */
void FREE_c_r_dsch_RRRM_ADD_CON_REFs (c_r_dsch_RRRM_ADD_CON_REFs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_RRRM_ADD_CON_REFs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_RRRM_BCMC_PROGRAMSs */
void FREE_c_r_dsch_RRRM_BCMC_PROGRAMSs (c_r_dsch_RRRM_BCMC_PROGRAMSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_RRRM_BCMC_PROGRAMSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_RRRM */
void FREE_c_r_dsch_RRRM (c_r_dsch_RRRM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->ADD_CON_REFs.items; i0++) {
		FREE_c_r_dsch_RRRM_ADD_CON_REFs_data (sp->ADD_CON_REFs.data[i0]);
		EDFree (sp->ADD_CON_REFs.data[i0]);
	}
	if (sp->ADD_CON_REFs.data) {EDFree (sp->ADD_CON_REFs.data); sp->ADD_CON_REFs.data = NULL; sp->ADD_CON_REFs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->BCMC_PROGRAMSs.items; i0++) {
		FREE_c_r_dsch_RRRM_BCMC_PROGRAMSs_data (sp->BCMC_PROGRAMSs.data[i0]);
		EDFree (sp->BCMC_PROGRAMSs.data[i0]);
	}
	if (sp->BCMC_PROGRAMSs.data) {EDFree (sp->BCMC_PROGRAMSs.data); sp->BCMC_PROGRAMSs.data = NULL; sp->BCMC_PROGRAMSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_RRRMM */
void FREE_c_r_dsch_RRRMM (c_r_dsch_RRRMM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_UZURM */
void FREE_c_r_dsch_UZURM (c_r_dsch_UZURM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_r_dsch_EOM_ALT_SOs */
void FREE_c_r_dsch_EOM_ALT_SOs (c_r_dsch_EOM_ALT_SOs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_EOM_ALT_SOs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_EOM_FIELDSs */
void FREE_c_r_dsch_EOM_FIELDSs (c_r_dsch_EOM_FIELDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_EOM_FIELDSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_EOM_RECSs */
void FREE_c_r_dsch_EOM_RECSs (c_r_dsch_EOM_RECSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_2_7_4_encrypted (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_EOM_ADD_SERV_INSTANCEs */
void FREE_c_r_dsch_EOM_ADD_SERV_INSTANCEs (c_r_dsch_EOM_ADD_SERV_INSTANCEs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_EOM_ADD_SERV_INSTANCEs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_EOM_BCMC_PROGRAMSs */
void FREE_c_r_dsch_EOM_BCMC_PROGRAMSs (c_r_dsch_EOM_BCMC_PROGRAMSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_EOM_BCMC_PROGRAMSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_EOM */
void FREE_c_r_dsch_EOM (c_r_dsch_EOM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->ALT_SOs.items; i0++) {
		FREE_c_r_dsch_EOM_ALT_SOs_data (sp->ALT_SOs.data[i0]);
		EDFree (sp->ALT_SOs.data[i0]);
	}
	if (sp->ALT_SOs.data) {EDFree (sp->ALT_SOs.data); sp->ALT_SOs.data = NULL; sp->ALT_SOs.allocatedItems=0; /*FR02A*/}
	if (sp->SO_BITMAP.value != NULL) {
		EDFree (sp->SO_BITMAP.value);
		sp->SO_BITMAP.value = NULL;
	}
	sp->SO_BITMAP.usedBits = 0;
	for (i0=0; i0<sp->FIELDSs.items; i0++) {
		FREE_c_r_dsch_EOM_FIELDSs_data (sp->FIELDSs.data[i0]);
		EDFree (sp->FIELDSs.data[i0]);
	}
	if (sp->FIELDSs.data) {EDFree (sp->FIELDSs.data); sp->FIELDSs.data = NULL; sp->FIELDSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->RECSs.items; i0++) {
		FREE_c_InfoRec_2_7_4_encrypted (sp->RECSs.data[i0]);
		EDFree (sp->RECSs.data[i0]);
	}
	if (sp->RECSs.data) {EDFree (sp->RECSs.data); sp->RECSs.data = NULL; sp->RECSs.allocatedItems=0; /*FR02A*/}
	if (sp->SYNC_ID.value != NULL) {
		EDFree (sp->SYNC_ID.value);
		sp->SYNC_ID.value = NULL;
	}
	sp->SYNC_ID.usedBits = 0;
	for (i0=0; i0<sp->ADD_SERV_INSTANCEs.items; i0++) {
		FREE_c_r_dsch_EOM_ADD_SERV_INSTANCEs_data (sp->ADD_SERV_INSTANCEs.data[i0]);
		EDFree (sp->ADD_SERV_INSTANCEs.data[i0]);
	}
	if (sp->ADD_SERV_INSTANCEs.data) {EDFree (sp->ADD_SERV_INSTANCEs.data); sp->ADD_SERV_INSTANCEs.data = NULL; sp->ADD_SERV_INSTANCEs.allocatedItems=0; /*FR02A*/}
	if (sp->TIME_STAMP_SHORT_Present) {
		if (sp->TIME_STAMP_SHORT->value != NULL) {
			EDFree (sp->TIME_STAMP_SHORT->value);
			sp->TIME_STAMP_SHORT->value = NULL;
		}
		sp->TIME_STAMP_SHORT->usedBits = 0;
		EDFree (sp->TIME_STAMP_SHORT);
		sp->TIME_STAMP_SHORT_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->BCMC_PROGRAMSs.items; i0++) {
		FREE_c_r_dsch_EOM_BCMC_PROGRAMSs_data (sp->BCMC_PROGRAMSs.data[i0]);
		EDFree (sp->BCMC_PROGRAMSs.data[i0]);
	}
	if (sp->BCMC_PROGRAMSs.data) {EDFree (sp->BCMC_PROGRAMSs.data); sp->BCMC_PROGRAMSs.data = NULL; sp->BCMC_PROGRAMSs.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_r_dsch_EOM_TIME_STAMP_SHORT (c_r_dsch_EOM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->TIME_STAMP_SHORT_Present == present) return;
	sp->TIME_STAMP_SHORT_Present = present;
	if (present) {
		/*-->*/sp->TIME_STAMP_SHORT = (c_r_dsch_EOM_TIME_STAMP_SHORT*)EDAlloc (sizeof (c_r_dsch_EOM_TIME_STAMP_SHORT));
		sp->TIME_STAMP_SHORT->value = NULL;
		sp->TIME_STAMP_SHORT->usedBits = 0;
	} else {
		if (sp->TIME_STAMP_SHORT->value != NULL) {
			EDFree (sp->TIME_STAMP_SHORT->value);
			sp->TIME_STAMP_SHORT->value = NULL;
		}
		sp->TIME_STAMP_SHORT->usedBits = 0;
		EDFree (sp->TIME_STAMP_SHORT);
	}
}

/* Free function for variable-sized sequence c_r_dsch_EFWIM_RECs */
void FREE_c_r_dsch_EFWIM_RECs (c_r_dsch_EFWIM_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_2_7_4_encrypted (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_EFWIM */
void FREE_c_r_dsch_EFWIM (c_r_dsch_EFWIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RECs.items; i0++) {
		FREE_c_InfoRec_2_7_4_encrypted (sp->RECs.data[i0]);
		EDFree (sp->RECs.data[i0]);
	}
	if (sp->RECs.data) {EDFree (sp->RECs.data); sp->RECs.data = NULL; sp->RECs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_EPSMM_PILOTSs */
void FREE_c_r_dsch_EPSMM_PILOTSs (c_r_dsch_EPSMM_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_EPSMM_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_r_dsch_EPSMM_PILOTSs_01 */
void FREE_c_r_dsch_EPSMM_PILOTSs_01 (c_r_dsch_EPSMM_PILOTSs_01* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_EPSMM_PILOTSs_01_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_EPSMM */
void FREE_c_r_dsch_EPSMM (c_r_dsch_EPSMM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	if (sp->records_Present) {
		FREE_c_r_dsch_EPSMM_records (sp->records);
		EDFree (sp->records);
		sp->records_Present = ED_FALSE;
	}
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_r_dsch_EPSMM_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}
	for (i0=0; i0<sp->PILOTSs_01.items; i0++) {
		FREE_c_r_dsch_EPSMM_PILOTSs_01_data (sp->PILOTSs_01.data[i0]);
		EDFree (sp->PILOTSs_01.data[i0]);
	}
	if (sp->PILOTSs_01.data) {EDFree (sp->PILOTSs_01.data); sp->PILOTSs_01.data = NULL; sp->PILOTSs_01.allocatedItems=0; /*FR02A*/}

}

void SETPRESENT_c_r_dsch_EPSMM_records (c_r_dsch_EPSMM* sp, ED_BOOLEAN present)
{
	/* Do nothing if already compliant to the request */
	if (sp->records_Present == present) return;
	sp->records_Present = present;
	if (present) {
		/*-->*/sp->records = (c_r_dsch_EPSMM_records*)EDAlloc (sizeof (c_r_dsch_EPSMM_records));
		INIT_c_r_dsch_EPSMM_records (sp->records);
	} else {
		FREE_c_r_dsch_EPSMM_records (sp->records);
		EDFree (sp->records);
	}
}

/* Free function for variable-sized sequence c_r_dsch_EHOCM_PILOTSs */
void FREE_c_r_dsch_EHOCM_PILOTSs (c_r_dsch_EHOCM_PILOTSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_EHOCM_PILOTSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_EHOCM */
void FREE_c_r_dsch_EHOCM (c_r_dsch_EHOCM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->PILOTSs.items; i0++) {
		FREE_c_r_dsch_EHOCM_PILOTSs_data (sp->PILOTSs.data[i0]);
		EDFree (sp->PILOTSs.data[i0]);
	}
	if (sp->PILOTSs.data) {EDFree (sp->PILOTSs.data); sp->PILOTSs.data = NULL; sp->PILOTSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_SMRM_RECSs */
void FREE_c_r_dsch_SMRM_RECSs (c_r_dsch_SMRM_RECSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_SMRM_RECSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_SMRM */
void FREE_c_r_dsch_SMRM (c_r_dsch_SMRM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RECSs.items; i0++) {
		FREE_c_r_dsch_SMRM_RECSs_data (sp->RECSs.data[i0]);
		EDFree (sp->RECSs.data[i0]);
	}
	if (sp->RECSs.data) {EDFree (sp->RECSs.data); sp->RECSs.data = NULL; sp->RECSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_CLCM_ADD_TAGs */
void FREE_c_r_dsch_CLCM_ADD_TAGs (c_r_dsch_CLCM_ADD_TAGs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_CLCM_ADD_TAGs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_CLCM */
void FREE_c_r_dsch_CLCM (c_r_dsch_CLCM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->ADD_TAGs.items; i0++) {
		FREE_c_r_dsch_CLCM_ADD_TAGs_data (sp->ADD_TAGs.data[i0]);
		EDFree (sp->ADD_TAGs.data[i0]);
	}
	if (sp->ADD_TAGs.data) {EDFree (sp->ADD_TAGs.data); sp->ADD_TAGs.data = NULL; sp->ADD_TAGs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_DIM_INFO_RECORDSs */
void FREE_c_r_dsch_DIM_INFO_RECORDSs (c_r_dsch_DIM_INFO_RECORDSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_InfoRec_2_7_4 (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_DIM */
void FREE_c_r_dsch_DIM (c_r_dsch_DIM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->INFO_RECORDSs.items; i0++) {
		FREE_c_InfoRec_2_7_4 (sp->INFO_RECORDSs.data[i0]);
		EDFree (sp->INFO_RECORDSs.data[i0]);
	}
	if (sp->INFO_RECORDSs.data) {EDFree (sp->INFO_RECORDSs.data); sp->INFO_RECORDSs.data = NULL; sp->INFO_RECORDSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_BSSREQM_RECORDs */
void FREE_c_r_dsch_BSSREQM_RECORDs (c_r_dsch_BSSREQM_RECORDs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_BSSREQM_RECORDs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_BSSREQM */
void FREE_c_r_dsch_BSSREQM (c_r_dsch_BSSREQM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->RECORDs.items; i0++) {
		FREE_c_r_dsch_BSSREQM_RECORDs_data (sp->RECORDs.data[i0]);
		EDFree (sp->RECORDs.data[i0]);
	}
	if (sp->RECORDs.data) {EDFree (sp->RECORDs.data); sp->RECORDs.data = NULL; sp->RECORDs.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_r_dsch_COTRM */
void FREE_c_r_dsch_COTRM (c_r_dsch_COTRM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_AURSPM */
void FREE_c_r_dsch_AURSPM (c_r_dsch_AURSPM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for struct c_r_dsch_AURSYNM */
void FREE_c_r_dsch_AURSYNM (c_r_dsch_AURSYNM* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs */
void FREE_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_ITBSPMRM */
void FREE_c_r_dsch_ITBSPMRM (c_r_dsch_ITBSPMRM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->BCMC_PROGRAMSs.items; i0++) {
		FREE_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data (sp->BCMC_PROGRAMSs.data[i0]);
		EDFree (sp->BCMC_PROGRAMSs.data[i0]);
	}
	if (sp->BCMC_PROGRAMSs.data) {EDFree (sp->BCMC_PROGRAMSs.data); sp->BCMC_PROGRAMSs.data = NULL; sp->BCMC_PROGRAMSs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_HOSINM_HO_SUP_RECs */
void FREE_c_r_dsch_HOSINM_HO_SUP_RECs (c_r_dsch_HOSINM_HO_SUP_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_HOSINM_HO_SUP_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_HOSINM */
void FREE_c_r_dsch_HOSINM (c_r_dsch_HOSINM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->HO_SUP_RECs.items; i0++) {
		FREE_c_r_dsch_HOSINM_HO_SUP_RECs_data (sp->HO_SUP_RECs.data[i0]);
		EDFree (sp->HO_SUP_RECs.data[i0]);
	}
	if (sp->HO_SUP_RECs.data) {EDFree (sp->HO_SUP_RECs.data); sp->HO_SUP_RECs.data = NULL; sp->HO_SUP_RECs.allocatedItems=0; /*FR02A*/}

}

/* Free function for variable-sized sequence c_r_dsch_GEM_GE_RECs */
void FREE_c_r_dsch_GEM_GE_RECs (c_r_dsch_GEM_GE_RECs* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
		FREE_c_r_dsch_GEM_GE_RECs_data (sp->data[i]);
		EDFree (sp->data[i]);
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_r_dsch_GEM */
void FREE_c_r_dsch_GEM (c_r_dsch_GEM* sp) {
	int i0;
ED_WARNING_REMOVER(sp);
	for (i0=0; i0<sp->GE_RECs.items; i0++) {
		FREE_c_r_dsch_GEM_GE_RECs_data (sp->GE_RECs.data[i0]);
		EDFree (sp->GE_RECs.data[i0]);
	}
	if (sp->GE_RECs.data) {EDFree (sp->GE_RECs.data); sp->GE_RECs.data = NULL; sp->GE_RECs.allocatedItems=0; /*FR02A*/}
	if (sp->MESSAGE_REC.value != NULL) {
		EDFree (sp->MESSAGE_REC.value);
		sp->MESSAGE_REC.value = NULL;
	}
	sp->MESSAGE_REC.usedBits = 0;

}

/* Free function for struct c_RIR_Capability_Information_Blob_Rel6 */
void FREE_c_RIR_Capability_Information_Blob_Rel6 (c_RIR_Capability_Information_Blob_Rel6* sp) {
ED_WARNING_REMOVER(sp);

}

/* Free function for variable-sized sequence c_RIR_Capability_Information_Blob_NAK_PER_ROUND_FWD */
void FREE_c_RIR_Capability_Information_Blob_NAK_PER_ROUND_FWD (c_RIR_Capability_Information_Blob_NAK_PER_ROUND_FWD* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for variable-sized sequence c_RIR_Capability_Information_Blob_NAK_PER_ROUND_REV */
void FREE_c_RIR_Capability_Information_Blob_NAK_PER_ROUND_REV (c_RIR_Capability_Information_Blob_NAK_PER_ROUND_REV* sp) {
	int i;
	for (i=0; i<sp->items; i++) {
	}

	if (sp->data) {EDFree (sp->data); sp->data=NULL; sp->allocatedItems = 0;/*FR03*/}

}
/* Free function for struct c_RIR_Capability_Information_Blob */
void FREE_c_RIR_Capability_Information_Blob (c_RIR_Capability_Information_Blob* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->NAK_PER_ROUND_FWD.data) {EDFree (sp->NAK_PER_ROUND_FWD.data); sp->NAK_PER_ROUND_FWD.data = NULL; sp->NAK_PER_ROUND_FWD.allocatedItems=0; /*FR02A*/}
	if (sp->NAK_PER_ROUND_REV.data) {EDFree (sp->NAK_PER_ROUND_REV.data); sp->NAK_PER_ROUND_REV.data = NULL; sp->NAK_PER_ROUND_REV.allocatedItems=0; /*FR02A*/}

}

/* Free function for struct c_GPM_Common_Fields */
void FREE_c_GPM_Common_Fields (c_GPM_Common_Fields* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->ADD_PFIELD.value != NULL) {
		EDFree (sp->ADD_PFIELD.value);
		sp->ADD_PFIELD.value = NULL;
	}
	sp->ADD_PFIELD.usedBits = 0;

}

/* Free function for struct c_f_csch_GPM_enhanced */
void FREE_c_f_csch_GPM_enhanced (c_f_csch_GPM_enhanced* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->ADD_BCAST_RECORD.value != NULL) {
		EDFree (sp->ADD_BCAST_RECORD.value);
		sp->ADD_BCAST_RECORD.value = NULL;
	}
	sp->ADD_BCAST_RECORD.usedBits = 0;

}

/* Free function for union c_f_csch_mini6 */
void FREE_c_f_csch_mini6 (c_f_csch_mini6* sp) {
	switch (sp->Present) {
		case U_c_f_csch_mini6_A41RANDM: {
			FREE_c_f_csch_A41RANDM (sp->u.A41RANDM);
			EDFree (sp->u.A41RANDM);

			break;
		}
		case U_c_f_csch_mini6_A41SPM: {
			FREE_c_f_csch_A41SPM (sp->u.A41SPM);
			EDFree (sp->u.A41SPM);

			break;
		}
		case U_c_f_csch_mini6_APIDM: {
			FREE_c_f_csch_APIDM (sp->u.APIDM);
			EDFree (sp->u.APIDM);

			break;
		}
		case U_c_f_csch_mini6_APIDTM: {
			FREE_c_f_csch_APIDTM (sp->u.APIDTM);
			EDFree (sp->u.APIDTM);

			break;
		}
		case U_c_f_csch_mini6_APM: {
			FREE_c_f_csch_APM (sp->u.APM);
			EDFree (sp->u.APM);

			break;
		}
		case U_c_f_csch_mini6_APPIM: {
			FREE_c_f_csch_APPIM (sp->u.APPIM);
			EDFree (sp->u.APPIM);

			break;
		}
		case U_c_f_csch_mini6_ATIM: {
			FREE_c_f_csch_ATIM (sp->u.ATIM);
			EDFree (sp->u.ATIM);

			break;
		}
		case U_c_f_csch_mini6_AUCM: {
			FREE_c_f_csch_AUCM (sp->u.AUCM);
			EDFree (sp->u.AUCM);

			break;
		}
		case U_c_f_csch_mini6_AUREQM: {
			FREE_c_f_csch_AUREQM (sp->u.AUREQM);
			EDFree (sp->u.AUREQM);

			break;
		}
		case U_c_f_csch_mini6_BSPM: {
			FREE_c_f_csch_BSPM (sp->u.BSPM);
			EDFree (sp->u.BSPM);

			break;
		}
		case U_c_f_csch_mini6_CAM: {
			FREE_c_f_csch_CAM (sp->u.CAM);
			EDFree (sp->u.CAM);

			break;
		}
		case U_c_f_csch_mini6_CCLM: {
			FREE_c_f_csch_CCLM (sp->u.CCLM);
			EDFree (sp->u.CCLM);

			break;
		}
		case U_c_f_csch_mini6_DBM: {
			FREE_c_f_csch_DBM (sp->u.DBM);
			EDFree (sp->u.DBM);

			break;
		}
		case U_c_f_csch_mini6_EAPM: {
			FREE_c_f_csch_EAPM (sp->u.EAPM);
			EDFree (sp->u.EAPM);

			break;
		}
		case U_c_f_csch_mini6_ECAM: {
			FREE_c_f_csch_ECAM (sp->u.ECAM);
			EDFree (sp->u.ECAM);

			break;
		}
		case U_c_f_csch_mini6_ECCLM: {
			FREE_c_f_csch_ECCLM (sp->u.ECCLM);
			EDFree (sp->u.ECCLM);

			break;
		}
		case U_c_f_csch_mini6_EGSRDM: {
			FREE_c_f_csch_EGSRDM (sp->u.EGSRDM);
			EDFree (sp->u.EGSRDM);

			break;
		}
		case U_c_f_csch_mini6_ENLM: {
			FREE_c_f_csch_ENLM (sp->u.ENLM);
			EDFree (sp->u.ENLM);

			break;
		}
		case U_c_f_csch_mini6_ESPM: {
			FREE_c_f_csch_ESPM (sp->u.ESPM);
			EDFree (sp->u.ESPM);

			break;
		}
		case U_c_f_csch_mini6_FDCCLM: {
			FREE_c_f_csch_FDCCLM (sp->u.FDCCLM);
			EDFree (sp->u.FDCCLM);

			break;
		}
		case U_c_f_csch_mini6_FGOIM: {
			FREE_c_f_csch_FGOIM (sp->u.FGOIM);
			EDFree (sp->u.FGOIM);

			break;
		}
		case U_c_f_csch_mini6_FNM: {
			FREE_c_f_csch_FNM (sp->u.FNM);
			EDFree (sp->u.FNM);

			break;
		}
		case U_c_f_csch_mini6_GAPM: {
			FREE_c_f_csch_GAPM (sp->u.GAPM);
			EDFree (sp->u.GAPM);

			break;
		}
		case U_c_f_csch_mini6_GEM: {
			FREE_c_f_csch_GEM (sp->u.GEM);
			EDFree (sp->u.GEM);

			break;
		}
		case U_c_f_csch_mini6_GNLM: {
			FREE_c_f_csch_GNLM (sp->u.GNLM);
			EDFree (sp->u.GNLM);

			break;
		}
		case U_c_f_csch_mini6_GOIM: {
			FREE_c_f_csch_GOIM (sp->u.GOIM);
			EDFree (sp->u.GOIM);

			break;
		}
		case U_c_f_csch_mini6_GPM: {
			FREE_c_f_csch_GPM (sp->u.GPM);
			EDFree (sp->u.GPM);

			break;
		}
		case U_c_f_csch_mini6_GSRDM: {
			FREE_c_f_csch_GSRDM (sp->u.GSRDM);
			EDFree (sp->u.GSRDM);

			break;
		}
		case U_c_f_csch_mini6_HDPPM: {
			FREE_c_f_csch_HDPPM (sp->u.HDPPM);
			EDFree (sp->u.HDPPM);

			break;
		}
		case U_c_f_csch_mini6_MCRRPM: {
			FREE_c_f_csch_MCRRPM (sp->u.MCRRPM);
			EDFree (sp->u.MCRRPM);

			break;
		}
		case U_c_f_csch_mini6_MECAM: {
			FREE_c_f_csch_MECAM (sp->u.MECAM);
			EDFree (sp->u.MECAM);

			break;
		}
		case U_c_f_csch_mini6_NLM: {
			FREE_c_f_csch_NLM (sp->u.NLM);
			EDFree (sp->u.NLM);

			break;
		}
		case U_c_f_csch_mini6_ORDM: {
			FREE_c_f_csch_ORDM (sp->u.ORDM);
			EDFree (sp->u.ORDM);

			break;
		}
		case U_c_f_csch_mini6_PACAM: {
			FREE_c_f_csch_PACAM (sp->u.PACAM);
			EDFree (sp->u.PACAM);

			break;
		}
		case U_c_f_csch_mini6_PNLM: {
			FREE_c_f_csch_PNLM (sp->u.PNLM);
			EDFree (sp->u.PNLM);

			break;
		}
		case U_c_f_csch_mini6_SMCM: {
			FREE_c_f_csch_SMCM (sp->u.SMCM);
			EDFree (sp->u.SMCM);

			break;
		}
		case U_c_f_csch_mini6_SPM: {
			FREE_c_f_csch_SPM (sp->u.SPM);
			EDFree (sp->u.SPM);

			break;
		}
		case U_c_f_csch_mini6_SRDM: {
			FREE_c_f_csch_SRDM (sp->u.SRDM);
			EDFree (sp->u.SRDM);

			break;
		}
		case U_c_f_csch_mini6_SSDUM: {
			FREE_c_f_csch_SSDUM (sp->u.SSDUM);
			EDFree (sp->u.SSDUM);

			break;
		}
		case U_c_f_csch_mini6_STRQM: {
			FREE_c_f_csch_STRQM (sp->u.STRQM);
			EDFree (sp->u.STRQM);

			break;
		}
		case U_c_f_csch_mini6_TASM: {
			FREE_c_f_csch_TASM (sp->u.TASM);
			EDFree (sp->u.TASM);

			break;
		}
		case U_c_f_csch_mini6_UNLM: {
			FREE_c_f_csch_UNLM (sp->u.UNLM);
			EDFree (sp->u.UNLM);

			break;
		}
		case U_c_f_csch_mini6_UZIM: {
			FREE_c_f_csch_UZIM (sp->u.UZIM);
			EDFree (sp->u.UZIM);

			break;
		}
		case U_c_f_csch_mini6_UZRM: {
			FREE_c_f_csch_UZRM (sp->u.UZRM);
			EDFree (sp->u.UZRM);

			break;
		}

		default:;
	}
	sp->Present = U_c_f_csch_mini6_NONE;
}

/* Set-present function for c_f_csch_mini6 LEVEL = -1 */
void GLOB_SETPRESENT_c_f_csch_mini6 (c_f_csch_mini6* sp, TPRESENT_c_f_csch_mini6 toBeSetPresent) 
{
	FREE_c_f_csch_mini6 (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_f_csch_mini6_A41RANDM: {
			sp->u.A41RANDM = (c_f_csch_A41RANDM*)EDAlloc (sizeof (c_f_csch_A41RANDM));
			INIT_c_f_csch_A41RANDM (sp->u.A41RANDM);

			break;
		}
		case U_c_f_csch_mini6_A41SPM: {
			sp->u.A41SPM = (c_f_csch_A41SPM*)EDAlloc (sizeof (c_f_csch_A41SPM));
			INIT_c_f_csch_A41SPM (sp->u.A41SPM);

			break;
		}
		case U_c_f_csch_mini6_APIDM: {
			sp->u.APIDM = (c_f_csch_APIDM*)EDAlloc (sizeof (c_f_csch_APIDM));
			INIT_c_f_csch_APIDM (sp->u.APIDM);

			break;
		}
		case U_c_f_csch_mini6_APIDTM: {
			sp->u.APIDTM = (c_f_csch_APIDTM*)EDAlloc (sizeof (c_f_csch_APIDTM));
			INIT_c_f_csch_APIDTM (sp->u.APIDTM);

			break;
		}
		case U_c_f_csch_mini6_APM: {
			sp->u.APM = (c_f_csch_APM*)EDAlloc (sizeof (c_f_csch_APM));
			INIT_c_f_csch_APM (sp->u.APM);

			break;
		}
		case U_c_f_csch_mini6_APPIM: {
			sp->u.APPIM = (c_f_csch_APPIM*)EDAlloc (sizeof (c_f_csch_APPIM));
			INIT_c_f_csch_APPIM (sp->u.APPIM);

			break;
		}
		case U_c_f_csch_mini6_ATIM: {
			sp->u.ATIM = (c_f_csch_ATIM*)EDAlloc (sizeof (c_f_csch_ATIM));
			INIT_c_f_csch_ATIM (sp->u.ATIM);

			break;
		}
		case U_c_f_csch_mini6_AUCM: {
			sp->u.AUCM = (c_f_csch_AUCM*)EDAlloc (sizeof (c_f_csch_AUCM));
			INIT_c_f_csch_AUCM (sp->u.AUCM);

			break;
		}
		case U_c_f_csch_mini6_AUREQM: {
			sp->u.AUREQM = (c_f_csch_AUREQM*)EDAlloc (sizeof (c_f_csch_AUREQM));
			INIT_c_f_csch_AUREQM (sp->u.AUREQM);

			break;
		}
		case U_c_f_csch_mini6_BSPM: {
			sp->u.BSPM = (c_f_csch_BSPM*)EDAlloc (sizeof (c_f_csch_BSPM));
			INIT_c_f_csch_BSPM (sp->u.BSPM);

			break;
		}
		case U_c_f_csch_mini6_CAM: {
			sp->u.CAM = (c_f_csch_CAM*)EDAlloc (sizeof (c_f_csch_CAM));
			INIT_c_f_csch_CAM (sp->u.CAM);

			break;
		}
		case U_c_f_csch_mini6_CCLM: {
			sp->u.CCLM = (c_f_csch_CCLM*)EDAlloc (sizeof (c_f_csch_CCLM));
			INIT_c_f_csch_CCLM (sp->u.CCLM);

			break;
		}
		case U_c_f_csch_mini6_DBM: {
			sp->u.DBM = (c_f_csch_DBM*)EDAlloc (sizeof (c_f_csch_DBM));
			INIT_c_f_csch_DBM (sp->u.DBM);

			break;
		}
		case U_c_f_csch_mini6_EAPM: {
			sp->u.EAPM = (c_f_csch_EAPM*)EDAlloc (sizeof (c_f_csch_EAPM));
			INIT_c_f_csch_EAPM (sp->u.EAPM);

			break;
		}
		case U_c_f_csch_mini6_ECAM: {
			sp->u.ECAM = (c_f_csch_ECAM*)EDAlloc (sizeof (c_f_csch_ECAM));
			INIT_c_f_csch_ECAM (sp->u.ECAM);

			break;
		}
		case U_c_f_csch_mini6_ECCLM: {
			sp->u.ECCLM = (c_f_csch_ECCLM*)EDAlloc (sizeof (c_f_csch_ECCLM));
			INIT_c_f_csch_ECCLM (sp->u.ECCLM);

			break;
		}
		case U_c_f_csch_mini6_EGSRDM: {
			sp->u.EGSRDM = (c_f_csch_EGSRDM*)EDAlloc (sizeof (c_f_csch_EGSRDM));
			INIT_c_f_csch_EGSRDM (sp->u.EGSRDM);

			break;
		}
		case U_c_f_csch_mini6_ENLM: {
			sp->u.ENLM = (c_f_csch_ENLM*)EDAlloc (sizeof (c_f_csch_ENLM));
			INIT_c_f_csch_ENLM (sp->u.ENLM);

			break;
		}
		case U_c_f_csch_mini6_ESPM: {
			sp->u.ESPM = (c_f_csch_ESPM*)EDAlloc (sizeof (c_f_csch_ESPM));
			INIT_c_f_csch_ESPM (sp->u.ESPM);

			break;
		}
		case U_c_f_csch_mini6_FDCCLM: {
			sp->u.FDCCLM = (c_f_csch_FDCCLM*)EDAlloc (sizeof (c_f_csch_FDCCLM));
			INIT_c_f_csch_FDCCLM (sp->u.FDCCLM);

			break;
		}
		case U_c_f_csch_mini6_FGOIM: {
			sp->u.FGOIM = (c_f_csch_FGOIM*)EDAlloc (sizeof (c_f_csch_FGOIM));
			INIT_c_f_csch_FGOIM (sp->u.FGOIM);

			break;
		}
		case U_c_f_csch_mini6_FNM: {
			sp->u.FNM = (c_f_csch_FNM*)EDAlloc (sizeof (c_f_csch_FNM));
			INIT_c_f_csch_FNM (sp->u.FNM);

			break;
		}
		case U_c_f_csch_mini6_GAPM: {
			sp->u.GAPM = (c_f_csch_GAPM*)EDAlloc (sizeof (c_f_csch_GAPM));
			INIT_c_f_csch_GAPM (sp->u.GAPM);

			break;
		}
		case U_c_f_csch_mini6_GEM: {
			sp->u.GEM = (c_f_csch_GEM*)EDAlloc (sizeof (c_f_csch_GEM));
			INIT_c_f_csch_GEM (sp->u.GEM);

			break;
		}
		case U_c_f_csch_mini6_GNLM: {
			sp->u.GNLM = (c_f_csch_GNLM*)EDAlloc (sizeof (c_f_csch_GNLM));
			INIT_c_f_csch_GNLM (sp->u.GNLM);

			break;
		}
		case U_c_f_csch_mini6_GOIM: {
			sp->u.GOIM = (c_f_csch_GOIM*)EDAlloc (sizeof (c_f_csch_GOIM));
			INIT_c_f_csch_GOIM (sp->u.GOIM);

			break;
		}
		case U_c_f_csch_mini6_GPM: {
			sp->u.GPM = (c_f_csch_GPM*)EDAlloc (sizeof (c_f_csch_GPM));
			INIT_c_f_csch_GPM (sp->u.GPM);

			break;
		}
		case U_c_f_csch_mini6_GSRDM: {
			sp->u.GSRDM = (c_f_csch_GSRDM*)EDAlloc (sizeof (c_f_csch_GSRDM));
			INIT_c_f_csch_GSRDM (sp->u.GSRDM);

			break;
		}
		case U_c_f_csch_mini6_HDPPM: {
			sp->u.HDPPM = (c_f_csch_HDPPM*)EDAlloc (sizeof (c_f_csch_HDPPM));
			INIT_c_f_csch_HDPPM (sp->u.HDPPM);

			break;
		}
		case U_c_f_csch_mini6_MCRRPM: {
			sp->u.MCRRPM = (c_f_csch_MCRRPM*)EDAlloc (sizeof (c_f_csch_MCRRPM));
			INIT_c_f_csch_MCRRPM (sp->u.MCRRPM);

			break;
		}
		case U_c_f_csch_mini6_MECAM: {
			sp->u.MECAM = (c_f_csch_MECAM*)EDAlloc (sizeof (c_f_csch_MECAM));
			INIT_c_f_csch_MECAM (sp->u.MECAM);

			break;
		}
		case U_c_f_csch_mini6_NLM: {
			sp->u.NLM = (c_f_csch_NLM*)EDAlloc (sizeof (c_f_csch_NLM));
			INIT_c_f_csch_NLM (sp->u.NLM);

			break;
		}
		case U_c_f_csch_mini6_ORDM: {
			sp->u.ORDM = (c_f_csch_ORDM*)EDAlloc (sizeof (c_f_csch_ORDM));
			INIT_c_f_csch_ORDM (sp->u.ORDM);

			break;
		}
		case U_c_f_csch_mini6_PACAM: {
			sp->u.PACAM = (c_f_csch_PACAM*)EDAlloc (sizeof (c_f_csch_PACAM));
			INIT_c_f_csch_PACAM (sp->u.PACAM);

			break;
		}
		case U_c_f_csch_mini6_PNLM: {
			sp->u.PNLM = (c_f_csch_PNLM*)EDAlloc (sizeof (c_f_csch_PNLM));
			INIT_c_f_csch_PNLM (sp->u.PNLM);

			break;
		}
		case U_c_f_csch_mini6_SMCM: {
			sp->u.SMCM = (c_f_csch_SMCM*)EDAlloc (sizeof (c_f_csch_SMCM));
			INIT_c_f_csch_SMCM (sp->u.SMCM);

			break;
		}
		case U_c_f_csch_mini6_SPM: {
			sp->u.SPM = (c_f_csch_SPM*)EDAlloc (sizeof (c_f_csch_SPM));
			INIT_c_f_csch_SPM (sp->u.SPM);

			break;
		}
		case U_c_f_csch_mini6_SRDM: {
			sp->u.SRDM = (c_f_csch_SRDM*)EDAlloc (sizeof (c_f_csch_SRDM));
			INIT_c_f_csch_SRDM (sp->u.SRDM);

			break;
		}
		case U_c_f_csch_mini6_SSDUM: {
			sp->u.SSDUM = (c_f_csch_SSDUM*)EDAlloc (sizeof (c_f_csch_SSDUM));
			INIT_c_f_csch_SSDUM (sp->u.SSDUM);

			break;
		}
		case U_c_f_csch_mini6_STRQM: {
			sp->u.STRQM = (c_f_csch_STRQM*)EDAlloc (sizeof (c_f_csch_STRQM));
			INIT_c_f_csch_STRQM (sp->u.STRQM);

			break;
		}
		case U_c_f_csch_mini6_TASM: {
			sp->u.TASM = (c_f_csch_TASM*)EDAlloc (sizeof (c_f_csch_TASM));
			INIT_c_f_csch_TASM (sp->u.TASM);

			break;
		}
		case U_c_f_csch_mini6_UNLM: {
			sp->u.UNLM = (c_f_csch_UNLM*)EDAlloc (sizeof (c_f_csch_UNLM));
			INIT_c_f_csch_UNLM (sp->u.UNLM);

			break;
		}
		case U_c_f_csch_mini6_UZIM: {
			sp->u.UZIM = (c_f_csch_UZIM*)EDAlloc (sizeof (c_f_csch_UZIM));
			INIT_c_f_csch_UZIM (sp->u.UZIM);

			break;
		}
		case U_c_f_csch_mini6_UZRM: {
			sp->u.UZRM = (c_f_csch_UZRM*)EDAlloc (sizeof (c_f_csch_UZRM));
			INIT_c_f_csch_UZRM (sp->u.UZRM);

			break;
		}
		default:;	}
}

/* Free function for union c_f_csch_mini6_sync */
void FREE_c_f_csch_mini6_sync (c_f_csch_mini6_sync* sp) {
	switch (sp->Present) {
		case U_c_f_csch_mini6_sync_SCHM: {
			FREE_c_f_csch_SCHM (sp->u.SCHM);
			EDFree (sp->u.SCHM);

			break;
		}

		default:;
	}
	sp->Present = U_c_f_csch_mini6_sync_NONE;
}

/* Set-present function for c_f_csch_mini6_sync LEVEL = -1 */
void GLOB_SETPRESENT_c_f_csch_mini6_sync (c_f_csch_mini6_sync* sp, TPRESENT_c_f_csch_mini6_sync toBeSetPresent) 
{
	FREE_c_f_csch_mini6_sync (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_f_csch_mini6_sync_SCHM: {
			sp->u.SCHM = (c_f_csch_SCHM*)EDAlloc (sizeof (c_f_csch_SCHM));
			INIT_c_f_csch_SCHM (sp->u.SCHM);

			break;
		}
		default:;	}
}

/* Free function for union c_f_dsch */
void FREE_c_f_dsch (c_f_dsch* sp) {
	switch (sp->Present) {
		case U_c_f_dsch_AHDM: {
			FREE_c_f_dsch_AHDM (sp->u.AHDM);
			EDFree (sp->u.AHDM);

			break;
		}
		case U_c_f_dsch_ATIM: {
			FREE_c_f_dsch_ATIM (sp->u.ATIM);
			EDFree (sp->u.ATIM);

			break;
		}
		case U_c_f_dsch_AUCM: {
			FREE_c_f_dsch_AUCM (sp->u.AUCM);
			EDFree (sp->u.AUCM);

			break;
		}
		case U_c_f_dsch_AUREQM: {
			FREE_c_f_dsch_AUREQM (sp->u.AUREQM);
			EDFree (sp->u.AUREQM);

			break;
		}
		case U_c_f_dsch_AWIM: {
			FREE_c_f_dsch_AWIM (sp->u.AWIM);
			EDFree (sp->u.AWIM);

			break;
		}
		case U_c_f_dsch_BDTMFM: {
			FREE_c_f_dsch_BDTMFM (sp->u.BDTMFM);
			EDFree (sp->u.BDTMFM);

			break;
		}
		case U_c_f_dsch_BSSRSPM: {
			FREE_c_f_dsch_BSSRSPM (sp->u.BSSRSPM);
			EDFree (sp->u.BSSRSPM);

			break;
		}
		case U_c_f_dsch_CFSCNM: {
			FREE_c_f_dsch_CFSCNM (sp->u.CFSCNM);
			EDFree (sp->u.CFSCNM);

			break;
		}
		case U_c_f_dsch_CFSRQM: {
			FREE_c_f_dsch_CFSRQM (sp->u.CFSRQM);
			EDFree (sp->u.CFSRQM);

			break;
		}
		case U_c_f_dsch_CLAM: {
			FREE_c_f_dsch_CLAM (sp->u.CLAM);
			EDFree (sp->u.CLAM);

			break;
		}
		case U_c_f_dsch_DBM: {
			FREE_c_f_dsch_DBM (sp->u.DBM);
			EDFree (sp->u.DBM);

			break;
		}
		case U_c_f_dsch_EAWIM: {
			FREE_c_f_dsch_EAWIM (sp->u.EAWIM);
			EDFree (sp->u.EAWIM);

			break;
		}
		case U_c_f_dsch_EFWIM: {
			FREE_c_f_dsch_EFWIM (sp->u.EFWIM);
			EDFree (sp->u.EFWIM);

			break;
		}
		case U_c_f_dsch_EHDM: {
			FREE_c_f_dsch_EHDM (sp->u.EHDM);
			EDFree (sp->u.EHDM);

			break;
		}
		case U_c_f_dsch_ENLUM: {
			FREE_c_f_dsch_ENLUM (sp->u.ENLUM);
			EDFree (sp->u.ENLUM);

			break;
		}
		case U_c_f_dsch_ERM: {
			FREE_c_f_dsch_ERM (sp->u.ERM);
			EDFree (sp->u.ERM);

			break;
		}
		case U_c_f_dsch_ESCAM: {
			FREE_c_f_dsch_ESCAM (sp->u.ESCAM);
			EDFree (sp->u.ESCAM);

			break;
		}
		case U_c_f_dsch_FWIM: {
			FREE_c_f_dsch_FWIM (sp->u.FWIM);
			EDFree (sp->u.FWIM);

			break;
		}
		case U_c_f_dsch_GEM: {
			FREE_c_f_dsch_GEM (sp->u.GEM);
			EDFree (sp->u.GEM);

			break;
		}
		case U_c_f_dsch_GHDM: {
			FREE_c_f_dsch_GHDM (sp->u.GHDM);
			EDFree (sp->u.GHDM);

			break;
		}
		case U_c_f_dsch_HOSISM: {
			FREE_c_f_dsch_HOSISM (sp->u.HOSISM);
			EDFree (sp->u.HOSISM);

			break;
		}
		case U_c_f_dsch_ITBSPM: {
			FREE_c_f_dsch_ITBSPM (sp->u.ITBSPM);
			EDFree (sp->u.ITBSPM);

			break;
		}
		case U_c_f_dsch_ITSPM: {
			FREE_c_f_dsch_ITSPM (sp->u.ITSPM);
			EDFree (sp->u.ITSPM);

			break;
		}
		case U_c_f_dsch_MABOPM: {
			FREE_c_f_dsch_MABOPM (sp->u.MABOPM);
			EDFree (sp->u.MABOPM);

			break;
		}
		case U_c_f_dsch_MSRM: {
			FREE_c_f_dsch_MSRM (sp->u.MSRM);
			EDFree (sp->u.MSRM);

			break;
		}
		case U_c_f_dsch_MUHDM: {
			FREE_c_f_dsch_MUHDM (sp->u.MUHDM);
			EDFree (sp->u.MUHDM);

			break;
		}
		case U_c_f_dsch_NLUM: {
			FREE_c_f_dsch_NLUM (sp->u.NLUM);
			EDFree (sp->u.NLUM);

			break;
		}
		case U_c_f_dsch_ORDRM: {
			FREE_c_f_dsch_ORDRM (sp->u.ORDRM);
			EDFree (sp->u.ORDRM);

			break;
		}
		case U_c_f_dsch_PCNM: {
			FREE_c_f_dsch_PCNM (sp->u.PCNM);
			EDFree (sp->u.PCNM);

			break;
		}
		case U_c_f_dsch_PCNPM: {
			FREE_c_f_dsch_PCNPM (sp->u.PCNPM);
			EDFree (sp->u.PCNPM);

			break;
		}
		case U_c_f_dsch_PUFCM: {
			FREE_c_f_dsch_PUFCM (sp->u.PUFCM);
			EDFree (sp->u.PUFCM);

			break;
		}
		case U_c_f_dsch_PUFM: {
			FREE_c_f_dsch_PUFM (sp->u.PUFM);
			EDFree (sp->u.PUFM);

			break;
		}
		case U_c_f_dsch_RAM: {
			FREE_c_f_dsch_RAM (sp->u.RAM);
			EDFree (sp->u.RAM);

			break;
		}
		case U_c_f_dsch_RATCHGM: {
			FREE_c_f_dsch_RATCHGM (sp->u.RATCHGM);
			EDFree (sp->u.RATCHGM);

			break;
		}
		case U_c_f_dsch_RCPM: {
			FREE_c_f_dsch_RCPM (sp->u.RCPM);
			EDFree (sp->u.RCPM);

			break;
		}
		case U_c_f_dsch_RTPM: {
			FREE_c_f_dsch_RTPM (sp->u.RTPM);
			EDFree (sp->u.RTPM);

			break;
		}
		case U_c_f_dsch_SCAM: {
			FREE_c_f_dsch_SCAM (sp->u.SCAM);
			EDFree (sp->u.SCAM);

			break;
		}
		case U_c_f_dsch_SCM: {
			FREE_c_f_dsch_SCM (sp->u.SCM);
			EDFree (sp->u.SCM);

			break;
		}
		case U_c_f_dsch_SMCM: {
			FREE_c_f_dsch_SMCM (sp->u.SMCM);
			EDFree (sp->u.SMCM);

			break;
		}
		case U_c_f_dsch_SOCM: {
			FREE_c_f_dsch_SOCM (sp->u.SOCM);
			EDFree (sp->u.SOCM);

			break;
		}
		case U_c_f_dsch_SRDM: {
			FREE_c_f_dsch_SRDM (sp->u.SRDM);
			EDFree (sp->u.SRDM);

			break;
		}
		case U_c_f_dsch_SRPM: {
			FREE_c_f_dsch_SRPM (sp->u.SRPM);
			EDFree (sp->u.SRPM);

			break;
		}
		case U_c_f_dsch_SRQM: {
			FREE_c_f_dsch_SRQM (sp->u.SRQM);
			EDFree (sp->u.SRQM);

			break;
		}
		case U_c_f_dsch_SSDUM: {
			FREE_c_f_dsch_SSDUM (sp->u.SSDUM);
			EDFree (sp->u.SSDUM);

			break;
		}
		case U_c_f_dsch_STPM: {
			FREE_c_f_dsch_STPM (sp->u.STPM);
			EDFree (sp->u.STPM);

			break;
		}
		case U_c_f_dsch_STRQM: {
			FREE_c_f_dsch_STRQM (sp->u.STRQM);
			EDFree (sp->u.STRQM);

			break;
		}
		case U_c_f_dsch_TASM: {
			FREE_c_f_dsch_TASM (sp->u.TASM);
			EDFree (sp->u.TASM);

			break;
		}
		case U_c_f_dsch_UHDM: {
			FREE_c_f_dsch_UHDM (sp->u.UHDM);
			EDFree (sp->u.UHDM);

			break;
		}
		case U_c_f_dsch_UZRM: {
			FREE_c_f_dsch_UZRM (sp->u.UZRM);
			EDFree (sp->u.UZRM);

			break;
		}
		case U_c_f_dsch_UZUM: {
			FREE_c_f_dsch_UZUM (sp->u.UZUM);
			EDFree (sp->u.UZUM);

			break;
		}

		default:;
	}
	sp->Present = U_c_f_dsch_NONE;
}

/* Set-present function for c_f_dsch LEVEL = -1 */
void GLOB_SETPRESENT_c_f_dsch (c_f_dsch* sp, TPRESENT_c_f_dsch toBeSetPresent) 
{
	FREE_c_f_dsch (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_f_dsch_AHDM: {
			sp->u.AHDM = (c_f_dsch_AHDM*)EDAlloc (sizeof (c_f_dsch_AHDM));
			INIT_c_f_dsch_AHDM (sp->u.AHDM);

			break;
		}
		case U_c_f_dsch_ATIM: {
			sp->u.ATIM = (c_f_dsch_ATIM*)EDAlloc (sizeof (c_f_dsch_ATIM));
			INIT_c_f_dsch_ATIM (sp->u.ATIM);

			break;
		}
		case U_c_f_dsch_AUCM: {
			sp->u.AUCM = (c_f_dsch_AUCM*)EDAlloc (sizeof (c_f_dsch_AUCM));
			INIT_c_f_dsch_AUCM (sp->u.AUCM);

			break;
		}
		case U_c_f_dsch_AUREQM: {
			sp->u.AUREQM = (c_f_dsch_AUREQM*)EDAlloc (sizeof (c_f_dsch_AUREQM));
			INIT_c_f_dsch_AUREQM (sp->u.AUREQM);

			break;
		}
		case U_c_f_dsch_AWIM: {
			sp->u.AWIM = (c_f_dsch_AWIM*)EDAlloc (sizeof (c_f_dsch_AWIM));
			INIT_c_f_dsch_AWIM (sp->u.AWIM);

			break;
		}
		case U_c_f_dsch_BDTMFM: {
			sp->u.BDTMFM = (c_f_dsch_BDTMFM*)EDAlloc (sizeof (c_f_dsch_BDTMFM));
			INIT_c_f_dsch_BDTMFM (sp->u.BDTMFM);

			break;
		}
		case U_c_f_dsch_BSSRSPM: {
			sp->u.BSSRSPM = (c_f_dsch_BSSRSPM*)EDAlloc (sizeof (c_f_dsch_BSSRSPM));
			INIT_c_f_dsch_BSSRSPM (sp->u.BSSRSPM);

			break;
		}
		case U_c_f_dsch_CFSCNM: {
			sp->u.CFSCNM = (c_f_dsch_CFSCNM*)EDAlloc (sizeof (c_f_dsch_CFSCNM));
			INIT_c_f_dsch_CFSCNM (sp->u.CFSCNM);

			break;
		}
		case U_c_f_dsch_CFSRQM: {
			sp->u.CFSRQM = (c_f_dsch_CFSRQM*)EDAlloc (sizeof (c_f_dsch_CFSRQM));
			INIT_c_f_dsch_CFSRQM (sp->u.CFSRQM);

			break;
		}
		case U_c_f_dsch_CLAM: {
			sp->u.CLAM = (c_f_dsch_CLAM*)EDAlloc (sizeof (c_f_dsch_CLAM));
			INIT_c_f_dsch_CLAM (sp->u.CLAM);

			break;
		}
		case U_c_f_dsch_DBM: {
			sp->u.DBM = (c_f_dsch_DBM*)EDAlloc (sizeof (c_f_dsch_DBM));
			INIT_c_f_dsch_DBM (sp->u.DBM);

			break;
		}
		case U_c_f_dsch_EAWIM: {
			sp->u.EAWIM = (c_f_dsch_EAWIM*)EDAlloc (sizeof (c_f_dsch_EAWIM));
			INIT_c_f_dsch_EAWIM (sp->u.EAWIM);

			break;
		}
		case U_c_f_dsch_EFWIM: {
			sp->u.EFWIM = (c_f_dsch_EFWIM*)EDAlloc (sizeof (c_f_dsch_EFWIM));
			INIT_c_f_dsch_EFWIM (sp->u.EFWIM);

			break;
		}
		case U_c_f_dsch_EHDM: {
			sp->u.EHDM = (c_f_dsch_EHDM*)EDAlloc (sizeof (c_f_dsch_EHDM));
			INIT_c_f_dsch_EHDM (sp->u.EHDM);

			break;
		}
		case U_c_f_dsch_ENLUM: {
			sp->u.ENLUM = (c_f_dsch_ENLUM*)EDAlloc (sizeof (c_f_dsch_ENLUM));
			INIT_c_f_dsch_ENLUM (sp->u.ENLUM);

			break;
		}
		case U_c_f_dsch_ERM: {
			sp->u.ERM = (c_f_dsch_ERM*)EDAlloc (sizeof (c_f_dsch_ERM));
			INIT_c_f_dsch_ERM (sp->u.ERM);

			break;
		}
		case U_c_f_dsch_ESCAM: {
			sp->u.ESCAM = (c_f_dsch_ESCAM*)EDAlloc (sizeof (c_f_dsch_ESCAM));
			INIT_c_f_dsch_ESCAM (sp->u.ESCAM);

			break;
		}
		case U_c_f_dsch_FWIM: {
			sp->u.FWIM = (c_f_dsch_FWIM*)EDAlloc (sizeof (c_f_dsch_FWIM));
			INIT_c_f_dsch_FWIM (sp->u.FWIM);

			break;
		}
		case U_c_f_dsch_GEM: {
			sp->u.GEM = (c_f_dsch_GEM*)EDAlloc (sizeof (c_f_dsch_GEM));
			INIT_c_f_dsch_GEM (sp->u.GEM);

			break;
		}
		case U_c_f_dsch_GHDM: {
			sp->u.GHDM = (c_f_dsch_GHDM*)EDAlloc (sizeof (c_f_dsch_GHDM));
			INIT_c_f_dsch_GHDM (sp->u.GHDM);

			break;
		}
		case U_c_f_dsch_HOSISM: {
			sp->u.HOSISM = (c_f_dsch_HOSISM*)EDAlloc (sizeof (c_f_dsch_HOSISM));
			INIT_c_f_dsch_HOSISM (sp->u.HOSISM);

			break;
		}
		case U_c_f_dsch_ITBSPM: {
			sp->u.ITBSPM = (c_f_dsch_ITBSPM*)EDAlloc (sizeof (c_f_dsch_ITBSPM));
			INIT_c_f_dsch_ITBSPM (sp->u.ITBSPM);

			break;
		}
		case U_c_f_dsch_ITSPM: {
			sp->u.ITSPM = (c_f_dsch_ITSPM*)EDAlloc (sizeof (c_f_dsch_ITSPM));
			INIT_c_f_dsch_ITSPM (sp->u.ITSPM);

			break;
		}
		case U_c_f_dsch_MABOPM: {
			sp->u.MABOPM = (c_f_dsch_MABOPM*)EDAlloc (sizeof (c_f_dsch_MABOPM));
			INIT_c_f_dsch_MABOPM (sp->u.MABOPM);

			break;
		}
		case U_c_f_dsch_MSRM: {
			sp->u.MSRM = (c_f_dsch_MSRM*)EDAlloc (sizeof (c_f_dsch_MSRM));
			INIT_c_f_dsch_MSRM (sp->u.MSRM);

			break;
		}
		case U_c_f_dsch_MUHDM: {
			sp->u.MUHDM = (c_f_dsch_MUHDM*)EDAlloc (sizeof (c_f_dsch_MUHDM));
			INIT_c_f_dsch_MUHDM (sp->u.MUHDM);

			break;
		}
		case U_c_f_dsch_NLUM: {
			sp->u.NLUM = (c_f_dsch_NLUM*)EDAlloc (sizeof (c_f_dsch_NLUM));
			INIT_c_f_dsch_NLUM (sp->u.NLUM);

			break;
		}
		case U_c_f_dsch_ORDRM: {
			sp->u.ORDRM = (c_f_dsch_ORDRM*)EDAlloc (sizeof (c_f_dsch_ORDRM));
			INIT_c_f_dsch_ORDRM (sp->u.ORDRM);

			break;
		}
		case U_c_f_dsch_PCNM: {
			sp->u.PCNM = (c_f_dsch_PCNM*)EDAlloc (sizeof (c_f_dsch_PCNM));
			INIT_c_f_dsch_PCNM (sp->u.PCNM);

			break;
		}
		case U_c_f_dsch_PCNPM: {
			sp->u.PCNPM = (c_f_dsch_PCNPM*)EDAlloc (sizeof (c_f_dsch_PCNPM));
			INIT_c_f_dsch_PCNPM (sp->u.PCNPM);

			break;
		}
		case U_c_f_dsch_PUFCM: {
			sp->u.PUFCM = (c_f_dsch_PUFCM*)EDAlloc (sizeof (c_f_dsch_PUFCM));
			INIT_c_f_dsch_PUFCM (sp->u.PUFCM);

			break;
		}
		case U_c_f_dsch_PUFM: {
			sp->u.PUFM = (c_f_dsch_PUFM*)EDAlloc (sizeof (c_f_dsch_PUFM));
			INIT_c_f_dsch_PUFM (sp->u.PUFM);

			break;
		}
		case U_c_f_dsch_RAM: {
			sp->u.RAM = (c_f_dsch_RAM*)EDAlloc (sizeof (c_f_dsch_RAM));
			INIT_c_f_dsch_RAM (sp->u.RAM);

			break;
		}
		case U_c_f_dsch_RATCHGM: {
			sp->u.RATCHGM = (c_f_dsch_RATCHGM*)EDAlloc (sizeof (c_f_dsch_RATCHGM));
			INIT_c_f_dsch_RATCHGM (sp->u.RATCHGM);

			break;
		}
		case U_c_f_dsch_RCPM: {
			sp->u.RCPM = (c_f_dsch_RCPM*)EDAlloc (sizeof (c_f_dsch_RCPM));
			INIT_c_f_dsch_RCPM (sp->u.RCPM);

			break;
		}
		case U_c_f_dsch_RTPM: {
			sp->u.RTPM = (c_f_dsch_RTPM*)EDAlloc (sizeof (c_f_dsch_RTPM));
			INIT_c_f_dsch_RTPM (sp->u.RTPM);

			break;
		}
		case U_c_f_dsch_SCAM: {
			sp->u.SCAM = (c_f_dsch_SCAM*)EDAlloc (sizeof (c_f_dsch_SCAM));
			INIT_c_f_dsch_SCAM (sp->u.SCAM);

			break;
		}
		case U_c_f_dsch_SCM: {
			sp->u.SCM = (c_f_dsch_SCM*)EDAlloc (sizeof (c_f_dsch_SCM));
			INIT_c_f_dsch_SCM (sp->u.SCM);

			break;
		}
		case U_c_f_dsch_SMCM: {
			sp->u.SMCM = (c_f_dsch_SMCM*)EDAlloc (sizeof (c_f_dsch_SMCM));
			INIT_c_f_dsch_SMCM (sp->u.SMCM);

			break;
		}
		case U_c_f_dsch_SOCM: {
			sp->u.SOCM = (c_f_dsch_SOCM*)EDAlloc (sizeof (c_f_dsch_SOCM));
			INIT_c_f_dsch_SOCM (sp->u.SOCM);

			break;
		}
		case U_c_f_dsch_SRDM: {
			sp->u.SRDM = (c_f_dsch_SRDM*)EDAlloc (sizeof (c_f_dsch_SRDM));
			INIT_c_f_dsch_SRDM (sp->u.SRDM);

			break;
		}
		case U_c_f_dsch_SRPM: {
			sp->u.SRPM = (c_f_dsch_SRPM*)EDAlloc (sizeof (c_f_dsch_SRPM));
			INIT_c_f_dsch_SRPM (sp->u.SRPM);

			break;
		}
		case U_c_f_dsch_SRQM: {
			sp->u.SRQM = (c_f_dsch_SRQM*)EDAlloc (sizeof (c_f_dsch_SRQM));
			INIT_c_f_dsch_SRQM (sp->u.SRQM);

			break;
		}
		case U_c_f_dsch_SSDUM: {
			sp->u.SSDUM = (c_f_dsch_SSDUM*)EDAlloc (sizeof (c_f_dsch_SSDUM));
			INIT_c_f_dsch_SSDUM (sp->u.SSDUM);

			break;
		}
		case U_c_f_dsch_STPM: {
			sp->u.STPM = (c_f_dsch_STPM*)EDAlloc (sizeof (c_f_dsch_STPM));
			INIT_c_f_dsch_STPM (sp->u.STPM);

			break;
		}
		case U_c_f_dsch_STRQM: {
			sp->u.STRQM = (c_f_dsch_STRQM*)EDAlloc (sizeof (c_f_dsch_STRQM));
			INIT_c_f_dsch_STRQM (sp->u.STRQM);

			break;
		}
		case U_c_f_dsch_TASM: {
			sp->u.TASM = (c_f_dsch_TASM*)EDAlloc (sizeof (c_f_dsch_TASM));
			INIT_c_f_dsch_TASM (sp->u.TASM);

			break;
		}
		case U_c_f_dsch_UHDM: {
			sp->u.UHDM = (c_f_dsch_UHDM*)EDAlloc (sizeof (c_f_dsch_UHDM));
			INIT_c_f_dsch_UHDM (sp->u.UHDM);

			break;
		}
		case U_c_f_dsch_UZRM: {
			sp->u.UZRM = (c_f_dsch_UZRM*)EDAlloc (sizeof (c_f_dsch_UZRM));
			INIT_c_f_dsch_UZRM (sp->u.UZRM);

			break;
		}
		case U_c_f_dsch_UZUM: {
			sp->u.UZUM = (c_f_dsch_UZUM*)EDAlloc (sizeof (c_f_dsch_UZUM));
			INIT_c_f_dsch_UZUM (sp->u.UZUM);

			break;
		}
		default:;	}
}

/* Free function for union c_f_dsch_mini3 */
void FREE_c_f_dsch_mini3 (c_f_dsch_mini3* sp) {
	switch (sp->Present) {
		case U_c_f_dsch_mini3_ERMM: {
			FREE_c_f_dsch_ERMM (sp->u.ERMM);
			EDFree (sp->u.ERMM);

			break;
		}
		case U_c_f_dsch_mini3_FSCAMM: {
			FREE_c_f_dsch_FSCAMM (sp->u.FSCAMM);
			EDFree (sp->u.FSCAMM);

			break;
		}
		case U_c_f_dsch_mini3_RAMM: {
			FREE_c_f_dsch_RAMM (sp->u.RAMM);
			EDFree (sp->u.RAMM);

			break;
		}
		case U_c_f_dsch_mini3_RSCAMM: {
			FREE_c_f_dsch_RSCAMM (sp->u.RSCAMM);
			EDFree (sp->u.RSCAMM);

			break;
		}

		default:;
	}
	sp->Present = U_c_f_dsch_mini3_NONE;
}

/* Set-present function for c_f_dsch_mini3 LEVEL = -1 */
void GLOB_SETPRESENT_c_f_dsch_mini3 (c_f_dsch_mini3* sp, TPRESENT_c_f_dsch_mini3 toBeSetPresent) 
{
	FREE_c_f_dsch_mini3 (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_f_dsch_mini3_ERMM: {
			sp->u.ERMM = (c_f_dsch_ERMM*)EDAlloc (sizeof (c_f_dsch_ERMM));
			INIT_c_f_dsch_ERMM (sp->u.ERMM);

			break;
		}
		case U_c_f_dsch_mini3_FSCAMM: {
			sp->u.FSCAMM = (c_f_dsch_FSCAMM*)EDAlloc (sizeof (c_f_dsch_FSCAMM));
			INIT_c_f_dsch_FSCAMM (sp->u.FSCAMM);

			break;
		}
		case U_c_f_dsch_mini3_RAMM: {
			sp->u.RAMM = (c_f_dsch_RAMM*)EDAlloc (sizeof (c_f_dsch_RAMM));
			INIT_c_f_dsch_RAMM (sp->u.RAMM);

			break;
		}
		case U_c_f_dsch_mini3_RSCAMM: {
			sp->u.RSCAMM = (c_f_dsch_RSCAMM*)EDAlloc (sizeof (c_f_dsch_RSCAMM));
			INIT_c_f_dsch_RSCAMM (sp->u.RSCAMM);

			break;
		}
		default:;	}
}

/* Free function for union c_r_csch_mini6 */
void FREE_c_r_csch_mini6 (c_r_csch_mini6* sp) {
	switch (sp->Present) {
		case U_c_r_csch_mini6_AUCRM: {
			FREE_c_r_csch_AUCRM (sp->u.AUCRM);
			EDFree (sp->u.AUCRM);

			break;
		}
		case U_c_r_csch_mini6_AURSPM: {
			FREE_c_r_csch_AURSPM (sp->u.AURSPM);
			EDFree (sp->u.AURSPM);

			break;
		}
		case U_c_r_csch_mini6_AURSYNM: {
			FREE_c_r_csch_AURSYNM (sp->u.AURSYNM);
			EDFree (sp->u.AURSYNM);

			break;
		}
		case U_c_r_csch_mini6_CRRM: {
			FREE_c_r_csch_CRRM (sp->u.CRRM);
			EDFree (sp->u.CRRM);

			break;
		}
		case U_c_r_csch_mini6_DBM: {
			FREE_c_r_csch_DBM (sp->u.DBM);
			EDFree (sp->u.DBM);

			break;
		}
		case U_c_r_csch_mini6_DIM: {
			FREE_c_r_csch_DIM (sp->u.DIM);
			EDFree (sp->u.DIM);

			break;
		}
		case U_c_r_csch_mini6_ESTRPM: {
			FREE_c_r_csch_ESTRPM (sp->u.ESTRPM);
			EDFree (sp->u.ESTRPM);

			break;
		}
		case U_c_r_csch_mini6_GEM: {
			FREE_c_r_csch_GEM (sp->u.GEM);
			EDFree (sp->u.GEM);

			break;
		}
		case U_c_r_csch_mini6_ORDM: {
			FREE_c_r_csch_ORDM (sp->u.ORDM);
			EDFree (sp->u.ORDM);

			break;
		}
		case U_c_r_csch_mini6_ORM: {
			FREE_c_r_csch_ORM (sp->u.ORM);
			EDFree (sp->u.ORM);

			break;
		}
		case U_c_r_csch_mini6_PACNM: {
			FREE_c_r_csch_PACNM (sp->u.PACNM);
			EDFree (sp->u.PACNM);

			break;
		}
		case U_c_r_csch_mini6_PRM: {
			FREE_c_r_csch_PRM (sp->u.PRM);
			EDFree (sp->u.PRM);

			break;
		}
		case U_c_r_csch_mini6_RCNM: {
			FREE_c_r_csch_RCNM (sp->u.RCNM);
			EDFree (sp->u.RCNM);

			break;
		}
		case U_c_r_csch_mini6_REM: {
			FREE_c_r_csch_REM (sp->u.REM);
			EDFree (sp->u.REM);

			break;
		}
		case U_c_r_csch_mini6_RGM: {
			FREE_c_r_csch_RGM (sp->u.RGM);
			EDFree (sp->u.RGM);

			break;
		}
		case U_c_r_csch_mini6_SMRM: {
			FREE_c_r_csch_SMRM (sp->u.SMRM);
			EDFree (sp->u.SMRM);

			break;
		}
		case U_c_r_csch_mini6_STRPM: {
			FREE_c_r_csch_STRPM (sp->u.STRPM);
			EDFree (sp->u.STRPM);

			break;
		}
		case U_c_r_csch_mini6_TACM: {
			FREE_c_r_csch_TACM (sp->u.TACM);
			EDFree (sp->u.TACM);

			break;
		}

		default:;
	}
	sp->Present = U_c_r_csch_mini6_NONE;
}

/* Set-present function for c_r_csch_mini6 LEVEL = -1 */
void GLOB_SETPRESENT_c_r_csch_mini6 (c_r_csch_mini6* sp, TPRESENT_c_r_csch_mini6 toBeSetPresent) 
{
	FREE_c_r_csch_mini6 (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_r_csch_mini6_AUCRM: {
			sp->u.AUCRM = (c_r_csch_AUCRM*)EDAlloc (sizeof (c_r_csch_AUCRM));
			INIT_c_r_csch_AUCRM (sp->u.AUCRM);

			break;
		}
		case U_c_r_csch_mini6_AURSPM: {
			sp->u.AURSPM = (c_r_csch_AURSPM*)EDAlloc (sizeof (c_r_csch_AURSPM));
			INIT_c_r_csch_AURSPM (sp->u.AURSPM);

			break;
		}
		case U_c_r_csch_mini6_AURSYNM: {
			sp->u.AURSYNM = (c_r_csch_AURSYNM*)EDAlloc (sizeof (c_r_csch_AURSYNM));
			INIT_c_r_csch_AURSYNM (sp->u.AURSYNM);

			break;
		}
		case U_c_r_csch_mini6_CRRM: {
			sp->u.CRRM = (c_r_csch_CRRM*)EDAlloc (sizeof (c_r_csch_CRRM));
			INIT_c_r_csch_CRRM (sp->u.CRRM);

			break;
		}
		case U_c_r_csch_mini6_DBM: {
			sp->u.DBM = (c_r_csch_DBM*)EDAlloc (sizeof (c_r_csch_DBM));
			INIT_c_r_csch_DBM (sp->u.DBM);

			break;
		}
		case U_c_r_csch_mini6_DIM: {
			sp->u.DIM = (c_r_csch_DIM*)EDAlloc (sizeof (c_r_csch_DIM));
			INIT_c_r_csch_DIM (sp->u.DIM);

			break;
		}
		case U_c_r_csch_mini6_ESTRPM: {
			sp->u.ESTRPM = (c_r_csch_ESTRPM*)EDAlloc (sizeof (c_r_csch_ESTRPM));
			INIT_c_r_csch_ESTRPM (sp->u.ESTRPM);

			break;
		}
		case U_c_r_csch_mini6_GEM: {
			sp->u.GEM = (c_r_csch_GEM*)EDAlloc (sizeof (c_r_csch_GEM));
			INIT_c_r_csch_GEM (sp->u.GEM);

			break;
		}
		case U_c_r_csch_mini6_ORDM: {
			sp->u.ORDM = (c_r_csch_ORDM*)EDAlloc (sizeof (c_r_csch_ORDM));
			INIT_c_r_csch_ORDM (sp->u.ORDM);

			break;
		}
		case U_c_r_csch_mini6_ORM: {
			sp->u.ORM = (c_r_csch_ORM*)EDAlloc (sizeof (c_r_csch_ORM));
			INIT_c_r_csch_ORM (sp->u.ORM);

			break;
		}
		case U_c_r_csch_mini6_PACNM: {
			sp->u.PACNM = (c_r_csch_PACNM*)EDAlloc (sizeof (c_r_csch_PACNM));
			INIT_c_r_csch_PACNM (sp->u.PACNM);

			break;
		}
		case U_c_r_csch_mini6_PRM: {
			sp->u.PRM = (c_r_csch_PRM*)EDAlloc (sizeof (c_r_csch_PRM));
			INIT_c_r_csch_PRM (sp->u.PRM);

			break;
		}
		case U_c_r_csch_mini6_RCNM: {
			sp->u.RCNM = (c_r_csch_RCNM*)EDAlloc (sizeof (c_r_csch_RCNM));
			INIT_c_r_csch_RCNM (sp->u.RCNM);

			break;
		}
		case U_c_r_csch_mini6_REM: {
			sp->u.REM = (c_r_csch_REM*)EDAlloc (sizeof (c_r_csch_REM));
			INIT_c_r_csch_REM (sp->u.REM);

			break;
		}
		case U_c_r_csch_mini6_RGM: {
			sp->u.RGM = (c_r_csch_RGM*)EDAlloc (sizeof (c_r_csch_RGM));
			INIT_c_r_csch_RGM (sp->u.RGM);

			break;
		}
		case U_c_r_csch_mini6_SMRM: {
			sp->u.SMRM = (c_r_csch_SMRM*)EDAlloc (sizeof (c_r_csch_SMRM));
			INIT_c_r_csch_SMRM (sp->u.SMRM);

			break;
		}
		case U_c_r_csch_mini6_STRPM: {
			sp->u.STRPM = (c_r_csch_STRPM*)EDAlloc (sizeof (c_r_csch_STRPM));
			INIT_c_r_csch_STRPM (sp->u.STRPM);

			break;
		}
		case U_c_r_csch_mini6_TACM: {
			sp->u.TACM = (c_r_csch_TACM*)EDAlloc (sizeof (c_r_csch_TACM));
			INIT_c_r_csch_TACM (sp->u.TACM);

			break;
		}
		default:;	}
}

/* Free function for union c_r_dsch */
void FREE_c_r_dsch (c_r_dsch* sp) {
	switch (sp->Present) {
		case U_c_r_dsch_AUCRM: {
			FREE_c_r_dsch_AUCRM (sp->u.AUCRM);
			EDFree (sp->u.AUCRM);

			break;
		}
		case U_c_r_dsch_AURSPM: {
			FREE_c_r_dsch_AURSPM (sp->u.AURSPM);
			EDFree (sp->u.AURSPM);

			break;
		}
		case U_c_r_dsch_AURSYNM: {
			FREE_c_r_dsch_AURSYNM (sp->u.AURSYNM);
			EDFree (sp->u.AURSYNM);

			break;
		}
		case U_c_r_dsch_BDTMFM: {
			FREE_c_r_dsch_BDTMFM (sp->u.BDTMFM);
			EDFree (sp->u.BDTMFM);

			break;
		}
		case U_c_r_dsch_BSSREQM: {
			FREE_c_r_dsch_BSSREQM (sp->u.BSSREQM);
			EDFree (sp->u.BSSREQM);

			break;
		}
		case U_c_r_dsch_CFSRPM: {
			FREE_c_r_dsch_CFSRPM (sp->u.CFSRPM);
			EDFree (sp->u.CFSRPM);

			break;
		}
		case U_c_r_dsch_CFSRSM: {
			FREE_c_r_dsch_CFSRSM (sp->u.CFSRSM);
			EDFree (sp->u.CFSRSM);

			break;
		}
		case U_c_r_dsch_CLCM: {
			FREE_c_r_dsch_CLCM (sp->u.CLCM);
			EDFree (sp->u.CLCM);

			break;
		}
		case U_c_r_dsch_COTRM: {
			FREE_c_r_dsch_COTRM (sp->u.COTRM);
			EDFree (sp->u.COTRM);

			break;
		}
		case U_c_r_dsch_DBM: {
			FREE_c_r_dsch_DBM (sp->u.DBM);
			EDFree (sp->u.DBM);

			break;
		}
		case U_c_r_dsch_DIM: {
			FREE_c_r_dsch_DIM (sp->u.DIM);
			EDFree (sp->u.DIM);

			break;
		}
		case U_c_r_dsch_EFWIM: {
			FREE_c_r_dsch_EFWIM (sp->u.EFWIM);
			EDFree (sp->u.EFWIM);

			break;
		}
		case U_c_r_dsch_EHOCM: {
			FREE_c_r_dsch_EHOCM (sp->u.EHOCM);
			EDFree (sp->u.EHOCM);

			break;
		}
		case U_c_r_dsch_EOM: {
			FREE_c_r_dsch_EOM (sp->u.EOM);
			EDFree (sp->u.EOM);

			break;
		}
		case U_c_r_dsch_EPSMM: {
			FREE_c_r_dsch_EPSMM (sp->u.EPSMM);
			EDFree (sp->u.EPSMM);

			break;
		}
		case U_c_r_dsch_ERRM: {
			FREE_c_r_dsch_ERRM (sp->u.ERRM);
			EDFree (sp->u.ERRM);

			break;
		}
		case U_c_r_dsch_FWIM: {
			FREE_c_r_dsch_FWIM (sp->u.FWIM);
			EDFree (sp->u.FWIM);

			break;
		}
		case U_c_r_dsch_GEM: {
			FREE_c_r_dsch_GEM (sp->u.GEM);
			EDFree (sp->u.GEM);

			break;
		}
		case U_c_r_dsch_HOCM: {
			FREE_c_r_dsch_HOCM (sp->u.HOCM);
			EDFree (sp->u.HOCM);

			break;
		}
		case U_c_r_dsch_HOSINM: {
			FREE_c_r_dsch_HOSINM (sp->u.HOSINM);
			EDFree (sp->u.HOSINM);

			break;
		}
		case U_c_r_dsch_ITBSPMRM: {
			FREE_c_r_dsch_ITBSPMRM (sp->u.ITBSPMRM);
			EDFree (sp->u.ITBSPMRM);

			break;
		}
		case U_c_r_dsch_OLRM: {
			FREE_c_r_dsch_OLRM (sp->u.OLRM);
			EDFree (sp->u.OLRM);

			break;
		}
		case U_c_r_dsch_ORCM: {
			FREE_c_r_dsch_ORCM (sp->u.ORCM);
			EDFree (sp->u.ORCM);

			break;
		}
		case U_c_r_dsch_ORDM: {
			FREE_c_r_dsch_ORDM (sp->u.ORDM);
			EDFree (sp->u.ORDM);

			break;
		}
		case U_c_r_dsch_PMRM: {
			FREE_c_r_dsch_PMRM (sp->u.PMRM);
			EDFree (sp->u.PMRM);

			break;
		}
		case U_c_r_dsch_PPSMM: {
			FREE_c_r_dsch_PPSMM (sp->u.PPSMM);
			EDFree (sp->u.PPSMM);

			break;
		}
		case U_c_r_dsch_PRSM: {
			FREE_c_r_dsch_PRSM (sp->u.PRSM);
			EDFree (sp->u.PRSM);

			break;
		}
		case U_c_r_dsch_PSMM: {
			FREE_c_r_dsch_PSMM (sp->u.PSMM);
			EDFree (sp->u.PSMM);

			break;
		}
		case U_c_r_dsch_RRM: {
			FREE_c_r_dsch_RRM (sp->u.RRM);
			EDFree (sp->u.RRM);

			break;
		}
		case U_c_r_dsch_RRRM: {
			FREE_c_r_dsch_RRRM (sp->u.RRRM);
			EDFree (sp->u.RRRM);

			break;
		}
		case U_c_r_dsch_SCCM: {
			FREE_c_r_dsch_SCCM (sp->u.SCCM);
			EDFree (sp->u.SCCM);

			break;
		}
		case U_c_r_dsch_SCRM: {
			FREE_c_r_dsch_SCRM (sp->u.SCRM);
			EDFree (sp->u.SCRM);

			break;
		}
		case U_c_r_dsch_SMRM: {
			FREE_c_r_dsch_SMRM (sp->u.SMRM);
			EDFree (sp->u.SMRM);

			break;
		}
		case U_c_r_dsch_SOCM: {
			FREE_c_r_dsch_SOCM (sp->u.SOCM);
			EDFree (sp->u.SOCM);

			break;
		}
		case U_c_r_dsch_SRPM: {
			FREE_c_r_dsch_SRPM (sp->u.SRPM);
			EDFree (sp->u.SRPM);

			break;
		}
		case U_c_r_dsch_SRQM: {
			FREE_c_r_dsch_SRQM (sp->u.SRQM);
			EDFree (sp->u.SRQM);

			break;
		}
		case U_c_r_dsch_STM: {
			FREE_c_r_dsch_STM (sp->u.STM);
			EDFree (sp->u.STM);

			break;
		}
		case U_c_r_dsch_STRPM: {
			FREE_c_r_dsch_STRPM (sp->u.STRPM);
			EDFree (sp->u.STRPM);

			break;
		}
		case U_c_r_dsch_TACM: {
			FREE_c_r_dsch_TACM (sp->u.TACM);
			EDFree (sp->u.TACM);

			break;
		}
		case U_c_r_dsch_UZURM: {
			FREE_c_r_dsch_UZURM (sp->u.UZURM);
			EDFree (sp->u.UZURM);

			break;
		}

		default:;
	}
	sp->Present = U_c_r_dsch_NONE;
}

/* Set-present function for c_r_dsch LEVEL = -1 */
void GLOB_SETPRESENT_c_r_dsch (c_r_dsch* sp, TPRESENT_c_r_dsch toBeSetPresent) 
{
	FREE_c_r_dsch (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_r_dsch_AUCRM: {
			sp->u.AUCRM = (c_r_dsch_AUCRM*)EDAlloc (sizeof (c_r_dsch_AUCRM));
			INIT_c_r_dsch_AUCRM (sp->u.AUCRM);

			break;
		}
		case U_c_r_dsch_AURSPM: {
			sp->u.AURSPM = (c_r_dsch_AURSPM*)EDAlloc (sizeof (c_r_dsch_AURSPM));
			INIT_c_r_dsch_AURSPM (sp->u.AURSPM);

			break;
		}
		case U_c_r_dsch_AURSYNM: {
			sp->u.AURSYNM = (c_r_dsch_AURSYNM*)EDAlloc (sizeof (c_r_dsch_AURSYNM));
			INIT_c_r_dsch_AURSYNM (sp->u.AURSYNM);

			break;
		}
		case U_c_r_dsch_BDTMFM: {
			sp->u.BDTMFM = (c_r_dsch_BDTMFM*)EDAlloc (sizeof (c_r_dsch_BDTMFM));
			INIT_c_r_dsch_BDTMFM (sp->u.BDTMFM);

			break;
		}
		case U_c_r_dsch_BSSREQM: {
			sp->u.BSSREQM = (c_r_dsch_BSSREQM*)EDAlloc (sizeof (c_r_dsch_BSSREQM));
			INIT_c_r_dsch_BSSREQM (sp->u.BSSREQM);

			break;
		}
		case U_c_r_dsch_CFSRPM: {
			sp->u.CFSRPM = (c_r_dsch_CFSRPM*)EDAlloc (sizeof (c_r_dsch_CFSRPM));
			INIT_c_r_dsch_CFSRPM (sp->u.CFSRPM);

			break;
		}
		case U_c_r_dsch_CFSRSM: {
			sp->u.CFSRSM = (c_r_dsch_CFSRSM*)EDAlloc (sizeof (c_r_dsch_CFSRSM));
			INIT_c_r_dsch_CFSRSM (sp->u.CFSRSM);

			break;
		}
		case U_c_r_dsch_CLCM: {
			sp->u.CLCM = (c_r_dsch_CLCM*)EDAlloc (sizeof (c_r_dsch_CLCM));
			INIT_c_r_dsch_CLCM (sp->u.CLCM);

			break;
		}
		case U_c_r_dsch_COTRM: {
			sp->u.COTRM = (c_r_dsch_COTRM*)EDAlloc (sizeof (c_r_dsch_COTRM));
			INIT_c_r_dsch_COTRM (sp->u.COTRM);

			break;
		}
		case U_c_r_dsch_DBM: {
			sp->u.DBM = (c_r_dsch_DBM*)EDAlloc (sizeof (c_r_dsch_DBM));
			INIT_c_r_dsch_DBM (sp->u.DBM);

			break;
		}
		case U_c_r_dsch_DIM: {
			sp->u.DIM = (c_r_dsch_DIM*)EDAlloc (sizeof (c_r_dsch_DIM));
			INIT_c_r_dsch_DIM (sp->u.DIM);

			break;
		}
		case U_c_r_dsch_EFWIM: {
			sp->u.EFWIM = (c_r_dsch_EFWIM*)EDAlloc (sizeof (c_r_dsch_EFWIM));
			INIT_c_r_dsch_EFWIM (sp->u.EFWIM);

			break;
		}
		case U_c_r_dsch_EHOCM: {
			sp->u.EHOCM = (c_r_dsch_EHOCM*)EDAlloc (sizeof (c_r_dsch_EHOCM));
			INIT_c_r_dsch_EHOCM (sp->u.EHOCM);

			break;
		}
		case U_c_r_dsch_EOM: {
			sp->u.EOM = (c_r_dsch_EOM*)EDAlloc (sizeof (c_r_dsch_EOM));
			INIT_c_r_dsch_EOM (sp->u.EOM);

			break;
		}
		case U_c_r_dsch_EPSMM: {
			sp->u.EPSMM = (c_r_dsch_EPSMM*)EDAlloc (sizeof (c_r_dsch_EPSMM));
			INIT_c_r_dsch_EPSMM (sp->u.EPSMM);

			break;
		}
		case U_c_r_dsch_ERRM: {
			sp->u.ERRM = (c_r_dsch_ERRM*)EDAlloc (sizeof (c_r_dsch_ERRM));
			INIT_c_r_dsch_ERRM (sp->u.ERRM);

			break;
		}
		case U_c_r_dsch_FWIM: {
			sp->u.FWIM = (c_r_dsch_FWIM*)EDAlloc (sizeof (c_r_dsch_FWIM));
			INIT_c_r_dsch_FWIM (sp->u.FWIM);

			break;
		}
		case U_c_r_dsch_GEM: {
			sp->u.GEM = (c_r_dsch_GEM*)EDAlloc (sizeof (c_r_dsch_GEM));
			INIT_c_r_dsch_GEM (sp->u.GEM);

			break;
		}
		case U_c_r_dsch_HOCM: {
			sp->u.HOCM = (c_r_dsch_HOCM*)EDAlloc (sizeof (c_r_dsch_HOCM));
			INIT_c_r_dsch_HOCM (sp->u.HOCM);

			break;
		}
		case U_c_r_dsch_HOSINM: {
			sp->u.HOSINM = (c_r_dsch_HOSINM*)EDAlloc (sizeof (c_r_dsch_HOSINM));
			INIT_c_r_dsch_HOSINM (sp->u.HOSINM);

			break;
		}
		case U_c_r_dsch_ITBSPMRM: {
			sp->u.ITBSPMRM = (c_r_dsch_ITBSPMRM*)EDAlloc (sizeof (c_r_dsch_ITBSPMRM));
			INIT_c_r_dsch_ITBSPMRM (sp->u.ITBSPMRM);

			break;
		}
		case U_c_r_dsch_OLRM: {
			sp->u.OLRM = (c_r_dsch_OLRM*)EDAlloc (sizeof (c_r_dsch_OLRM));
			INIT_c_r_dsch_OLRM (sp->u.OLRM);

			break;
		}
		case U_c_r_dsch_ORCM: {
			sp->u.ORCM = (c_r_dsch_ORCM*)EDAlloc (sizeof (c_r_dsch_ORCM));
			INIT_c_r_dsch_ORCM (sp->u.ORCM);

			break;
		}
		case U_c_r_dsch_ORDM: {
			sp->u.ORDM = (c_r_dsch_ORDM*)EDAlloc (sizeof (c_r_dsch_ORDM));
			INIT_c_r_dsch_ORDM (sp->u.ORDM);

			break;
		}
		case U_c_r_dsch_PMRM: {
			sp->u.PMRM = (c_r_dsch_PMRM*)EDAlloc (sizeof (c_r_dsch_PMRM));
			INIT_c_r_dsch_PMRM (sp->u.PMRM);

			break;
		}
		case U_c_r_dsch_PPSMM: {
			sp->u.PPSMM = (c_r_dsch_PPSMM*)EDAlloc (sizeof (c_r_dsch_PPSMM));
			INIT_c_r_dsch_PPSMM (sp->u.PPSMM);

			break;
		}
		case U_c_r_dsch_PRSM: {
			sp->u.PRSM = (c_r_dsch_PRSM*)EDAlloc (sizeof (c_r_dsch_PRSM));
			INIT_c_r_dsch_PRSM (sp->u.PRSM);

			break;
		}
		case U_c_r_dsch_PSMM: {
			sp->u.PSMM = (c_r_dsch_PSMM*)EDAlloc (sizeof (c_r_dsch_PSMM));
			INIT_c_r_dsch_PSMM (sp->u.PSMM);

			break;
		}
		case U_c_r_dsch_RRM: {
			sp->u.RRM = (c_r_dsch_RRM*)EDAlloc (sizeof (c_r_dsch_RRM));
			INIT_c_r_dsch_RRM (sp->u.RRM);

			break;
		}
		case U_c_r_dsch_RRRM: {
			sp->u.RRRM = (c_r_dsch_RRRM*)EDAlloc (sizeof (c_r_dsch_RRRM));
			INIT_c_r_dsch_RRRM (sp->u.RRRM);

			break;
		}
		case U_c_r_dsch_SCCM: {
			sp->u.SCCM = (c_r_dsch_SCCM*)EDAlloc (sizeof (c_r_dsch_SCCM));
			INIT_c_r_dsch_SCCM (sp->u.SCCM);

			break;
		}
		case U_c_r_dsch_SCRM: {
			sp->u.SCRM = (c_r_dsch_SCRM*)EDAlloc (sizeof (c_r_dsch_SCRM));
			INIT_c_r_dsch_SCRM (sp->u.SCRM);

			break;
		}
		case U_c_r_dsch_SMRM: {
			sp->u.SMRM = (c_r_dsch_SMRM*)EDAlloc (sizeof (c_r_dsch_SMRM));
			INIT_c_r_dsch_SMRM (sp->u.SMRM);

			break;
		}
		case U_c_r_dsch_SOCM: {
			sp->u.SOCM = (c_r_dsch_SOCM*)EDAlloc (sizeof (c_r_dsch_SOCM));
			INIT_c_r_dsch_SOCM (sp->u.SOCM);

			break;
		}
		case U_c_r_dsch_SRPM: {
			sp->u.SRPM = (c_r_dsch_SRPM*)EDAlloc (sizeof (c_r_dsch_SRPM));
			INIT_c_r_dsch_SRPM (sp->u.SRPM);

			break;
		}
		case U_c_r_dsch_SRQM: {
			sp->u.SRQM = (c_r_dsch_SRQM*)EDAlloc (sizeof (c_r_dsch_SRQM));
			INIT_c_r_dsch_SRQM (sp->u.SRQM);

			break;
		}
		case U_c_r_dsch_STM: {
			sp->u.STM = (c_r_dsch_STM*)EDAlloc (sizeof (c_r_dsch_STM));
			INIT_c_r_dsch_STM (sp->u.STM);

			break;
		}
		case U_c_r_dsch_STRPM: {
			sp->u.STRPM = (c_r_dsch_STRPM*)EDAlloc (sizeof (c_r_dsch_STRPM));
			INIT_c_r_dsch_STRPM (sp->u.STRPM);

			break;
		}
		case U_c_r_dsch_TACM: {
			sp->u.TACM = (c_r_dsch_TACM*)EDAlloc (sizeof (c_r_dsch_TACM));
			INIT_c_r_dsch_TACM (sp->u.TACM);

			break;
		}
		case U_c_r_dsch_UZURM: {
			sp->u.UZURM = (c_r_dsch_UZURM*)EDAlloc (sizeof (c_r_dsch_UZURM));
			INIT_c_r_dsch_UZURM (sp->u.UZURM);

			break;
		}
		default:;	}
}

/* Free function for union c_r_dsch_mini3 */
void FREE_c_r_dsch_mini3 (c_r_dsch_mini3* sp) {
	switch (sp->Present) {
		case U_c_r_dsch_mini3_PSMMM: {
			FREE_c_r_dsch_PSMMM (sp->u.PSMMM);
			EDFree (sp->u.PSMMM);

			break;
		}
		case U_c_r_dsch_mini3_SCRMM: {
			FREE_c_r_dsch_SCRMM (sp->u.SCRMM);
			EDFree (sp->u.SCRMM);

			break;
		}

		default:;
	}
	sp->Present = U_c_r_dsch_mini3_NONE;
}

/* Set-present function for c_r_dsch_mini3 LEVEL = -1 */
void GLOB_SETPRESENT_c_r_dsch_mini3 (c_r_dsch_mini3* sp, TPRESENT_c_r_dsch_mini3 toBeSetPresent) 
{
	FREE_c_r_dsch_mini3 (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_r_dsch_mini3_PSMMM: {
			sp->u.PSMMM = (c_r_dsch_PSMMM*)EDAlloc (sizeof (c_r_dsch_PSMMM));
			INIT_c_r_dsch_PSMMM (sp->u.PSMMM);

			break;
		}
		case U_c_r_dsch_mini3_SCRMM: {
			sp->u.SCRMM = (c_r_dsch_SCRMM*)EDAlloc (sizeof (c_r_dsch_SCRMM));
			INIT_c_r_dsch_SCRMM (sp->u.SCRMM);

			break;
		}
		default:;	}
}

/* Free function for union c_r_dsch_mini6 */
void FREE_c_r_dsch_mini6 (c_r_dsch_mini6* sp) {
	switch (sp->Present) {
		case U_c_r_dsch_mini6_ERRMM: {
			FREE_c_r_dsch_ERRMM (sp->u.ERRMM);
			EDFree (sp->u.ERRMM);

			break;
		}
		case U_c_r_dsch_mini6_RRMM: {
			FREE_c_r_dsch_RRMM (sp->u.RRMM);
			EDFree (sp->u.RRMM);

			break;
		}
		case U_c_r_dsch_mini6_RRRMM: {
			FREE_c_r_dsch_RRRMM (sp->u.RRRMM);
			EDFree (sp->u.RRRMM);

			break;
		}

		default:;
	}
	sp->Present = U_c_r_dsch_mini6_NONE;
}

/* Set-present function for c_r_dsch_mini6 LEVEL = -1 */
void GLOB_SETPRESENT_c_r_dsch_mini6 (c_r_dsch_mini6* sp, TPRESENT_c_r_dsch_mini6 toBeSetPresent) 
{
	FREE_c_r_dsch_mini6 (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_r_dsch_mini6_ERRMM: {
			sp->u.ERRMM = (c_r_dsch_ERRMM*)EDAlloc (sizeof (c_r_dsch_ERRMM));
			INIT_c_r_dsch_ERRMM (sp->u.ERRMM);

			break;
		}
		case U_c_r_dsch_mini6_RRMM: {
			sp->u.RRMM = (c_r_dsch_RRMM*)EDAlloc (sizeof (c_r_dsch_RRMM));
			INIT_c_r_dsch_RRMM (sp->u.RRMM);

			break;
		}
		case U_c_r_dsch_mini6_RRRMM: {
			sp->u.RRRMM = (c_r_dsch_RRRMM*)EDAlloc (sizeof (c_r_dsch_RRRMM));
			INIT_c_r_dsch_RRRMM (sp->u.RRRMM);

			break;
		}
		default:;	}
}

/* Free function for struct c_GenericTLV_cs0005 */
void FREE_c_GenericTLV_cs0005 (c_GenericTLV_cs0005* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->TLV_Data.value != NULL) {
		EDFree (sp->TLV_Data.value);
		sp->TLV_Data.value = NULL;
	}
	sp->TLV_Data.usedBits = 0;

}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data_REV_PDCH_RETRX_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCCH_EP_SIZE_1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_RETRX_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1 (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data_infoRec1_Type_specific_fields_REV_PDCH_EP_SIZE_1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs'. Existing items are not changed. */
int SETITEMS_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data** tmp;
		tmp = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data**)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data));
			INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data_records_Type_specific_fields_c06_HRPD_NGHBRs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data_SUBCLASS_SUP'. Existing items are not changed. */
int SETITEMS_c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data_SUBCLASS_SUP (c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data_SUBCLASS_SUP* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_BOOLEAN* tmp;
		tmp = (ED_BOOLEAN*)EDAlloc (sizeof (ED_BOOLEAN) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_BOOLEAN) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data_PARTITIONSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data** tmp;
		tmp = (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data**)EDAlloc (sizeof (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data*)EDAlloc (sizeof (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data));
			INIT_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data** tmp;
		tmp = (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data**)EDAlloc (sizeof (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data*)EDAlloc (sizeof (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data));
			INIT_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data'. Existing items are not changed. */
int SETITEMS_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data (c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data** tmp;
		tmp = (c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data**)EDAlloc (sizeof (c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data*)EDAlloc (sizeof (c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data));
			INIT_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Channel_Configuration_Capability_Information_FOR_SCH_Type_specific_fields_FOR_SCH_Type_Specific_Fields_data_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data'. Existing items are not changed. */
int SETITEMS_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data (c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data** tmp;
		tmp = (c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data**)EDAlloc (sizeof (c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data*)EDAlloc (sizeof (c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data));
			INIT_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Channel_Configuration_Capability_Information_REV_SCH_Type_specific_fields_REV_SCH_Type_Specific_Fields_data_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Keypad_Facility_CHARi'. Existing items are not changed. */
int SETITEMS_c_RIR_Keypad_Facility_CHARi (c_RIR_Keypad_Facility_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Called_Party_Number_CHARi'. Existing items are not changed. */
int SETITEMS_c_RIR_Called_Party_Number_CHARi (c_RIR_Called_Party_Number_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Calling_Party_Number_CHARi'. Existing items are not changed. */
int SETITEMS_c_RIR_Calling_Party_Number_CHARi (c_RIR_Calling_Party_Number_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Terminal_Information_SERVICE_OPTION'. Existing items are not changed. */
int SETITEMS_c_RIR_Terminal_Information_SERVICE_OPTION (c_RIR_Terminal_Information_SERVICE_OPTION* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Roaming_Information_SID_NID'. Existing items are not changed. */
int SETITEMS_c_RIR_Roaming_Information_SID_NID (c_RIR_Roaming_Information_SID_NID* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Roaming_Information_SID_NID_data** tmp;
		tmp = (c_RIR_Roaming_Information_SID_NID_data**)EDAlloc (sizeof (c_RIR_Roaming_Information_SID_NID_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Roaming_Information_SID_NID_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Roaming_Information_SID_NID_data*)EDAlloc (sizeof (c_RIR_Roaming_Information_SID_NID_data));
			INIT_c_RIR_Roaming_Information_SID_NID_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Roaming_Information_SID_NID_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Connected_Number_CHARi'. Existing items are not changed. */
int SETITEMS_c_RIR_Connected_Number_CHARi (c_RIR_Connected_Number_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Service_Option_Information_info'. Existing items are not changed. */
int SETITEMS_c_RIR_Service_Option_Information_info (c_RIR_Service_Option_Information_info* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Service_Option_Information_Data** tmp;
		tmp = (c_RIR_Service_Option_Information_Data**)EDAlloc (sizeof (c_RIR_Service_Option_Information_Data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Service_Option_Information_Data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Service_Option_Information_Data*)EDAlloc (sizeof (c_RIR_Service_Option_Information_Data));
			INIT_c_RIR_Service_Option_Information_Data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Service_Option_Information_Data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Multiplex_Option_Information_info'. Existing items are not changed. */
int SETITEMS_c_RIR_Multiplex_Option_Information_info (c_RIR_Multiplex_Option_Information_info* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Multiplex_Option_Information_Data** tmp;
		tmp = (c_RIR_Multiplex_Option_Information_Data**)EDAlloc (sizeof (c_RIR_Multiplex_Option_Information_Data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Multiplex_Option_Information_Data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Multiplex_Option_Information_Data*)EDAlloc (sizeof (c_RIR_Multiplex_Option_Information_Data));
			INIT_c_RIR_Multiplex_Option_Information_Data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Multiplex_Option_Information_Data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Service_Configuration_Information_CON_RECs'. Existing items are not changed. */
int SETITEMS_c_RIR_Service_Configuration_Information_CON_RECs (c_RIR_Service_Configuration_Information_CON_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Service_Configuration_Information_CON_RECs_data** tmp;
		tmp = (c_RIR_Service_Configuration_Information_CON_RECs_data**)EDAlloc (sizeof (c_RIR_Service_Configuration_Information_CON_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Service_Configuration_Information_CON_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Service_Configuration_Information_CON_RECs_data*)EDAlloc (sizeof (c_RIR_Service_Configuration_Information_CON_RECs_data));
			INIT_c_RIR_Service_Configuration_Information_CON_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Service_Configuration_Information_CON_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Service_Configuration_Information_FOR_SCHs'. Existing items are not changed. */
int SETITEMS_c_RIR_Service_Configuration_Information_FOR_SCHs (c_RIR_Service_Configuration_Information_FOR_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Service_Configuration_Information_FOR_SCHs_data** tmp;
		tmp = (c_RIR_Service_Configuration_Information_FOR_SCHs_data**)EDAlloc (sizeof (c_RIR_Service_Configuration_Information_FOR_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Service_Configuration_Information_FOR_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Service_Configuration_Information_FOR_SCHs_data*)EDAlloc (sizeof (c_RIR_Service_Configuration_Information_FOR_SCHs_data));
			INIT_c_RIR_Service_Configuration_Information_FOR_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Service_Configuration_Information_FOR_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Service_Configuration_Information_REV_SCHs'. Existing items are not changed. */
int SETITEMS_c_RIR_Service_Configuration_Information_REV_SCHs (c_RIR_Service_Configuration_Information_REV_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Service_Configuration_Information_REV_SCHs_data** tmp;
		tmp = (c_RIR_Service_Configuration_Information_REV_SCHs_data**)EDAlloc (sizeof (c_RIR_Service_Configuration_Information_REV_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Service_Configuration_Information_REV_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Service_Configuration_Information_REV_SCHs_data*)EDAlloc (sizeof (c_RIR_Service_Configuration_Information_REV_SCHs_data));
			INIT_c_RIR_Service_Configuration_Information_REV_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Service_Configuration_Information_REV_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Called_Party_Subaddress_CHARi'. Existing items are not changed. */
int SETITEMS_c_RIR_Called_Party_Subaddress_CHARi (c_RIR_Called_Party_Subaddress_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Calling_Party_Subaddress_CHARi'. Existing items are not changed. */
int SETITEMS_c_RIR_Calling_Party_Subaddress_CHARi (c_RIR_Calling_Party_Subaddress_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Connected_Subaddress_CHARi'. Existing items are not changed. */
int SETITEMS_c_RIR_Connected_Subaddress_CHARi (c_RIR_Connected_Subaddress_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data** tmp;
		tmp = (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data**)EDAlloc (sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data*)EDAlloc (sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data));
			INIT_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_FCH_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH (c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data** tmp;
		tmp = (c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data**)EDAlloc (sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data*)EDAlloc (sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data));
			INIT_c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_FCH_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Multiplex_Option_Information_MO_FOR_DCCH'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_DCCH (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_DCCH* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Multiplex_Option_Information_MO_REV_DCCH'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Multiplex_Option_Information_MO_REV_DCCH (c_RIR_Extended_Multiplex_Option_Information_MO_REV_DCCH* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data** tmp;
		tmp = (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data**)EDAlloc (sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data*)EDAlloc (sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data));
			INIT_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_SCH_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH (c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data** tmp;
		tmp = (c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data**)EDAlloc (sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data*)EDAlloc (sizeof (c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data));
			INIT_c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Extended_Multiplex_Option_Information_MO_REV_SCH_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Multiplex_Option_Information_MO_FOR_PDCH'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Multiplex_Option_Information_MO_FOR_PDCH (c_RIR_Extended_Multiplex_Option_Information_MO_FOR_PDCH* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Multiplex_Option_Information_MO_REV_PDCH'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Multiplex_Option_Information_MO_REV_PDCH (c_RIR_Extended_Multiplex_Option_Information_MO_REV_PDCH* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Global_Emergency_Call_CHARi'. Existing items are not changed. */
int SETITEMS_c_RIR_Global_Emergency_Call_CHARi (c_RIR_Global_Emergency_Call_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Global_Emergency_Call_CHARi_data** tmp;
		tmp = (c_RIR_Global_Emergency_Call_CHARi_data**)EDAlloc (sizeof (c_RIR_Global_Emergency_Call_CHARi_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Global_Emergency_Call_CHARi_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Global_Emergency_Call_CHARi_data*)EDAlloc (sizeof (c_RIR_Global_Emergency_Call_CHARi_data));
			sequence->data[i]->value = NULL;
			sequence->data[i]->usedBits = 0;
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				if (sequence->data[i]->value != NULL) {
					EDFree (sequence->data[i]->value);
					sequence->data[i]->value = NULL;
				}
				sequence->data[i]->usedBits = 0;
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Keypad_Facility_CHARi'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Keypad_Facility_CHARi (c_RIR_Extended_Keypad_Facility_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_SYNC_ID_SYNC_IDs'. Existing items are not changed. */
int SETITEMS_c_RIR_SYNC_ID_SYNC_IDs (c_RIR_SYNC_ID_SYNC_IDs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_SYNC_ID_SYNC_IDs_data** tmp;
		tmp = (c_RIR_SYNC_ID_SYNC_IDs_data**)EDAlloc (sizeof (c_RIR_SYNC_ID_SYNC_IDs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_SYNC_ID_SYNC_IDs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_SYNC_ID_SYNC_IDs_data*)EDAlloc (sizeof (c_RIR_SYNC_ID_SYNC_IDs_data));
			INIT_c_RIR_SYNC_ID_SYNC_IDs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_SYNC_ID_SYNC_IDs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Terminal_Information_SERVICE_OPTION'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Terminal_Information_SERVICE_OPTION (c_RIR_Extended_Terminal_Information_SERVICE_OPTION* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Terminal_Information_SO_GROUP'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Terminal_Information_SO_GROUP (c_RIR_Extended_Terminal_Information_SO_GROUP* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Extended_Terminal_Information_SO_GROUP_data** tmp;
		tmp = (c_RIR_Extended_Terminal_Information_SO_GROUP_data**)EDAlloc (sizeof (c_RIR_Extended_Terminal_Information_SO_GROUP_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Extended_Terminal_Information_SO_GROUP_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Extended_Terminal_Information_SO_GROUP_data*)EDAlloc (sizeof (c_RIR_Extended_Terminal_Information_SO_GROUP_data));
			INIT_c_RIR_Extended_Terminal_Information_SO_GROUP_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Extended_Terminal_Information_SO_GROUP_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Service_Option_Information_SO_GROUP'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Service_Option_Information_SO_GROUP (c_RIR_Extended_Service_Option_Information_SO_GROUP* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Extended_Service_Option_Information_SO_GROUP_data** tmp;
		tmp = (c_RIR_Extended_Service_Option_Information_SO_GROUP_data**)EDAlloc (sizeof (c_RIR_Extended_Service_Option_Information_SO_GROUP_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Extended_Service_Option_Information_SO_GROUP_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Extended_Service_Option_Information_SO_GROUP_data*)EDAlloc (sizeof (c_RIR_Extended_Service_Option_Information_SO_GROUP_data));
			INIT_c_RIR_Extended_Service_Option_Information_SO_GROUP_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Extended_Service_Option_Information_SO_GROUP_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Extended_Service_Option_Information_supports'. Existing items are not changed. */
int SETITEMS_c_RIR_Extended_Service_Option_Information_supports (c_RIR_Extended_Service_Option_Information_supports* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Extended_Service_Option_Information_supports_data** tmp;
		tmp = (c_RIR_Extended_Service_Option_Information_supports_data**)EDAlloc (sizeof (c_RIR_Extended_Service_Option_Information_supports_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Extended_Service_Option_Information_supports_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Extended_Service_Option_Information_supports_data*)EDAlloc (sizeof (c_RIR_Extended_Service_Option_Information_supports_data));
			INIT_c_RIR_Extended_Service_Option_Information_supports_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Extended_Service_Option_Information_supports_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS'. Existing items are not changed. */
int SETITEMS_c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS (c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data** tmp;
		tmp = (c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data**)EDAlloc (sizeof (c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data*)EDAlloc (sizeof (c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data));
			INIT_c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_RIR_Band_Class_and_Band_Subclass_Information_BAND_CLASS_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Extended_Display_data_01_data_data_02'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Extended_Display_data_01_data_data_02 (c_InfoRec_Extended_Display_data_01_data_data_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Extended_Display_data_01_data_data_02_data** tmp;
		tmp = (c_InfoRec_Extended_Display_data_01_data_data_02_data**)EDAlloc (sizeof (c_InfoRec_Extended_Display_data_01_data_data_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Extended_Display_data_01_data_data_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Extended_Display_data_01_data_data_02_data*)EDAlloc (sizeof (c_InfoRec_Extended_Display_data_01_data_data_02_data));
			INIT_c_InfoRec_Extended_Display_data_01_data_data_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Extended_Display_data_01_data_data_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05 (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data_data_05_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07 (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data_data_07_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data_ROWSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data_ROWS_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data** tmp;
		tmp = (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data**)EDAlloc (sizeof (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data*)EDAlloc (sizeof (c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data));
			INIT_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data_RECORDs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data** tmp;
		tmp = (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data**)EDAlloc (sizeof (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data*)EDAlloc (sizeof (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data));
			INIT_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data_RECORDs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_Record_data_11'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_Record_data_11 (c_InfoRec_Non_Negotiable_Service_Configuration_Record_data_11* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_rec_data_11_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs'. Existing items are not changed. */
int SETITEMS_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data** tmp;
		tmp = (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data**)EDAlloc (sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data*)EDAlloc (sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data));
			INIT_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data_NGHBRs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* Free function for struct c_R_InformationRecords_p_generic */
void FREE_c_R_InformationRecords_p_generic (c_R_InformationRecords_p_generic* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->FieldData.value != NULL) {
		EDFree (sp->FieldData.value);
		sp->FieldData.value = NULL;
	}
	sp->FieldData.usedBits = 0;

}

/* SETITEMS commands for type 'c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs'. Existing items are not changed. */
int SETITEMS_c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data** tmp;
		tmp = (c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data**)EDAlloc (sizeof (c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*)EDAlloc (sizeof (c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data));
			INIT_c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_F_BCMC_Order_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4 (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs'. Existing items are not changed. */
int SETITEMS_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data** tmp;
		tmp = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data**)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data));
			INIT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data_UZIDs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_BAND'. Existing items are not changed. */
int SETITEMS_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_BAND (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_BAND* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_FREQ'. Existing items are not changed. */
int SETITEMS_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_FREQ (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data_NGHBR_FREQ* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1'. Existing items are not changed. */
int SETITEMS_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data** tmp;
		tmp = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data**)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data));
			INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2'. Existing items are not changed. */
int SETITEMS_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data** tmp;
		tmp = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data**)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data));
			INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c01_PILOTSs_2_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1'. Existing items are not changed. */
int SETITEMS_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data** tmp;
		tmp = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data**)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data));
			INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2'. Existing items are not changed. */
int SETITEMS_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data** tmp;
		tmp = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data**)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data));
			INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_2_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3'. Existing items are not changed. */
int SETITEMS_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data** tmp;
		tmp = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data**)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data));
			INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c02_PILOTSs_3_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1'. Existing items are not changed. */
int SETITEMS_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data** tmp;
		tmp = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data**)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data));
			INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2'. Existing items are not changed. */
int SETITEMS_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data** tmp;
		tmp = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data**)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data));
			INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_2_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3'. Existing items are not changed. */
int SETITEMS_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data** tmp;
		tmp = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data**)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data));
			INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_3_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4'. Existing items are not changed. */
int SETITEMS_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4 (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data** tmp;
		tmp = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data**)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data*)EDAlloc (sizeof (c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data));
			INIT_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MECAM_Additional_record_fields_c100_CH_RECORD_FIELDS_c03_PILOTSs_4_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Display_CHARi'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Display_CHARi (c_InfoRec_Display_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Called_Party_Number_CHARi'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Called_Party_Number_CHARi (c_InfoRec_Called_Party_Number_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Calling_Party_Number_CHARi'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Calling_Party_Number_CHARi (c_InfoRec_Calling_Party_Number_CHARi* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Connected_Number_data_01'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Connected_Number_data_01 (c_InfoRec_Connected_Number_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Connected_Number_data_01_data** tmp;
		tmp = (c_InfoRec_Connected_Number_data_01_data**)EDAlloc (sizeof (c_InfoRec_Connected_Number_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Connected_Number_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Connected_Number_data_01_data*)EDAlloc (sizeof (c_InfoRec_Connected_Number_data_01_data));
			INIT_c_InfoRec_Connected_Number_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Connected_Number_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Service_Configuration_CON_RECs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Service_Configuration_CON_RECs (c_InfoRec_Service_Configuration_CON_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Service_Configuration_CON_RECs_data** tmp;
		tmp = (c_InfoRec_Service_Configuration_CON_RECs_data**)EDAlloc (sizeof (c_InfoRec_Service_Configuration_CON_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Service_Configuration_CON_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Service_Configuration_CON_RECs_data*)EDAlloc (sizeof (c_InfoRec_Service_Configuration_CON_RECs_data));
			INIT_c_InfoRec_Service_Configuration_CON_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Service_Configuration_CON_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Service_Configuration_FOR_SCHs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Service_Configuration_FOR_SCHs (c_InfoRec_Service_Configuration_FOR_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Service_Configuration_FOR_SCHs_data** tmp;
		tmp = (c_InfoRec_Service_Configuration_FOR_SCHs_data**)EDAlloc (sizeof (c_InfoRec_Service_Configuration_FOR_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Service_Configuration_FOR_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Service_Configuration_FOR_SCHs_data*)EDAlloc (sizeof (c_InfoRec_Service_Configuration_FOR_SCHs_data));
			INIT_c_InfoRec_Service_Configuration_FOR_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Service_Configuration_FOR_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Service_Configuration_REV_SCHs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Service_Configuration_REV_SCHs (c_InfoRec_Service_Configuration_REV_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Service_Configuration_REV_SCHs_data** tmp;
		tmp = (c_InfoRec_Service_Configuration_REV_SCHs_data**)EDAlloc (sizeof (c_InfoRec_Service_Configuration_REV_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Service_Configuration_REV_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Service_Configuration_REV_SCHs_data*)EDAlloc (sizeof (c_InfoRec_Service_Configuration_REV_SCHs_data));
			INIT_c_InfoRec_Service_Configuration_REV_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Service_Configuration_REV_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Called_Party_Subaddress_data_01'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Called_Party_Subaddress_data_01 (c_InfoRec_Called_Party_Subaddress_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Called_Party_Subaddress_data_01_data** tmp;
		tmp = (c_InfoRec_Called_Party_Subaddress_data_01_data**)EDAlloc (sizeof (c_InfoRec_Called_Party_Subaddress_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Called_Party_Subaddress_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Called_Party_Subaddress_data_01_data*)EDAlloc (sizeof (c_InfoRec_Called_Party_Subaddress_data_01_data));
			INIT_c_InfoRec_Called_Party_Subaddress_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Called_Party_Subaddress_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Calling_Party_Subaddress_data_01'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Calling_Party_Subaddress_data_01 (c_InfoRec_Calling_Party_Subaddress_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Calling_Party_Subaddress_data_01_data** tmp;
		tmp = (c_InfoRec_Calling_Party_Subaddress_data_01_data**)EDAlloc (sizeof (c_InfoRec_Calling_Party_Subaddress_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Calling_Party_Subaddress_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Calling_Party_Subaddress_data_01_data*)EDAlloc (sizeof (c_InfoRec_Calling_Party_Subaddress_data_01_data));
			INIT_c_InfoRec_Calling_Party_Subaddress_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Calling_Party_Subaddress_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Connected_Subaddress_data_01'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Connected_Subaddress_data_01 (c_InfoRec_Connected_Subaddress_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Connected_Subaddress_data_01_data** tmp;
		tmp = (c_InfoRec_Connected_Subaddress_data_01_data**)EDAlloc (sizeof (c_InfoRec_Connected_Subaddress_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Connected_Subaddress_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Connected_Subaddress_data_01_data*)EDAlloc (sizeof (c_InfoRec_Connected_Subaddress_data_01_data));
			INIT_c_InfoRec_Connected_Subaddress_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Connected_Subaddress_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Redirecting_Number_data_01'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Redirecting_Number_data_01 (c_InfoRec_Redirecting_Number_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Redirecting_Number_data_01_data** tmp;
		tmp = (c_InfoRec_Redirecting_Number_data_01_data**)EDAlloc (sizeof (c_InfoRec_Redirecting_Number_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Redirecting_Number_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Redirecting_Number_data_01_data*)EDAlloc (sizeof (c_InfoRec_Redirecting_Number_data_01_data));
			INIT_c_InfoRec_Redirecting_Number_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Redirecting_Number_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Redirecting_Subaddress_data_01'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Redirecting_Subaddress_data_01 (c_InfoRec_Redirecting_Subaddress_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Redirecting_Subaddress_data_01_data** tmp;
		tmp = (c_InfoRec_Redirecting_Subaddress_data_01_data**)EDAlloc (sizeof (c_InfoRec_Redirecting_Subaddress_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Redirecting_Subaddress_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Redirecting_Subaddress_data_01_data*)EDAlloc (sizeof (c_InfoRec_Redirecting_Subaddress_data_01_data));
			INIT_c_InfoRec_Redirecting_Subaddress_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Redirecting_Subaddress_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Parametric_Alerting_GROUPSs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Parametric_Alerting_GROUPSs (c_InfoRec_Parametric_Alerting_GROUPSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Parametric_Alerting_GROUPSs_data** tmp;
		tmp = (c_InfoRec_Parametric_Alerting_GROUPSs_data**)EDAlloc (sizeof (c_InfoRec_Parametric_Alerting_GROUPSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Parametric_Alerting_GROUPSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Parametric_Alerting_GROUPSs_data*)EDAlloc (sizeof (c_InfoRec_Parametric_Alerting_GROUPSs_data));
			INIT_c_InfoRec_Parametric_Alerting_GROUPSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Parametric_Alerting_GROUPSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Extended_Display_data_01'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Extended_Display_data_01 (c_InfoRec_Extended_Display_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Extended_Display_data_01_data** tmp;
		tmp = (c_InfoRec_Extended_Display_data_01_data**)EDAlloc (sizeof (c_InfoRec_Extended_Display_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Extended_Display_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Extended_Display_data_01_data*)EDAlloc (sizeof (c_InfoRec_Extended_Display_data_01_data));
			INIT_c_InfoRec_Extended_Display_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Extended_Display_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_01'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_01 (c_InfoRec_Non_Negotiable_Service_Configuration_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_02'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_02 (c_InfoRec_Non_Negotiable_Service_Configuration_data_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_03'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_03 (c_InfoRec_Non_Negotiable_Service_Configuration_data_03* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_03_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_RECs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_RECs (c_InfoRec_Non_Negotiable_Service_Configuration_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_04'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_04 (c_InfoRec_Non_Negotiable_Service_Configuration_data_04* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_04_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_06'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_06 (c_InfoRec_Non_Negotiable_Service_Configuration_data_06* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_06_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_10'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_10 (c_InfoRec_Non_Negotiable_Service_Configuration_data_10* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_10_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_PARTITION_TABLESs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_BCMC_PROGRAMSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_12'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_12 (c_InfoRec_Non_Negotiable_Service_Configuration_data_12* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_12_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_13'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_13 (c_InfoRec_Non_Negotiable_Service_Configuration_data_13* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_13_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Non_Negotiable_Service_Configuration_data_14'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Non_Negotiable_Service_Configuration_data_14 (c_InfoRec_Non_Negotiable_Service_Configuration_data_14* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data** tmp;
		tmp = (c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data**)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data*)EDAlloc (sizeof (c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data));
			INIT_c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Non_Negotiable_Service_Configuration_data_14_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Multiple_Character_Extended_Display_data_01'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Multiple_Character_Extended_Display_data_01 (c_InfoRec_Multiple_Character_Extended_Display_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Multiple_Character_Extended_Display_data_01_data** tmp;
		tmp = (c_InfoRec_Multiple_Character_Extended_Display_data_01_data**)EDAlloc (sizeof (c_InfoRec_Multiple_Character_Extended_Display_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Multiple_Character_Extended_Display_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Multiple_Character_Extended_Display_data_01_data*)EDAlloc (sizeof (c_InfoRec_Multiple_Character_Extended_Display_data_01_data));
			INIT_c_InfoRec_Multiple_Character_Extended_Display_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Multiple_Character_Extended_Display_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs'. Existing items are not changed. */
int SETITEMS_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data** tmp;
		tmp = (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data**)EDAlloc (sizeof (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data*)EDAlloc (sizeof (c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data));
			INIT_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_Enhanced_Multiple_Character_Extended_Display_DISPLAYSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_PDCCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data_FOR_GCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_EGSRDM_Type_Specific_Fields_2_CHANSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_EGSRDM_Type_Specific_Fields_2_CHANSs (c_f_csch_EGSRDM_Type_Specific_Fields_2_CHANSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data** tmp;
		tmp = (c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data**)EDAlloc (sizeof (c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data*)EDAlloc (sizeof (c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data));
			INIT_c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_EGSRDM_Type_specific_fields_field_2_CHANSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_EGSRDM_Type_Specific_Fields_2_data_02'. Existing items are not changed. */
int SETITEMS_c_f_csch_EGSRDM_Type_Specific_Fields_2_data_02 (c_f_csch_EGSRDM_Type_Specific_Fields_2_data_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data** tmp;
		tmp = (c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data**)EDAlloc (sizeof (c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data*)EDAlloc (sizeof (c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data));
			INIT_c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_EGSRDM_Type_specific_fields_field_2_data_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06'. Existing items are not changed. */
int SETITEMS_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06 (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data** tmp;
		tmp = (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data**)EDAlloc (sizeof (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data*)EDAlloc (sizeof (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data));
			INIT_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c02_data_06_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08'. Existing items are not changed. */
int SETITEMS_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08 (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data** tmp;
		tmp = (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data**)EDAlloc (sizeof (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data*)EDAlloc (sizeof (c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data));
			INIT_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_APIDM_data_01_data_HO_INFO_VALUE_c03_data_08_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data'. Existing items are not changed. */
int SETITEMS_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data** tmp;
		tmp = (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data**)EDAlloc (sizeof (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data*)EDAlloc (sizeof (c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data));
			INIT_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_APPIM_APPI_RECs_data_AP_PN_REC_c01_data_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data** tmp;
		tmp = (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data**)EDAlloc (sizeof (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data*)EDAlloc (sizeof (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data));
			INIT_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data** tmp;
		tmp = (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data**)EDAlloc (sizeof (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data*)EDAlloc (sizeof (c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data));
			INIT_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs (c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data** tmp;
		tmp = (c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data**)EDAlloc (sizeof (c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data*)EDAlloc (sizeof (c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data));
			INIT_c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data** tmp;
		tmp = (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data**)EDAlloc (sizeof (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data*)EDAlloc (sizeof (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data));
			INIT_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c01_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data** tmp;
		tmp = (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data**)EDAlloc (sizeof (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data*)EDAlloc (sizeof (c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data));
			INIT_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_FGOIM_GOI_RECs_data_GOI_REC_c02_NETWORKSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_SCHs** tmp;
		tmp = (c_f_dsch_UHDM_SCHs**)EDAlloc (sizeof (c_f_dsch_UHDM_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_SCHs*)EDAlloc (sizeof (c_f_dsch_UHDM_SCHs));
			INIT_c_f_dsch_UHDM_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_CCSH_ENCODER_TYPE_BY_PILOT_SCHs'. Existing items are not changed. */
int SETITEMS_c_CCSH_ENCODER_TYPE_BY_PILOT_SCHs (c_CCSH_ENCODER_TYPE_BY_PILOT_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_SCHs** tmp;
		tmp = (c_f_dsch_UHDM_SCHs**)EDAlloc (sizeof (c_f_dsch_UHDM_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_SCHs*)EDAlloc (sizeof (c_f_dsch_UHDM_SCHs));
			INIT_c_f_dsch_UHDM_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_SCHs** tmp;
		tmp = (c_f_dsch_UHDM_SCHs**)EDAlloc (sizeof (c_f_dsch_UHDM_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_SCHs*)EDAlloc (sizeof (c_f_dsch_UHDM_SCHs));
			INIT_c_f_dsch_UHDM_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_GCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs (c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data** tmp;
		tmp = (c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data**)EDAlloc (sizeof (c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data*)EDAlloc (sizeof (c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data));
			INIT_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_GEM_GE_RECs_data_GE_REC_c01_RC_PARAMS_RECORDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_SCHs** tmp;
		tmp = (c_f_dsch_MUHDM_SCHs**)EDAlloc (sizeof (c_f_dsch_MUHDM_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_SCHs*)EDAlloc (sizeof (c_f_dsch_MUHDM_SCHs));
			INIT_c_f_dsch_MUHDM_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM_SCHs'. Existing items are not changed. */
int SETITEMS_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM_SCHs (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs_data_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_SCHs** tmp;
		tmp = (c_f_dsch_MUHDM_SCHs**)EDAlloc (sizeof (c_f_dsch_MUHDM_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_SCHs*)EDAlloc (sizeof (c_f_dsch_MUHDM_SCHs));
			INIT_c_f_dsch_MUHDM_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data_data_06_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_SCHs** tmp;
		tmp = (c_f_dsch_MUHDM_SCHs**)EDAlloc (sizeof (c_f_dsch_MUHDM_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_SCHs*)EDAlloc (sizeof (c_f_dsch_MUHDM_SCHs));
			INIT_c_f_dsch_MUHDM_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_FOR_PDCCH_WALSHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs (c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data** tmp;
		tmp = (c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data**)EDAlloc (sizeof (c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data*)EDAlloc (sizeof (c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data));
			INIT_c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs (c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data** tmp;
		tmp = (c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data**)EDAlloc (sizeof (c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data*)EDAlloc (sizeof (c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data));
			INIT_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_GEM_GE_RECs_data_GE_REC_c01_BAND_SUBCLASSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs'. Existing items are not changed. */
int SETITEMS_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data** tmp;
		tmp = (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data**)EDAlloc (sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data*)EDAlloc (sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data));
			INIT_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data** tmp;
		tmp = (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data**)EDAlloc (sizeof (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data*)EDAlloc (sizeof (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data));
			INIT_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data_FOR_SUPs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data** tmp;
		tmp = (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data**)EDAlloc (sizeof (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data*)EDAlloc (sizeof (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data));
			INIT_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data_LPM_ENTRIESs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data** tmp;
		tmp = (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data));
			INIT_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data_SUP_SHOs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_F_BCMC_Order_BCMC_PROGRAMSs'. Existing items are not changed. */
int SETITEMS_c_F_BCMC_Order_BCMC_PROGRAMSs (c_F_BCMC_Order_BCMC_PROGRAMSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_F_BCMC_Order_BCMC_PROGRAMSs_data** tmp;
		tmp = (c_F_BCMC_Order_BCMC_PROGRAMSs_data**)EDAlloc (sizeof (c_F_BCMC_Order_BCMC_PROGRAMSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_F_BCMC_Order_BCMC_PROGRAMSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_F_BCMC_Order_BCMC_PROGRAMSs_data*)EDAlloc (sizeof (c_F_BCMC_Order_BCMC_PROGRAMSs_data));
			INIT_c_F_BCMC_Order_BCMC_PROGRAMSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_F_BCMC_Order_BCMC_PROGRAMSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_F_Service_Status_Order_data_01'. Existing items are not changed. */
int SETITEMS_c_F_Service_Status_Order_data_01 (c_F_Service_Status_Order_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_F_Service_Status_Order_data_01_data** tmp;
		tmp = (c_F_Service_Status_Order_data_01_data**)EDAlloc (sizeof (c_F_Service_Status_Order_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_F_Service_Status_Order_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_F_Service_Status_Order_data_01_data*)EDAlloc (sizeof (c_F_Service_Status_Order_data_01_data));
			INIT_c_F_Service_Status_Order_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_F_Service_Status_Order_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* Free function for struct c_F_InformationRecords_p_generic */
void FREE_c_F_InformationRecords_p_generic (c_F_InformationRecords_p_generic* sp) {
ED_WARNING_REMOVER(sp);
	if (sp->FieldData.value != NULL) {
		EDFree (sp->FieldData.value);
		sp->FieldData.value = NULL;
	}
	sp->FieldData.usedBits = 0;

}

/* SETITEMS commands for type 'c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs'. Existing items are not changed. */
int SETITEMS_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data** tmp;
		tmp = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data**)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data));
			INIT_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_PRI_NGHBRs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs'. Existing items are not changed. */
int SETITEMS_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data** tmp;
		tmp = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data**)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data));
			INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data** tmp;
		tmp = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data**)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data));
			INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_NGHBRs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02'. Existing items are not changed. */
int SETITEMS_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02 (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data** tmp;
		tmp = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data**)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data));
			INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_data_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR'. Existing items are not changed. */
int SETITEMS_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data** tmp;
		tmp = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data**)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data));
			INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_analog_ANALOG_NGHBR_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR'. Existing items are not changed. */
int SETITEMS_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data** tmp;
		tmp = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data**)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data));
			INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_HRPD_HRPD_NGHBR_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs (c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data** tmp;
		tmp = (c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data**)EDAlloc (sizeof (c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data*)EDAlloc (sizeof (c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data));
			INIT_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs (c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data** tmp;
		tmp = (c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data**)EDAlloc (sizeof (c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data*)EDAlloc (sizeof (c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data));
			INIT_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_GHDM_PILOTSs_data_FOR_SUP_CHAN_REC_Record_FOR_SUPs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs (c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data** tmp;
		tmp = (c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data**)EDAlloc (sizeof (c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data*)EDAlloc (sizeof (c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data));
			INIT_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_BSSRSPM_RECORDSs_data_Record_type_specific_fields_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs (c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data** tmp;
		tmp = (c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data**)EDAlloc (sizeof (c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data*)EDAlloc (sizeof (c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data));
			INIT_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data_Radio_Interface_Type_Specific_Fields_010_HRPD_NGHBRs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs (c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data** tmp;
		tmp = (c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data**)EDAlloc (sizeof (c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data*)EDAlloc (sizeof (c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data));
			INIT_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_BSSREQM_RECORDs_data_Record_Type_Specific_Fields_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_CAM_Additional_record_fields_c02_data_03'. Existing items are not changed. */
int SETITEMS_c_f_csch_CAM_Additional_record_fields_c02_data_03 (c_f_csch_CAM_Additional_record_fields_c02_data_03* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_CAM_Additional_record_fields_c02_data_03_data** tmp;
		tmp = (c_f_csch_CAM_Additional_record_fields_c02_data_03_data**)EDAlloc (sizeof (c_f_csch_CAM_Additional_record_fields_c02_data_03_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_CAM_Additional_record_fields_c02_data_03_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_CAM_Additional_record_fields_c02_data_03_data*)EDAlloc (sizeof (c_f_csch_CAM_Additional_record_fields_c02_data_03_data));
			INIT_c_f_csch_CAM_Additional_record_fields_c02_data_03_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_CAM_Additional_record_fields_c02_data_03_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_CAM_Additional_record_fields_c04_data_06'. Existing items are not changed. */
int SETITEMS_c_f_csch_CAM_Additional_record_fields_c04_data_06 (c_f_csch_CAM_Additional_record_fields_c04_data_06* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_CAM_Additional_record_fields_c04_data_06_data** tmp;
		tmp = (c_f_csch_CAM_Additional_record_fields_c04_data_06_data**)EDAlloc (sizeof (c_f_csch_CAM_Additional_record_fields_c04_data_06_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_CAM_Additional_record_fields_c04_data_06_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_CAM_Additional_record_fields_c04_data_06_data*)EDAlloc (sizeof (c_f_csch_CAM_Additional_record_fields_c04_data_06_data));
			INIT_c_f_csch_CAM_Additional_record_fields_c04_data_06_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_CAM_Additional_record_fields_c04_data_06_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs (c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c000_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs (c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c001_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data** tmp;
		tmp = (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data**)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data*)EDAlloc (sizeof (c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data));
			INIT_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECAM_Additional_record_fields_c101_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_FOR_SCHs** tmp;
		tmp = (c_f_dsch_UHDM_FOR_SCHs**)EDAlloc (sizeof (c_f_dsch_UHDM_FOR_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_FOR_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_FOR_SCHs*)EDAlloc (sizeof (c_f_dsch_UHDM_FOR_SCHs));
			INIT_c_f_dsch_UHDM_FOR_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_FOR_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_REV_SCHs** tmp;
		tmp = (c_f_dsch_UHDM_REV_SCHs**)EDAlloc (sizeof (c_f_dsch_UHDM_REV_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_REV_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_REV_SCHs*)EDAlloc (sizeof (c_f_dsch_UHDM_REV_SCHs));
			INIT_c_f_dsch_UHDM_REV_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_REV_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_CCSH_ENCODER_TYPE_BY_PILOT** tmp;
		tmp = (c_CCSH_ENCODER_TYPE_BY_PILOT**)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_CCSH_ENCODER_TYPE_BY_PILOT*)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT));
			INIT_c_CCSH_ENCODER_TYPE_BY_PILOT (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_FOR_SCHs** tmp;
		tmp = (c_f_dsch_UHDM_FOR_SCHs**)EDAlloc (sizeof (c_f_dsch_UHDM_FOR_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_FOR_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_FOR_SCHs*)EDAlloc (sizeof (c_f_dsch_UHDM_FOR_SCHs));
			INIT_c_f_dsch_UHDM_FOR_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_FOR_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_REV_SCHs** tmp;
		tmp = (c_f_dsch_UHDM_REV_SCHs**)EDAlloc (sizeof (c_f_dsch_UHDM_REV_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_REV_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_REV_SCHs*)EDAlloc (sizeof (c_f_dsch_UHDM_REV_SCHs));
			INIT_c_f_dsch_UHDM_REV_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_REV_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_CCSH_ENCODER_TYPE_BY_PILOT** tmp;
		tmp = (c_CCSH_ENCODER_TYPE_BY_PILOT**)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_CCSH_ENCODER_TYPE_BY_PILOT*)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT));
			INIT_c_CCSH_ENCODER_TYPE_BY_PILOT (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTS3_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_REV_SCHs** tmp;
		tmp = (c_f_dsch_UHDM_REV_SCHs**)EDAlloc (sizeof (c_f_dsch_UHDM_REV_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_REV_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_REV_SCHs*)EDAlloc (sizeof (c_f_dsch_UHDM_REV_SCHs));
			INIT_c_f_dsch_UHDM_REV_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_REV_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_CCSH_ENCODER_TYPE_BY_PILOT** tmp;
		tmp = (c_CCSH_ENCODER_TYPE_BY_PILOT**)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_CCSH_ENCODER_TYPE_BY_PILOT*)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT));
			INIT_c_CCSH_ENCODER_TYPE_BY_PILOT (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTS3_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_FOR_SCHs** tmp;
		tmp = (c_f_dsch_UHDM_FOR_SCHs**)EDAlloc (sizeof (c_f_dsch_UHDM_FOR_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_FOR_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_FOR_SCHs*)EDAlloc (sizeof (c_f_dsch_UHDM_FOR_SCHs));
			INIT_c_f_dsch_UHDM_FOR_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_FOR_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_REV_SCHs** tmp;
		tmp = (c_f_dsch_UHDM_REV_SCHs**)EDAlloc (sizeof (c_f_dsch_UHDM_REV_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_REV_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_REV_SCHs*)EDAlloc (sizeof (c_f_dsch_UHDM_REV_SCHs));
			INIT_c_f_dsch_UHDM_REV_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_REV_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data** tmp;
		tmp = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data**)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data*)EDAlloc (sizeof (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data));
			INIT_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2 (c_f_dsch_UHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_CCSH_ENCODER_TYPE_BY_PILOT** tmp;
		tmp = (c_CCSH_ENCODER_TYPE_BY_PILOT**)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_CCSH_ENCODER_TYPE_BY_PILOT*)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT));
			INIT_c_CCSH_ENCODER_TYPE_BY_PILOT (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_CCSH_ENCODER_TYPE_BY_PILOT (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_FOR_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_FOR_SCHs** tmp;
		tmp = (c_f_dsch_MUHDM_FOR_SCHs**)EDAlloc (sizeof (c_f_dsch_MUHDM_FOR_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_FOR_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_FOR_SCHs*)EDAlloc (sizeof (c_f_dsch_MUHDM_FOR_SCHs));
			INIT_c_f_dsch_MUHDM_FOR_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_FOR_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_REV_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_REV_SCHs** tmp;
		tmp = (c_f_dsch_MUHDM_REV_SCHs**)EDAlloc (sizeof (c_f_dsch_MUHDM_REV_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_REV_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_REV_SCHs*)EDAlloc (sizeof (c_f_dsch_MUHDM_REV_SCHs));
			INIT_c_f_dsch_MUHDM_REV_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_REV_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_PILOTSs2_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c01_CCSH_ENCODER_TYPEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM** tmp;
		tmp = (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM**)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM));
			INIT_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_FOR_SCHs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_FOR_SCHs** tmp;
		tmp = (c_f_dsch_MUHDM_FOR_SCHs**)EDAlloc (sizeof (c_f_dsch_MUHDM_FOR_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_FOR_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_FOR_SCHs*)EDAlloc (sizeof (c_f_dsch_MUHDM_FOR_SCHs));
			INIT_c_f_dsch_MUHDM_FOR_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_FOR_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_REV_SCHs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_REV_SCHs** tmp;
		tmp = (c_f_dsch_MUHDM_REV_SCHs**)EDAlloc (sizeof (c_f_dsch_MUHDM_REV_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_REV_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_REV_SCHs*)EDAlloc (sizeof (c_f_dsch_MUHDM_REV_SCHs));
			INIT_c_f_dsch_MUHDM_REV_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_REV_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c02_CCSH_ENCODER_TYPEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM** tmp;
		tmp = (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM**)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM));
			INIT_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_FOR_SCHs_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_REV_SCHs_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_REV_SCHs** tmp;
		tmp = (c_f_dsch_MUHDM_REV_SCHs**)EDAlloc (sizeof (c_f_dsch_MUHDM_REV_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_REV_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_REV_SCHs*)EDAlloc (sizeof (c_f_dsch_MUHDM_REV_SCHs));
			INIT_c_f_dsch_MUHDM_REV_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_REV_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_04_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_PILOTSs_05_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c03_CCSH_ENCODER_TYPEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM** tmp;
		tmp = (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM**)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM));
			INIT_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_FOR_SCHs_03* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_FOR_SCHs** tmp;
		tmp = (c_f_dsch_MUHDM_FOR_SCHs**)EDAlloc (sizeof (c_f_dsch_MUHDM_FOR_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_FOR_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_FOR_SCHs*)EDAlloc (sizeof (c_f_dsch_MUHDM_FOR_SCHs));
			INIT_c_f_dsch_MUHDM_FOR_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_FOR_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_REV_SCHs_03* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_REV_SCHs** tmp;
		tmp = (c_f_dsch_MUHDM_REV_SCHs**)EDAlloc (sizeof (c_f_dsch_MUHDM_REV_SCHs*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_REV_SCHs*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_REV_SCHs*)EDAlloc (sizeof (c_f_dsch_MUHDM_REV_SCHs));
			INIT_c_f_dsch_MUHDM_REV_SCHs (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_REV_SCHs (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PDCCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data** tmp;
		tmp = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data));
			INIT_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS1_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2 (c_f_dsch_MUHDM_ACTIVE_SET_REC_FIELDS_c04_PILOTS2* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM** tmp;
		tmp = (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM**)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM*)EDAlloc (sizeof (c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM));
			INIT_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_CCSH_ENCODER_TYPE_BY_PILOT_MUHDM (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03'. Existing items are not changed. */
int SETITEMS_c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03 (c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data** tmp;
		tmp = (c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data**)EDAlloc (sizeof (c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data*)EDAlloc (sizeof (c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data));
			INIT_c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ESPM_CAND_BAND_CLASSs_data_data_03_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECCLM_BANDs_data_data_02'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECCLM_BANDs_data_data_02 (c_f_csch_ECCLM_BANDs_data_data_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECCLM_BANDs_data_data_02_data** tmp;
		tmp = (c_f_csch_ECCLM_BANDs_data_data_02_data**)EDAlloc (sizeof (c_f_csch_ECCLM_BANDs_data_data_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECCLM_BANDs_data_data_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECCLM_BANDs_data_data_02_data*)EDAlloc (sizeof (c_f_csch_ECCLM_BANDs_data_data_02_data));
			INIT_c_f_csch_ECCLM_BANDs_data_data_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECCLM_BANDs_data_data_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECCLM_BANDs_data_ADD_FREQs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECCLM_BANDs_data_ADD_FREQs (c_f_csch_ECCLM_BANDs_data_ADD_FREQs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data** tmp;
		tmp = (c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data**)EDAlloc (sizeof (c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data*)EDAlloc (sizeof (c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data));
			INIT_c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECCLM_BANDs_data_ADD_FREQs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01 (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data** tmp;
		tmp = (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data**)EDAlloc (sizeof (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data*)EDAlloc (sizeof (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data));
			INIT_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs'. Existing items are not changed. */
int SETITEMS_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data** tmp;
		tmp = (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data**)EDAlloc (sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*)EDAlloc (sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data));
			INIT_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01 (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data** tmp;
		tmp = (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data**)EDAlloc (sizeof (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data*)EDAlloc (sizeof (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data));
			INIT_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs'. Existing items are not changed. */
int SETITEMS_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data** tmp;
		tmp = (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data**)EDAlloc (sizeof (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data*)EDAlloc (sizeof (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data));
			INIT_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data_FREQs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_SCAM_data_02_data_SUP_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data** tmp;
		tmp = (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data**)EDAlloc (sizeof (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data*)EDAlloc (sizeof (c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data));
			INIT_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_SCAM_data_02_data_SUP_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ERM_data_01_data_PILOTS_D2Is'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ERM_data_01_data_PILOTS_D2Is (c_f_dsch_ERM_data_01_data_PILOTS_D2Is* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data** tmp;
		tmp = (c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data**)EDAlloc (sizeof (c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data*)EDAlloc (sizeof (c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data));
			INIT_c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ERM_data_01_data_PILOTS_D2Is_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data** tmp;
		tmp = (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data**)EDAlloc (sizeof (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*)EDAlloc (sizeof (c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data));
			INIT_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs (c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data** tmp;
		tmp = (c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data));
			INIT_c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_FOR_CFG_RECSs_data_SUP_SHOs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data** tmp;
		tmp = (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data));
			INIT_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_P3X_CFGs_data_P3X_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data** tmp;
		tmp = (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data**)EDAlloc (sizeof (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*)EDAlloc (sizeof (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data));
			INIT_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs'. Existing items are not changed. */
int SETITEMS_c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data** tmp;
		tmp = (c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data**)EDAlloc (sizeof (c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*)EDAlloc (sizeof (c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data));
			INIT_c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_RGM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs'. Existing items are not changed. */
int SETITEMS_c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data** tmp;
		tmp = (c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data**)EDAlloc (sizeof (c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*)EDAlloc (sizeof (c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data));
			INIT_c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_ORM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs'. Existing items are not changed. */
int SETITEMS_c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data** tmp;
		tmp = (c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data**)EDAlloc (sizeof (c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*)EDAlloc (sizeof (c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data));
			INIT_c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_PRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data** tmp;
		tmp = (c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data**)EDAlloc (sizeof (c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*)EDAlloc (sizeof (c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data));
			INIT_c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_RRRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_EOM_ADD_SERV_INSTANCEs_data_RECS'. Existing items are not changed. */
int SETITEMS_c_r_dsch_EOM_ADD_SERV_INSTANCEs_data_RECS (c_r_dsch_EOM_ADD_SERV_INSTANCEs_data_RECS* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_2_7_4_encrypted** tmp;
		tmp = (c_InfoRec_2_7_4_encrypted**)EDAlloc (sizeof (c_InfoRec_2_7_4_encrypted*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_2_7_4_encrypted*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_2_7_4_encrypted*)EDAlloc (sizeof (c_InfoRec_2_7_4_encrypted));
			INIT_c_InfoRec_2_7_4_encrypted (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_2_7_4_encrypted (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data** tmp;
		tmp = (c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data**)EDAlloc (sizeof (c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*)EDAlloc (sizeof (c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data));
			INIT_c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_EOM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data** tmp;
		tmp = (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data**)EDAlloc (sizeof (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data*)EDAlloc (sizeof (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data));
			INIT_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data_FLOW_DISCRIMINATORs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* Set-present function for c_R_Orders_parameters LEVEL = -1 */
void GLOB_SETPRESENT_c_R_Orders_parameters (c_R_Orders_parameters* sp, TPRESENT_c_R_Orders_parameters toBeSetPresent) 
{
	FREE_c_R_Orders_parameters (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_R_Orders_parameters_p_R_Base_Station_Challenge_Order: {
			sp->u.p_R_Base_Station_Challenge_Order = (c_R_Base_Station_Challenge_Order*)EDAlloc (sizeof (c_R_Base_Station_Challenge_Order));
			INIT_c_R_Base_Station_Challenge_Order (sp->u.p_R_Base_Station_Challenge_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Service_Option_Request_Order: {
			sp->u.p_R_Service_Option_Request_Order = (c_R_Service_Option_Request_Order*)EDAlloc (sizeof (c_R_Service_Option_Request_Order));
			INIT_c_R_Service_Option_Request_Order (sp->u.p_R_Service_Option_Request_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Service_Option_Response_Order: {
			sp->u.p_R_Service_Option_Response_Order = (c_R_Service_Option_Response_Order*)EDAlloc (sizeof (c_R_Service_Option_Response_Order));
			INIT_c_R_Service_Option_Response_Order (sp->u.p_R_Service_Option_Response_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Mobile_Station_Reject_Order: {
			sp->u.p_R_Mobile_Station_Reject_Order = (c_R_Mobile_Station_Reject_Order*)EDAlloc (sizeof (c_R_Mobile_Station_Reject_Order));
			INIT_c_R_Mobile_Station_Reject_Order (sp->u.p_R_Mobile_Station_Reject_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Release_Order: {
			sp->u.p_R_Release_Order = (c_R_Release_Order*)EDAlloc (sizeof (c_R_Release_Order));
			INIT_c_R_Release_Order (sp->u.p_R_Release_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Fast_Call_Setup_Order: {
			sp->u.p_R_Fast_Call_Setup_Order = (c_R_Fast_Call_Setup_Order*)EDAlloc (sizeof (c_R_Fast_Call_Setup_Order));
			INIT_c_R_Fast_Call_Setup_Order (sp->u.p_R_Fast_Call_Setup_Order);

			break;
		}
		case U_c_R_Orders_parameters_p_R_Generic_Order: {
			sp->u.p_R_Generic_Order = (c_R_Orders_parameters_p_R_Generic_Order*)EDAlloc (sizeof (c_R_Orders_parameters_p_R_Generic_Order));
			ED_RESET_MEM (sp->u.p_R_Generic_Order, sizeof (c_R_Orders_parameters_p_R_Generic_Order));
			(sp->u.p_R_Generic_Order->ORDQ) = 0;

			break;
		}
		default:;	}
}

/* Free function for struct c_R_Orders_parameters_p_R_Generic_Order */
void FREE_c_R_Orders_parameters_p_R_Generic_Order (c_R_Orders_parameters_p_R_Generic_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* Set-present function for c_F_Orders_parameters LEVEL = -1 */
void GLOB_SETPRESENT_c_F_Orders_parameters (c_F_Orders_parameters* sp, TPRESENT_c_F_Orders_parameters toBeSetPresent) 
{
	FREE_c_F_Orders_parameters (sp);
	sp->Present = toBeSetPresent;
	
	switch (toBeSetPresent) {
		case U_c_F_Orders_parameters_p_F_Base_Station_Challenge_Confirmation_Order: {
			sp->u.p_F_Base_Station_Challenge_Confirmation_Order = (c_F_Base_Station_Challenge_Confirmation_Order*)EDAlloc (sizeof (c_F_Base_Station_Challenge_Confirmation_Order));
			INIT_c_F_Base_Station_Challenge_Confirmation_Order (sp->u.p_F_Base_Station_Challenge_Confirmation_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Service_Option_Request_Order: {
			sp->u.p_F_Service_Option_Request_Order = (c_F_Service_Option_Request_Order*)EDAlloc (sizeof (c_F_Service_Option_Request_Order));
			INIT_c_F_Service_Option_Request_Order (sp->u.p_F_Service_Option_Request_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Service_Option_Response_Order: {
			sp->u.p_F_Service_Option_Response_Order = (c_F_Service_Option_Response_Order*)EDAlloc (sizeof (c_F_Service_Option_Response_Order));
			INIT_c_F_Service_Option_Response_Order (sp->u.p_F_Service_Option_Response_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Status_Request_Order: {
			sp->u.p_F_Status_Request_Order = (c_F_Status_Request_Order*)EDAlloc (sizeof (c_F_Status_Request_Order));
			INIT_c_F_Status_Request_Order (sp->u.p_F_Status_Request_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Registration_Accepted_Order: {
			sp->u.p_F_Registration_Accepted_Order = (c_F_Registration_Accepted_Order*)EDAlloc (sizeof (c_F_Registration_Accepted_Order));
			INIT_c_F_Registration_Accepted_Order (sp->u.p_F_Registration_Accepted_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Periodic_Pilot_Measurement_Request_Order: {
			sp->u.p_F_Periodic_Pilot_Measurement_Request_Order = (c_F_Periodic_Pilot_Measurement_Request_Order*)EDAlloc (sizeof (c_F_Periodic_Pilot_Measurement_Request_Order));
			INIT_c_F_Periodic_Pilot_Measurement_Request_Order (sp->u.p_F_Periodic_Pilot_Measurement_Request_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Retry_Order: {
			sp->u.p_F_Retry_Order = (c_F_Retry_Order*)EDAlloc (sizeof (c_F_Retry_Order));
			INIT_c_F_Retry_Order (sp->u.p_F_Retry_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Base_Station_Reject_Order: {
			sp->u.p_F_Base_Station_Reject_Order = (c_F_Base_Station_Reject_Order*)EDAlloc (sizeof (c_F_Base_Station_Reject_Order));
			INIT_c_F_Base_Station_Reject_Order (sp->u.p_F_Base_Station_Reject_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_BCMC_Order: {
			sp->u.p_F_BCMC_Order = (c_F_BCMC_Order*)EDAlloc (sizeof (c_F_BCMC_Order));
			INIT_c_F_BCMC_Order (sp->u.p_F_BCMC_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Fast_Call_Setup_Order: {
			sp->u.p_F_Fast_Call_Setup_Order = (c_F_Fast_Call_Setup_Order*)EDAlloc (sizeof (c_F_Fast_Call_Setup_Order));
			INIT_c_F_Fast_Call_Setup_Order (sp->u.p_F_Fast_Call_Setup_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Service_Status_Order: {
			sp->u.p_F_Service_Status_Order = (c_F_Service_Status_Order*)EDAlloc (sizeof (c_F_Service_Status_Order));
			INIT_c_F_Service_Status_Order (sp->u.p_F_Service_Status_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_Location_Services_Order: {
			sp->u.p_F_Location_Services_Order = (c_F_Location_Services_Order*)EDAlloc (sizeof (c_F_Location_Services_Order));
			INIT_c_F_Location_Services_Order (sp->u.p_F_Location_Services_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_F_EACH_Selective_Acknowledgment_Order: {
			sp->u.p_F_EACH_Selective_Acknowledgment_Order = (c_F_EACH_Selective_Acknowledgment_Order*)EDAlloc (sizeof (c_F_EACH_Selective_Acknowledgment_Order));
			INIT_c_F_EACH_Selective_Acknowledgment_Order (sp->u.p_F_EACH_Selective_Acknowledgment_Order);

			break;
		}
		case U_c_F_Orders_parameters_p_R_Generic_Order: {
			sp->u.p_R_Generic_Order = (c_F_Orders_parameters_p_R_Generic_Order*)EDAlloc (sizeof (c_F_Orders_parameters_p_R_Generic_Order));
			ED_RESET_MEM (sp->u.p_R_Generic_Order, sizeof (c_F_Orders_parameters_p_R_Generic_Order));
			(sp->u.p_R_Generic_Order->ORDQ) = 0;

			break;
		}
		default:;	}
}

/* Free function for struct c_F_Orders_parameters_p_R_Generic_Order */
void FREE_c_F_Orders_parameters_p_R_Generic_Order (c_F_Orders_parameters_p_R_Generic_Order* sp) {
ED_WARNING_REMOVER(sp);

}

/* SETITEMS commands for type 'c_f_csch_SRDM_Type_specific_fields_2_CHANSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_SRDM_Type_specific_fields_2_CHANSs (c_f_csch_SRDM_Type_specific_fields_2_CHANSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data** tmp;
		tmp = (c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data**)EDAlloc (sizeof (c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data*)EDAlloc (sizeof (c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data));
			INIT_c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_SRDM_Type_specific_fields_2_CHANSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GSRDM_Type_specific_fields_2_CHANSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_GSRDM_Type_specific_fields_2_CHANSs (c_f_csch_GSRDM_Type_specific_fields_2_CHANSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data** tmp;
		tmp = (c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data**)EDAlloc (sizeof (c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data*)EDAlloc (sizeof (c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data));
			INIT_c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GSRDM_Type_specific_fields_2_CHANSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_SRDM_Type_specific_fields_2_CHANSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_SRDM_Type_specific_fields_2_CHANSs (c_f_dsch_SRDM_Type_specific_fields_2_CHANSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data** tmp;
		tmp = (c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data**)EDAlloc (sizeof (c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data*)EDAlloc (sizeof (c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data));
			INIT_c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_SRDM_Type_specific_fields_2_CHANSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs (c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data** tmp;
		tmp = (c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data**)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data));
			INIT_c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_CFSRQM_Mode_specific_fields_1_ANALOG_FREQs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data** tmp;
		tmp = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data**)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data));
			INIT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs (c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data** tmp;
		tmp = (c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data**)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data));
			INIT_c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_CF_SRCH_OFFSET_INCLs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02'. Existing items are not changed. */
int SETITEMS_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02 (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data** tmp;
		tmp = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data**)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data*)EDAlloc (sizeof (c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data));
			INIT_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_CFSRQM_Mode_specific_fields_0_PILOTSs_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data** tmp;
		tmp = (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data**)EDAlloc (sizeof (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data*)EDAlloc (sizeof (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data));
			INIT_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01'. Existing items are not changed. */
int SETITEMS_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01 (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data** tmp;
		tmp = (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data**)EDAlloc (sizeof (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data*)EDAlloc (sizeof (c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data));
			INIT_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0000_PILOTSs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS'. Existing items are not changed. */
int SETITEMS_c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS (c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data** tmp;
		tmp = (c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data**)EDAlloc (sizeof (c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data*)EDAlloc (sizeof (c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data));
			INIT_c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_CFSRPM_Mode_specific_fields_0001_ANALOG_FREQS_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_APM_ACCT_SOs'. Existing items are not changed. */
int SETITEMS_c_f_csch_APM_ACCT_SOs (c_f_csch_APM_ACCT_SOs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_APM_ACCT_SOs_data** tmp;
		tmp = (c_f_csch_APM_ACCT_SOs_data**)EDAlloc (sizeof (c_f_csch_APM_ACCT_SOs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_APM_ACCT_SOs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_APM_ACCT_SOs_data*)EDAlloc (sizeof (c_f_csch_APM_ACCT_SOs_data));
			INIT_c_f_csch_APM_ACCT_SOs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_APM_ACCT_SOs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_APM_ACCT_SO_GRPs'. Existing items are not changed. */
int SETITEMS_c_f_csch_APM_ACCT_SO_GRPs (c_f_csch_APM_ACCT_SO_GRPs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_APM_ACCT_SO_GRPs_data** tmp;
		tmp = (c_f_csch_APM_ACCT_SO_GRPs_data**)EDAlloc (sizeof (c_f_csch_APM_ACCT_SO_GRPs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_APM_ACCT_SO_GRPs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_APM_ACCT_SO_GRPs_data*)EDAlloc (sizeof (c_f_csch_APM_ACCT_SO_GRPs_data));
			INIT_c_f_csch_APM_ACCT_SO_GRPs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_APM_ACCT_SO_GRPs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_NLM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_NLM_data_01 (c_f_csch_NLM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_NLM_data_01_data** tmp;
		tmp = (c_f_csch_NLM_data_01_data**)EDAlloc (sizeof (c_f_csch_NLM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_NLM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_NLM_data_01_data*)EDAlloc (sizeof (c_f_csch_NLM_data_01_data));
			INIT_c_f_csch_NLM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_NLM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_CCLM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_CCLM_data_01 (c_f_csch_CCLM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_CCLM_data_01_data** tmp;
		tmp = (c_f_csch_CCLM_data_01_data**)EDAlloc (sizeof (c_f_csch_CCLM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_CCLM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_CCLM_data_01_data*)EDAlloc (sizeof (c_f_csch_CCLM_data_01_data));
			INIT_c_f_csch_CCLM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_CCLM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_DBM_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_DBM_FIELDSs (c_f_csch_DBM_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_DBM_FIELDSs_data** tmp;
		tmp = (c_f_csch_DBM_FIELDSs_data**)EDAlloc (sizeof (c_f_csch_DBM_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_DBM_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_DBM_FIELDSs_data*)EDAlloc (sizeof (c_f_csch_DBM_FIELDSs_data));
			INIT_c_f_csch_DBM_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_DBM_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_FNM_INFO_RECORDs'. Existing items are not changed. */
int SETITEMS_c_f_csch_FNM_INFO_RECORDs (c_f_csch_FNM_INFO_RECORDs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_3_7_5** tmp;
		tmp = (c_InfoRec_3_7_5**)EDAlloc (sizeof (c_InfoRec_3_7_5*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_3_7_5*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_3_7_5*)EDAlloc (sizeof (c_InfoRec_3_7_5));
			INIT_c_InfoRec_3_7_5 (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_3_7_5 (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ESPM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_ESPM_data_01 (c_f_csch_ESPM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ESPM_data_01_data** tmp;
		tmp = (c_f_csch_ESPM_data_01_data**)EDAlloc (sizeof (c_f_csch_ESPM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ESPM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ESPM_data_01_data*)EDAlloc (sizeof (c_f_csch_ESPM_data_01_data));
			INIT_c_f_csch_ESPM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ESPM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ESPM_data_02'. Existing items are not changed. */
int SETITEMS_c_f_csch_ESPM_data_02 (c_f_csch_ESPM_data_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ESPM_data_02_data** tmp;
		tmp = (c_f_csch_ESPM_data_02_data**)EDAlloc (sizeof (c_f_csch_ESPM_data_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ESPM_data_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ESPM_data_02_data*)EDAlloc (sizeof (c_f_csch_ESPM_data_02_data));
			INIT_c_f_csch_ESPM_data_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ESPM_data_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ESPM_PDCCHs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ESPM_PDCCHs (c_f_csch_ESPM_PDCCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ESPM_PDCCHs_data** tmp;
		tmp = (c_f_csch_ESPM_PDCCHs_data**)EDAlloc (sizeof (c_f_csch_ESPM_PDCCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ESPM_PDCCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ESPM_PDCCHs_data*)EDAlloc (sizeof (c_f_csch_ESPM_PDCCHs_data));
			INIT_c_f_csch_ESPM_PDCCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ESPM_PDCCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ESPM_CAND_BAND_CLASSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ESPM_CAND_BAND_CLASSs (c_f_csch_ESPM_CAND_BAND_CLASSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ESPM_CAND_BAND_CLASSs_data** tmp;
		tmp = (c_f_csch_ESPM_CAND_BAND_CLASSs_data**)EDAlloc (sizeof (c_f_csch_ESPM_CAND_BAND_CLASSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ESPM_CAND_BAND_CLASSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ESPM_CAND_BAND_CLASSs_data*)EDAlloc (sizeof (c_f_csch_ESPM_CAND_BAND_CLASSs_data));
			INIT_c_f_csch_ESPM_CAND_BAND_CLASSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ESPM_CAND_BAND_CLASSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ENLM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_ENLM_data_01 (c_f_csch_ENLM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ENLM_data_01_data** tmp;
		tmp = (c_f_csch_ENLM_data_01_data**)EDAlloc (sizeof (c_f_csch_ENLM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ENLM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ENLM_data_01_data*)EDAlloc (sizeof (c_f_csch_ENLM_data_01_data));
			INIT_c_f_csch_ENLM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ENLM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_STRQM_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_STRQM_FIELDSs (c_f_csch_STRQM_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_STRQM_FIELDSs_data** tmp;
		tmp = (c_f_csch_STRQM_FIELDSs_data**)EDAlloc (sizeof (c_f_csch_STRQM_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_STRQM_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_STRQM_FIELDSs_data*)EDAlloc (sizeof (c_f_csch_STRQM_FIELDSs_data));
			INIT_c_f_csch_STRQM_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_STRQM_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GNLM_NGHBRs'. Existing items are not changed. */
int SETITEMS_c_f_csch_GNLM_NGHBRs (c_f_csch_GNLM_NGHBRs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GNLM_NGHBRs_data** tmp;
		tmp = (c_f_csch_GNLM_NGHBRs_data**)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GNLM_NGHBRs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GNLM_NGHBRs_data*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_data));
			INIT_c_f_csch_GNLM_NGHBRs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GNLM_NGHBRs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GNLM_ANALOG_NGHBRs'. Existing items are not changed. */
int SETITEMS_c_f_csch_GNLM_ANALOG_NGHBRs (c_f_csch_GNLM_ANALOG_NGHBRs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GNLM_ANALOG_NGHBRs_data** tmp;
		tmp = (c_f_csch_GNLM_ANALOG_NGHBRs_data**)EDAlloc (sizeof (c_f_csch_GNLM_ANALOG_NGHBRs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GNLM_ANALOG_NGHBRs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GNLM_ANALOG_NGHBRs_data*)EDAlloc (sizeof (c_f_csch_GNLM_ANALOG_NGHBRs_data));
			INIT_c_f_csch_GNLM_ANALOG_NGHBRs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GNLM_ANALOG_NGHBRs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GNLM_NGHBRs_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_GNLM_NGHBRs_01 (c_f_csch_GNLM_NGHBRs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GNLM_NGHBRs_01_data** tmp;
		tmp = (c_f_csch_GNLM_NGHBRs_01_data**)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GNLM_NGHBRs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GNLM_NGHBRs_01_data*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_01_data));
			INIT_c_f_csch_GNLM_NGHBRs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GNLM_NGHBRs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GNLM_NGHBRs_02'. Existing items are not changed. */
int SETITEMS_c_f_csch_GNLM_NGHBRs_02 (c_f_csch_GNLM_NGHBRs_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GNLM_NGHBRs_02_data** tmp;
		tmp = (c_f_csch_GNLM_NGHBRs_02_data**)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GNLM_NGHBRs_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GNLM_NGHBRs_02_data*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_02_data));
			INIT_c_f_csch_GNLM_NGHBRs_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GNLM_NGHBRs_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GNLM_NGHBRs_03'. Existing items are not changed. */
int SETITEMS_c_f_csch_GNLM_NGHBRs_03 (c_f_csch_GNLM_NGHBRs_03* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GNLM_NGHBRs_03_data** tmp;
		tmp = (c_f_csch_GNLM_NGHBRs_03_data**)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_03_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GNLM_NGHBRs_03_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GNLM_NGHBRs_03_data*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_03_data));
			INIT_c_f_csch_GNLM_NGHBRs_03_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GNLM_NGHBRs_03_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GNLM_NGHBRs_04'. Existing items are not changed. */
int SETITEMS_c_f_csch_GNLM_NGHBRs_04 (c_f_csch_GNLM_NGHBRs_04* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GNLM_NGHBRs_04_data** tmp;
		tmp = (c_f_csch_GNLM_NGHBRs_04_data**)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_04_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GNLM_NGHBRs_04_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GNLM_NGHBRs_04_data*)EDAlloc (sizeof (c_f_csch_GNLM_NGHBRs_04_data));
			INIT_c_f_csch_GNLM_NGHBRs_04_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GNLM_NGHBRs_04_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GNLM_HRPD_NGHBRs'. Existing items are not changed. */
int SETITEMS_c_f_csch_GNLM_HRPD_NGHBRs (c_f_csch_GNLM_HRPD_NGHBRs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GNLM_HRPD_NGHBRs_data** tmp;
		tmp = (c_f_csch_GNLM_HRPD_NGHBRs_data**)EDAlloc (sizeof (c_f_csch_GNLM_HRPD_NGHBRs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GNLM_HRPD_NGHBRs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GNLM_HRPD_NGHBRs_data*)EDAlloc (sizeof (c_f_csch_GNLM_HRPD_NGHBRs_data));
			INIT_c_f_csch_GNLM_HRPD_NGHBRs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GNLM_HRPD_NGHBRs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_UZIM_UZIDs'. Existing items are not changed. */
int SETITEMS_c_f_csch_UZIM_UZIDs (c_f_csch_UZIM_UZIDs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_UZIM_UZIDs_data** tmp;
		tmp = (c_f_csch_UZIM_UZIDs_data**)EDAlloc (sizeof (c_f_csch_UZIM_UZIDs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_UZIM_UZIDs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_UZIM_UZIDs_data*)EDAlloc (sizeof (c_f_csch_UZIM_UZIDs_data));
			INIT_c_f_csch_UZIM_UZIDs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_UZIM_UZIDs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_PNLM_RADIO_INTERFACEs'. Existing items are not changed. */
int SETITEMS_c_f_csch_PNLM_RADIO_INTERFACEs (c_f_csch_PNLM_RADIO_INTERFACEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_PNLM_RADIO_INTERFACEs_data** tmp;
		tmp = (c_f_csch_PNLM_RADIO_INTERFACEs_data**)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_PNLM_RADIO_INTERFACEs_data*)EDAlloc (sizeof (c_f_csch_PNLM_RADIO_INTERFACEs_data));
			INIT_c_f_csch_PNLM_RADIO_INTERFACEs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_PNLM_RADIO_INTERFACEs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_EGSRDM_ADD_RECORDs'. Existing items are not changed. */
int SETITEMS_c_f_csch_EGSRDM_ADD_RECORDs (c_f_csch_EGSRDM_ADD_RECORDs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_EGSRDM_ADD_RECORDs_data** tmp;
		tmp = (c_f_csch_EGSRDM_ADD_RECORDs_data**)EDAlloc (sizeof (c_f_csch_EGSRDM_ADD_RECORDs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_EGSRDM_ADD_RECORDs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_EGSRDM_ADD_RECORDs_data*)EDAlloc (sizeof (c_f_csch_EGSRDM_ADD_RECORDs_data));
			INIT_c_f_csch_EGSRDM_ADD_RECORDs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_EGSRDM_ADD_RECORDs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECCLM_FREQs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECCLM_FREQs (c_f_csch_ECCLM_FREQs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECCLM_FREQs_data** tmp;
		tmp = (c_f_csch_ECCLM_FREQs_data**)EDAlloc (sizeof (c_f_csch_ECCLM_FREQs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECCLM_FREQs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECCLM_FREQs_data*)EDAlloc (sizeof (c_f_csch_ECCLM_FREQs_data));
			INIT_c_f_csch_ECCLM_FREQs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECCLM_FREQs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECCLM_FREQs_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECCLM_FREQs_01 (c_f_csch_ECCLM_FREQs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECCLM_FREQs_01_data** tmp;
		tmp = (c_f_csch_ECCLM_FREQs_01_data**)EDAlloc (sizeof (c_f_csch_ECCLM_FREQs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECCLM_FREQs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECCLM_FREQs_01_data*)EDAlloc (sizeof (c_f_csch_ECCLM_FREQs_01_data));
			INIT_c_f_csch_ECCLM_FREQs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECCLM_FREQs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECCLM_FREQs_02'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECCLM_FREQs_02 (c_f_csch_ECCLM_FREQs_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECCLM_FREQs_02_data** tmp;
		tmp = (c_f_csch_ECCLM_FREQs_02_data**)EDAlloc (sizeof (c_f_csch_ECCLM_FREQs_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECCLM_FREQs_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECCLM_FREQs_02_data*)EDAlloc (sizeof (c_f_csch_ECCLM_FREQs_02_data));
			INIT_c_f_csch_ECCLM_FREQs_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECCLM_FREQs_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECCLM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECCLM_data_01 (c_f_csch_ECCLM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECCLM_data_01_data** tmp;
		tmp = (c_f_csch_ECCLM_data_01_data**)EDAlloc (sizeof (c_f_csch_ECCLM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECCLM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECCLM_data_01_data*)EDAlloc (sizeof (c_f_csch_ECCLM_data_01_data));
			INIT_c_f_csch_ECCLM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECCLM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECCLM_FREQs_03'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECCLM_FREQs_03 (c_f_csch_ECCLM_FREQs_03* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECCLM_FREQs_03_data** tmp;
		tmp = (c_f_csch_ECCLM_FREQs_03_data**)EDAlloc (sizeof (c_f_csch_ECCLM_FREQs_03_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECCLM_FREQs_03_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECCLM_FREQs_03_data*)EDAlloc (sizeof (c_f_csch_ECCLM_FREQs_03_data));
			INIT_c_f_csch_ECCLM_FREQs_03_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECCLM_FREQs_03_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ECCLM_BANDs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ECCLM_BANDs (c_f_csch_ECCLM_BANDs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ECCLM_BANDs_data** tmp;
		tmp = (c_f_csch_ECCLM_BANDs_data**)EDAlloc (sizeof (c_f_csch_ECCLM_BANDs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ECCLM_BANDs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ECCLM_BANDs_data*)EDAlloc (sizeof (c_f_csch_ECCLM_BANDs_data));
			INIT_c_f_csch_ECCLM_BANDs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ECCLM_BANDs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_UZRM_ASSIGN_UZID'. Existing items are not changed. */
int SETITEMS_c_f_csch_UZRM_ASSIGN_UZID (c_f_csch_UZRM_ASSIGN_UZID* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MCRRPM_FCCCHs'. Existing items are not changed. */
int SETITEMS_c_f_csch_MCRRPM_FCCCHs (c_f_csch_MCRRPM_FCCCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MCRRPM_FCCCHs_data** tmp;
		tmp = (c_f_csch_MCRRPM_FCCCHs_data**)EDAlloc (sizeof (c_f_csch_MCRRPM_FCCCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MCRRPM_FCCCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MCRRPM_FCCCHs_data*)EDAlloc (sizeof (c_f_csch_MCRRPM_FCCCHs_data));
			INIT_c_f_csch_MCRRPM_FCCCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MCRRPM_FCCCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MCRRPM_BCCH_BCASTs'. Existing items are not changed. */
int SETITEMS_c_f_csch_MCRRPM_BCCH_BCASTs (c_f_csch_MCRRPM_BCCH_BCASTs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MCRRPM_BCCH_BCASTs_data** tmp;
		tmp = (c_f_csch_MCRRPM_BCCH_BCASTs_data**)EDAlloc (sizeof (c_f_csch_MCRRPM_BCCH_BCASTs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MCRRPM_BCCH_BCASTs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MCRRPM_BCCH_BCASTs_data*)EDAlloc (sizeof (c_f_csch_MCRRPM_BCCH_BCASTs_data));
			INIT_c_f_csch_MCRRPM_BCCH_BCASTs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MCRRPM_BCCH_BCASTs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MCRRPM_QPCHs'. Existing items are not changed. */
int SETITEMS_c_f_csch_MCRRPM_QPCHs (c_f_csch_MCRRPM_QPCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MCRRPM_QPCHs_data** tmp;
		tmp = (c_f_csch_MCRRPM_QPCHs_data**)EDAlloc (sizeof (c_f_csch_MCRRPM_QPCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MCRRPM_QPCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MCRRPM_QPCHs_data*)EDAlloc (sizeof (c_f_csch_MCRRPM_QPCHs_data));
			INIT_c_f_csch_MCRRPM_QPCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MCRRPM_QPCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MCRRPM_PDCCHs'. Existing items are not changed. */
int SETITEMS_c_f_csch_MCRRPM_PDCCHs (c_f_csch_MCRRPM_PDCCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MCRRPM_PDCCHs_data** tmp;
		tmp = (c_f_csch_MCRRPM_PDCCHs_data**)EDAlloc (sizeof (c_f_csch_MCRRPM_PDCCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MCRRPM_PDCCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MCRRPM_PDCCHs_data*)EDAlloc (sizeof (c_f_csch_MCRRPM_PDCCHs_data));
			INIT_c_f_csch_MCRRPM_PDCCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MCRRPM_PDCCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_MCRRPM_CAND_BAND_CLASSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_MCRRPM_CAND_BAND_CLASSs (c_f_csch_MCRRPM_CAND_BAND_CLASSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_MCRRPM_CAND_BAND_CLASSs_data** tmp;
		tmp = (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data**)EDAlloc (sizeof (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data*)EDAlloc (sizeof (c_f_csch_MCRRPM_CAND_BAND_CLASSs_data));
			INIT_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_MCRRPM_CAND_BAND_CLASSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_EAPM_MODE_SELECTION_ENTRIESs'. Existing items are not changed. */
int SETITEMS_c_f_csch_EAPM_MODE_SELECTION_ENTRIESs (c_f_csch_EAPM_MODE_SELECTION_ENTRIESs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data** tmp;
		tmp = (c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data**)EDAlloc (sizeof (c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data*)EDAlloc (sizeof (c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data));
			INIT_c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_EAPM_MODE_SELECTION_ENTRIESs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_EAPM_MODE_PARM_RECs'. Existing items are not changed. */
int SETITEMS_c_f_csch_EAPM_MODE_PARM_RECs (c_f_csch_EAPM_MODE_PARM_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_EAPM_MODE_PARM_RECs_data** tmp;
		tmp = (c_f_csch_EAPM_MODE_PARM_RECs_data**)EDAlloc (sizeof (c_f_csch_EAPM_MODE_PARM_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_EAPM_MODE_PARM_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_EAPM_MODE_PARM_RECs_data*)EDAlloc (sizeof (c_f_csch_EAPM_MODE_PARM_RECs_data));
			INIT_c_f_csch_EAPM_MODE_PARM_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_EAPM_MODE_PARM_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_EAPM_CACHs'. Existing items are not changed. */
int SETITEMS_c_f_csch_EAPM_CACHs (c_f_csch_EAPM_CACHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_EAPM_CACHs_data** tmp;
		tmp = (c_f_csch_EAPM_CACHs_data**)EDAlloc (sizeof (c_f_csch_EAPM_CACHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_EAPM_CACHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_EAPM_CACHs_data*)EDAlloc (sizeof (c_f_csch_EAPM_CACHs_data));
			INIT_c_f_csch_EAPM_CACHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_EAPM_CACHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_EAPM_CPCCHs'. Existing items are not changed. */
int SETITEMS_c_f_csch_EAPM_CPCCHs (c_f_csch_EAPM_CPCCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_EAPM_CPCCHs_data** tmp;
		tmp = (c_f_csch_EAPM_CPCCHs_data**)EDAlloc (sizeof (c_f_csch_EAPM_CPCCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_EAPM_CPCCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_EAPM_CPCCHs_data*)EDAlloc (sizeof (c_f_csch_EAPM_CPCCHs_data));
			INIT_c_f_csch_EAPM_CPCCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_EAPM_CPCCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_EAPM_ACCT_SOs'. Existing items are not changed. */
int SETITEMS_c_f_csch_EAPM_ACCT_SOs (c_f_csch_EAPM_ACCT_SOs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_EAPM_ACCT_SOs_data** tmp;
		tmp = (c_f_csch_EAPM_ACCT_SOs_data**)EDAlloc (sizeof (c_f_csch_EAPM_ACCT_SOs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_EAPM_ACCT_SOs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_EAPM_ACCT_SOs_data*)EDAlloc (sizeof (c_f_csch_EAPM_ACCT_SOs_data));
			INIT_c_f_csch_EAPM_ACCT_SOs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_EAPM_ACCT_SOs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_EAPM_ACCT_SO_GRPs'. Existing items are not changed. */
int SETITEMS_c_f_csch_EAPM_ACCT_SO_GRPs (c_f_csch_EAPM_ACCT_SO_GRPs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_EAPM_ACCT_SO_GRPs_data** tmp;
		tmp = (c_f_csch_EAPM_ACCT_SO_GRPs_data**)EDAlloc (sizeof (c_f_csch_EAPM_ACCT_SO_GRPs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_EAPM_ACCT_SO_GRPs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_EAPM_ACCT_SO_GRPs_data*)EDAlloc (sizeof (c_f_csch_EAPM_ACCT_SO_GRPs_data));
			INIT_c_f_csch_EAPM_ACCT_SO_GRPs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_EAPM_ACCT_SO_GRPs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_UNLM_RADIO_INTERFACEs'. Existing items are not changed. */
int SETITEMS_c_f_csch_UNLM_RADIO_INTERFACEs (c_f_csch_UNLM_RADIO_INTERFACEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_UNLM_RADIO_INTERFACEs_data** tmp;
		tmp = (c_f_csch_UNLM_RADIO_INTERFACEs_data**)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_UNLM_RADIO_INTERFACEs_data*)EDAlloc (sizeof (c_f_csch_UNLM_RADIO_INTERFACEs_data));
			INIT_c_f_csch_UNLM_RADIO_INTERFACEs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_UNLM_RADIO_INTERFACEs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_BSPM_FSCHs'. Existing items are not changed. */
int SETITEMS_c_f_csch_BSPM_FSCHs (c_f_csch_BSPM_FSCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_BSPM_FSCHs_data** tmp;
		tmp = (c_f_csch_BSPM_FSCHs_data**)EDAlloc (sizeof (c_f_csch_BSPM_FSCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_BSPM_FSCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_BSPM_FSCHs_data*)EDAlloc (sizeof (c_f_csch_BSPM_FSCHs_data));
			INIT_c_f_csch_BSPM_FSCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_BSPM_FSCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_BSPM_BCMC_PROGRAMSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_BSPM_BCMC_PROGRAMSs (c_f_csch_BSPM_BCMC_PROGRAMSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_BSPM_BCMC_PROGRAMSs_data** tmp;
		tmp = (c_f_csch_BSPM_BCMC_PROGRAMSs_data**)EDAlloc (sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_BSPM_BCMC_PROGRAMSs_data*)EDAlloc (sizeof (c_f_csch_BSPM_BCMC_PROGRAMSs_data));
			INIT_c_f_csch_BSPM_BCMC_PROGRAMSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_BSPM_BCMC_PROGRAMSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_BSPM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_BSPM_data_01 (c_f_csch_BSPM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_BSPM_data_01_data** tmp;
		tmp = (c_f_csch_BSPM_data_01_data**)EDAlloc (sizeof (c_f_csch_BSPM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_BSPM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_BSPM_data_01_data*)EDAlloc (sizeof (c_f_csch_BSPM_data_01_data));
			INIT_c_f_csch_BSPM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_BSPM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_APIDM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_csch_APIDM_data_01 (c_f_csch_APIDM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_APIDM_data_01_data** tmp;
		tmp = (c_f_csch_APIDM_data_01_data**)EDAlloc (sizeof (c_f_csch_APIDM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_APIDM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_APIDM_data_01_data*)EDAlloc (sizeof (c_f_csch_APIDM_data_01_data));
			INIT_c_f_csch_APIDM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_APIDM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_APIDTM_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_APIDTM_FIELDSs (c_f_csch_APIDTM_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_APIDTM_FIELDSs_data** tmp;
		tmp = (c_f_csch_APIDTM_FIELDSs_data**)EDAlloc (sizeof (c_f_csch_APIDTM_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_APIDTM_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_APIDTM_FIELDSs_data*)EDAlloc (sizeof (c_f_csch_APIDTM_FIELDSs_data));
			INIT_c_f_csch_APIDTM_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_APIDTM_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_APPIM_APPI_RECs'. Existing items are not changed. */
int SETITEMS_c_f_csch_APPIM_APPI_RECs (c_f_csch_APPIM_APPI_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_APPIM_APPI_RECs_data** tmp;
		tmp = (c_f_csch_APPIM_APPI_RECs_data**)EDAlloc (sizeof (c_f_csch_APPIM_APPI_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_APPIM_APPI_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_APPIM_APPI_RECs_data*)EDAlloc (sizeof (c_f_csch_APPIM_APPI_RECs_data));
			INIT_c_f_csch_APPIM_APPI_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_APPIM_APPI_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GOIM_GOI_RECs'. Existing items are not changed. */
int SETITEMS_c_f_csch_GOIM_GOI_RECs (c_f_csch_GOIM_GOI_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GOIM_GOI_RECs_data** tmp;
		tmp = (c_f_csch_GOIM_GOI_RECs_data**)EDAlloc (sizeof (c_f_csch_GOIM_GOI_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GOIM_GOI_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GOIM_GOI_RECs_data*)EDAlloc (sizeof (c_f_csch_GOIM_GOI_RECs_data));
			INIT_c_f_csch_GOIM_GOI_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GOIM_GOI_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_FDCCLM_CAND_BAND_CLASSs'. Existing items are not changed. */
int SETITEMS_c_f_csch_FDCCLM_CAND_BAND_CLASSs (c_f_csch_FDCCLM_CAND_BAND_CLASSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_FDCCLM_CAND_BAND_CLASSs_data** tmp;
		tmp = (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data**)EDAlloc (sizeof (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data*)EDAlloc (sizeof (c_f_csch_FDCCLM_CAND_BAND_CLASSs_data));
			INIT_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_FDCCLM_CAND_BAND_CLASSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_GEM_GE_RECs'. Existing items are not changed. */
int SETITEMS_c_f_csch_GEM_GE_RECs (c_f_csch_GEM_GE_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_GEM_GE_RECs_data** tmp;
		tmp = (c_f_csch_GEM_GE_RECs_data**)EDAlloc (sizeof (c_f_csch_GEM_GE_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_GEM_GE_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_GEM_GE_RECs_data*)EDAlloc (sizeof (c_f_csch_GEM_GE_RECs_data));
			INIT_c_f_csch_GEM_GE_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_GEM_GE_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_ATIM_RADIO_INTERFACEs'. Existing items are not changed. */
int SETITEMS_c_f_csch_ATIM_RADIO_INTERFACEs (c_f_csch_ATIM_RADIO_INTERFACEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_ATIM_RADIO_INTERFACEs_data** tmp;
		tmp = (c_f_csch_ATIM_RADIO_INTERFACEs_data**)EDAlloc (sizeof (c_f_csch_ATIM_RADIO_INTERFACEs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_ATIM_RADIO_INTERFACEs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_ATIM_RADIO_INTERFACEs_data*)EDAlloc (sizeof (c_f_csch_ATIM_RADIO_INTERFACEs_data));
			INIT_c_f_csch_ATIM_RADIO_INTERFACEs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_ATIM_RADIO_INTERFACEs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_FGOIM_GOI_RECs'. Existing items are not changed. */
int SETITEMS_c_f_csch_FGOIM_GOI_RECs (c_f_csch_FGOIM_GOI_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_FGOIM_GOI_RECs_data** tmp;
		tmp = (c_f_csch_FGOIM_GOI_RECs_data**)EDAlloc (sizeof (c_f_csch_FGOIM_GOI_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_FGOIM_GOI_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_FGOIM_GOI_RECs_data*)EDAlloc (sizeof (c_f_csch_FGOIM_GOI_RECs_data));
			INIT_c_f_csch_FGOIM_GOI_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_FGOIM_GOI_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_csch_HDPPM_records'. Existing items are not changed. */
int SETITEMS_c_f_csch_HDPPM_records (c_f_csch_HDPPM_records* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_csch_HDPPM_records_data** tmp;
		tmp = (c_f_csch_HDPPM_records_data**)EDAlloc (sizeof (c_f_csch_HDPPM_records_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_csch_HDPPM_records_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_csch_HDPPM_records_data*)EDAlloc (sizeof (c_f_csch_HDPPM_records_data));
			INIT_c_f_csch_HDPPM_records_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_csch_HDPPM_records_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_AWIM_INFO_RECORDs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_AWIM_INFO_RECORDs (c_f_dsch_AWIM_INFO_RECORDs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_3_7_5_encrypted** tmp;
		tmp = (c_InfoRec_3_7_5_encrypted**)EDAlloc (sizeof (c_InfoRec_3_7_5_encrypted*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_3_7_5_encrypted*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_3_7_5_encrypted*)EDAlloc (sizeof (c_InfoRec_3_7_5_encrypted));
			INIT_c_InfoRec_3_7_5_encrypted (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_3_7_5_encrypted (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_DBM_MOBILE_COUNTRY_CODE'. Existing items are not changed. */
int SETITEMS_c_f_dsch_DBM_MOBILE_COUNTRY_CODE (c_f_dsch_DBM_MOBILE_COUNTRY_CODE* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_DBM_COUNTRY_BURST_TYPE'. Existing items are not changed. */
int SETITEMS_c_f_dsch_DBM_COUNTRY_BURST_TYPE (c_f_dsch_DBM_COUNTRY_BURST_TYPE* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_DBM_EXTENDED_BURST_TYPE'. Existing items are not changed. */
int SETITEMS_c_f_dsch_DBM_EXTENDED_BURST_TYPE (c_f_dsch_DBM_EXTENDED_BURST_TYPE* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_DBM_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_DBM_FIELDSs (c_f_dsch_DBM_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_DBM_FIELDSs_data** tmp;
		tmp = (c_f_dsch_DBM_FIELDSs_data**)EDAlloc (sizeof (c_f_dsch_DBM_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_DBM_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_DBM_FIELDSs_data*)EDAlloc (sizeof (c_f_dsch_DBM_FIELDSs_data));
			INIT_c_f_dsch_DBM_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_DBM_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_NLUM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_NLUM_data_01 (c_f_dsch_NLUM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_NLUM_data_01_data** tmp;
		tmp = (c_f_dsch_NLUM_data_01_data**)EDAlloc (sizeof (c_f_dsch_NLUM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_NLUM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_NLUM_data_01_data*)EDAlloc (sizeof (c_f_dsch_NLUM_data_01_data));
			INIT_c_f_dsch_NLUM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_NLUM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_BDTMFM_DIGITSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_BDTMFM_DIGITSs (c_f_dsch_BDTMFM_DIGITSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_BDTMFM_DIGITSs_data** tmp;
		tmp = (c_f_dsch_BDTMFM_DIGITSs_data**)EDAlloc (sizeof (c_f_dsch_BDTMFM_DIGITSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_BDTMFM_DIGITSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_BDTMFM_DIGITSs_data*)EDAlloc (sizeof (c_f_dsch_BDTMFM_DIGITSs_data));
			INIT_c_f_dsch_BDTMFM_DIGITSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_BDTMFM_DIGITSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_RTPM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_RTPM_data_01 (c_f_dsch_RTPM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_RTPM_data_01_data** tmp;
		tmp = (c_f_dsch_RTPM_data_01_data**)EDAlloc (sizeof (c_f_dsch_RTPM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_RTPM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_RTPM_data_01_data*)EDAlloc (sizeof (c_f_dsch_RTPM_data_01_data));
			INIT_c_f_dsch_RTPM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_RTPM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_STPM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_STPM_data_01 (c_f_dsch_STPM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_STPM_data_01_data** tmp;
		tmp = (c_f_dsch_STPM_data_01_data**)EDAlloc (sizeof (c_f_dsch_STPM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_STPM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_STPM_data_01_data*)EDAlloc (sizeof (c_f_dsch_STPM_data_01_data));
			INIT_c_f_dsch_STPM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_STPM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_FWIM_INFO_RECORDs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_FWIM_INFO_RECORDs (c_f_dsch_FWIM_INFO_RECORDs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_3_7_5_encrypted** tmp;
		tmp = (c_InfoRec_3_7_5_encrypted**)EDAlloc (sizeof (c_InfoRec_3_7_5_encrypted*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_3_7_5_encrypted*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_3_7_5_encrypted*)EDAlloc (sizeof (c_InfoRec_3_7_5_encrypted));
			INIT_c_InfoRec_3_7_5_encrypted (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_3_7_5_encrypted (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_STRQM_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_STRQM_FIELDSs (c_f_dsch_STRQM_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_STRQM_FIELDSs_data** tmp;
		tmp = (c_f_dsch_STRQM_FIELDSs_data**)EDAlloc (sizeof (c_f_dsch_STRQM_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_STRQM_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_STRQM_FIELDSs_data*)EDAlloc (sizeof (c_f_dsch_STRQM_FIELDSs_data));
			INIT_c_f_dsch_STRQM_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_STRQM_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_EHDM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_EHDM_data_01 (c_f_dsch_EHDM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_EHDM_data_01_data** tmp;
		tmp = (c_f_dsch_EHDM_data_01_data**)EDAlloc (sizeof (c_f_dsch_EHDM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_EHDM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_EHDM_data_01_data*)EDAlloc (sizeof (c_f_dsch_EHDM_data_01_data));
			INIT_c_f_dsch_EHDM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_EHDM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_SCM_CALLS_ASSIGNs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_SCM_CALLS_ASSIGNs (c_f_dsch_SCM_CALLS_ASSIGNs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_SCM_CALLS_ASSIGNs_data** tmp;
		tmp = (c_f_dsch_SCM_CALLS_ASSIGNs_data**)EDAlloc (sizeof (c_f_dsch_SCM_CALLS_ASSIGNs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_SCM_CALLS_ASSIGNs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_SCM_CALLS_ASSIGNs_data*)EDAlloc (sizeof (c_f_dsch_SCM_CALLS_ASSIGNs_data));
			INIT_c_f_dsch_SCM_CALLS_ASSIGNs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_SCM_CALLS_ASSIGNs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_SCAM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_SCAM_data_01 (c_f_dsch_SCAM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_SCAM_data_01_data** tmp;
		tmp = (c_f_dsch_SCAM_data_01_data**)EDAlloc (sizeof (c_f_dsch_SCAM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_SCAM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_SCAM_data_01_data*)EDAlloc (sizeof (c_f_dsch_SCAM_data_01_data));
			INIT_c_f_dsch_SCAM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_SCAM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_SCAM_data_02'. Existing items are not changed. */
int SETITEMS_c_f_dsch_SCAM_data_02 (c_f_dsch_SCAM_data_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_SCAM_data_02_data** tmp;
		tmp = (c_f_dsch_SCAM_data_02_data**)EDAlloc (sizeof (c_f_dsch_SCAM_data_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_SCAM_data_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_SCAM_data_02_data*)EDAlloc (sizeof (c_f_dsch_SCAM_data_02_data));
			INIT_c_f_dsch_SCAM_data_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_SCAM_data_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_PCNM_SUPs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_PCNM_SUPs (c_f_dsch_PCNM_SUPs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_PCNM_SUPs_data** tmp;
		tmp = (c_f_dsch_PCNM_SUPs_data**)EDAlloc (sizeof (c_f_dsch_PCNM_SUPs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_PCNM_SUPs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_PCNM_SUPs_data*)EDAlloc (sizeof (c_f_dsch_PCNM_SUPs_data));
			INIT_c_f_dsch_PCNM_SUPs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_PCNM_SUPs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ENLUM_NGHBRs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ENLUM_NGHBRs (c_f_dsch_ENLUM_NGHBRs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ENLUM_NGHBRs_data** tmp;
		tmp = (c_f_dsch_ENLUM_NGHBRs_data**)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ENLUM_NGHBRs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ENLUM_NGHBRs_data*)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_data));
			INIT_c_f_dsch_ENLUM_NGHBRs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ENLUM_NGHBRs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ENLUM_NGHBRs_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ENLUM_NGHBRs_01 (c_f_dsch_ENLUM_NGHBRs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ENLUM_NGHBRs_01_data** tmp;
		tmp = (c_f_dsch_ENLUM_NGHBRs_01_data**)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ENLUM_NGHBRs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ENLUM_NGHBRs_01_data*)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_01_data));
			INIT_c_f_dsch_ENLUM_NGHBRs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ENLUM_NGHBRs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ENLUM_NGHBRs_02'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ENLUM_NGHBRs_02 (c_f_dsch_ENLUM_NGHBRs_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ENLUM_NGHBRs_02_data** tmp;
		tmp = (c_f_dsch_ENLUM_NGHBRs_02_data**)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ENLUM_NGHBRs_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ENLUM_NGHBRs_02_data*)EDAlloc (sizeof (c_f_dsch_ENLUM_NGHBRs_02_data));
			INIT_c_f_dsch_ENLUM_NGHBRs_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ENLUM_NGHBRs_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_GHDM_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_GHDM_PILOTSs (c_f_dsch_GHDM_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_GHDM_PILOTSs_data** tmp;
		tmp = (c_f_dsch_GHDM_PILOTSs_data**)EDAlloc (sizeof (c_f_dsch_GHDM_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_GHDM_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_GHDM_PILOTSs_data*)EDAlloc (sizeof (c_f_dsch_GHDM_PILOTSs_data));
			INIT_c_f_dsch_GHDM_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_GHDM_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ERM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ERM_data_01 (c_f_dsch_ERM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ERM_data_01_data** tmp;
		tmp = (c_f_dsch_ERM_data_01_data**)EDAlloc (sizeof (c_f_dsch_ERM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ERM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ERM_data_01_data*)EDAlloc (sizeof (c_f_dsch_ERM_data_01_data));
			INIT_c_f_dsch_ERM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ERM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ERM_FSCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ERM_FSCHs (c_f_dsch_ERM_FSCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ERM_FSCHs_data** tmp;
		tmp = (c_f_dsch_ERM_FSCHs_data**)EDAlloc (sizeof (c_f_dsch_ERM_FSCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ERM_FSCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ERM_FSCHs_data*)EDAlloc (sizeof (c_f_dsch_ERM_FSCHs_data));
			INIT_c_f_dsch_ERM_FSCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ERM_FSCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ERM_BCMC_PROGRAMSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ERM_BCMC_PROGRAMSs (c_f_dsch_ERM_BCMC_PROGRAMSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ERM_BCMC_PROGRAMSs_data** tmp;
		tmp = (c_f_dsch_ERM_BCMC_PROGRAMSs_data**)EDAlloc (sizeof (c_f_dsch_ERM_BCMC_PROGRAMSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ERM_BCMC_PROGRAMSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ERM_BCMC_PROGRAMSs_data*)EDAlloc (sizeof (c_f_dsch_ERM_BCMC_PROGRAMSs_data));
			INIT_c_f_dsch_ERM_BCMC_PROGRAMSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ERM_BCMC_PROGRAMSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_FOR_ASSIGNs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_FOR_ASSIGNs (c_f_dsch_UHDM_FOR_ASSIGNs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_FOR_ASSIGNs_data** tmp;
		tmp = (c_f_dsch_UHDM_FOR_ASSIGNs_data**)EDAlloc (sizeof (c_f_dsch_UHDM_FOR_ASSIGNs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_FOR_ASSIGNs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_FOR_ASSIGNs_data*)EDAlloc (sizeof (c_f_dsch_UHDM_FOR_ASSIGNs_data));
			INIT_c_f_dsch_UHDM_FOR_ASSIGNs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_FOR_ASSIGNs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_REV_ASSIGNs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_REV_ASSIGNs (c_f_dsch_UHDM_REV_ASSIGNs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_REV_ASSIGNs_data** tmp;
		tmp = (c_f_dsch_UHDM_REV_ASSIGNs_data**)EDAlloc (sizeof (c_f_dsch_UHDM_REV_ASSIGNs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_REV_ASSIGNs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_REV_ASSIGNs_data*)EDAlloc (sizeof (c_f_dsch_UHDM_REV_ASSIGNs_data));
			INIT_c_f_dsch_UHDM_REV_ASSIGNs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_REV_ASSIGNs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_CALLS_ASSIGNs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_CALLS_ASSIGNs (c_f_dsch_UHDM_CALLS_ASSIGNs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_CALLS_ASSIGNs_data** tmp;
		tmp = (c_f_dsch_UHDM_CALLS_ASSIGNs_data**)EDAlloc (sizeof (c_f_dsch_UHDM_CALLS_ASSIGNs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_CALLS_ASSIGNs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_CALLS_ASSIGNs_data*)EDAlloc (sizeof (c_f_dsch_UHDM_CALLS_ASSIGNs_data));
			INIT_c_f_dsch_UHDM_CALLS_ASSIGNs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_CALLS_ASSIGNs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_UHDM_FOR_ASSIGNs_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_UHDM_FOR_ASSIGNs_01 (c_f_dsch_UHDM_FOR_ASSIGNs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_UHDM_FOR_ASSIGNs_01_data** tmp;
		tmp = (c_f_dsch_UHDM_FOR_ASSIGNs_01_data**)EDAlloc (sizeof (c_f_dsch_UHDM_FOR_ASSIGNs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_UHDM_FOR_ASSIGNs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_UHDM_FOR_ASSIGNs_01_data*)EDAlloc (sizeof (c_f_dsch_UHDM_FOR_ASSIGNs_01_data));
			INIT_c_f_dsch_UHDM_FOR_ASSIGNs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_UHDM_FOR_ASSIGNs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_REV_CFG_RECSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_REV_CFG_RECSs (c_f_dsch_ESCAM_REV_CFG_RECSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_REV_CFG_RECSs_data** tmp;
		tmp = (c_f_dsch_ESCAM_REV_CFG_RECSs_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_REV_CFG_RECSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_REV_CFG_RECSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_REV_CFG_RECSs_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_REV_CFG_RECSs_data));
			INIT_c_f_dsch_ESCAM_REV_CFG_RECSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_REV_CFG_RECSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_REV_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_REV_SCHs (c_f_dsch_ESCAM_REV_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_REV_SCHs_data** tmp;
		tmp = (c_f_dsch_ESCAM_REV_SCHs_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_REV_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_REV_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_REV_SCHs_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_REV_SCHs_data));
			INIT_c_f_dsch_ESCAM_REV_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_REV_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_FOR_CFG_RECSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_FOR_CFG_RECSs (c_f_dsch_ESCAM_FOR_CFG_RECSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_FOR_CFG_RECSs_data** tmp;
		tmp = (c_f_dsch_ESCAM_FOR_CFG_RECSs_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_FOR_CFG_RECSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_FOR_CFG_RECSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_FOR_CFG_RECSs_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_FOR_CFG_RECSs_data));
			INIT_c_f_dsch_ESCAM_FOR_CFG_RECSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_FOR_CFG_RECSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_FOR_SCHs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_FOR_SCHs (c_f_dsch_ESCAM_FOR_SCHs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_FOR_SCHs_data** tmp;
		tmp = (c_f_dsch_ESCAM_FOR_SCHs_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_FOR_SCHs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_FOR_SCHs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_FOR_SCHs_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_FOR_SCHs_data));
			INIT_c_f_dsch_ESCAM_FOR_SCHs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_FOR_SCHs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_SUPs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_SUPs (c_f_dsch_ESCAM_SUPs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_SUPs_data** tmp;
		tmp = (c_f_dsch_ESCAM_SUPs_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_SUPs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_SUPs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_SUPs_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_SUPs_data));
			INIT_c_f_dsch_ESCAM_SUPs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_SUPs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_data_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_data_01 (c_f_dsch_ESCAM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_data_01_data** tmp;
		tmp = (c_f_dsch_ESCAM_data_01_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_data_01_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_data_01_data));
			INIT_c_f_dsch_ESCAM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_P3X_CFGs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_P3X_CFGs (c_f_dsch_ESCAM_P3X_CFGs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_P3X_CFGs_data** tmp;
		tmp = (c_f_dsch_ESCAM_P3X_CFGs_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_P3X_CFGs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_P3X_CFGs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_P3X_CFGs_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_P3X_CFGs_data));
			INIT_c_f_dsch_ESCAM_P3X_CFGs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_P3X_CFGs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_data_02'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_data_02 (c_f_dsch_ESCAM_data_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_data_02_data** tmp;
		tmp = (c_f_dsch_ESCAM_data_02_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_data_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_data_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_data_02_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_data_02_data));
			INIT_c_f_dsch_ESCAM_data_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_data_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_FOR_SCHs_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_FOR_SCHs_01 (c_f_dsch_ESCAM_FOR_SCHs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_FOR_SCHs_01_data** tmp;
		tmp = (c_f_dsch_ESCAM_FOR_SCHs_01_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_FOR_SCHs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_FOR_SCHs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_FOR_SCHs_01_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_FOR_SCHs_01_data));
			INIT_c_f_dsch_ESCAM_FOR_SCHs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_FOR_SCHs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_REV_SCHs_01'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_REV_SCHs_01 (c_f_dsch_ESCAM_REV_SCHs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_REV_SCHs_01_data** tmp;
		tmp = (c_f_dsch_ESCAM_REV_SCHs_01_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_REV_SCHs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_REV_SCHs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_REV_SCHs_01_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_REV_SCHs_01_data));
			INIT_c_f_dsch_ESCAM_REV_SCHs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_REV_SCHs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ESCAM_FOR_SCHs_02'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ESCAM_FOR_SCHs_02 (c_f_dsch_ESCAM_FOR_SCHs_02* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ESCAM_FOR_SCHs_02_data** tmp;
		tmp = (c_f_dsch_ESCAM_FOR_SCHs_02_data**)EDAlloc (sizeof (c_f_dsch_ESCAM_FOR_SCHs_02_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ESCAM_FOR_SCHs_02_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ESCAM_FOR_SCHs_02_data*)EDAlloc (sizeof (c_f_dsch_ESCAM_FOR_SCHs_02_data));
			INIT_c_f_dsch_ESCAM_FOR_SCHs_02_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ESCAM_FOR_SCHs_02_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_CLAM_ADD_CALL_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_CLAM_ADD_CALL_RECORDSs (c_f_dsch_CLAM_ADD_CALL_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_CLAM_ADD_CALL_RECORDSs_data** tmp;
		tmp = (c_f_dsch_CLAM_ADD_CALL_RECORDSs_data**)EDAlloc (sizeof (c_f_dsch_CLAM_ADD_CALL_RECORDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_CLAM_ADD_CALL_RECORDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_CLAM_ADD_CALL_RECORDSs_data*)EDAlloc (sizeof (c_f_dsch_CLAM_ADD_CALL_RECORDSs_data));
			INIT_c_f_dsch_CLAM_ADD_CALL_RECORDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_CLAM_ADD_CALL_RECORDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_EAWIM_RECs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_EAWIM_RECs (c_f_dsch_EAWIM_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_3_7_5_encrypted** tmp;
		tmp = (c_InfoRec_3_7_5_encrypted**)EDAlloc (sizeof (c_InfoRec_3_7_5_encrypted*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_3_7_5_encrypted*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_3_7_5_encrypted*)EDAlloc (sizeof (c_InfoRec_3_7_5_encrypted));
			INIT_c_InfoRec_3_7_5_encrypted (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_3_7_5_encrypted (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_EFWIM_RECs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_EFWIM_RECs (c_f_dsch_EFWIM_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_3_7_5_encrypted** tmp;
		tmp = (c_InfoRec_3_7_5_encrypted**)EDAlloc (sizeof (c_InfoRec_3_7_5_encrypted*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_3_7_5_encrypted*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_3_7_5_encrypted*)EDAlloc (sizeof (c_InfoRec_3_7_5_encrypted));
			INIT_c_InfoRec_3_7_5_encrypted (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_3_7_5_encrypted (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_SMCM_RECSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_SMCM_RECSs (c_f_dsch_SMCM_RECSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_SMCM_RECSs_data** tmp;
		tmp = (c_f_dsch_SMCM_RECSs_data**)EDAlloc (sizeof (c_f_dsch_SMCM_RECSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_SMCM_RECSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_SMCM_RECSs_data*)EDAlloc (sizeof (c_f_dsch_SMCM_RECSs_data));
			INIT_c_f_dsch_SMCM_RECSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_SMCM_RECSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_BSSRSPM_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_BSSRSPM_RECORDSs (c_f_dsch_BSSRSPM_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_BSSRSPM_RECORDSs_data** tmp;
		tmp = (c_f_dsch_BSSRSPM_RECORDSs_data**)EDAlloc (sizeof (c_f_dsch_BSSRSPM_RECORDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_BSSRSPM_RECORDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_BSSRSPM_RECORDSs_data*)EDAlloc (sizeof (c_f_dsch_BSSRSPM_RECORDSs_data));
			INIT_c_f_dsch_BSSRSPM_RECORDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_BSSRSPM_RECORDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_RATCHGM_RECSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_RATCHGM_RECSs (c_f_dsch_RATCHGM_RECSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_RATCHGM_RECSs_data** tmp;
		tmp = (c_f_dsch_RATCHGM_RECSs_data**)EDAlloc (sizeof (c_f_dsch_RATCHGM_RECSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_RATCHGM_RECSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_RATCHGM_RECSs_data*)EDAlloc (sizeof (c_f_dsch_RATCHGM_RECSs_data));
			INIT_c_f_dsch_RATCHGM_RECSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_RATCHGM_RECSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ITBSPM_BCMC_PROGRAMSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ITBSPM_BCMC_PROGRAMSs (c_f_dsch_ITBSPM_BCMC_PROGRAMSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data** tmp;
		tmp = (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data**)EDAlloc (sizeof (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data*)EDAlloc (sizeof (c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data));
			INIT_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ITBSPM_BCMC_PROGRAMSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_RCPM_RC_PARAMS_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_RCPM_RC_PARAMS_RECORDSs (c_f_dsch_RCPM_RC_PARAMS_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data** tmp;
		tmp = (c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data**)EDAlloc (sizeof (c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data*)EDAlloc (sizeof (c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data));
			INIT_c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_RCPM_RC_PARAMS_RECORDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_HOSISM_PILOT_PNs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_HOSISM_PILOT_PNs (c_f_dsch_HOSISM_PILOT_PNs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_HOSISM_PILOT_PNs_data** tmp;
		tmp = (c_f_dsch_HOSISM_PILOT_PNs_data**)EDAlloc (sizeof (c_f_dsch_HOSISM_PILOT_PNs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_HOSISM_PILOT_PNs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_HOSISM_PILOT_PNs_data*)EDAlloc (sizeof (c_f_dsch_HOSISM_PILOT_PNs_data));
			INIT_c_f_dsch_HOSISM_PILOT_PNs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_HOSISM_PILOT_PNs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_GEM_GE_RECs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_GEM_GE_RECs (c_f_dsch_GEM_GE_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_GEM_GE_RECs_data** tmp;
		tmp = (c_f_dsch_GEM_GE_RECs_data**)EDAlloc (sizeof (c_f_dsch_GEM_GE_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_GEM_GE_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_GEM_GE_RECs_data*)EDAlloc (sizeof (c_f_dsch_GEM_GE_RECs_data));
			INIT_c_f_dsch_GEM_GE_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_GEM_GE_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_ATIM_RADIO_INTERFACEs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_ATIM_RADIO_INTERFACEs (c_f_dsch_ATIM_RADIO_INTERFACEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_ATIM_RADIO_INTERFACEs_data** tmp;
		tmp = (c_f_dsch_ATIM_RADIO_INTERFACEs_data**)EDAlloc (sizeof (c_f_dsch_ATIM_RADIO_INTERFACEs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_ATIM_RADIO_INTERFACEs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_ATIM_RADIO_INTERFACEs_data*)EDAlloc (sizeof (c_f_dsch_ATIM_RADIO_INTERFACEs_data));
			INIT_c_f_dsch_ATIM_RADIO_INTERFACEs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_ATIM_RADIO_INTERFACEs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_FOR_ASSIGNs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_FOR_ASSIGNs (c_f_dsch_MUHDM_FOR_ASSIGNs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_FOR_ASSIGNs_data** tmp;
		tmp = (c_f_dsch_MUHDM_FOR_ASSIGNs_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_FOR_ASSIGNs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_FOR_ASSIGNs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_FOR_ASSIGNs_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_FOR_ASSIGNs_data));
			INIT_c_f_dsch_MUHDM_FOR_ASSIGNs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_FOR_ASSIGNs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_REV_ASSIGNs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_REV_ASSIGNs (c_f_dsch_MUHDM_REV_ASSIGNs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_REV_ASSIGNs_data** tmp;
		tmp = (c_f_dsch_MUHDM_REV_ASSIGNs_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_REV_ASSIGNs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_REV_ASSIGNs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_REV_ASSIGNs_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_REV_ASSIGNs_data));
			INIT_c_f_dsch_MUHDM_REV_ASSIGNs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_REV_ASSIGNs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_f_dsch_MUHDM_CALLS_ASSIGNs'. Existing items are not changed. */
int SETITEMS_c_f_dsch_MUHDM_CALLS_ASSIGNs (c_f_dsch_MUHDM_CALLS_ASSIGNs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_f_dsch_MUHDM_CALLS_ASSIGNs_data** tmp;
		tmp = (c_f_dsch_MUHDM_CALLS_ASSIGNs_data**)EDAlloc (sizeof (c_f_dsch_MUHDM_CALLS_ASSIGNs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_f_dsch_MUHDM_CALLS_ASSIGNs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_f_dsch_MUHDM_CALLS_ASSIGNs_data*)EDAlloc (sizeof (c_f_dsch_MUHDM_CALLS_ASSIGNs_data));
			INIT_c_f_dsch_MUHDM_CALLS_ASSIGNs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_f_dsch_MUHDM_CALLS_ASSIGNs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_RGM_BCMC_PROGRAMSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_RGM_BCMC_PROGRAMSs (c_r_csch_RGM_BCMC_PROGRAMSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_RGM_BCMC_PROGRAMSs_data** tmp;
		tmp = (c_r_csch_RGM_BCMC_PROGRAMSs_data**)EDAlloc (sizeof (c_r_csch_RGM_BCMC_PROGRAMSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_RGM_BCMC_PROGRAMSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_RGM_BCMC_PROGRAMSs_data*)EDAlloc (sizeof (c_r_csch_RGM_BCMC_PROGRAMSs_data));
			INIT_c_r_csch_RGM_BCMC_PROGRAMSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_RGM_BCMC_PROGRAMSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_RGM_BAND_SUBCLASSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_RGM_BAND_SUBCLASSs (c_r_csch_RGM_BAND_SUBCLASSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_RGM_BAND_SUBCLASSs_data** tmp;
		tmp = (c_r_csch_RGM_BAND_SUBCLASSs_data**)EDAlloc (sizeof (c_r_csch_RGM_BAND_SUBCLASSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_RGM_BAND_SUBCLASSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_RGM_BAND_SUBCLASSs_data*)EDAlloc (sizeof (c_r_csch_RGM_BAND_SUBCLASSs_data));
			INIT_c_r_csch_RGM_BAND_SUBCLASSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_RGM_BAND_SUBCLASSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_DBM_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_DBM_FIELDSs (c_r_csch_DBM_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_DBM_FIELDSs_data** tmp;
		tmp = (c_r_csch_DBM_FIELDSs_data**)EDAlloc (sizeof (c_r_csch_DBM_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_DBM_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_DBM_FIELDSs_data*)EDAlloc (sizeof (c_r_csch_DBM_FIELDSs_data));
			INIT_c_r_csch_DBM_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_DBM_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_ORM_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_ORM_FIELDSs (c_r_csch_ORM_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_ORM_FIELDSs_data** tmp;
		tmp = (c_r_csch_ORM_FIELDSs_data**)EDAlloc (sizeof (c_r_csch_ORM_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_ORM_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_ORM_FIELDSs_data*)EDAlloc (sizeof (c_r_csch_ORM_FIELDSs_data));
			INIT_c_r_csch_ORM_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_ORM_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_ORM_ALT_SOs'. Existing items are not changed. */
int SETITEMS_c_r_csch_ORM_ALT_SOs (c_r_csch_ORM_ALT_SOs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_ORM_ALT_SOs_data** tmp;
		tmp = (c_r_csch_ORM_ALT_SOs_data**)EDAlloc (sizeof (c_r_csch_ORM_ALT_SOs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_ORM_ALT_SOs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_ORM_ALT_SOs_data*)EDAlloc (sizeof (c_r_csch_ORM_ALT_SOs_data));
			INIT_c_r_csch_ORM_ALT_SOs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_ORM_ALT_SOs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_ORM_ADD_SERV_INSTANCEs'. Existing items are not changed. */
int SETITEMS_c_r_csch_ORM_ADD_SERV_INSTANCEs (c_r_csch_ORM_ADD_SERV_INSTANCEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_ORM_ADD_SERV_INSTANCEs_data** tmp;
		tmp = (c_r_csch_ORM_ADD_SERV_INSTANCEs_data**)EDAlloc (sizeof (c_r_csch_ORM_ADD_SERV_INSTANCEs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_ORM_ADD_SERV_INSTANCEs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_ORM_ADD_SERV_INSTANCEs_data*)EDAlloc (sizeof (c_r_csch_ORM_ADD_SERV_INSTANCEs_data));
			INIT_c_r_csch_ORM_ADD_SERV_INSTANCEs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_ORM_ADD_SERV_INSTANCEs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_ORM_BCMC_PROGRAMSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_ORM_BCMC_PROGRAMSs (c_r_csch_ORM_BCMC_PROGRAMSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_ORM_BCMC_PROGRAMSs_data** tmp;
		tmp = (c_r_csch_ORM_BCMC_PROGRAMSs_data**)EDAlloc (sizeof (c_r_csch_ORM_BCMC_PROGRAMSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_ORM_BCMC_PROGRAMSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_ORM_BCMC_PROGRAMSs_data*)EDAlloc (sizeof (c_r_csch_ORM_BCMC_PROGRAMSs_data));
			INIT_c_r_csch_ORM_BCMC_PROGRAMSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_ORM_BCMC_PROGRAMSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_ORM_BAND_SUBCLASSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_ORM_BAND_SUBCLASSs (c_r_csch_ORM_BAND_SUBCLASSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_ORM_BAND_SUBCLASSs_data** tmp;
		tmp = (c_r_csch_ORM_BAND_SUBCLASSs_data**)EDAlloc (sizeof (c_r_csch_ORM_BAND_SUBCLASSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_ORM_BAND_SUBCLASSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_ORM_BAND_SUBCLASSs_data*)EDAlloc (sizeof (c_r_csch_ORM_BAND_SUBCLASSs_data));
			INIT_c_r_csch_ORM_BAND_SUBCLASSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_ORM_BAND_SUBCLASSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_PRM_ALT_SOs'. Existing items are not changed. */
int SETITEMS_c_r_csch_PRM_ALT_SOs (c_r_csch_PRM_ALT_SOs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_PRM_ALT_SOs_data** tmp;
		tmp = (c_r_csch_PRM_ALT_SOs_data**)EDAlloc (sizeof (c_r_csch_PRM_ALT_SOs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_PRM_ALT_SOs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_PRM_ALT_SOs_data*)EDAlloc (sizeof (c_r_csch_PRM_ALT_SOs_data));
			INIT_c_r_csch_PRM_ALT_SOs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_PRM_ALT_SOs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_PRM_BCMC_PROGRAMSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_PRM_BCMC_PROGRAMSs (c_r_csch_PRM_BCMC_PROGRAMSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_PRM_BCMC_PROGRAMSs_data** tmp;
		tmp = (c_r_csch_PRM_BCMC_PROGRAMSs_data**)EDAlloc (sizeof (c_r_csch_PRM_BCMC_PROGRAMSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_PRM_BCMC_PROGRAMSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_PRM_BCMC_PROGRAMSs_data*)EDAlloc (sizeof (c_r_csch_PRM_BCMC_PROGRAMSs_data));
			INIT_c_r_csch_PRM_BCMC_PROGRAMSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_PRM_BCMC_PROGRAMSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_PRM_BAND_SUBCLASSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_PRM_BAND_SUBCLASSs (c_r_csch_PRM_BAND_SUBCLASSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_PRM_BAND_SUBCLASSs_data** tmp;
		tmp = (c_r_csch_PRM_BAND_SUBCLASSs_data**)EDAlloc (sizeof (c_r_csch_PRM_BAND_SUBCLASSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_PRM_BAND_SUBCLASSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_PRM_BAND_SUBCLASSs_data*)EDAlloc (sizeof (c_r_csch_PRM_BAND_SUBCLASSs_data));
			INIT_c_r_csch_PRM_BAND_SUBCLASSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_PRM_BAND_SUBCLASSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_STRPM_INFO_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_STRPM_INFO_RECORDSs (c_r_csch_STRPM_INFO_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_2_7_4** tmp;
		tmp = (c_InfoRec_2_7_4**)EDAlloc (sizeof (c_InfoRec_2_7_4*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_2_7_4*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_2_7_4*)EDAlloc (sizeof (c_InfoRec_2_7_4));
			INIT_c_InfoRec_2_7_4 (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_2_7_4 (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_ESTRPM_INFO_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_ESTRPM_INFO_RECORDSs (c_r_csch_ESTRPM_INFO_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_2_7_4** tmp;
		tmp = (c_InfoRec_2_7_4**)EDAlloc (sizeof (c_InfoRec_2_7_4*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_2_7_4*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_2_7_4*)EDAlloc (sizeof (c_InfoRec_2_7_4));
			INIT_c_InfoRec_2_7_4 (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_2_7_4 (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_DIM_INFO_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_DIM_INFO_RECORDSs (c_r_csch_DIM_INFO_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_2_7_4** tmp;
		tmp = (c_InfoRec_2_7_4**)EDAlloc (sizeof (c_InfoRec_2_7_4*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_2_7_4*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_2_7_4*)EDAlloc (sizeof (c_InfoRec_2_7_4));
			INIT_c_InfoRec_2_7_4 (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_2_7_4 (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_RCNM_ADD_SERV_INSTANCEs'. Existing items are not changed. */
int SETITEMS_c_r_csch_RCNM_ADD_SERV_INSTANCEs (c_r_csch_RCNM_ADD_SERV_INSTANCEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_RCNM_ADD_SERV_INSTANCEs_data** tmp;
		tmp = (c_r_csch_RCNM_ADD_SERV_INSTANCEs_data**)EDAlloc (sizeof (c_r_csch_RCNM_ADD_SERV_INSTANCEs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_RCNM_ADD_SERV_INSTANCEs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_RCNM_ADD_SERV_INSTANCEs_data*)EDAlloc (sizeof (c_r_csch_RCNM_ADD_SERV_INSTANCEs_data));
			INIT_c_r_csch_RCNM_ADD_SERV_INSTANCEs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_RCNM_ADD_SERV_INSTANCEs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_RCNM_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_r_csch_RCNM_FIELDSs (c_r_csch_RCNM_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_RCNM_FIELDSs_data** tmp;
		tmp = (c_r_csch_RCNM_FIELDSs_data**)EDAlloc (sizeof (c_r_csch_RCNM_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_RCNM_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_RCNM_FIELDSs_data*)EDAlloc (sizeof (c_r_csch_RCNM_FIELDSs_data));
			INIT_c_r_csch_RCNM_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_RCNM_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_csch_GEM_GE_RECs'. Existing items are not changed. */
int SETITEMS_c_r_csch_GEM_GE_RECs (c_r_csch_GEM_GE_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_csch_GEM_GE_RECs_data** tmp;
		tmp = (c_r_csch_GEM_GE_RECs_data**)EDAlloc (sizeof (c_r_csch_GEM_GE_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_csch_GEM_GE_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_csch_GEM_GE_RECs_data*)EDAlloc (sizeof (c_r_csch_GEM_GE_RECs_data));
			INIT_c_r_csch_GEM_GE_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_csch_GEM_GE_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_FWIM_INFO_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_FWIM_INFO_RECORDSs (c_r_dsch_FWIM_INFO_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_2_7_4_encrypted** tmp;
		tmp = (c_InfoRec_2_7_4_encrypted**)EDAlloc (sizeof (c_InfoRec_2_7_4_encrypted*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_2_7_4_encrypted*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_2_7_4_encrypted*)EDAlloc (sizeof (c_InfoRec_2_7_4_encrypted));
			INIT_c_InfoRec_2_7_4_encrypted (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_2_7_4_encrypted (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_DBM_MOBILE_COUNTRY_CODE'. Existing items are not changed. */
int SETITEMS_c_r_dsch_DBM_MOBILE_COUNTRY_CODE (c_r_dsch_DBM_MOBILE_COUNTRY_CODE* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_DBM_COUNTRY_BURST_TYPE'. Existing items are not changed. */
int SETITEMS_c_r_dsch_DBM_COUNTRY_BURST_TYPE (c_r_dsch_DBM_COUNTRY_BURST_TYPE* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_DBM_EXTENDED_BURST_TYPE'. Existing items are not changed. */
int SETITEMS_c_r_dsch_DBM_EXTENDED_BURST_TYPE (c_r_dsch_DBM_EXTENDED_BURST_TYPE* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_LONG* tmp;
		tmp = (ED_LONG*)EDAlloc (sizeof (ED_LONG) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_LONG) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_DBM_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_DBM_FIELDSs (c_r_dsch_DBM_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_DBM_FIELDSs_data** tmp;
		tmp = (c_r_dsch_DBM_FIELDSs_data**)EDAlloc (sizeof (c_r_dsch_DBM_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_DBM_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_DBM_FIELDSs_data*)EDAlloc (sizeof (c_r_dsch_DBM_FIELDSs_data));
			INIT_c_r_dsch_DBM_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_DBM_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_PSMM_data_01'. Existing items are not changed. */
int SETITEMS_c_r_dsch_PSMM_data_01 (c_r_dsch_PSMM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_PSMM_data_01_data** tmp;
		tmp = (c_r_dsch_PSMM_data_01_data**)EDAlloc (sizeof (c_r_dsch_PSMM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_PSMM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_PSMM_data_01_data*)EDAlloc (sizeof (c_r_dsch_PSMM_data_01_data));
			INIT_c_r_dsch_PSMM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_PSMM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_PMRM_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_PMRM_PILOTSs (c_r_dsch_PMRM_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_PMRM_PILOTSs_data** tmp;
		tmp = (c_r_dsch_PMRM_PILOTSs_data**)EDAlloc (sizeof (c_r_dsch_PMRM_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_PMRM_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_PMRM_PILOTSs_data*)EDAlloc (sizeof (c_r_dsch_PMRM_PILOTSs_data));
			INIT_c_r_dsch_PMRM_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_PMRM_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_BDTMFM_DIGITSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_BDTMFM_DIGITSs (c_r_dsch_BDTMFM_DIGITSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_BDTMFM_DIGITSs_data** tmp;
		tmp = (c_r_dsch_BDTMFM_DIGITSs_data**)EDAlloc (sizeof (c_r_dsch_BDTMFM_DIGITSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_BDTMFM_DIGITSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_BDTMFM_DIGITSs_data*)EDAlloc (sizeof (c_r_dsch_BDTMFM_DIGITSs_data));
			INIT_c_r_dsch_BDTMFM_DIGITSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_BDTMFM_DIGITSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_ORCM_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_ORCM_FIELDSs (c_r_dsch_ORCM_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_ORCM_FIELDSs_data** tmp;
		tmp = (c_r_dsch_ORCM_FIELDSs_data**)EDAlloc (sizeof (c_r_dsch_ORCM_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_ORCM_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_ORCM_FIELDSs_data*)EDAlloc (sizeof (c_r_dsch_ORCM_FIELDSs_data));
			INIT_c_r_dsch_ORCM_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_ORCM_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_ORCM_INFO_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_ORCM_INFO_RECORDSs (c_r_dsch_ORCM_INFO_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_2_7_4_encrypted** tmp;
		tmp = (c_InfoRec_2_7_4_encrypted**)EDAlloc (sizeof (c_InfoRec_2_7_4_encrypted*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_2_7_4_encrypted*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_2_7_4_encrypted*)EDAlloc (sizeof (c_InfoRec_2_7_4_encrypted));
			INIT_c_InfoRec_2_7_4_encrypted (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_2_7_4_encrypted (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_HOCM_data_01'. Existing items are not changed. */
int SETITEMS_c_r_dsch_HOCM_data_01 (c_r_dsch_HOCM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_HOCM_data_01_data** tmp;
		tmp = (c_r_dsch_HOCM_data_01_data**)EDAlloc (sizeof (c_r_dsch_HOCM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_HOCM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_HOCM_data_01_data*)EDAlloc (sizeof (c_r_dsch_HOCM_data_01_data));
			INIT_c_r_dsch_HOCM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_HOCM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_PRSM_data_01'. Existing items are not changed. */
int SETITEMS_c_r_dsch_PRSM_data_01 (c_r_dsch_PRSM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_PRSM_data_01_data** tmp;
		tmp = (c_r_dsch_PRSM_data_01_data**)EDAlloc (sizeof (c_r_dsch_PRSM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_PRSM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_PRSM_data_01_data*)EDAlloc (sizeof (c_r_dsch_PRSM_data_01_data));
			INIT_c_r_dsch_PRSM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_PRSM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_STRPM_INFO_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_STRPM_INFO_RECORDSs (c_r_dsch_STRPM_INFO_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_2_7_4** tmp;
		tmp = (c_InfoRec_2_7_4**)EDAlloc (sizeof (c_InfoRec_2_7_4*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_2_7_4*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_2_7_4*)EDAlloc (sizeof (c_InfoRec_2_7_4));
			INIT_c_InfoRec_2_7_4 (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_2_7_4 (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_SCRM_ACT_PNs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_SCRM_ACT_PNs (c_r_dsch_SCRM_ACT_PNs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_SCRM_ACT_PNs_data** tmp;
		tmp = (c_r_dsch_SCRM_ACT_PNs_data**)EDAlloc (sizeof (c_r_dsch_SCRM_ACT_PNs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_SCRM_ACT_PNs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_SCRM_ACT_PNs_data*)EDAlloc (sizeof (c_r_dsch_SCRM_ACT_PNs_data));
			INIT_c_r_dsch_SCRM_ACT_PNs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_SCRM_ACT_PNs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_SCRM_NGHBR_PNs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_SCRM_NGHBR_PNs (c_r_dsch_SCRM_NGHBR_PNs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_SCRM_NGHBR_PNs_data** tmp;
		tmp = (c_r_dsch_SCRM_NGHBR_PNs_data**)EDAlloc (sizeof (c_r_dsch_SCRM_NGHBR_PNs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_SCRM_NGHBR_PNs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_SCRM_NGHBR_PNs_data*)EDAlloc (sizeof (c_r_dsch_SCRM_NGHBR_PNs_data));
			INIT_c_r_dsch_SCRM_NGHBR_PNs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_SCRM_NGHBR_PNs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_SCRM_ACT_PNs_01'. Existing items are not changed. */
int SETITEMS_c_r_dsch_SCRM_ACT_PNs_01 (c_r_dsch_SCRM_ACT_PNs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_SCRM_ACT_PNs_01_data** tmp;
		tmp = (c_r_dsch_SCRM_ACT_PNs_01_data**)EDAlloc (sizeof (c_r_dsch_SCRM_ACT_PNs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_SCRM_ACT_PNs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_SCRM_ACT_PNs_01_data*)EDAlloc (sizeof (c_r_dsch_SCRM_ACT_PNs_01_data));
			INIT_c_r_dsch_SCRM_ACT_PNs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_SCRM_ACT_PNs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_SCRM_NGHBR_PNs_01'. Existing items are not changed. */
int SETITEMS_c_r_dsch_SCRM_NGHBR_PNs_01 (c_r_dsch_SCRM_NGHBR_PNs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_SCRM_ACT_PNs_01_data** tmp;
		tmp = (c_r_dsch_SCRM_ACT_PNs_01_data**)EDAlloc (sizeof (c_r_dsch_SCRM_ACT_PNs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_SCRM_ACT_PNs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_SCRM_ACT_PNs_01_data*)EDAlloc (sizeof (c_r_dsch_SCRM_ACT_PNs_01_data));
			INIT_c_r_dsch_SCRM_ACT_PNs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_SCRM_ACT_PNs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_PPSMM_PILOTs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_PPSMM_PILOTs (c_r_dsch_PPSMM_PILOTs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_PPSMM_PILOTs_data** tmp;
		tmp = (c_r_dsch_PPSMM_PILOTs_data**)EDAlloc (sizeof (c_r_dsch_PPSMM_PILOTs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_PPSMM_PILOTs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_PPSMM_PILOTs_data*)EDAlloc (sizeof (c_r_dsch_PPSMM_PILOTs_data));
			INIT_c_r_dsch_PPSMM_PILOTs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_PPSMM_PILOTs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_PPSMM_PILOTs_01'. Existing items are not changed. */
int SETITEMS_c_r_dsch_PPSMM_PILOTs_01 (c_r_dsch_PPSMM_PILOTs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_PPSMM_PILOTs_01_data** tmp;
		tmp = (c_r_dsch_PPSMM_PILOTs_01_data**)EDAlloc (sizeof (c_r_dsch_PPSMM_PILOTs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_PPSMM_PILOTs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_PPSMM_PILOTs_01_data*)EDAlloc (sizeof (c_r_dsch_PPSMM_PILOTs_01_data));
			INIT_c_r_dsch_PPSMM_PILOTs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_PPSMM_PILOTs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_PPSMM_data_01'. Existing items are not changed. */
int SETITEMS_c_r_dsch_PPSMM_data_01 (c_r_dsch_PPSMM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_PPSMM_data_01_data** tmp;
		tmp = (c_r_dsch_PPSMM_data_01_data**)EDAlloc (sizeof (c_r_dsch_PPSMM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_PPSMM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_PPSMM_data_01_data*)EDAlloc (sizeof (c_r_dsch_PPSMM_data_01_data));
			INIT_c_r_dsch_PPSMM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_PPSMM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_OLRM_data_01'. Existing items are not changed. */
int SETITEMS_c_r_dsch_OLRM_data_01 (c_r_dsch_OLRM_data_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_OLRM_data_01_data** tmp;
		tmp = (c_r_dsch_OLRM_data_01_data**)EDAlloc (sizeof (c_r_dsch_OLRM_data_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_OLRM_data_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_OLRM_data_01_data*)EDAlloc (sizeof (c_r_dsch_OLRM_data_01_data));
			INIT_c_r_dsch_OLRM_data_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_OLRM_data_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_RRRM_ADD_CON_REFs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_RRRM_ADD_CON_REFs (c_r_dsch_RRRM_ADD_CON_REFs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_RRRM_ADD_CON_REFs_data** tmp;
		tmp = (c_r_dsch_RRRM_ADD_CON_REFs_data**)EDAlloc (sizeof (c_r_dsch_RRRM_ADD_CON_REFs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_RRRM_ADD_CON_REFs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_RRRM_ADD_CON_REFs_data*)EDAlloc (sizeof (c_r_dsch_RRRM_ADD_CON_REFs_data));
			INIT_c_r_dsch_RRRM_ADD_CON_REFs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_RRRM_ADD_CON_REFs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_RRRM_BCMC_PROGRAMSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_RRRM_BCMC_PROGRAMSs (c_r_dsch_RRRM_BCMC_PROGRAMSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_RRRM_BCMC_PROGRAMSs_data** tmp;
		tmp = (c_r_dsch_RRRM_BCMC_PROGRAMSs_data**)EDAlloc (sizeof (c_r_dsch_RRRM_BCMC_PROGRAMSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_RRRM_BCMC_PROGRAMSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_RRRM_BCMC_PROGRAMSs_data*)EDAlloc (sizeof (c_r_dsch_RRRM_BCMC_PROGRAMSs_data));
			INIT_c_r_dsch_RRRM_BCMC_PROGRAMSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_RRRM_BCMC_PROGRAMSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_EOM_ALT_SOs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_EOM_ALT_SOs (c_r_dsch_EOM_ALT_SOs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_EOM_ALT_SOs_data** tmp;
		tmp = (c_r_dsch_EOM_ALT_SOs_data**)EDAlloc (sizeof (c_r_dsch_EOM_ALT_SOs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_EOM_ALT_SOs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_EOM_ALT_SOs_data*)EDAlloc (sizeof (c_r_dsch_EOM_ALT_SOs_data));
			INIT_c_r_dsch_EOM_ALT_SOs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_EOM_ALT_SOs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_EOM_FIELDSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_EOM_FIELDSs (c_r_dsch_EOM_FIELDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_EOM_FIELDSs_data** tmp;
		tmp = (c_r_dsch_EOM_FIELDSs_data**)EDAlloc (sizeof (c_r_dsch_EOM_FIELDSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_EOM_FIELDSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_EOM_FIELDSs_data*)EDAlloc (sizeof (c_r_dsch_EOM_FIELDSs_data));
			INIT_c_r_dsch_EOM_FIELDSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_EOM_FIELDSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_EOM_RECSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_EOM_RECSs (c_r_dsch_EOM_RECSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_2_7_4_encrypted** tmp;
		tmp = (c_InfoRec_2_7_4_encrypted**)EDAlloc (sizeof (c_InfoRec_2_7_4_encrypted*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_2_7_4_encrypted*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_2_7_4_encrypted*)EDAlloc (sizeof (c_InfoRec_2_7_4_encrypted));
			INIT_c_InfoRec_2_7_4_encrypted (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_2_7_4_encrypted (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_EOM_ADD_SERV_INSTANCEs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_EOM_ADD_SERV_INSTANCEs (c_r_dsch_EOM_ADD_SERV_INSTANCEs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_EOM_ADD_SERV_INSTANCEs_data** tmp;
		tmp = (c_r_dsch_EOM_ADD_SERV_INSTANCEs_data**)EDAlloc (sizeof (c_r_dsch_EOM_ADD_SERV_INSTANCEs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_EOM_ADD_SERV_INSTANCEs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_EOM_ADD_SERV_INSTANCEs_data*)EDAlloc (sizeof (c_r_dsch_EOM_ADD_SERV_INSTANCEs_data));
			INIT_c_r_dsch_EOM_ADD_SERV_INSTANCEs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_EOM_ADD_SERV_INSTANCEs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_EOM_BCMC_PROGRAMSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_EOM_BCMC_PROGRAMSs (c_r_dsch_EOM_BCMC_PROGRAMSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_EOM_BCMC_PROGRAMSs_data** tmp;
		tmp = (c_r_dsch_EOM_BCMC_PROGRAMSs_data**)EDAlloc (sizeof (c_r_dsch_EOM_BCMC_PROGRAMSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_EOM_BCMC_PROGRAMSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_EOM_BCMC_PROGRAMSs_data*)EDAlloc (sizeof (c_r_dsch_EOM_BCMC_PROGRAMSs_data));
			INIT_c_r_dsch_EOM_BCMC_PROGRAMSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_EOM_BCMC_PROGRAMSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_EFWIM_RECs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_EFWIM_RECs (c_r_dsch_EFWIM_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_2_7_4_encrypted** tmp;
		tmp = (c_InfoRec_2_7_4_encrypted**)EDAlloc (sizeof (c_InfoRec_2_7_4_encrypted*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_2_7_4_encrypted*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_2_7_4_encrypted*)EDAlloc (sizeof (c_InfoRec_2_7_4_encrypted));
			INIT_c_InfoRec_2_7_4_encrypted (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_2_7_4_encrypted (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_EPSMM_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_EPSMM_PILOTSs (c_r_dsch_EPSMM_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_EPSMM_PILOTSs_data** tmp;
		tmp = (c_r_dsch_EPSMM_PILOTSs_data**)EDAlloc (sizeof (c_r_dsch_EPSMM_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_EPSMM_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_EPSMM_PILOTSs_data*)EDAlloc (sizeof (c_r_dsch_EPSMM_PILOTSs_data));
			INIT_c_r_dsch_EPSMM_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_EPSMM_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_EPSMM_PILOTSs_01'. Existing items are not changed. */
int SETITEMS_c_r_dsch_EPSMM_PILOTSs_01 (c_r_dsch_EPSMM_PILOTSs_01* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_EPSMM_PILOTSs_01_data** tmp;
		tmp = (c_r_dsch_EPSMM_PILOTSs_01_data**)EDAlloc (sizeof (c_r_dsch_EPSMM_PILOTSs_01_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_EPSMM_PILOTSs_01_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_EPSMM_PILOTSs_01_data*)EDAlloc (sizeof (c_r_dsch_EPSMM_PILOTSs_01_data));
			INIT_c_r_dsch_EPSMM_PILOTSs_01_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_EPSMM_PILOTSs_01_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_EHOCM_PILOTSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_EHOCM_PILOTSs (c_r_dsch_EHOCM_PILOTSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_EHOCM_PILOTSs_data** tmp;
		tmp = (c_r_dsch_EHOCM_PILOTSs_data**)EDAlloc (sizeof (c_r_dsch_EHOCM_PILOTSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_EHOCM_PILOTSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_EHOCM_PILOTSs_data*)EDAlloc (sizeof (c_r_dsch_EHOCM_PILOTSs_data));
			INIT_c_r_dsch_EHOCM_PILOTSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_EHOCM_PILOTSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_SMRM_RECSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_SMRM_RECSs (c_r_dsch_SMRM_RECSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_SMRM_RECSs_data** tmp;
		tmp = (c_r_dsch_SMRM_RECSs_data**)EDAlloc (sizeof (c_r_dsch_SMRM_RECSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_SMRM_RECSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_SMRM_RECSs_data*)EDAlloc (sizeof (c_r_dsch_SMRM_RECSs_data));
			INIT_c_r_dsch_SMRM_RECSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_SMRM_RECSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_CLCM_ADD_TAGs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_CLCM_ADD_TAGs (c_r_dsch_CLCM_ADD_TAGs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_CLCM_ADD_TAGs_data** tmp;
		tmp = (c_r_dsch_CLCM_ADD_TAGs_data**)EDAlloc (sizeof (c_r_dsch_CLCM_ADD_TAGs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_CLCM_ADD_TAGs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_CLCM_ADD_TAGs_data*)EDAlloc (sizeof (c_r_dsch_CLCM_ADD_TAGs_data));
			INIT_c_r_dsch_CLCM_ADD_TAGs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_CLCM_ADD_TAGs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_DIM_INFO_RECORDSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_DIM_INFO_RECORDSs (c_r_dsch_DIM_INFO_RECORDSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_InfoRec_2_7_4** tmp;
		tmp = (c_InfoRec_2_7_4**)EDAlloc (sizeof (c_InfoRec_2_7_4*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_InfoRec_2_7_4*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_InfoRec_2_7_4*)EDAlloc (sizeof (c_InfoRec_2_7_4));
			INIT_c_InfoRec_2_7_4 (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_InfoRec_2_7_4 (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_BSSREQM_RECORDs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_BSSREQM_RECORDs (c_r_dsch_BSSREQM_RECORDs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_BSSREQM_RECORDs_data** tmp;
		tmp = (c_r_dsch_BSSREQM_RECORDs_data**)EDAlloc (sizeof (c_r_dsch_BSSREQM_RECORDs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_BSSREQM_RECORDs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_BSSREQM_RECORDs_data*)EDAlloc (sizeof (c_r_dsch_BSSREQM_RECORDs_data));
			INIT_c_r_dsch_BSSREQM_RECORDs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_BSSREQM_RECORDs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data** tmp;
		tmp = (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data**)EDAlloc (sizeof (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data*)EDAlloc (sizeof (c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data));
			INIT_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_ITBSPMRM_BCMC_PROGRAMSs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_HOSINM_HO_SUP_RECs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_HOSINM_HO_SUP_RECs (c_r_dsch_HOSINM_HO_SUP_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_HOSINM_HO_SUP_RECs_data** tmp;
		tmp = (c_r_dsch_HOSINM_HO_SUP_RECs_data**)EDAlloc (sizeof (c_r_dsch_HOSINM_HO_SUP_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_HOSINM_HO_SUP_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_HOSINM_HO_SUP_RECs_data*)EDAlloc (sizeof (c_r_dsch_HOSINM_HO_SUP_RECs_data));
			INIT_c_r_dsch_HOSINM_HO_SUP_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_HOSINM_HO_SUP_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_r_dsch_GEM_GE_RECs'. Existing items are not changed. */
int SETITEMS_c_r_dsch_GEM_GE_RECs (c_r_dsch_GEM_GE_RECs* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		c_r_dsch_GEM_GE_RECs_data** tmp;
		tmp = (c_r_dsch_GEM_GE_RECs_data**)EDAlloc (sizeof (c_r_dsch_GEM_GE_RECs_data*) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (c_r_dsch_GEM_GE_RECs_data*) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			sequence->data[i] = (c_r_dsch_GEM_GE_RECs_data*)EDAlloc (sizeof (c_r_dsch_GEM_GE_RECs_data));
			INIT_c_r_dsch_GEM_GE_RECs_data (sequence->data[i]);
		}
	}
	 else {
		for (i=desiredItems; i<sequence->items; i++) {
				FREE_c_r_dsch_GEM_GE_RECs_data (sequence->data[i]);
				EDFree (sequence->data[i]);
			}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Capability_Information_Blob_NAK_PER_ROUND_FWD'. Existing items are not changed. */
int SETITEMS_c_RIR_Capability_Information_Blob_NAK_PER_ROUND_FWD (c_RIR_Capability_Information_Blob_NAK_PER_ROUND_FWD* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

/* SETITEMS commands for type 'c_RIR_Capability_Information_Blob_NAK_PER_ROUND_REV'. Existing items are not changed. */
int SETITEMS_c_RIR_Capability_Information_Blob_NAK_PER_ROUND_REV (c_RIR_Capability_Information_Blob_NAK_PER_ROUND_REV* sequence, int desiredItems)
{
	int i;
	/* ALK-UNI */
	if (sequence->allocatedItems < desiredItems) {
		int newSize = desiredItems + 0;
		ED_OCTET* tmp;
		tmp = (ED_OCTET*)EDAlloc (sizeof (ED_OCTET) * (size_t)(newSize));
		if (sequence->data) {
			if (tmp != NULL && sequence->items > 0) {
				memcpy (tmp, sequence->data, sizeof (ED_OCTET) * (size_t)sequence->items);
			}
			EDFree (sequence->data);
		}
		sequence->allocatedItems = newSize;
		sequence->data = tmp;
	
	}

	if (desiredItems > sequence->items) {
		for (i=sequence->items; i<desiredItems; i++) {
			(sequence->data[i]) = 0;
		}
	}

	sequence->items = desiredItems;
	return ED_NO_ERRORS;
}

